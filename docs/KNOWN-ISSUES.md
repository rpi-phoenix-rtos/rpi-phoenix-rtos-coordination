# Known open issues

Every issue we know about and have **not** fixed, in one list. Nothing else.

- **Open only.** A fixed issue leaves this file. Closed rows are kept, verbatim, in
  [`done/closed-issues-archive.md`](done/closed-issues-archive.md) so nothing is re-litigated.
- **No decisions recorded here.** Fix / postpone / won't-fix is the owner's call, made from this
  list; earlier verdicts have been stripped deliberately.
- **No history.** Each row says what is wrong and what we last measured. Analyses live in
  `docs/misc/` and `docs/done/`.
- Open `TD-NN` transitional-debt items are issues too and are listed here.
  [`TEMPORARY-FIXES-AND-FUTURE-CLEANUP.md`](TEMPORARY-FIXES-AND-FUTURE-CLEANUP.md) keeps their
  engineering detail; anything marked RESOLVED there is not repeated here.

Verified against the tree, the git history and the boot-log archive on **2026-09-22**. Where a
row's old claim turned out to be stale, the correction is noted in the row.

## 1. Correctness — crashes and data loss

| # | Issue | Last measured |
|---|---|---|
| C1 | **A stray four-byte write corrupts the high half of one heap header field, and every later `free()` from that heap then leaks.** `heap->size` sits at offset 0 of a page-aligned heap; the low 32 bits survive intact and only the high half is wrong, so the write lands at `heap+4`. Masked, both observed heaps are entirely self-consistent — legal `0xd000` size, chunks inside it, ordinary grid. ★ The corrupting word is **not constant** (`0x80000000` and `0x80000001` on two heaps in one run), which reads like a refcount or lock word with a flag in the MSB rather than a marker. Guards: `free() of a corrupt chunk header`, `large-bin lookup returned a non-chunk` / `a wild pointer`, `chunk handed out twice`. | **Live but rare, and a distinct signature from the historical bulk.** Archive-wide `hsize` is printed 24 231 times: 24 226 sane (`0x2000`) and **5 corrupt** — all 5 in one run (`gate-stk`, 2026-09-22, `why=5`, 0 faults, 1189 frames, still passed visually). Every high-count log predates the 2026-09-12 BO-ownership fix, so the pre-fix bulk was a bad chunk in a good heap and this residue is a good chunk in a bad heap. Next occurrence is instrumented to decide it — `hlo32` / `hhi32` / `hfixed`, and since 2026-09-23 from **every** reporter that prints a heap size, not just the corrupt-header one, so whichever guard fires first is decisive. The instrument is itself verified against an injected reproduction of the exact field state (`tools/malloc-harness`, `hi32-write`). ⛔ Do not re-walk the eliminated set: double BO ownership (measured clean, 0 overlapping reuses in 2 827 events), application overrun, or naming the `operator delete` caller (`-fomit-frame-pointer` makes it impossible). ⚠ **The `caller=` field cannot be symbolized across builds, so every historical one in the archive is unreadable.** It is `__builtin_return_address(0)`, an absolute address in a no-ASLR static binary — but SuperTuxKart is relinked by every `--scope core` build. Proved 2026-09-23: the 2026-09-22 value `0x1b2900c` lands mid-sequence inside today's binary (the instruction before it is `str`, not `bl`), so it cannot be a return address there, and `addr2line` nonetheless names a plausible-looking function for it. Any attribution made that way is fiction. Fixed going forward — `artifacts/c1-symbols/` keeps the unstripped binary keyed by sha, and the export is re-synced so log and symbols come from one build. ★ Eliminated by reading 2026-09-23, none of them worth re-walking: the corrupt heap is **not** libstdc++'s emergency-exception arena (that is `obj_count 256 × (6×8 + R 128 + D 112)` = **0x12000**, measured with the cross compiler, not 0xd000); that pool tags no bit of any size field; and the allocator's own chunk flags are `CHUNK_PUSED`/`CHUNK_CUSED` = bits 0 and 1, so **bit 63 is not an allocator flag**. Independently confirmed that the low half really is intact: a failing chunk sits at heap offset `0x8f40`, which only fits if the heap is genuinely ~`0xd000`. Heaps come from `CEIL(sizeof(heap_t) + size, PAGE)`, so a `0xd000` heap means one ~49–53 KB request — normal heaps in the archive are `0x2000`. ★★ **2026-09-23 — the stray write is identified as a Mesa `ralloc` GC block header, and the hunt moves out of libphoenix.** Two independent lines meet. (1) *Who owns the victim heap:* every archived fire smashes a `0xd000` heap, so a bounded instrument was added to `_malloc_heapAlloc` reporting the creator of exactly that size (verified on the host harness **before** any Pi cycle — it fires, with `c1req = 0xbfff`, and `CEIL(16 + 0xbfff, 4096) = 0xd000`). One STK run: 15 reports, **12 of them from `gc_zalloc_size`** — Mesa's GC allocator in `src/util/ralloc.c`, which is in the V3D driver build. Symbolized against the binary from that same run, archived in `artifacts/c1-symbols/`. (2) *A structurally unguarded writer in that same allocator:* `NDEBUG` is on for the Mesa build (confirmed from the binary — zero `GC_CANARY` immediates in `gc_alloc_size`/`gc_zalloc_size`), so the canary field is compiled out, `gc_block_header` is 4 bytes, and `get_gc_block_header()` does `if (c_ptr[-1] & IS_PADDING) c_ptr -= c_ptr[-1] & ~IS_PADDING; c_ptr -= sizeof(gc_block_header);` followed by `assert(info->canary == GC_CANARY)` — which `NDEBUG` deletes. A `gc_*` call on anything that is not a live GC block therefore walks back up to 127 garbage-controlled bytes, validates nothing, and hands its caller a header pointer to write through. ↩ **Retracted before it spread: I claimed the corrupting words ARE a `gc_block_header`.** With `flags = IS_PADDING` and `slab_offset` 0/1 the struct does read `0x80000000` / `0x80000001`, which is a seductive fit — but checking *every* `flags` write in `ralloc.c` gives `flags ∈ {0,1,2,3}` (`current_gen|IS_USED`, then `&= ~IS_USED`, `^= CURRENT_GENERATION`), and a real padding byte is `IS_PADDING | padcount` with `padcount >= 4`, i.e. `0x84`/`0x88`/`0x8c`. **Neither ever yields exactly `0x80`.** So the value is NOT identified; only bit 7 matched, and that was pattern-matching. What the write does preserve is the low half — consistent with a 4-byte store at `heap+4` or a 64-bit read-modify-write, nothing narrower. **Mechanism:** `get_gc_block_header()` does `if (c_ptr[-1] & IS_PADDING) c_ptr -= c_ptr[-1] & ~IS_PADDING; c_ptr -= sizeof(gc_block_header);` and then `assert(info->canary == GC_CANARY)` — which `NDEBUG` deletes. So a `gc_*` call on a pointer that is not a live GC block walks back by up to 127 garbage-controlled bytes, validates nothing, and writes a header at a wild address. ⚠ *Still inferred, not proven:* that such a call is what performs the write. **Next step:** re-enable the GC canary in our Mesa fork (4 bytes per block) so the mechanism self-reports, rather than hunting a ~2% event. **Rate, measured across 137 comparable `hacienda` runs: 3 firing (23, 5 and 5 fires) = ~2.2%** — which also corrects this row's earlier 'all 5 in one run': the 2026-09-15 run had 23. ⚠ The `TODO(C1-hunt)` instrument in `malloc_dl.c` comes out when this closes. ★★★ **2026-09-23 (later) — a fire was caught on a run whose symbols were archived, and it overturns two things I had been reasoning from.** (a) **The victim is NOT always a `0xd000` heap.** This fire's victim is a standard **`0x2000`** heap at `0xc9000` (`hlo32 = 0x2000`, `hfixed = 1`), with `heapHi` only `0xde000`. So the row's old 'always a `0xd000` bin-14 heap' is **wrong**, and with it the whole reason `gc_zalloc_size` was interesting — that finding answers 'who creates `0xd000` heaps', which is now known not to be the victim class. Treat the Mesa/ralloc line as **unconnected to C1** unless something re-links it. (b) **The gc-header mechanism is refuted for this occurrence:** a plausibility check added inside Mesa's `get_gc_header()` (`bucket > NUM_FREELIST_BUCKETS`, or `slab_offset >= SLAB_SIZE` only when slab-backed — host-verified to fire on garbage and stay quiet on all five legal shapes, including the direct path that legitimately leaves `slab_offset` uninitialised) reported **0 times** in the very run that fired. ✔ **What IS now solid:** the corrupting high word is invariant — `hhi32 = 0x80000001` on a `0x2000` heap here and on `0xd000` heaps before, with the low half intact both times, so the *write* is the characteristic thing and the victim is incidental. ✔ **The consequence chain, traced end to end in one run for the first time:** corrupt heap header (`why=5`, heap `0xc9000`) → two small bins from that same heap dropped as `small-bin head is not a chunk` (chunks `0xcabb8`, `0xcaf90`) → at shutdown `free()` → `lib_rbRemove` → `lib_rbRemoveBalance` (`sys/rb.c:147`) reads `x->parent->left` with `x->parent == NULL` → **Data Abort EL0, `far=0x8`, read** (`esr=0x92000007`), and SuperTuxKart dies at exit. ⛔ **`caller=` is untrustworthy and must not be used for attribution** — on BOTH fires it resolves to an address that is not preceded by any `bl` (2026-09-22 `0x1b2900c`; 2026-09-23 `0x404e84`, which is `mov x0, #0x1b` before a `bl operator new`, with no `free`/`operator delete` call site within 768 bytes), and the second was checked against the exact binary from that run. The capture code is correct (`xpaclri; mov x24, x30` — this toolchain builds with pointer authentication), and the kernel-dumped `lr` from the same log symbolizes perfectly, so return addresses work in general. Why this one field is wrong is **open, and is the next thing to fix** — it has twice nearly produced a false attribution. ↩ *Follow-up the same day: the field is probably fine and the LOG is not. **4 of 202 hex literals in that log contain a non-hex character**, and digit-for-digit corruption is invisible — while `0x404e84` is one hex digit from `0x404e8c`, a real return site. Every address read from a loaded UART log is therefore suspect. `malloc` now prints `callerx=` (the complement) beside `caller=`: `caller ^ callerx` must be all ones, else discard the address rather than symbolize it. libphoenix `c246654`.* ★★★★ **2026-09-23 — C1 is one symptom of a SYSTEMIC 64-bit high-half corruption, and it reaches the KERNEL.** Re-reading the fire log with the new `c1-report.sh` surfaced an **EL1 (kernel) Data Abort** I had walked past: `pc=0xffffffffc0025cac`, `far=0x8000000100000008`, `x0=0x8000000100000000`, `esr=0x96000004` (EL1 data abort, translation fault L0, read). Symbolization is **self-validated** — the instruction at `pc` is `ldr x4, [x0, #8]` and `far == x0 + 8` exactly, so the ELF provably matches the run: **kernel `rb_transplant()` (`lib/rb.c:223`), called from `lib_rbRemove()` (`rb.c:329`), dereferenced a red-black-tree node pointer of `0x8000000100000000`.** The kernel's own rbtree held the same bit pattern that corrupts userspace heap headers. Archive-wide the pattern is everywhere — **41 logs**, and the distinct values are the giveaway: `800000010cdb1000`, `800000010ce69000`, `800000010ce77000`, `800000010cf03000`, `800000010d009d78`, `800000010c845168` are all **real page-aligned / heap-like addresses whose top 32 bits have become `0x80000001`**, while the low 32 bits stay correct. One 2026-09-12 dump shows all three states at once: `x0=0x000000000cf10000` (clean pointer), `x1=0x800000010cf03000` (same kind of pointer, clobbered), `x2=0x800000010000d000` (the heap *size*, clobbered). ⚠ *Not every match is a pointer — `first8=8000000008000000` in the SD `mbrrate` logs is raw MBR sector data and must be excluded.* So the invariant is not "a heap header gets smashed" but **"the high half of a 64-bit word becomes 0x80000000/0x80000001 while the low half survives"**, in both address spaces. That reframes the hunt: the allocator is a *victim and a detector*, not the locus. **The kernel instance is the tractable one** — our code, a small well-defined structure, and a fault that is currently survived but is an EL1 abort in a teardown path (the same class as the C2 wedge). Next step: identify which kernel rbtree, bearing in mind that printing from a lock-holding kernel path self-deadlocks, so the report must be deferred, not inline. Tooling: `scripts/c1-report.sh` extracts and validates this evidence and refuses to symbolize an address whose `callerx` complement does not check out. ↩ *Sixth hypothesis eliminated the same night: a **cold Mesa shader cache is NOT the trigger**. Today's six runs looked like a perfect correlation (2/2 cold fired, 4/4 warm clean) — but across the whole archive it is **3/34 cold (8.8%) vs 26/375 warm (6.9%)**, i.e. no signal. Checked before acting on it.* ★★ **2026-09-23 — the post-fix defect has only ever appeared with the V3D GPU running, and the pre-2026-09-12 bulk is confirmed to be a different thing.** Measured across the whole archive: of the 23 no-GPU firing logs, **all are from 2026-09-12** and **23 of 24 carry no `0x80000001` high-half at all** — so the historical bulk really is a separate phenomenon, as this row claimed, and it is now measured rather than asserted. Post-2026-09-12: **GPU active 1212 runs / 6 firing; no GPU 834 runs / 0 firing**, and every one of the 6 has `v3d-winsys` in its log. ⚠ *Read that as suggestive, not conclusive: the two populations differ in exposure, not only in GPU presence — the non-GPU runs are largely short boot captures, while an STK run is ~460 s of heavy allocation. Under a uniform rate the 834 would predict only ~2.4 fires, so seeing 0 has p ≈ 0.09 on its own.* Still, it is the first constraint that narrows WHERE to look: the V3D driver, its DMA, or Mesa — rather than the allocator, which is only the detector. ⛔ Also eliminated (seventh): the constant is not a `bit31 | count` flag idiom anywhere in libphoenix, the kernel, the V3D driver or Mesa's v3d/broadcom trees — the only `1u<<31` hits are V3D *register* bits (`MMU_ILLEGAL_ENABLE`, `TFU_COEF0_USECOEF`), not memory writes. ↩ *Later the same night — a careful set of negatives, recorded so they are not re-walked.* **(a) The rate analysis corrects itself:** 8 consecutive clean trials with the sweep armed, against 2 fires in the 4 runs before it, looked like the instrument masking the defect. It is not — the archive base rate is ~2–3% (3/137), which predicts ~0.2 fires in 8 runs, so **0/8 is exactly what is expected and today's 2/4 was the anomaly**. No evidence the sweep perturbs anything. **(b) Blind trials are poor value at that rate** — ~30–50 runs per fire. **(c) Two more mechanisms eliminated on the now-constrained GPU path:** `MMU_ILLEGAL_ADDR` is programmed as `(scratch_pa >> 12) | MMU_ILLEGAL_ENABLE`, which is the only place in the tree that naturally makes a `0x8000000X` value — but it yields `0x8000c000`-ish for a real scratch PA, is an **MMIO** write, and sits at register offset `0x1230`, not `+4`; `TFU_COEF0_USECOEF` is likewise a register bit at offset `0x424`. **(d) A promising log-shape signal turned out to be my own broken tool:** a normalised diff appeared to show `User Config: Unknown value` bursts only in the firing run — but `comm` had warned *"file is not in sorted order"* and its output was meaningless. Measured directly, **every hacienda run has exactly 21 user-config errors**, firing and clean alike. Same class of near-miss as the shader-cache correlation, caught faster. ★★ **2026-09-23 — a tripwire, because we have been watching a tiny fraction of the dartboard.** C1 is only ever *noticed* when the stray write lands on a heap header: 16 bytes per heap, a few hundred heaps, so a few KB of sensitive target inside hundreds of MB. Every other landing is invisible, which plausibly explains why the observed rate is ~2.5%/run while the underlying event may be far more common. `tools/memtrip` fills 64 MiB with `0xa5a5a5a5` and scans it alongside the workload — roughly **4000× the cross-section of the heap headers** — reporting address, value, page-alignment, offset-in-page and the eight surrounding words, which settles the isolated-store-vs-foreign-structure question the allocator's own instrument cannot. **Verified on the Pi, not on paper:** `memtrip 16 selftest` plants the exact `0x80000001` and the scan reports it on pass 1 with full context, then runs 80 further passes clean. ⚠ **A negative is informative too:** if the tripwire stays clean across runs in which the allocator DOES fire, the write is not uniformly distributed but aimed at something about heap headers (page+4) — a different search entirely. **Soak so far: 4 runs, 0 stray words — but also 0 allocator fires, so the silence is not yet informative.** ⚠ **Scope limit on the tripwire, stated so its silence is not over-read:** `memtrip` is a SEPARATE process with its own page tables, so a CPU wild pointer inside SuperTuxKart can never reach it. It tests only writers that reach memory by physical address — DMA / the GPU — or kernel-side writers. That happens to be the leading hypothesis (the GPU constraint above), and it is also what the cross-address-space evidence already suggests: the same `0x80000001` pattern corrupts **both** a user heap header and a **kernel** rbtree node, which no single userspace wild pointer can do. But a clean tripwire does **not** exonerate a CPU-side wild pointer in STK/Mesa. **Soak: 5 runs × ~1050 scan passes over 64 MiB (~67 GiB scanned per run), 0 stray words — and 0 allocator fires in those runs.** The tripwire does bound one thing already: were the write a uniform scribble at ≥10 events/run, with the tripwire covering roughly 15% of allocated memory, 5 clean runs would have p ≈ 0.01. So it is not a frequent uniform scribble; it is rare, or targeted. ★★★★★ **2026-09-23 — the two historical signatures are ONE defect, and the target is `page + 4`.** A fire on the run with both new instruments armed came back with the heap **intact** (`hsize = 0xd000`, `hhi32 = 0`) and the corruption in a **chunk** header instead: `why = 8`, `size = 0x800000010000002b` — the identical `0x80000001` high half, low half intact (`0x2b`, a legal small-chunk size with flags). `ptr = 0x0b7e3010`, so the chunk base is `ptr - CHUNK_OVERHEAD` = **`0x0b7e3000`, page-aligned** — and `size` sits at offset 0, so the corrupted word is at **page + 4**. A heap header is page-aligned by construction with `size` at offset 0, so *its* corruption is at page + 4 too. **Same address shape, same value, same low-half survival — the "bad chunk in a good heap" and "good chunk in a bad heap" readings are the same write.** ⚠ That means this row's long-standing split between the pre-2026-09-12 bulk and the current residue may be wrong, and the two should be re-examined as one phenomenon. **Two consequences that sharpen the hunt:** (1) the free-block poison **structurally cannot see this** — it starts at `chunk+32`, and the write is at `chunk+4`; that is not a negative result, it is a blind spot, now known. (2) `memtrip` **does** cover `page+4` for all 16 384 of its pages and saw nothing in the same run, so the writer is **not** picking pages at random — it hits pages that something already knows about (the allocator's, or the GPU's). Next instrument should watch `page+4` of pages the allocator owns, which is a far smaller and better-aimed net than 64 MiB of anonymous memory. ★★★★★ **2026-09-23 — CAUGHT. The aimed instrument fired, and C1 is a STALE POINTER, not a rare cosmic event.** Poisoning `page+4` inside large free chunks (one 32-bit word per 4 KiB) produced **4 reports in a single run in which the allocator's own guard fired ZERO times** — so the underlying write is far more frequent than the ~2.5% guard rate; the guard only ever saw the small fraction that landed on live metadata. All four reports are **identical**: `p4page = 0x0ba8f000`, `p4got = 0x80000001`, `p4chunk = 0x0ba8e010`, `p4csize = 0x4ff0` — the same page, the same value, inside the same **free** chunk — recurring through the run at flip totals 150 / 272 / 457 / 995, i.e. every few seconds. **Something holds a pointer to that page and keeps writing `0x80000001` at +4 while the allocator considers the memory free.** That is a use-after-free or an aliased mapping, and it explains everything the earlier evidence only hinted at: why the value is a constant (one writer, one field), why the low half always survives (a 4-byte store), why both heap and chunk headers are hit (whatever page the stale pointer lands in), and why it correlates with the GPU. ⚠ In the same run `memtrip` (64 MiB anonymous, page+4 covered) and the free-payload poison were both **clean** — consistent with one targeted stale pointer rather than a scatter, and the reason 15 blind trials found nothing. The `callerx` checksum earned its keep here: the reported caller validated (XOR = all ones) on first use in anger. **Next: the page-head dump is now armed** — whoever writes one field probably writes others, so the page should still hold their structure and name them. ★★★★★ **2026-09-23 — ROOT CAUSE CLASS IDENTIFIED: a USE-AFTER-FREE in the render path.** The page-head dump on the next fire settles it. Page `0x09a86000`, inside **free** chunk `0x09a82010` (`size 0x4ff0`), contains **live graphics data**: `0x3f800000` (= 1.0f exactly), `0x3f7ffe97` (0.99999), `0x3f7653a0` (0.962), `0xbda83299` (−0.082), `0x3f36092e` (0.711), `0x4167327d` (14.45), `0x42e7f2cc` (115.97), plus `0xffffffff` (white RGBA) and `0x000000ff` (alpha). Floats clustered around 1.0 with a colour and an alpha byte is vertex / material / uniform data, and `0x80000001` sits among them as an integer field — so the renderer is still **writing this buffer every few frames after it has been freed**. That closes the whole evidence trail: the constant value (one owner, one field), the surviving low half (a 4-byte store), heap headers *and* chunk headers as victims (whatever the stale buffer overlaps once the allocator reuses the page), the GPU correlation, the cascade once it starts, and the clean `memtrip` (a stale pointer is targeted, not a scatter). ⚠ **C1 is therefore not an allocator defect at all** — the allocator is the detector. The remaining work is naming the allocation: chunk size **`0x4ff0`** (20 464 bytes) is distinctive and recurs across fires, so a per-large-chunk record of the ALLOCATING caller is the direct route to the buggy site. ↩ **2026-09-23, later — I over-claimed the root cause and am walking it back one step.** I wrote that "the renderer is still writing this buffer after freeing it" on the strength of one page dump containing graphics data. That does **not** follow: only `page+4` is poisoned, so the surrounding vertex-shaped words are unpoisoned and are equally consistent with **stale contents** of a previously-freed block. **What IS established, and it is still a lot:** (1) an **active** writer stores `0x80000001` into `page+4` repeatedly — the poison is re-applied every time the chunk is re-binned and is broken again, 4 times per firing run, so this is not one historical event; (2) the victim chunk is **`0x4ff0` (20 464 bytes) in all three fires** — one distinctive, repeating allocation; (3) the page around it holds a **2D/HUD vertex array** — 20-byte stride of `{float x, float y, RGBA, 0, 0}`, x ≈ 797→802 stepping 5.0, y ≈ 22–24, colour `0xff000000` — with `0x80000001` sitting exactly where one of the trailing zeros belongs; (4) the 256-entry free ring reports **no logged large free covering the page**, so the block was not freed through `free()` recently. ⚠ **The experiment that would settle live-vs-stale — poisoning the whole large chunk body and counting broken words — CRASHED the allocator** (harness rc=3, 0 seeds completing) and was reverted; the page+4 instrument is untouched and still validated. Redoing it needs a careful look at which parts of a large free chunk are genuinely unused, which the first attempt got wrong. ↩ **2026-09-23, later still — the "it crashes the allocator" verdict was WRONG, and the decisive instrument is now shipped.** The whole-body poison made the harness exit 3 with no output, which I read as a crash and reverted. It is not a crash: with `--no-selftest` the harness runs clean. The conflict is with the harness's own **detector self-test**, which deliberately corrupts free chunks to prove its detectors fire — and this poison overwrites that corruption. An allocator carrying it needs `--no-selftest`; the allocator was never at fault. Re-validated and shipped: 6 seeds × 60 000 ops, **2 944 251 free chunks, 0 violations, 0 false positives**. ★ **Why it matters:** `page+4` is the only poisoned word and a report only happens when it differs, so "page+4 is always the corrupted word" is **selection bias, not evidence** — and the vertex-looking data around it is unpoisoned, hence equally consistent with stale contents of a previously-freed block. The body poison ends that: `p4bodyN` / `p4bodyB` count how many body words come back broken, so **1 = an isolated stray store, many = somebody is still writing the block**. That is the live-vs-stale question the whole hunt now turns on. Soak so far: 3 runs, no fire (the page+4 rate is ~25%/run, so this is unremarkable). ⚠ **2026-09-23 — I broke the netboot bench with my own instrument, and graded seven void runs as clean.** The body poison lives in **libphoenix**, so it costs every process. Unrestricted it memset the whole body on every large free, which slowed the **lwip daemon** enough that DHCP no longer completed inside `nfs-fs`'s 30 s window — the Pi came up on the RAM root, where `/bin` does not exist, and every command returned `not found`. **Seven consecutive soaks were void** and I reported them as "no fire", because `0 poison reports` is also what a run that never executed the workload produces; the tell was an empty flip counter in all seven. Fixed by gating the body poison to the victim size class (`0x4000..0x6000`; every fire has been `0x4ff0`), which lwip's buffers fall outside. Bench verified restored: `dhcp_fail=0`, NFS mounted, `/dev/umass0` present. ⛔ **Two rules out of this:** anything added to libphoenix is paid by every process, so weigh it against the daemons the bench depends on — not just the app under test; and an instrument's silence is evidence only after the workload is asserted to have run, in the same command that reads the instrument. ⚠ **2026-09-23 — `memtrip` dropped from the soak: it was costing runs, not buying evidence.** A void run (caught immediately by the new flip-count assertion) turned out to be SuperTuxKart crashing at startup with two EL0 Data Aborts, `far=0x10`, and **no allocator guard firing**. Symbolized with self-validation — the instruction at `pc` is `ldr x0, [x28, #16]`, and 16 = `far` — as **`FontManager::loadFonts()`**, immediately after five `FontManager: Something wrong when ... is loaded` errors. That is a NULL font face dereferenced without a check, i.e. an allocation failure under memory pressure, **not C1**. The pressure is `memtrip`'s 64 MiB: it has never fired in ~10 runs, and being a separate process it structurally cannot see a CPU-side writer anyway (only DMA/GPU or kernel writers). So it costs runs and buys nothing on the current hypothesis — the in-process `page+4` poison is the live instrument. Soak continues without it. ↩ **2026-09-23 — the body poison is REVERTED: it appears to suppress the event it was built to measure.** Eight valid runs after it landed gave **0 page+4 fires** against the ~25%/run rate before it (p ≈ 0.10), and the mechanism is plausible rather than merely statistical — its size gate covers exactly the victim class, so it bulk-writes the very pages the corruption lands in, on every free, changing their cache and timing state right where the race happens. An instrument that is only useful when the event fires, and that seems to stop it firing, is worse than none. Kept: `page+4`, the recent-large-free ring, the page-head dump — the configuration that produced every fire so far. ✔ Corroboration: the harness detector-self-test conflict vanished with the revert, confirming the body poison was what it tripped over. **The live-vs-stale question stays open**; the next design should not bulk-write the victim pages — e.g. a checksum of the page taken at poison time and compared at verify time, two words instead of a whole body. ⚠ **2026-09-23 — the fire rate was over-estimated from a cluster, and I have been chasing it. 15 valid runs, 0 fires.** The page+4 instrument fired in 3 of its first 7 runs, which I took as ~40%/run and used to judge every later configuration. Since then: **15 consecutive valid runs (flip count asserted every time), 0 fires**, across four different configurations. Each suspicion in turn failed to hold up — the body poison (0/8 with it, but also 0/7 after removing it); `memtrip`'s presence (3/11 with, 0/11 without, Fisher p ≈ 0.21 once the later runs are counted, and restoring it at 32 and 64 MiB produced nothing). **The simplest reading is that the true rate is nearer 10–15% and the opening 3-in-7 was a lucky cluster**: at p = 0.15, seeing 3+ in 7 has probability 0.17 and 0 in 15 has 0.09, so both observations are unremarkable together, whereas at 43% the drought would be p ≈ 0.0002. ⛔ **Lesson for this row: do not re-derive a rate from the first handful of fires and then treat every later change as a suspect.** Four builds were spent on configuration churn that the statistics never justified. The instrument set is now stable — `page+4` poison, recent-large-free ring, page-head dump, and the new page-head checksum (`p4ckOK`, the live-vs-stale discriminator, host-validated on all four cases) — and the right move is to leave it alone and accumulate runs. ★★★★ **2026-09-23 — a designed A/B says ONE EXTRA CPU WRITE to the same cache line suppresses the corruption, which points hard at a NON-COHERENT (DMA/GPU) writer.** After 23 valid runs with no fire, the only instrument delta from the last firing run was the page-head checksum's extra store at `page+8`. Rather than guess again, the store was disabled and **N = 8 declared in advance**. Result: 6 valid runs, **2 fires** (3 and 4 reports; the other 2 runs were the known memtrip-64 OOM crash, 2 EL0 aborts in `FontManager::loadFonts()`, and are excluded). **2/6 vs 0/23, Fisher p ≈ 0.03.** ⭑ *Why this matters more than the rate:* `page+4` and `page+8` are in the **same 64-byte cache line**, so the only thing the extra store changes is that line's CPU-side state. A **CPU** wild pointer writes through the same coherent cache and could not be masked by another CPU write — but a **non-coherent DMA/GPU** write to RAM can be, since a later cache writeback simply restores the CPU's copy. So the suppression is evidence for the DMA/GPU hypothesis and against a CPU-side stray pointer. ⚠ The mechanism is inference; the suppression itself is measured. Both fires again had `p4csize = 0x4ff0` (4th and 5th time) and again `no logged large free covers this page`, and the page held a **20-byte-stride textured 2D vertex array** — `{float u, float v, float x, float y, RGBA}`, x ≈ 1815, y ≈ 145–175, i.e. 1080p HUD coordinates. ⚠ Also re-confirmed: `memtrip 64` costs ~25% of runs to an OOM crash; use 32. ★★★ **2026-09-24 — a second designed A/B points at the BO-close `munmap` as the route, but does NOT prove it.** Reading the V3D winsys with the non-coherent-writer result in hand: `ioc_close_bo()` clears the GPU PT (`va_free`, l.1143) **before** `munmap`ing the BO's CPU mapping, and that munmap hands the physical pages back to the kernel — where malloc can immediately re-acquire them. If any V3D cache line is still dirty for those pages, its writeback lands in the new owner. The driver already ships the knob for this (`V3D_KEEP_CLOSED_BO=1`), so it was flipped to default-on (psh has no `export`) with **N = 8 declared ahead**, and every run asserted the arm was live via the driver's own banner. Result: **munmap ON 2 fires / 6 valid; munmap OFF 0 fires / 8 valid**. ⚠ **Fisher p ≈ 0.17 — suggestive, NOT significant**, and confounded: the OFF arm used `memtrip 32` and the ON arm `memtrip 64`. So this is a lead, not a result. ✔ *Ruled out along the way:* the close path does clear the PT, and does so before the munmap, so the naive "munmap without unmapping the GPU" story is wrong; and the driver does issue `L2TCACTL` `FLM_CLEAN` write-backs in several post-job paths, so there is no blanket missing flush. **Next:** more runs in both arms to get past p ≈ 0.05, with the memtrip size held constant — and if it holds, the question becomes which V3D cache can still be dirty for a BO's pages at `GEM_CLOSE`. ⚠ The A/B arm has been reverted; the tree carries the documented default (munmap ON) again. ★★★★ **2026-09-24 — the missing piece named: we advertise `SUPPORTS_CACHE_FLUSH` and never honour it.** ↩ *Correcting the line above* ("the driver does issue `L2TCACTL` `FLM_CLEAN` write-backs in several post-job paths, so there is no blanket missing flush"): the write-backs exist, but the CL path's is **issued and never awaited**, and **nothing on the CL path drains the TMU write combiner** — the only drain in the file is the TFU epilogue, whose own comment states the combiner holds *partial cache-line writes that never reach RAM* on their own. That distinction is the whole point: a late L2T line writeback would restore a full 64 bytes of tile data, **not** a single stray u32 with its neighbours intact, so only a write combiner can produce the observed signature. Upstream confirms the contract — Mesa sets `DRM_V3D_SUBMIT_CL_FLUSH_CACHE` when `job->tmu_dirty_rcl && screen->has_cache_flush` (`v3d_job.c:694`), and Linux answers it with a `CACHE_CLEAN` job running `v3d_clean_caches()` (TMUWCF drain **and wait**, then L2T clean **and wait**) before the job is signalled (`v3d_submit.c:998` → `v3d_gem.c:202`). **`ioc_get_param` returns `SUPPORTS_CACHE_FLUSH = 1` (l.2584)** — which is also what makes Mesa expose shader images and SSBOs at all (`v3d_screen.c:173,178,305`) — **and `ioc_submit_cl` never reads `s->flags`.** We enable the TMU-writing feature set, promise the clean, and never do it. ✔ De-confounding of A/B #2 has begun: the first munmap-ON run at **`memtrip 32`** (matching the OFF arm) fired `p4=2`, with `flips=2606` asserting the workload ran — so the confound resolves in favour of the munmap variable. ★ **Rather than pay ~14 runs per arm for one bit, the next step watches the hypothesised dartboard directly.** `V3D_BO_QUARANTINE=1` (devices `24b3d5e`) holds each closed BO's pages instead of releasing them, fills them with `0xa5a5a5a5`, and scans them before they go back — reporting handle, offset, value and closes-since-close for anything that writes a **closed** BO. It is trustworthy because default BOs are `MAP_UNCACHED`, so fill and scan both hit DRAM with no cache line of ours to hide a foreign write behind; cacheable BOs (our dirty lines would mask it) and scanout BOs (decoupled scratch pages) are skipped because they could not answer. `=selftest` plants the exact C1 word so the detector is seen to fire before its silence is trusted. Arm B is `V3D_CL_CACHE_CLEAN=1`, the Linux sequence. **Predictions declared ahead:** quarantine fires *and* arm B silences it ⇒ diagnosis and fix together; quarantine fires and arm B does not ⇒ not a V3D cache; quarantine silent across runs that fire in the heap ⇒ the recycled-closed-BO story is wrong and A/B #2 was the memtrip confound. ⚠ A quarantine build changes malloc's page supply, so its `p4=` count is **not** comparable to earlier arms — grade that build by the tripwire, not by `p4=`. ⚠ Watch for new visual corruption under quarantine: the poison turns a would-be fault into garbage if Mesa ever reads a BO it has closed, which would itself be a finding. ✔ **A/B #2 de-confounded, 2026-09-24.** Four more munmap-ON runs at `memtrip 32` (matching the OFF arm), all four valid (`flips` 2606/2771/2668/2625): **2 fires / 4**. The memtrip size turns out not to matter much — the 32 subset (2/4) and the 64 subset (2/6) agree — so pooling is defensible: **munmap ON 4 fires / 10 valid, munmap OFF 0 / 8, Fisher p ≈ 0.069.** ⚠ Still *suggestive, not significant*, and the honest reading is that chasing the remaining 0.02 of p would cost most of a night for one bit. That is the argument for the quarantine instrument rather than more runs. ★ *Why the write-combiner mechanism is plausible for this specific workload:* `tmu_dirty_rcl` is set by three things in the compiler, and the third is **register spilling** (`vir_register_allocate.c:614`) — any shader complex enough to spill writes its registers to scratch through the TMU. The other two are image stores/atomics (`v3d_tex.c:623`) and non-load SSBO/shared/scratch/global intrinsics (`nir_to_vir.c:533`). So SuperTuxKart does not need to use shader images for this to fire; it only needs a shader that spills. The flagged-job counter in the build measures this rather than assuming it. ❌ **2026-09-24 — the write-combiner half of that hypothesis is DEAD, killed by its own counter.** First quarantine run (`c1qt-1`, 2531 frames at 8.04 fps — a full session, ~5% slower than the uninstrumented arm): **zero `FLUSH_CACHE`-flagged jobs in the entire run.** Checked the two ways that count could be wrong before believing it: Mesa *does* query the cap (`v3d_screen.c:827` → `v3d_has_feature`, and we answer 1), and `struct drm_v3d_submit_cl` is **byte-identical** between our vendored header and Mesa's, so `s->flags` is read at the right offset. So SuperTuxKart genuinely produces no job whose shaders write through the TMU — no spills reaching it, no image or SSBO stores. Nothing is parked in the write combiner, and a combiner that is never loaded cannot drain late. ⚠ The *contract breach* is still real and still worth fixing (we advertise `SUPPORTS_CACHE_FLUSH` and ignore the flag, so any future TMU-writing shader silently gets no clean), but it is **not** C1's writer. ✔ **What the quarantine itself says so far, and it is a real negative:** ~256 closed BOs held and fully scanned, **0 stray writes**, with the scan *proven on that same run* — the planted `0x80000001` was found at +4 of handle 8. ⚠ **Scope of that negative, stated carefully:** it catches a write **that targets a closed BO** — a stale pointer into the BO still resolves to the held pages, and a late cache writeback still targets their physical addresses. It says **nothing** about a stray into non-BO memory. ★ And that distinction exposes an inconsistency in my own KEEP=1 story, which has to be resolved rather than papered over: if `V3D_KEEP_CLOSED_BO=1` suppresses C1 because the write lands harmlessly on the retained page, then the quarantine retains those same pages and **must** catch it — and it did not. So either **(a)** the writer targets the BO's pages, and the next firing run's quarantine decides it; or **(b)** the writer targets something else entirely, and KEEP=1 and the quarantine both change the rate for a quite different reason — *they stop closed-BO pages returning to the kernel free pool*, which changes which pages malloc is handed. Reading (b) fits the observed content better than (a) does: the fired page held a **live** 20-byte-stride HUD vertex array, which in (b) is malloc memory that STK or Mesa still holds a pointer into — i.e. an ordinary **CPU-side use-after-free**, not a GPU or cache artefact at all. Both readings are held open until a firing run decides. ⚠ **But that run also had `p4=0`**, and at the ~40 %/run base rate a quiet run proves nothing. The decisive observation is a run with `p4 > 0` **and** `strays = 0` (⇒ the event happened and did not come via a closed BO's pages) or `strays > 0` (⇒ route confirmed). That is what the next runs are for. ★★★★★ **2026-09-24 — the first SIGNIFICANT result of this hunt: page retention suppresses C1, and the retained pages are provably never written.** Five quarantine runs (`c1qt-1..5`, 2531–2725 frames each, selftest found on every one): **0 heap fires, 0 stray writes into ~1000 held-and-fully-scanned closed BOs.** Pooling the two retention arms against the default — **retention (KEEP=1 8 runs + quarantine 5 runs) 0 fires / 13; default 4 / 10; Fisher p ≈ 0.024** — so the suppression is real, not the p ≈ 0.07 the munmap A/B alone could show. ⭑ **The two halves together are the constraint that matters:** the event needs closed-BO pages to go back to the kernel (retention stops it), yet nothing writes those pages while we hold them (a proven scan over ~1000 of them saw nothing). A writer simply aimed at the BO's memory would have been caught. ⚠ **And `memtrip`'s long-standing null has to be re-read, including in my own earlier notes.** It has never fired — not even in `c1armA-1` and `c1armA-3`, which fired `p4` 2 and 4 times in the same runs. I had taken that as evidence the write is *aimed* at something about heap headers. It is not evidence of that: **memtrip `mmap`s its region at process start, before any BO has been closed, so it can never receive a recycled BO page.** Its silence is exactly what the retention result predicts, and it constrains nothing. ✔ *Also settled this round:* 0 MMU/PT aborts under quarantine, so no stale-GPU-VA write is occurring; and the kernel's cache-attribute maintenance is NOT the gap — `_pmap_cacheOpBeforeChange` (`hal/aarch64/pmap.c:271`) does flush on cached→uncached and on unmap, which kills a cache-aliasing story before it cost a build. **Next:** attribute a *fired* page. `V3D_BO_TRACE` is now default-on and the quarantine default-off (it suppresses the very event it would report), so a firing run can be cross-referenced with `scripts/c1-was-it-a-bo.sh`: if the fired page falls inside a previously-CLOSED BO's `cpu=` range the closed-BO route is real; if it never was a BO, C1 is a CPU-side use-after-free and the whole GPU framing goes. ⚠ **Attribution arm, first four runs: 0 fires / 4 — and I changed two things at once, which is my error, not a result.** `c1attr-1..4` ran the new build (`V3D_BO_TRACE` on, ~1470 trace lines per run, quarantine confirmed off in the log) **without `memtrip`**, which I dropped because I had just shown it cannot see this event. But all 10 runs that established the 4/10 baseline had memtrip resident, and a continuously-touched 32 MiB region changes page-recycling dynamics — the very mechanism under test. So these four runs are not comparable to the baseline and say nothing about whether the trace perturbs the fire. Re-running the identical baseline configuration (memtrip 32 restored) with only the trace added. ★★★ **2026-09-24 — the pattern that reframes the hunt: EVERY instrument so far suppresses the event.** The BO-trace arm finished **0 fires / 8** (4 without `memtrip`, 4 with it restored to match the baseline). The allocator instrument was live in all of them — 12–16 `C1-hunt: created` lines per run, checked explicitly, so these are real zeros and not void runs. Tabulated against the same baseline: **default 4/10 · `V3D_KEEP_CLOSED_BO=1` 0/8 · quarantine 0/5 · BO-trace 0/8.** Individually none of the three clears 0.05 (0.07, 0.15, 0.07); jointly, three unrelated interventions each pushing the rate to zero is the signature of a bug that is **fragile to perturbation** — a race or a layout-dependent latent write — rather than a deterministic bad pointer. ⛔ **That makes UART tracing and page-withholding the wrong tools**, and it explains in retrospect why nine-plus mechanisms have been 'eliminated' by arms that may simply have been suppressing the bug. ★ **New instrument, built for that constraint: silent and passive.** The winsys and malloc share a process, so malloc now simply asks the driver what a corrupted page used to be — `c1_seen[]` records every BO's CPU range and close ordinal (two stores per BO, zero output) and `v3d_c1_lookup_page()` is called only at fire time, printing `p4bon` (how many BOs covered that page — above one means the address was recycled between BOs), `p4boh`, `p4booff` and `p4bocl` (close ordinal, 0 = still open). Declared weak in libphoenix so a driverless binary still links and says so. devices `5a2a077` + libphoenix `1ea2083`; verified `T v3d_c1_lookup_page` is a strong definition in the linked binary, so the weak reference really did resolve. `V3D_BO_TRACE` and the quarantine are both back to default-off. ⛔⛔ **2026-09-24 — METHODOLOGICAL CORRECTION that invalidates the causal reading of every A/B in this hunt, including my own `p ≈ 0.024`. `psh` DOES have `export`.** The claim "psh has no `export`" is false and is repeated in this row, in the weekly log and in my working notes. It is a registered builtin — `pshapp/env.c:154`, `app_export`, calling `setenv(name, value, 1)` — and it **propagates to children**: measured directly on the bench (`c1envtest`), `export V3D_BO_TRACE=1` produced **1342 trace lines in a binary where the trace is compiled default-OFF**. ⭑ *Why this matters so much here:* because of that false premise, **every arm of every A/B was compiled in as a default, so every comparison was between DIFFERENT BINARIES** — and this bug is demonstrably layout/perturbation-sensitive. Post-baseline the tally is **0 fires in 24 runs** across four separate builds (KEEP 0/8, quarantine 0/5, BO-trace 0/8, passive 0/4) against 4/10 in the baseline binary; at a 40 % rate that is p ≈ 5e-6, so the rate is not stable across relinks. Even the 'passive' instrument adds 96 KB of static BSS to the driver, which shifts every address after it. **So `retention 0/13 vs default 4/10` is confounded with 'two rebuilt binaries vs one older binary', and the same objection applies to the munmap and checksum A/Bs.** The *observations* stand; the *attributions to the knob* do not. ✔ **Fix, and it is cheap:** arms are now selectable inside ONE binary — `export V3D_KEEP_CLOSED_BO=1` as a psh line before the workload, with the arm asserted from the driver's own banner as before. Every future C1 A/B must be run that way, interleaved, in a single build. ✔ **The firing layout was RESTORED, which proves the layout sensitivity outright — and the first sound A/B is inconclusive.** Reverting just the two source files touched tonight (`v3d_phoenix_winsys.c` to `24b3d5e^`, `malloc_dl.c` to `1ea2083^`) and rebuilding brought the bug straight back: `ENV-off-1` fired `p4=4` on the first run, after **0 fires in 24 runs** across the four instrumented builds. Same sources, same bench, same night — so the suppression really is the relink, not drift or luck. ⭑ Then the A/B as it should always have been run — **one binary, arms chosen by `export`, interleaved off/on/off/on/off/on**, each arm asserted from the driver's own banner (`keepbanner` 0/1/0/1/0/1) and the allocator instrument verified live in all six (14–16 heaps): **KEEP off 1 fire / 3, KEEP on 0 / 3.** ⚠ **That is no evidence either way** — 3 runs per arm cannot resolve a ~30 % effect (Fisher p = 1.0), and the off-arm rate of 1/3 is simply consistent with the 4/10 baseline. **It does not confirm the retention effect and it does not refute it.** The value of the run is that the method is now sound and the firing binary is reproducible. **Next:** continue this exact interleaved A/B to ~12 runs per arm in this binary — about 4 hours of bench — before any further mechanism work. ⚠ **Standing trap for whoever picks this up:** the attribution instrument and the firing layout are mutually exclusive as things stand, because `c1_seen[]` is compiled in unconditionally and its 96 KB of BSS shifts every address after it. Gate it behind a compile-time switch (default off) so master builds the firing layout, and accept that turning it on is itself a new layout. To recreate the firing binary: `git -C sources/phoenix-rtos-devices checkout 24b3d5e^ -- gpu/rpi4-v3d/mesa/v3d_phoenix_winsys.c`, `git -C sources/libphoenix checkout 1ea2083^ -- stdlib/malloc_dl.c`, then `rebuild-rpi4b-fast.sh --with-tests --with-ports --with-showcase` and `sync-netboot-tree.sh`. ★★★★★ **2026-09-24 — the restored build is BYTE-IDENTICAL to the baseline, and reading the fire it produced reframes C1 entirely.** `sha256` of the rebuilt unstripped binary matches the archived 2026-09-23 one to the byte (`b427922a09694e51…`), so builds are deterministic and the firing layout is exactly recoverable — and the baseline runs and tonight's `off` runs are the SAME binary and pool: **off 5 fires / 13, KEEP-on 0 / 3 in that same binary (Fisher p ≈ 0.51, underpowered).** ⭑⭑ **The four fires in `c1env-off-1` carry the answer the rate never would.** All four: `p4got = 0x80000001`, `p4csize = 0x4ff0` (now 8 times running), `p4call` valid by its complement check and symbolizing to `operator new(unsigned long)` with `bl malloc` immediately before it — a genuine return site, though it names the *detector's* caller, not the corrupter. Two distinct pages, each reported twice. **And the page dumps are structure, not noise:** at page `0x0b916000` the words are `[+0]=0x0b916000, [+4]=0x80000001, [+8]=0x0b916048` — a pointer to the page itself, and one to page+0x48. Read as little-endian 64-bit, bytes 0–7 are **`0x800000010b916000`: a valid heap address with `0x80000001` in the UPPER 32 bits** — which is exactly the signature of the separately-recorded "STK high-bits pointer crash" (`far=0x800000010c845168`). **The two are very likely one defect.** ⭑⭑⭑ **Decisive field, and I had not been weighting it: `p4ckOK = 0` on all four fires.** The instrument's own documented semantics: `p4ckOK=1` means only page+4 changed (an isolated stray store into otherwise stale memory); **`p4ckOK=0` means the page was STILL BEING WRITTEN — a live buffer that the allocator believes is free.** So C1 is most economically read not as a stray 4-byte write at all, but as **a chunk on malloc's free list that is still in use** — a use-after-free or an allocator bookkeeping error — with `0x80000001` simply a field of whatever object lives there. That also explains why the closed-BO quarantine found nothing, why `memtrip` never fires, and why every mechanism aimed at a GPU-side writer has come up empty. It connects directly to the open allocator-bin-corruption issue rather than to the graphics stack. ✅ **The sound A/B, finished: 12 interleaved runs in ONE binary — off 3/6, on 0/6, Fisher one-tailed p = 0.091; pooled with the 2026-09-23 baseline (same byte-identical binary) off 7/16 vs on 0/6, p = 0.067.** Every arm asserted from the driver's banner (`keepbanner` alternating 0/1 across all 12), allocator instrument live in all 12 (14–16 heaps each), `memtrip 32` in all 12. **Suggestive, not significant** — the retention effect survives de-confounding in *direction* and *size* but still does not clear 0.05, and it needs roughly 12 runs per arm rather than 6. ⚠ Read it as the honest replacement for the withdrawn `p ≈ 0.024`, not as confirmation of it. ➡ **To continue: the bench is on the firing layout and master rebuilds it** — the hunt instruments are now behind `-DV3D_C1_HUNT` (devices `b57a7a6`, libphoenix `e69c33d`), verified to compile both ways, so a stock build keeps reproducing C1 and turning the instruments on is an explicit, re-baselined choice. Just run more `export V3D_KEEP_CLOSED_BO=1` / unset pairs. ⭑ **But the higher-value lead is now the `p4ckOK = 0` reading above** — a live object sitting in a chunk malloc thinks is free, with `0x80000001` as the high half of a 64-bit pointer. That points at the allocator/use-after-free line of enquiry and at the high-bits crash signature, not at the V3D page lifecycle the retention knob touches. ✅ **Close-out check: master reproduces C1.** The guarded rebuild is *not* byte-identical to the firing binary (`47a7020625f0…` vs `b427922a0969…`) — the guards cover the instruments, but two genuine unguarded changes remain in the same file: the `cacheable` field added to `struct pbo`, and the `FLUSH_CACHE` handling in `ioc_submit_cl`. Byte-identity was only ever a convenience, so it was checked the way that matters instead: three runs on the shipped master build, of which **one was VOID** (boot reached psh, lwip, DHCP and an IP, but the harness closed its window at 30 s before sending the commands — `frames=` and `heaps=0`, so it is recorded as void, not clean) and **1 of the 2 valid runs fired `p4=4`**. So the default build still reproduces the bug and the hunt can continue on master without the two-file revert. ★★★★★ **2026-09-24 — ROOT CAUSE IDENTIFIED (mechanism, not yet fix-verified): a VideoCore mailbox page is `munmap`ed while the firmware still owns its PHYSICAL address.** Four independent sites show the identical signature — the **high 32 bits** of a 64-bit word replaced by `0x80000001`, low half intact: the malloc heap header (`0x80000001_0000d000`), a Mesa `hash_table::mem_ctx` (`0x80000001_0b916000`), the STK crash `far` (`0x80000001_0c845168`), and — decisively — **a KERNEL zone list link** (`vm/kmalloc.c:62`: `next = 0x80000001c46ccf80` where `0xffffffffc46ccf80` belonged). `malloc_dl.c:1436` records it as **154 of 154 identical**. ⭑ **No userspace use-after-free can corrupt a kernel pointer**, so the entire allocator/GPU-BO framing this hunt has been built on is the wrong tree. A *constant* 32-bit value landing at **+4 of a page in both address spaces** is a device writing to a PHYSICAL address — and `0x8000000x` is the VideoCore mailbox property-interface response code, written by the firmware into **`msg[1]`, i.e. offset +4** of the property buffer (`VC_MBOX_RESP_OK 0x80000000` is used in four of our drivers). ⭑⭑ **The defect, in source:** `mboxProp()` (`gpu/rpi4-v3d/v3d_gpu.c:268`) `mmap`s a fresh page per call, takes `va2pa()`, rings the doorbell, waits for the reply with a **bounded** `MBOX_SPINS` loop, and then `munmap`s the page **unconditionally — including on the timeout path, after the request has already been sent.** The page returns to the kernel free pool while the VideoCore still holds its PA; the late reply lands at +4 of whatever owns that page next. `rpi4-wifi.c:132`, `bt/rpi4-hci.c:117` and `pcie/server/pcie.c:232` repeat the same mmap/va2pa/bounded-wait/munmap shape. The one user that is SAFE is `misc/rpi4-vcmbox`, which keeps a **persistent** bounce buffer (`vcmbox.buf`/`buf_pa`) and never unmaps it — that is the model for the fix. ✔ *Every stubborn observation now has an explanation:* the constant value and exact +4 offset (it is `msg[1]`); kernel **and** user corruption (physical write, MMU-independent); why `memtrip` never fired and the closed-BO quarantine saw nothing (a mailbox page is neither); why page-retention knobs moved the rate (they change which page is recycled into the line of fire); and `p4ckOK = 0` (a live foreign writer — the firmware). ⭑ Corroboration: the V3D clock-race note records **`MBOX_FAIL` at 9 % of calls** before serialisation — 9 % of mailbox calls took exactly this timeout-then-munmap path. ⚠ **Not yet fix-verified**, and proving it by fire rate alone is unsound here: four instrumented rebuilds already produced 0 fires / 24 purely from relinking. The fix must be argued from the source defect and checked for a *causal* link (log the post-doorbell munmap), not from a handful of quiet runs. ↩ **DOWNGRADE, same day: "ROOT CAUSE IDENTIFIED" above is too strong — call it a strong candidate mechanism that is NOT yet shown to occur.** The signature analysis stands (one constant at +4 of a page, in kernel *and* user space, 154/154 — a device writing to a physical address, and the value is the mailbox response code), and the `munmap`-after-doorbell defect is real and now fixed. What is missing is evidence that the defect's **precondition actually fires**: a post-doorbell timeout. Two measurements, both currently negative. (1) `v3d_phoenix_powerOn` runs in **every** STK run including all six that fired — so the in-process `mboxProp` is definitely on the path — but its clock-confirm retry line reads **0 retries in all 37 graded runs**. ⚠ That is weak evidence only, because the `SET_CLOCK_STATE` / `SET_QPU_ENABLE` / `SET_DOMAIN_STATE` calls are `(void)mboxProp(...)` with the return value **discarded**, so a timeout there is silent and would never show as a retry. (2) The fix adds a log line that fires exactly on a post-doorbell timeout; the first run with it reads **`leak=0`** — and that run also did not fire, so it is consistent either way and settles nothing. ➡ **The decisive observation is a run with `p4 > 0` and `leak = 0`: that would exclude this mechanism for that fire.** Conversely any `leak > 0` proves the precondition occurs. Timing favours the hypothesis — `v3d_phoenix_powerOn` runs at STK startup, moments before the corruption appears, and a late firmware reply arrives in microseconds, not minutes, which also rules OUT the boot-time wifi caller as a source of gameplay-time corruption. ⚠ Keep the fix regardless: handing a page back to the kernel while a device still holds its PA is a defect on its own terms. ⚖ **Fix build, 4 runs: `leak = 0` and `p4 = 0` in all four** (2568–2688 frames, allocator instrument live 14–16 heaps each). **Uninformative on both counts, deliberately recorded as such:** 0 fires in 4 is exactly what a relink produces here regardless of the fix, and `leak = 0` means the post-doorbell timeout never executed, so the candidate mechanism had no opportunity to act. ✔ The `leak` counter is a *clean* measurement though, not a proxy — it lives inside `mboxProp()` itself, so it also covers the `(void)mboxProp(...)` callers whose return value is discarded. **C1 stays OPEN.** The fix is kept on its own merits. ★★★ **2026-09-24 — the response code's MEANING sharpens the candidate into a self-consistent loop.** In the RPi mailbox property interface the request/response word is `0x00000000` = process request, **`0x80000000` = success**, **`0x80000001` = "error parsing request buffer"**. Every C1 observation is the **error** code, never the success code — and the firmware only emits it when the buffer it read was malformed. That closes the loop: a slow firmware makes our bounded spin time out → we `munmap` the page → the kernel reuses it → the firmware *finally reads* it, finds somebody else's data instead of a property message, and writes `0x80000001` back at **+4** of the now-reused page. ⭑ Note this moves the critical window from "firmware writes late" to **"firmware READS late"**, which is far wider and also explains why the timeout happened at all. It further predicts the fix's `leak` counter must be non-zero in any run that fires — a sharp, falsifiable link. ✔ *Audit of the bug class across the Pi 4 tree (the `va2pa`-then-release sites) produced no second candidate:* of the transient-PA users actually built for this target — mailbox, xHCI (`libusbxhci`; **EHCI is not built here**, the Pi 4 uses VL805 xHCI), genet packet buffers, SD ADMA2 descriptors, V3D BO pages — only the mailbox can write a **constant** value at a **fixed** offset; packet and descriptor writes vary in both. ⚠ Separately noted, not actioned (different target, not built here): `usb/ehci/ehci.c` `ehci_transAbort()` forces every transfer finished and frees its qTDs without the EHCI Interrupt-on-Async-Advance doorbell handshake, which is the same release-before-the-device-is-done class. ⚖ **Three nulls against the mailbox candidate — but read their COVERAGE before weighing them.** Across every C1 log ever captured: **0** `RACED` (the vcmbox server never consumed a non-matching FIFO entry, which a stray late response would produce), **0** `BADRESP`, **0** `WRFULL`, and **0** clock-confirm retries in 37 graded runs — i.e. not one mailbox transaction is recorded as having failed. ⚠ **However, none of those three covers the suspect path.** The in-process `mboxProp` (`v3d_phoenix_power.c`) prints nothing on failure and its callers are `(void)mboxProp(...)`, so its failures are invisible; and the `RACED`/`BADRESP`/`WRFULL` diagnostics belong to the **vcmbox server**, covering only the server's own calls. The single instrument that does cover the suspect path is the `leak` counter added with the fix, and it stands at **0 across 4 runs, none of which fired** — consistent with either answer. ➡ So the candidate is **undetermined, not refuted**; my earlier ~70 % confidence is not supportable on this evidence and should be read as roughly a coin-flip until a firing run is graded with the `leak` column. ★★★ **2026-09-24 — a POSITIVE CONTROL now exists, and its first run fired.** Waiting for the precondition to occur naturally was not converging (0 fires / 6 on the fix build, `leak=0` throughout), so the defect is now provoked on demand: `export V3D_MBOX_LEAKTEST=<n>` (devices `1477be7`, corrected in the follow-up) makes power-on issue n benign `GET_FIRMWARE_REVISION` queries that ring the doorbell and release their page **unwaited** — the defect, n times, with the real power sequence untouched. ⚠ First attempt at n=256 **did not finish**: posting 256 requests while consuming no replies filled the FIFO, so every later call burned its whole 4,000,000-spin budget on the pre-doorbell FULL check and STK never rendered (system stayed healthy — psh reached, GPU powered on). Fixed by releasing the page *first* (that ordering **is** the defect) and only then reconciling one FIFO entry. ✔ **At n=64 the control completes and the run FIRED: `p4=4`, 2586 frames, 0 faults** — the first fire on any post-fix build. ⚠ **Not yet evidence:** one run against a ~1-in-3 baseline proves nothing, and the fire's page held ordinary float/RGBA vertex data rather than a mailbox message — which the hypothesis predicts anyway (the page is recycled, refilled by the app, *then* the late reply lands on it), so **content cannot discriminate and rate must**. An interleaved off/on A/B is running **in the one binary**, arms selected by `export`, so it is not confounded by a relink. ⚖ **Control A/B #1 (release AFTER the doorbell), one binary, arms by `export`: ON 2 fires / 4, OFF 0 / 3, Fisher p ≈ 0.29.** Directionally right, not conclusive — and the weakness is the control's design as much as the sample size: `GET_FIRMWARE_REVISION` is answered quickly, so the firmware had usually already written its reply before the page was freed, giving a **microsecond** window where the real defect opens a timeout-scale one. **Rebuilt to release the page BEFORE the doorbell**, which guarantees VideoCore reads and replies into an already-reclaimed page and removes the confound. A/B #2 running on that. ⛔ **STOPPING HERE per the two-attempts rule — a careful negative.** Control A/B #2 (page released **before** the doorbell, so VideoCore is guaranteed to touch an already-reclaimed page): **ON 0 fires / 3, OFF 1 / 3** — no effect, if anything inverted. Both controls, one binary each, arms by `export`: **#1 (release AFTER the doorbell) ON 2/4 vs OFF 0/3, p ≈ 0.29; #2 (release BEFORE) ON 0/3 vs OFF 1/3.** ⭑ **Why #2's null does NOT refute the mechanism, and why #1 is the meaningful design:** freeing the page before the doorbell means the firmware reads a garbage size/tag word, so it can simply **reject the request without writing anything**. The real defect needs a *valid* request to be accepted first and the page freed afterwards — which is #1, and #1 is the one that leaned positive. The two therefore bracket the mechanism rather than settling it, and the honest summary is that **two designed experiments both came out inconclusive**. ➡ **Verdict: the mailbox candidate is NOT established and C1 stays open.** The `munmap`-after-doorbell fix is kept on its own merits (handing a page back while a device holds its PA is a defect regardless). The control is now compile-gated behind `-DV3D_C1_HUNT` — it corrupts memory deliberately, so an env var alone is too easy to trip on a shipped image. ➡ **To continue:** run control **#1** (not #2) to ~12 runs per arm — it is the design that matches the defect's ordering, and at 2/4 vs 0/3 it needs power, not redesign. What still stands independently of all this is the **signature analysis**: a constant 32-bit value at +4 of a page, in kernel *and* user space, is a device writing a physical address, and no userspace use-after-free can produce the kernel instance. ⛔⛔ **REFUTED, cleanly, by the close-out run: the timeout-leak mechanism is NOT what fires C1.** The shipped master build (fix in, control compiled out — verified by `strings`) fired **`p4=4`**, 2507 frames, 0 faults, psh reached. Put that beside the other measurement: the `leak` counter reads **0 in every run of the fixed build**, i.e. **the post-doorbell timeout path never executes at all** — so the code the fix guards is never reached, yet C1 fires anyway. A mechanism that never runs cannot be causing the corruption. That is a real refutation of the *specific* mechanism, arrived at without needing the underpowered control A/Bs. ✔ **What survives:** (a) the `munmap`-after-doorbell defect is real and the fix stays — it is simply latent here; (b) the **signature analysis is untouched** — a constant 32-bit value at +4 of a page, appearing in kernel *and* user address spaces, is a device writing to a physical address, and no userspace use-after-free can produce the kernel instance (`vm/kmalloc.c:62`). So the search is still "which device writes a constant at a fixed page offset", but it is **not** the V3D power mailbox's timeout path. ➡ Next candidates must be devices that write a constant to a physical address *on a path that actually executes during a run*; the `va2pa`-then-release audit already ruled out xHCI, genet, SD ADMA2 and V3D BOs on the grounds that their writes vary in value and offset — that reasoning should be re-examined now that the mailbox is out, since it was the argument that left the mailbox as the unique fit. ⛔⛔ **2026-09-24 — the "always at page+4" premise is an ARTEFACT OF THE INSTRUMENT.** The poison armed exactly **one word per 4 KiB page, at +4** (`malloc_c1P4Poison`), so it was blind to 1023/1024 of every page and could never have reported any other offset. Everything built on "the writer targets a fixed offset" is therefore unsupported — including the exclusions of genet, xHCI and SD ADMA2 ("their writes vary in offset"), which is precisely what left the mailbox looking like the unique fit. ✔ **Fixed:** four probes per page at `+4`, `+0x404`, `+0x804`, `+0xc04`, each keyed on its own address so a block copy cannot alias one onto another, and the report now prints `p4off`/`p4addr` and dumps around the *fired* word rather than the page head. ✔ **Proven able to fail before use** (`tools/malloc-harness` `hz_p4Probes()`): each offset corrupted in its own cycle, all four reported correctly with distinct poison values. ➡ Six runs in flight: **only +4 fires ⇒ offset-specific writer, framing survives; other offsets fire ⇒ the framing and every exclusion built on it must be redone.** ⛔⛔⛔ **RESULT: the offset experiment is itself suppressed — 0 fires / 6 with four probes armed, against ~1-in-3 with one.** So the "always page+4" premise **remains untested**; it is not confirmed and not refuted, and every exclusion built on it (genet, xHCI, SD ADMA2) is still unsupported rather than wrong. ⭑⭑ **This is now the single most important fact about C1, and it outranks every mechanism hypothesis: FIVE consecutive instrument changes have each driven the fire rate to zero** — `V3D_KEEP_CLOSED_BO` (0/8), the closed-BO quarantine (0/5), the BO trace (0/8), the passive attribution table (0/4), and the 4-probe poison (0/6) — while the unmodified binary fires about 1 run in 3 and a byte-identical rebuild of it fires again on demand. **C1 cannot currently be studied by ADDING instrumentation**: any change to the binary's write pattern or layout stops the event happening. ✔ **Consequences applied:** the 4-probe set is now opt-in (`-DC1_P4_WIDE`), so master keeps reproducing the bug; both configurations syntax-check; `tools/malloc-harness` `hz_p4Probes()` proves every offset can fail, so the widened instrument is trustworthy whenever someone chooses to pay the suppression cost. ➡ **Methodological rule for whoever continues: stop adding instruments.** The only moves that do not destroy the phenomenon are (a) env-selected arms inside ONE unmodified binary (`export`, now known to work), and (b) longer runs on the stock build. Anything requiring a relink needs its own baseline re-measured before it means anything. ⚠ **Close-out caveat, stated rather than glossed: master's reproduction is NOT re-verified.** After returning the poison to one probe, 3 runs gave 0 fires — at ~1-in-3 that is P ≈ 0.30, i.e. consistent with the rate being restored but no evidence of it. And master is **not** the historical firing binary in any case: even at one probe the poison is now keyed on the probe address rather than the page, so the values and the code differ. ➡ **The reliable reproducer is the archived one**, and it is byte-identical recoverable — ↩ but **the two-file recipe first recorded here was WRONG and produced `1d883a9a…` instead**. The STK binary links three devices files, not one: revert **`gpu/rpi4-v3d/mesa/v3d_phoenix_winsys.c`, `gpu/rpi4-v3d/mesa/v3d_phoenix_power.c` and `gpu/rpi4-v3d/v3d_gpu.c`** to `24b3d5e^`, plus `stdlib/malloc_dl.c` to `1ea2083^` (`wifi/rpi4-wifi/rpi4-wifi.c` also changed but is not linked into STK), rebuild with `--with-tests --with-ports --with-showcase`, and check `sha256` against `artifacts/c1-symbols/supertuxkart-b427922a09694e51.elf`. **Verified 2026-09-24: that recipe reproduces `b427922a09694e51…` exactly.** ⚠ The lesson generalises — "revert the file I edited" is not the same as "restore the binary"; always confirm by hash before pooling results across sessions. That binary fired on its first run after 0-in-24 across four instrumented builds. Start any future measurement there. ✅✅ **2026-09-24 — FIRST STATISTICALLY SUPPORTED RESULT IN THIS ISSUE: `V3D_KEEP_CLOSED_BO=1` suppresses C1. OFF 7 fires / 19, ON 0 / 12, Fisher one-tailed p = 0.019.** Every one of those 31 runs is the **same byte-identical binary** (`b427922a09694e51…`, restored and hash-verified this session), with the arm chosen by `export` rather than by a rebuild — so for the first time here the comparison is free of the relink confound that invalidated every earlier A/B. Arms asserted per run from the driver's own banner, allocator instrument live in all 31 (12–16 heaps each). ⚠ **Honest about where the power comes from:** tonight's 9 runs *alone* are uninformative (ON 0/6, OFF 0/3, p = 1.0); the significance comes from pooling the 19 OFF runs accumulated across three sessions **in the identical binary**. That pooling is legitimate — same binary, same harness, same workload — but it is pooling, and a single session has never reached significance on its own. ⭑⭑ **What it does and does not say.** It establishes that **retaining closed-BO pages prevents the event**. It does **not** say the writer targets those pages: the closed-BO quarantine held ~1000 of them across 5 runs with a per-run-proven scan and found **nothing written to them**. Both facts together point at one reading — *the event requires closed-BO pages to be RECYCLED into the kernel's general pool*, and whatever lands on them after recycling is still unidentified. ➡ That is now the sharpest open question, and it is answerable without adding instrumentation: compare what the kernel does with a page freed by `munmap` of an uncached `MAP_CONTIGUOUS` BO against an ordinary anonymous page. 🔧 **2026-09-24 — the measured lever turned into a bounded MITIGATION (not a fix): a BO page pool, `V3D_BO_POOL=1`.** `V3D_KEEP_CLOSED_BO=1` suppresses C1 but is unshippable (never releases; ~299 closes / ~96 MiB per six-lap run). The pool gets the same "pages never go back to the kernel" property with bounded memory by recycling them in-driver: a closed BO's mapping goes on a size-keyed free list and the next create of that exact size reuses it. Exact-size only, cacheable BOs never pooled, zeroing and PT setup shared with the fresh path, capped at 256 entries / 24 MiB with spill to `munmap`. Off by default so both arms live in one binary. **First working run: 202 hits / 1464 takes, peak 24 MiB (at the cap), 25 spilled, 2630 frames, 0 faults.** ⚠ **A ~12× reduction in exposure, NOT elimination** — 25 pages a run still return to the kernel, so if the mechanism is real some residual rate should remain; raising the cap would trade memory for further reduction. ⚠ It is a mitigation: the writer is still unidentified. ⚖ **BO-pool A/B, one binary, arms by `export`: ON 0 fires / 6, OFF 2 / 6, Fisher one-tailed p = 0.227.** Directionally consistent with the KEEP result and worth having as an **independent replication in a DIFFERENT binary** — but underpowered on its own, and complete suppression was never the prediction: the pool still spills ~25 pages a run to `munmap`, so some exposure remains by design. Both fires landed in the OFF arm; the pool arm was quiet in all 6 while holding ~24 MiB. ⛔ **Deliberately NOT made the default.** 24 MiB of retained mapping is a real cost on this board, the evidence is p = 0.23 rather than a fix, and the writer is still unidentified — flipping a GPU-driver default on that basis would be exactly the kind of unearned change this issue has already produced too many of. It stays `V3D_BO_POOL=1`, off by default, ready for an owner decision. ➡ To settle it: more runs per arm in this binary, and raise `BOPOOL_MAX_BYTES` to cut the spills if the effect holds. |
| C3 | **Two libc globals read back zero, killing the process at `main`.** `stderr` and psh's applet list are both unset, so `fprintf` dereferences a NULL `FILE*` (`far=0x30`). Both live in one 4 KiB `.bss` page. A source review gave a clean negative on every kernel mapping path, so "those writes definitely execute" is the assumption the next occurrence has to test. | **2 sightings (2026-09-07, 2026-09-15), none since.** Impact limited to `ntpclient`, which only sets the clock. The label added to make a recurrence self-explaining (`applet list is empty`) has **never fired** in the whole archive. ✔ **2026-09-24 — two constraints added, and one suspect cleared.** (a) **The two globals come from DIFFERENT mechanisms at different times**: `stderr` is assigned in `stdio/file.c:1565` reached from `_libc_init()`, while psh's applet list is filled by an `__attribute__((constructor))` (`pshapp/env.c`) run later from `_init_array()` — and `crt0-common.c:_startc` runs `_libc_init()` *then* `_init_array()` *then* `main()`. So this is **not** "constructors didn't run": two independent writes, both lost, which supports the page-level framing (a written page replaced by a fresh zero page) over an init-order one. (b) ⛔ **`vm/amap.c:amap_create()` is CLEARED as a suspect — do not re-walk it.** It looks wrong at first read: it does `*offset = *offset / SIZE_PAGE` and the whole amap API otherwise takes BYTE offsets (`amap_page`/`amap_clear` both do `offset / SIZE_PAGE` internally; `e->aoffs` is byte-based at every site in `map.c`, incl. `+= len` on entry split). But the division is only a scratch reuse of the caller's out-parameter to index the OLD amap in the copy loop, and the function **ends with `*offset = 0`**, which is the correct new value (the entry starts at the beginning of the freshly created amap). No early return sits between the two, so it is correct today — though reusing the caller's variable as scratch is fragile, and a future early return there would silently corrupt every later `amap_*` index for that entry. |
| C4 | **Intermittent V3D binner wedge on long GPU runs.** Auto-recovered by a GPU reset that reports loudly (`RENDER TIMEOUT` / `DROPPED job`). Not an MMU fault — a CT0 front-end stall with zero error status, `ct0ca` parked inside a valid BCL BO. `q3dm7` is the documented reproducer. | **Not reproduced since 2026-09-09** (and that was an experiment-labelled run; last sighting on a shipping build 2026-08-22). 0 wedge signatures in every log 09-10 → 09-22, including 15 Quake III runs and both six-app gates. Diagnostics are armed, so the next occurrence self-explains. |
| C5 | **PWM audio DMA stalls at startup on some boots**, which used to hang Quake II inside `SDL_OpenAudio()`. Captured three times: the channel is alive but parked for a DREQ that never comes, ring full, 0 samples moved. Mechanism open. Blast radius is contained — the driver now grades the channel by progress, re-arms, and degrades to a paced null sink. | **Mechanism unfixed; symptom quiet.** 25/25 Quake II runs since 2026-09-18 reached `SDL audio initialized` and drew frames; 0 stall aborts in any log 09-19 → 09-22. ⚠ Timing-sensitive: the rate has moved across functionally neutral relinks, so short clean runs bound it rather than clear it. |
| C6 | **psh forks `ntpclient` fire-and-forget on session start**, so a ~56-year wall-clock step can land anywhere in a ~2-minute window. Any app that measures a duration in *wall-clock* time across its own startup sees nonsense. | **The one app that ever failed this way is fixed; the systemic exposure remains.** ✅ quake3's `Sys_Milliseconds` now uses `CLOCK_MONOTONIC` (`ports fd34552`) — it was the only port glue still using `gettimeofday`; quakespasm, vkquake and sdl2 already used monotonic. The property is measured, not assumed: `tools/clockstep` steps the wall clock 56 years on hardware and monotonic moves by exactly the `sleep(1)` (`mono-delta 1000 ms` vs `real-delta 1766015999858 ms`). ⏸ Still open and owner-gated: making psh *wait* for the clock is what its comment says was tried and reverted, and a bounded wait delays the prompt on every cold boot on every target since `pshapp.c` is shared. Historical rate of a visible failure: ~1 in 174 quake3 runs. |
| C8 | **A name on the NFS root intermittently stops resolving for a few operations, then starts again.** On 2026-09-23 three consecutive cases in the libc `stat_nlink_size_blk_tim` group each failed at their **first** resolution of the same relative path by name — `stat()`, `truncate()` and `lstat()`, all `Expected 0 Was -1` — while the two cases in between that touch a *different* name passed, and the group recovered by `st_dev_ino`. The file is created by the group's `TEST_SETUP` immediately after four `remove()` calls. | **~1%, and NOT a regression** — 1 firing in 90 archived runs of the group (89 of them on the netboot NFS root); the same binary then passed 212/0 **twice** on the next boot. The earlier "regression since 2026-09-22" reading was a 1-vs-1 comparison against a single archived run and is withdrawn. **Two** nfs-fs mechanisms are eliminated **by reading**: the attribute cache is positive-only (`fe3979d`), so it cannot hide a file that exists, and `nfs_node_detachPath()` unbinds the name, so a detached node cannot shadow a fresh lookup. A **third is open** — `nfs_ops_unlink()` does drop a cached node for the path, but only *after* a `nfs_lstat64()` that, on **any** failure including a transient RPC error, returns `-ENOENT` at `nfs_ops.c:1206` and never reaches that code. The group's `TEST_SETUP` calls `remove()` on three names that are expected not to exist, so it takes that early return every time; whether a node bound to the path can survive it with a lazily-cached `fh` (the idle LRU keeps one at `refs==0`) is unread and is the next thing to check. Since 2026-09-23 the next firing is self-explaining: the setup's `open()` is asserted with its errno, and the failing case reports whether `fstat()` on that same descriptor still works — `fd` valid + `fstat` OK + by-name `ENOENT` would mean the inode is fine and the **name** is what went missing, the only outcome that points back into nfs-fs. Instrument verified against real syscalls on the host, both failure shapes and the healthy one. |

## 2. Graphics and UI

| # | Issue | Last measured |
|---|---|---|
| G1 | **Windowed GL in X11 is bandwidth-bound, ~10 fps.** A GL app drawing into a window copies pixels GPU → CPU → socket → CPU → GPU every frame: 96 ms/frame at 640×480, of which `XPutImage` is 58 ms. No DRI3/DMA-BUF equivalent exists, so a client can only hand the server pixels, never a buffer reference. Full-screen GL is unaffected (the games bypass X). | Architectural. All cheaper mitigations measured and closed; a point fix was half-proven on hardware with a ceiling of only ~1.7×. |
| G2 | **xterm resize artefacts while dragging** on the GPU X desktop (owner-reported). | **Unreproduced.** No new evidence since 2026-09-14; resize *correctness* is verified once settled. The missing measurement is a deliberate drag test with the mouse released. |
| G3 | **A stale Mesa shader-cache blob renders as green speckle** over an otherwise-valid frame. Phoenix has no ELF build-id, so the cache keys on shader source only and a host toolchain change invalidates nothing. | **Mitigated, mechanism intact.** Both invalidation controls verified present 2026-09-22, and the `rsync --delete` hole that made the host-side one inert was closed 2026-09-16. No speckle report since. ⚠ The SD image ships **no** warm cache, so the first GL app on a freshly flashed card pays the full recompile. ✔ **2026-09-25 — the headline understates the protection, and it matters because it changes what work is worth doing.** The host-side control in `sync-netboot-tree.sh:99-115` does **not** rely on the hand-maintained `V3D_PHX_CACHE_VERSION`: it fingerprints the **sha256 of the three compiled Mesa archives** (`libv3d-phoenix.a`, `libGL-phoenix.a`, `libv3dv-phoenix.a`) and wipes the cache whenever that changes. That is **content-based**, so it catches exactly the case the headline calls unprotected — a host toolchain or sysroot change that alters the compiled driver without touching shader source — and it does not depend on anyone remembering to bump anything. ⛔ **Do not build a git-history or version-bump checker for this**: I wrote one, it reported "65 driver commits since the last bump" as a warning, and that was a **false alarm** — none of those 65 touched Mesa compiler sources (they are winsys/power/GPU-server glue), and the fingerprint would have caught it regardless. The tool was deleted rather than shipped. |

## 3. Platform limitations

| # | Issue | Last measured |
|---|---|---|
| P1 | **Only the 4 GB Pi 4B is validated, and 2/8 GB boards are known to be mis-mapped** — plo's syspage memory map is still hardcoded. The DTB parser also assumes a single interrupt controller. (TD-06, TD-15 remainder) | Open. Do not simply try an 8 GB board. Also in TD-15: VC4 quiesce + mailbox-move (low value), and drivers use identity va2pa (⚠ do not naively wire `dtb_armToBus` — it would break GENET). |
| P2 | **Asynchronous SError is masked in early kernel paths** because a live PCIe/VL805 USB external-abort SError is not root-caused; unmasking regresses boot. A dump-and-halt handler is implemented and armed. ⚠ This is why an MMIO read of an unclocked block **hangs silently** instead of aborting. (TD-10) | Open, HW-gated. |
| P3 | **`dc zva` fast path disabled in `hal_memset` on the Cortex-A72.** Performance-only, correctness-safe. (TD-20) | Open — but **no longer blocked on the EL2 question, which is now ANSWERED**. `tools/dczid-probe/` read `DCZID_EL0` on the real Pi 4: `raw=0x4`, **DZP=0 (dc zva PERMITTED)**, `BS=4` (64-byte block), and executing it zeroed exactly 64 bytes with the neighbouring byte intact. **`HCR_EL2.TDZ` is not set**, so the trap this row waited on does not exist. ⛔ Do not re-run that probe. ⚠ **The gate still stands for a better reason:** `hal_memset`'s own header says it *"may not work for uncached memory"*, and the recorded failure is a **hang with no exception output** in `_log_init`'s first large zeroing right after the D-cache is enabled — which fits `dc zva` on memory that is not Normal cacheable at that moment, not a trap (a trap would have raised an exception). ➡ Lifting it now requires showing **no caller of `hal_memset` can pass uncached memory** — a whole-kernel argument, not a measurement. Enabling it without that trades correctness for a perf-only gain, so it was deliberately left alone. |
| P4 | **The generic aarch64 early-console path hardcodes the UART at VA `0xffffffffffe00000`**, because it must print before the DTB-discovered base is available. On the Pi 4B this alias equals the discovered pl011 base. Matters only when porting to another aarch64 board. | Open for a future cross-board port; inert on this board. |
| P5 | **Netboot NFS-root: one narrow boot-order race.** plo launches psh as a sibling of the NFS takeover without gating on it, so a command issued at the prompt can still hit the pre-takeover RAM root and report `not found`. The clean fix is a plo boot-order gate. | Open; invisible in practice because the test harness waits for the takeover marker. Confirmed still ungated in `user.plo.yaml` on 2026-09-22. |
| P7 | **libphoenix has no UTF-8 support: the multibyte layer is C/POSIX byte-identity, so any text above U+00FF cannot be encoded at all.** Every conversion function maps one byte to one `wchar_t` (documented in a comment above each one in `wchar/wchar.c`). Measured on the host against glibc by `tools/libwchar-hosttest/` (4083 comparisons): `wcstombs` of a string containing U+1F600 returns **-1**; `mbstowcs("\xe4\xbd\xa0\xe5\xa5\xbd")` yields **6 wide chars, one per byte**, where UTF-8 would give 2; `wcwidth` reports **1 column** for East Asian Wide characters and emoji where glibc reports 2, so terminal alignment drifts. | Open, and **newly written down rather than newly broken** — this is long-standing deliberate behaviour, not a regression. ✔ **What is NOT affected:** the same harness proves all 17 locale-independent functions (`wcslen wcscmp wcsncmp wcschr wcsrchr wcsstr wcsspn wcscspn wcspbrk wmemchr wmemcmp wmemset wcscpy wcsncpy wcscat wcstol`) match glibc **exactly, 0 differences** — the wide-string core is correct. ⚠ **Exposure is NOT audited per port.** It matters only where a port asks *libc* to convert; a port carrying its own codecs (CPython does) is unaffected. ✔ **`%ls` behaviour MEASURED end-to-end 2026-09-25** (not inferred), driving libphoenix's own `format.c` against its own `wchar.c`: `L"caf\u00e9"` emits `63 61 66 **e9**` — Latin-1, where UTF-8 would be `c3 a9` — U+00FF emits `ff`, and **U+0100 and above return rc=-1**. So the failure above U+00FF is LOUD (the whole `printf` call fails, which is correct C for an encoding error) rather than silent mojibake; below U+0100 it is silently Latin-1. That asymmetry is the thing to know before relying on `%ls` for UI text. Related: D3 (STK's wide-printf shim). ➡ Fixing means a real UTF-8 encoder/decoder in the mb* family plus a Unicode width table; both are self-contained and now testable on the host in ~1 s, so this is a well-bounded piece of work if the owner wants it. |

## 4. Incomplete features

| # | Issue | Last measured |
|---|---|---|
| F1 | **WiFi has no userspace in the image at all.** ↩ *Corrected 2026-09-23 — this row said the netif was wired up and only the join was manual, "start the `rpi4-wifi` daemon". You cannot: nothing builds it.* `rpi4-wifi` lives in `devices/wifi/rpi4-wifi/` with its own `build-standalone.sh` (deliberately outside the project build, since the BCM43455 firmware/NVRAM/CLM blobs are compiled-in C arrays under a Cypress EULA and are gitignored). It is **not** in any target program list, and no `rpi4-wifi` or `wifi` binary exists in `prog/`. What IS in the image is the lwip side: `wifi43455` registers a netif on every boot and then prints `waiting for /dev/wifidata`. The proven results (WPA2 join, 4-way key, full DHCP lease) came from hand-built binaries, not from anything shipped. | Open. ✅ Partly unblocked 2026-09-23: `build-standalone.sh` run against today's tree, **0 undefined symbols**, and `rpi4-wifi` + `wifi` staged to the NFS root — so WiFi is now actually runnable on the netboot lane (`rpi4-wifi &` then `wifi scan`). ⚠ The previously staged copy dated from **2026-09-04**, i.e. linked against a months-old libphoenix — the known "a tool left over from before a core rebuild measures the old libc" trap. ⚠ The SD image has no WiFi userspace either. Deciding this means choosing whether to bring the driver into the project build, which needs an answer on shipping the EULA'd blobs. ★ **2026-09-23 — WiFi re-verified end to end against a host AP, on today's tree.** `radio-ap-up.sh` brings up a WPA2 AP (`PhoenixNet`, ch6, `10.43.0.1/24`) on the host's spare `wlp3s0` via NetworkManager AP mode, on a subnet separate from netboot — both `10.42.0.1` and `10.43.0.1` and both dnsmasq instances verified coexisting. Rebuilt `rpi4-wifi`/`wifi` with `build-standalone.sh` first, because the staged copies dated 04:04 and libphoenix changed four times today (the "tool built before a core rebuild measures the old libc" trap). Result: `join ssid=PhoenixNet ... link=1`, then `DHCP-RESULT offer=1 ack=1 bound_ip=10.43.0.89 => BOUND`. ⚠ **But throughput still cannot be measured, and that is the real gap** — the driver does DHCP itself from raw frames; nothing binds an IP stack to the WiFi netif. `WIFISTATS frames tx_ok=0 rx_ok=3`: the Pi never answers the ARP for its own lease. `glom descs=0 supers=0 subframes=0` in that session is expected, not a defect — three RX frames give aggregation nothing to aggregate. So the landed `0.14 → 3.0 MB/s` claim remains **unverified by measurement**, and what it needs is the `/dev/wifidata` lwip seam carrying bulk traffic, not an AP. |
| F2 | **Bluetooth is driver-level only.** `/dev/hci0` comes up, patchram loads (323/323), a real BD_ADDR is read and an HCI Inquiry completes. There is **no host stack** — no L2CAP, SDP, GATT or RFCOMM anywhere in the tree, so no pairing, profiles or audio. | Open. Last `bt/` commit 2026-09-04 was packaging. |
| F3 | **No I²C, SPI, general-purpose PWM, camera (CSI-2) or DSI display drivers for the BCM2711.** ⚠ *Corrected 2026-09-22 — USB mass storage was on this list and works*: `/dev/umass0`/`umass1` enumerate at SuperSpeed and mount ext2, `e2fsck`-clean. `rpi4-audio`'s internal PWM1 use is a driver-internal path, not a general-purpose PWM driver. | Open, not started. |
| F4 | **PWM audio has had no audible sign-off on real headphones.** The path is verified end to end in software (DMA ring, mixer backend, self-test feeds 8960 samples). | Open; needs an owner at the hardware. The support matrix agrees. |

## 5. Debt and cleanup

| # | Issue | Last measured |
|---|---|---|
| D1 | **`RPI4AUDIO_ARMTRIALS` is a permanent diagnostic ABI in a published header** — an ioctl plus struct in `rpi4-audio.h` for a facility that can block the driver's only message thread. (TD-23) | Open by design; removal was tied to C6 closing, which has not happened. Still present at `rpi4-audio.h:65`. |
| D2 | **Three TD-14 fallbacks still soften Pi-4 boot, and the slowness they were written for is gone.** All three say "restore the fatal path once the underlying IPC slowness is rooted out": `pl011-tty.c:1271` treats `/dev/tty0` registration failure as non-fatal; `pl011-tty.c:1290` keeps an extra `portRegister()` alias for `/dev/console` beside `create_dev()`; `pshapp.c:1701` lets psh continue on inherited stdio when `/dev/console` cannot be opened (upstream does `return err`). ↩ *Corrected 2026-09-23:* the registry also listed a fourth ID, `TD-14-psh-ttyopen-errno`, as separately active — it has **no marker in source**; what it described is the errno print at `pshapp.c:1710`, part of the third item. And the retry budget was misquoted as a tolerance extension: **upstream is 5 × 100 ms and ours is 50 × 10 ms — the same 500 ms total**, differing only in poll granularity, so ours comes up sooner and is arguably the better default. The real deviation is the non-fatal treatment, not the count. | **Measured across 7 067 boot logs: `tty0 register failed` 0, `failed to register /dev/console` 0, `ttyopen … failed` 1.** (Sound evidence — these messages are unconditional on their failure paths.) So restoring the fatal paths is now a decision about what a rare failure should do — die, or boot degraded — rather than a wait for latency to improve. |
| D3 | **SuperTuxKart still carries its own wide-printf shim**, because Irrlicht passes `wchar_t*` to `%s` — which the standard defines as a **multibyte** string (`%ls` is the wide one). ✅ *Half of this closed 2026-09-23: libphoenix now implements conforming `swprintf`/`vswprintf`* (`libphoenix 07a0a29`, 11 target tests in `test-libc-printf`), so any other port needing wide formatted output just works. | Open, and narrower than before. What remains is only STK's own non-conforming format strings: handing those call sites the conforming `swprintf` would reinterpret every wide string, so the shim now redirects by macro (`ports 6257d19`) instead of colliding with the real symbol. Closing it means changing Irrlicht's `%s` → `%ls`, not deleting the patch. ✅ **2026-09-24 — the printf half is now DONE too: `%ls`, `%lc`, `%S` and `%C` implemented** (libphoenix `21d3b32`, tests `42b9a0a`). `format.c` handled only `'s'`/`'c'`, and `l` before them merely set the 64-bit flag — so `%ls` read a `wchar_t*` as `char*` and printed `"L"` for `L"Lorem"`, stopping at the first zero byte. Silent wrong output for a standard conversion. New `FLAG_WIDE` + `format_printWide()`, converting via `wcrtomb()` in two passes (size, then emit) because the field padding must be known before the first byte and a wide string cannot be staged in the one contiguous buffer `format_printBuffer()` needs. Precision counts BYTES and never splits a character; an encoding error fails the call rather than truncating. **Gated both ways on hardware: 4/4 red without the change, 4/4 green with it; printf suite 129 → 133 tests, 0 failures; full libc sweep 1214 tests, 0 failures, 0 faults.** ⚠ **This does NOT let SuperTuxKart's shim go** — re-read the defect above: Irrlicht passes `wchar_t*` to **`%s`**, which is a *caller* bug, so a correct `%ls` does not help it. The shim stays until Irrlicht's call sites are fixed; what closes here is the libc gap, so any other port needing wide formatted output now just works. ⚠ The upstream `stdio_printf_cspn` `ls`/`S`/`C` cases stay ignored: they expect `L"\4399\0ns"` → `"#99"`, which needs a locale whose `wcrtomb()` can represent U+011B, and Phoenix's C locale is 1:1 onto bytes with `EILSEQ` above 0xff — a conforming implementation cannot produce that string. New standard-conforming cases were added instead. |
| D4 | **plo ships a debug exception vector table: every slot prints one tag character and halts** (`plo/hal/aarch64/generic/_init.S:479`), and `VBAR_EL3`, `VBAR_EL2` and `VBAR_EL1` all point at it. So any exception inside the bootloader gives a single letter instead of a register dump. | **Open; root cause identified 2026-09-23, fix is not a small edit.** The real dispatcher exists (`hal/aarch64/_exceptions.S:24`) but reads `spsr_el3` / `elr_el3` / `esr_el3` / `far_el3`, while that one table is installed at **three** exception levels — so at EL1 those EL3-only accesses trap and the crash gets worse, which is exactly why the tag table replaced it. Closing it means making the dispatcher `CurrentEL`-aware, or installing one table per EL. ⚠ Bootloader boot path, and plo takes no exception on a healthy boot, so this is diagnosability and upstreamability rather than correctness — the project's "attended" class. Recovery if it goes wrong is cheap (the loader is re-fetched over TFTP each boot). |
| D5 | **VL805 firmware-load race workarounds in xhci** — config-space/BAR0 access races the boot ROM (the bridge returns `0xdead…`), so the code waits, capped at 300 ms; plus a pmap workaround pre-creating the xhci MMIO mapping because a late `mmap` empirically reads `0xdead`. (TD-USB, TD-USB-pmap, 4 sites) | Open; real workarounds, not cosmetic. |
| D6 | **`vkCmdSetDepthBias` is a no-op in the vkQuake Vulkan trampoline.** Deliberate: V3DV leaves `CmdSetDepthBias2EXT` unpopulated, so the indirect call jumped to garbage. Cost is polygon-offset z-fighting cosmetics. Proper fix: populate that entry in V3DV's dispatch. | Open; vkQuake renders correctly in the gate. |
| D7 | **Three intentional upstream deviations are still carried**: `TD-14-devfs-direct` (devfs fast-path predicate), `TD-14-console-open-fastpath` (strdup short-circuit for `/dev/console`), `TD-14-tiocspgrp-pgrp` (correct semantics, upstreamable as-is). | Open only in the sense that they are deviations; all three work as designed. |
| D8 | **Five unmerged local branches hold work that was never landed or discarded.** ★ `devices/wip/rpi4-wifi-glom` triaged 2026-09-23 and it is **landable**: SDPCM glom de-aggregation (the firmware packs several subframes into one SDIO superframe; without de-aggregation you get one per read, which is the 0.14 MB/s), **one file, +201/−14, zero leftover diagnostics**, it **compiles clean against today's tree**, and `diag_wifiFrameRx` puts it in the **real RX path** feeding the `/dev/wifidata` lwip seam — so the claimed 0.14 → 3.0 MB/s is about networking, not a probe. The others: `lwip/agent/rpi4-genet` (+115), `lwip/full-history-backup` (+178), `lwip/rpi4-port-clean` (+6, drops unused Cypress WHD source), `lwip/wifi-wip` (+146). ✅ *22 merged branches deleted 2026-09-23, each verified in `master` first.* | Open — a keep/land/discard call per branch. **`rpi4-wifi-glom` needs only an AP + credentials to verify**, which is yours; before landing, check its file-scope `g_glom_*` state against however many consumers the driver has. ↩ *Correction to my own note: `syntax-check.sh` cannot check this file not because of a tooling gap but because the driver is **not in the project build at all** — it is standalone by design. The right instrument is its own `build-standalone.sh`, and under that **both master and this branch link with 0 undefined symbols** against today's tree.* ↩ **2026-09-23 — `rpi4-wifi-glom` never needed an AP: it was already landed.** Master carries `4e8a7e9 rpi4-wifi: de-aggregate SDPCM glom frames -- RX 0.14 -> 3.0 MB/s`, and master's `rpi4-wifi.c` has 69 `glom` mentions. Diffed master→branch: **+2/−16**, and the "+2" are merely the *pre-improvement* versions of lines master later fixed (`843d193`, mailbox-failure reporting) — the branch is strictly behind master with nothing unique. **Deleted.** ⚠ My earlier `master..branch` reading of "93 files, −10150 lines" was the wrong comparison: the branch forks from 2026-09-02, so that diff shows master's newer work as deletions. The branch's own change is the `+201/−14` the row already claimed. **Four `lwip/*` branches remain** (`agent/rpi4-genet`, `full-history-backup`, `rpi4-port-clean`, `wifi-wip`) and are still a keep/land/discard call. | ✔ **2026-09-25 — the four `lwip/*` branches are NOT redundant, and the WHD question is now a MEASURED fact.** Each has real unique content vs master (`agent/rpi4-genet` +8696 lines/19 files incl. `xhci-rig-handoff.h`; `wifi-wip` +2152/17; `rpi4-port-clean` and `full-history-backup` both **−49085 lines**), so none can simply be deleted — the commit counts quoted above are commits, not lines. ⭑ **What the two big ones remove is verified dead weight:** `sources/phoenix-rtos-lwip/wi-fi/whd/` is **1.6 MB / 75 files** of vendored Cypress WHD, and (a) its build is gated behind `ifeq (${LWIP_WIFI_BUILD},yes)`, which **every** project in the tree sets to `no` (`aarch64a72-generic-rpi4b/build.project:23`), so it compiles for nothing; (b) **no file outside `wi-fi/` references any `whd_` symbol**; (c) our actual WiFi driver `devices/wifi/rpi4-wifi/rpi4-wifi.c` has **0** `whd_` references — it is an independent implementation, not built on WHD. ➡ So "unused" is now established rather than assumed. ⚠ **Publication angle:** it is third-party code (Cypress/Infineon, Apache-2.0) that ships in a repo about to be published; keeping it means carrying its attribution, dropping it removes 49k lines that nothing builds. **Still the owner's keep/land/discard call** — not deleted unilaterally.
| D9 | **Every boot log opens with ~20 unlabelled characters of bring-up scaffolding**: `1111444422225555AS0` then `TR0`–`TR3`. Decoded 2026-09-23 — `1 4 2 5` are armstub milestones (errata 859971+1319367, L2 prefetch policy, SMPEN, L2CTLR timing) printed by **each of the 4 cores**; `AS0` ends the armstub; `TR0`–`TR3` come from `phoenix-kernel8-reloc.S`, one per core; plo's `hal_exitToEL1` adds `A` plus a digit for the EL it exited. Each is commented in source but opaque on the wire. | Open, cosmetic — a publication first-impression issue, not correctness. ↩ *The row used to say "residual debug prints remain in the kernel and plo", which was wrong in both places: the kernel has none (its five `diag` hits are structured `lib_sprintf` into diagnostic buffers), and the noise is mostly the **armstub**.* Fix shape: drop them, or emit short labelled strings. ⚠ Worth doing in one supervised pass across all three components — it is the first code that runs, on all four cores, and splitting it across turns is how it would get half-done. |
