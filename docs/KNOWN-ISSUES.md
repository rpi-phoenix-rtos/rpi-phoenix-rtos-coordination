# Known open issues

Every issue we know about and have **not** fixed, in one list. Nothing else.

- **Open only.** A fixed issue leaves this file. Closed rows are kept, verbatim, in
  [`done/closed-issues-archive.md`](done/closed-issues-archive.md) so nothing is re-litigated.
- **No decisions recorded here.** Fix / postpone / won't-fix is the owner's call, made from this
  list; earlier verdicts have been stripped deliberately.
- **No history.** Each row says what is wrong and what we last measured. Analyses live in
  `docs/misc/` and `docs/done/`.
- Open `TD-NN` transitional-debt items are issues too and are listed here.
  [`TEMPORARY-FIXES-AND-FUTURE-CLEANUP.md`](TEMPORARY-FIXES-AND-FUTURE-CLEANUP.md) keeps their
  engineering detail; anything marked RESOLVED there is not repeated here.

Verified against the tree, the git history and the boot-log archive on **2026-09-22**. Where a
row's old claim turned out to be stale, the correction is noted in the row.

## 1. Correctness — crashes and data loss

| # | Issue | Last measured |
|---|---|---|
| C1 | **A stray four-byte write corrupts the high half of one heap header field, and every later `free()` from that heap then leaks.** `heap->size` sits at offset 0 of a page-aligned heap; the low 32 bits survive intact and only the high half is wrong, so the write lands at `heap+4`. Masked, both observed heaps are entirely self-consistent — legal `0xd000` size, chunks inside it, ordinary grid. ★ The corrupting word is **not constant** (`0x80000000` and `0x80000001` on two heaps in one run), which reads like a refcount or lock word with a flag in the MSB rather than a marker. Guards: `free() of a corrupt chunk header`, `large-bin lookup returned a non-chunk` / `a wild pointer`, `chunk handed out twice`. | **Open and ACTIVE; root cause open.** ➔ Records: **[hunt of 2026-09-25](misc/2026-09-25-c1-hunt-night.md)** (tonight, with three verbatim copies of this cell) and **[the dossier](misc/2026-09-25-c1-dossier.md)** (everything before). ★ **Invariant — the only thing to quote:** a **32-bit store of `0x80000000` or `0x80000001` at PAGE+4**. Seen as the high half of `heap->size` (`heap+4`, heaps being page-aligned; sizes `0xd000` *and* `0x2000`), as the high half of a `chunk->heap` pointer (a chunk at page−8 puts that field's high half on page+4), as a broken poison word (`p4off=4`), and in the STK crash `far=0x800000010c845168`. **Three detectors, two of them independent of the poison probe** — so page+4 is measured, not an artifact of where the probe samples. ⚠ Bounded by `tools/memtrip`, which watched page+4 across 64 MiB of anonymous pages in another process and never saw it: the targets are **specific** pages, not arbitrary ones. ★★ **Address and value both match a VideoCore mailbox response, derived independently.** Every mailbox buffer here is an mmap'd **page** and the firmware's response code goes to `msg[1]` — exactly page+4 — and `0x80000000`/`0x80000001` are exactly its success and parse-error codes. ⚠ **Evidence for and against, both stated:** *for* — that double coincidence; *against* — `V3D_MBOX_LEAKTEST` forced this mechanism (page freed **before** the doorbell) and measured **0/3 vs 1/3**, no effect. ⛔ Do not quote that control as a refutation: at a few-%-per-run rate **n=3 cannot show an effect either way** — it is underpowered, not negative. ➕ **Leading hypothesis:** the firmware writes a stale response into a page the kernel has reclaimed. The **`munmap`-release variant is audited out** (all 9 in-tree mailbox sites are structurally safe, and `mboxProp`'s leak counter reads 0), so the open question is **which release path** — untested: **process exit while a call is in flight** (kernel reclaims, no `munmap`, no counter), or a coherency window between the FIFO reply and the buffer write. ➕ **The test that settles it is built and proven both ways:** `p4pa` prints the broken page's **physical** address, `v3d_phoenix_power.c` logs each request buffer's physical page, and `c1-report.sh` correlates them (`rpi4-vcmbox`'s persistent `buf_pa` is the reference that must never match). **Rate (by signature, never by guard name):** 24/793 GPU runs before 2026-09-24 16:00, 4/65 since; by app **STK 27/552 (4.9%)**, quake3 1/84, **0** in quake2/quakespasm/vkquake — hunt on STK. Sampling series #1: **0/6**, all reaching teardown at ~3400 frames (unremarkable at this rate); series #2 trial 1 **fired heavily**. ⚠ **Report counts are not event counts.** That run showed `SIG=552` from just **4 distinct corrupted heaps** (`0x0bd1f000`×58, `0x0bd60000`×56, `0x0a088000`×21, `0x0bd00000`×1, all `0xd000`) — a corrupt heap is re-reported on every later free from it, so quoting `SIG` as an event count overstates the evidence by two orders of magnitude. `c1-bench-table.sh` now prints a **VICT** column (distinct victims) beside it. Three of the four victims cluster within 384 KB, consistent with consecutive heap allocations. ⚠ That run also produced **0 poison breaks**, so `p4pa` never printed and the mailbox comparison had nothing to work with — the header path carries ~all the events, so it now reports `hpa` too (libphoenix `f2d47c1`). **Impact — ⚠ SEVERITY RAISED 2026-09-26: C1 CAN HALT THE KERNEL.** Run `c1pfn1` produced **`KFLT=2`** — an EL1 Data Abort in **`lib_idtreeAugment`** (`lib/idtree.c:36`), `far=80000001c4718610`: a kernel pointer whose **high 32 bits became `0x80000001`**, at **exactly page+4** of a page-aligned node (established by disassembly — see the hunt-night record). The fault follows teardown and **no progress lines follow it**: the system stopped. ★ No userspace use-after-free can corrupt kernel memory, so this is direct confirmation of the **device-write** model. ★★ **And "kernel memory" vs "recycled BO page" is a FALSE CHOICE:** kernel heap zones take their pages from `vm_pageAlloc` (`vm/zone.c:177`) while user mappings return them via `vm_pageFree` (`vm/map.c`) — **the same allocator** — so a frame freed by a user `munmap` can be re-issued to a kernel zone. The victim is almost certainly a **`resource_t`** (it and `port_t` are the only idtree users beginning with `idnode_t linkage`, and `process->resources` is the tree walked during teardown; `thread_t` puts `idlinkage` ~0x38 in, so it cannot be page-aligned). The model therefore needs **no special case** for a kernel victim, and a kernel victim is **not** evidence against the BO-recycling route. ↩ The previous wording here — "crashes STK inside malloc's large-bin rbtree (`lib_rbRemoveBalance`, `sys/rb.c:148`) during shutdown; the **kernel survives** (0 EL1 faults)" — described the userspace manifestation and is **no longer the worst case**. ✅ **One supported fact:** `V3D_KEEP_CLOSED_BO=1` suppresses (0/20 by signature). ★★ **AND THE COMPANION CONSTRAINT, which shapes every future instrument:** the closed-BO quarantine (`V3D_BO_QUARANTINE=1`) held and fully scanned **~1000 closed BOs across 5 runs and found NOTHING written to them** — with the scan proven on every run by a deliberately planted C1 word. Those same 5 runs produced **0 heap fires against a 4/10 baseline**. ➡ So the stray write does **not** land while the driver still holds the page; it requires the page to have gone back to the **kernel's free pool**. Any instrument that retains pages in order to watch them therefore cannot see the event — which is why retention-based detectors keep returning silence, and why the question is attribution of a page that has already FIRED. ⛔ **Do not re-walk:** double BO ownership; the allocator itself (400 seeds / 7.45 bn chunks, plus 161 M allocs / 16 threads, 0 violations); **ralloc `get_gc_block_header` — ELIMINATED** by a positive control proving the function is reached while C1 was rampant and the check stayed silent; and the claims **"always `0xd000`"** and **"the victim is a whole `0x5000` heap"**, both refuted. ⚠ **Six measurement retractions tonight**, every one from a mis-sized selector — five too narrow, one too broad. Select on the **property**, and check the known positives survive the filter. Grade every run on **SIG**, **TDOWN** and **LIVE**; each has misled once. ★★★ **2026-09-26 — THE "4-IN-10 BASELINE" IS NOT IN THE ARCHIVE.** Counting every STK-class run by day gives **30 fires / 276 runs = 10.9 % pooled** (range 0–22 %; 09-24 was 15 %, 09-26 22 %). No day reproduces the 4-in-10 figure quoted in `v3d_phoenix_winsys.c`, and **most "this instrument suppressed C1" claims are computed against it**: recomputed at 10.9 % they give **p = 0.25–0.56** and none is significant. ⛔ **None of the single-arm nulls survive.** ★★★★ **AND THE HEADLINE CONSEQUENCE IS REFUTED OUTRIGHT:** `malloc_dl.c` called it "the single most important fact about C1" that it **cannot be studied by adding instrumentation**, because any change to write pattern or layout silences it. **Disproved by a POSITIVE result** (retracted in libphoenix `bffb9e2`): a build carrying `capa`, `carel`, `hbopa`, `p4bopa` and a 4 KiB closed-BO table — changing both — **fired 2 times in 23 runs (8.7 %)** against the 10.9 % baseline — expected 2.5, observed 2, P(≤2 in 23) = 0.53, i.e. the instrumented binary tracks baseline **exactly** — one of those fires **halting the kernel**. ➕ That now includes an **8-trial `-DC1_P4_WIDE` series (0 fires, P(0 in 8) = 0.40)**, so there is **no evidence the wide probe arm suppresses** either — the stated reason it had been left off. ➡ **Every experiment deferred on that basis is unblocked**, including the 96 KiB attribution table. ⚠ Precisely: the *strong* claim is refuted; a specific heavy instrument might still suppress, and tonight's build is mostly **fire-only**, so it does not test a 96 KiB table. ➡ **Best next series: `-DC1_P4_WIDE`** — its suppression evidence is a coin flip (0 in 6 ⇒ p = 0.50), the detector is **proven capable** (all four offsets report on the host harness), and it tests the *fixed-offset* premise that the **genet / xHCI / ADMA2 exclusions were built on**. ✅ The paired A/B `V3D_KEEP_CLOSED_BO=1` **0/12 vs 7/19, Fisher p=0.019** is unaffected — a two-arm comparison assumes no baseline, which is why it is the one suppression result to trust. ⚠ Bound: 276 runs pool many builds, so 10.9 % represents no single arm — but if the baseline swings that much by build, a single-arm null against *any* assumed baseline is unreliable, which is the point. ★ **2026-09-26 — the rate above is CONFIRMED independently, and its consequence spelled out.** A fresh count of 66 STK-class trials in one night's archive gives **4 fired = 6.1 % (1 in 16.5)**, agreeing with the 4.9 % already recorded here. ⚠ The figure to stop using is the **"~1 in 3"** that circulated in working notes and drove every power calculation — it is ~5x optimistic. So **37 trials (~8.6 h of bench) are needed for a 90 % chance of a single fire**, and a 14-trial drought has probability 0.42 — *quiet stretches are not evidence of suppression*. ⛔ Compute power against 6 %, not 1-in-3, and prefer experiments that need **no** fire at all (e.g. log every heap's physical page and test it offline against the mailbox request buffers' pages — a CLEAN run answers that). Caveat: the 66 trials pool several builds, so it is a pooled rate. ★★★★ **2026-09-26 — THE DECISIVE COMPARISON RAN, AND IT IS A NEGATIVE FOR THE MAILBOX ROUTE.** Run `c1coin` fired (`SIG=107`, **`VICT=1`**, 3158 frames, teardown, `KFLT=0`) with physical-address logging on both sides. Victim: one heap, VA `0x09ec0000`, `hsize=0x80000001_0000d000`. **Corrupted pages** — heap `hpa=0x8112000`, poison breaks `0x83be000`/`0x842c000`/`0x84e4000`/`0x85d4000` — all in a tight `0x081…`–`0x085…` band. **Mailbox request buffers** (20 distinct) sit in `0x058a…`, `0x0b0c…`–`0x0e2b…`, `0x2bf0…`, `0xf851…`–`0xface…`. ➡ **No match, not even adjacent.** ⚠ **Bound, by LINE NUMBER (an earlier "all at startup" claim here was wrong):** in the 2005-line log rendering starts at 898, the last logged mailbox call is at **965** (~48 % through, *during* the run) and the first poison break is at **996** — 31 lines later. The logged window thus reaches nearly to the first fire; what it cannot cover is calls after 965 (bound raised to 256, devices `e859b7d`). ⚠ The four `p4pa` pages are **not confirmed genuine** — they may be residue from released heaps, and `carel` cannot say because its budget expired at line 803. ★★ **AND THE BOUND DOES NOT LIMIT THE CONCLUSION:** the driver's deliberate-leak message (`timed out AFTER the doorbell`) is **unbounded** and fired **0 times**, so **no mailbox call timed out anywhere in the run** — and that timeout path is the only one that returns a page while the firmware still owns it. With zero timeouts no mailbox page was left stale, *including the unlogged calls*. ➡ **The in-process v3d mailbox path is EXCLUDED for this run**, established for the first time on a **firing** run rather than from a leak counter on clean ones. ⛔ Ruled out by address too: the **framebuffer** (`0x3d3b2000`–`0x3db9b000`) and the **vcmbox persistent buffer** (`0x03d3f000`) — neither overlaps the corrupted `0x0811…`–`0x085d…` band. **The PA negative rests on `hpa` alone** (50:1, a live heap's header page), which is sufficient. ★ The crash PC addr2line's to **`_mesa_delete_vao`**, not the archived `lib_rbRemoveBalance` — a second, unrelated consumer of the corrupted word. ✅ Independently: **every mailbox page allocated was also released** (20/20, empty set difference) — first runtime corroboration of the nine-site audit. ★ **Method:** at the ~1.3 % UART corruption rate `sort -u` invented a second `hpa`; frequency settled it **50:1** (and `hsize` 49:1). Use majority vote, not distinctness, on a lossy link. ➕ **Direction this opens:** `0x80000000`/`0x80000001` is also the classic **MSB OWN/VALID bit of a DMA descriptor status word**, and a device writing back to a stale physical address produces exactly this shape. With the mailbox route now weakened by direct PA evidence, the other DMA masters (genet, ADMA2/eMMC, V3D BOs, HVS) deserve the same PA-logging treatment. ★★★★ **2026-09-26 — WHEN it fires, from the 33-run archive (`scripts/c1-fire-position.sh`).** Not one fire before **76 s of render** in 32 STK runs, then a sharp peak at 76–110 s, then a thin tail. ⚠ The anchor is **time since the first frame**, *not* frames and *not* the race: STK's race start is bimodal (70–74 s vs 80–84 s) on one binary, and across that split the frame count at the fire differs **1.9×** while the elapsed time agrees to **3 %** — regressing fire time on race start gives slope **−0.21 ± 0.38**, rejecting race-tracking at p ≈ 0.005. ⚠ "Not paced by frames" is **not** "not paced by work": the sub-1-fps load phase allocates heavily while rendering almost nothing, so an allocation-count anchor looks identical on this data. ✔ Not a detector artefact — all three C1 detectors are work-paced (`malloc_c1Scan` runs every `C1_SCAN_EVERY` *calls*). ★★★★ **`V3D_KEEP_CLOSED_BO=1` stops TWO recyclings, not one.** `va_alloc()` serves a GPU VA from the holes list — a range a closed BO owned — before bumping into fresh address space, so the switch removes both the **physical frame** returning to the kernel and the **GPU VA** being handed to the next BO. Only (1) had ever been counted, so the suppression cannot say which half matters. A `v3d-winsys: pace` line now carries `t/frames/boc/bore/vaa/var/heaps/heapkb` and `scripts/c1-pace-read.sh` prints both onsets, so one fire separates them. ⛔ Not done by flipping `V3D_VA_NO_RECYCLE`: two binaries for a layout-sensitive bug, and its 2 GiB page table can exhaust and kill STK, which would read as suppression. ⚖ Wide-arm probe (4 offsets) pooled **16 trials, 0 fires** — P = 0.157 at the measured rate, so it bounds nothing. ★★★★ **2026-09-26 `c1pace1` — first fire carrying BO attribution, and it is NEGATIVE.** Three detections of ONE event (same page `0x0a2c7000` / PA `0x0847c000`, same `0xd000` heap, same `0x4ff0` chunk, one wrong word with zero neighbours). `p4bopa = 0` on all three against **181/202/214** closed BOs recorded in a 512-entry ring that had **not wrapped** — so the corrupted page was **not** a page released by a closed BO. ⚠ **n = 1**: `p4bopa` appears in exactly one log in the whole archive. ⚠⚠ And it has no power without a denominator — if malloc essentially never receives a freed BO frame in this workload, `p4bopa = 0` is what *every* page would show. `hbo`/`hbop` (heaps landing on a closed-BO page, out of heaps probed) supply that and are in the next build; **do not quote the negative without them.** ⚠ It does not exclude a **live** BO (the ring records closes only, so a double mapping would not appear). ★★ **If it holds, the tension is with the hunt's one solid fact** — `KEEP_CLOSED_BO=1` suppressing C1 at p = 0.019 — and the reading that fits both is that it suppresses by perturbing **layout** (it stops every `munmap`) rather than by removing a mechanism, which would demote that result from mechanism to artefact. ✔ Also from this trial: the **~90 s-after-first-frame anchor reproduced out-of-sample** (fire at t = 88.9 s on a `--max-cmd-secs` no archived run used), and **heap growth flattens exactly there** — 4245.8 kB/s over 40–75 s to **0.0 kB/s** over 95–130 s, flat for the remaining 230 s. ⛔ `p4off = 4` on all three is **tautological**: `C1_P4_WIDE` is a compile define and this was the narrow build. ⛔ The "C1 lives in the FIRST BO recycles" framing is dead — `bore` was already 4 at t = 8.5 s and 151 at the fire. |
| C3 | **Two libc globals read back zero, killing the process at `main`.** `stderr` and psh's applet list are both unset, so `fprintf` dereferences a NULL `FILE*` (`far=0x30`). Both live in one 4 KiB `.bss` page. A source review gave a clean negative on every kernel mapping path, so "those writes definitely execute" is the assumption the next occurrence has to test. | **2 sightings (2026-09-07, 2026-09-15), none since.** Impact limited to `ntpclient`, which only sets the clock. The label added to make a recurrence self-explaining (`applet list is empty`) has **never fired** in the whole archive. ✔ **2026-09-24 — two constraints added, and one suspect cleared.** (a) **The two globals come from DIFFERENT mechanisms at different times**: `stderr` is assigned in `stdio/file.c:1565` reached from `_libc_init()`, while psh's applet list is filled by an `__attribute__((constructor))` (`pshapp/env.c`) run later from `_init_array()` — and `crt0-common.c:_startc` runs `_libc_init()` *then* `_init_array()` *then* `main()`. So this is **not** "constructors didn't run": two independent writes, both lost, which supports the page-level framing (a written page replaced by a fresh zero page) over an init-order one. (b) ⛔ **`vm/amap.c:amap_create()` is CLEARED as a suspect — do not re-walk it.** It looks wrong at first read: it does `*offset = *offset / SIZE_PAGE` and the whole amap API otherwise takes BYTE offsets (`amap_page`/`amap_clear` both do `offset / SIZE_PAGE` internally; `e->aoffs` is byte-based at every site in `map.c`, incl. `+= len` on entry split). But the division is only a scratch reuse of the caller's out-parameter to index the OLD amap in the copy loop, and the function **ends with `*offset = 0`**, which is the correct new value (the entry starts at the beginning of the freshly created amap). No early return sits between the two, so it is correct today — though reusing the caller's variable as scratch is fragile, and a future early return there would silently corrupt every later `amap_*` index for that entry. ✔ **That fragility is now removed** (kernel `ef4500cb`, 2026-09-25): the page-index scratch lives in a local, so the caller's byte-based offset is never transiently a page index. No behaviour change — C3 itself is unaffected and still waiting on a recurrence. ✔ **2026-09-25 — "ntpclient crashed" is NOT the same as C3; grade by the signature, not the victim.** The archive holds **four** ntpclient deaths and only **two** are this defect, so counting by process name would have inflated this row from 2 sightings to 4. C3's signature is a **read** fault at `far=0x30` (`esr=0x92000007`), and exactly two logs carry it: 2026-09-07 (`pc=0x41bfb4`) and 2026-09-15 (`pc=0x41bff4`) — so **"2 sightings, none since" stands**. The other two are different bugs. **2026-09-11** (`g-relF3-q2`) is a **write** fault at `far=0x0` (`esr=0x92000047`, `pc=0x4153c8`) that then storms — 2070 dump lines from one process — and is tracked nowhere; it is a distinct unresolved ntpclient crash, not evidence for C3. **2026-09-25** (`c1hpa01`) is a read at `far=0x5000` inside `malloc_heapSizeValid`, i.e. a C1-hunt *instrument* dereferencing an unmapped `live[]` entry — my own defect, fixed in libphoenix `819e461`, and never a C3 recurrence. ⚠ `pc` is not comparable across builds, so the signature is the `esr`/`far` pair plus the addr2line'd function name, never the raw address. |
| C4 | **Intermittent V3D binner wedge on long GPU runs.** Auto-recovered by a GPU reset that reports loudly (`RENDER TIMEOUT` / `DROPPED job`). Not an MMU fault — a CT0 front-end stall with zero error status, `ct0ca` parked inside a valid BCL BO. `q3dm7` is the documented reproducer. | **Not reproduced since 2026-09-09** (and that was an experiment-labelled run; last sighting on a shipping build 2026-08-22). 0 wedge signatures in every log 09-10 → 09-22, including 15 Quake III runs and both six-app gates. ✔ **Window extended to 2026-09-25**: 0 occurrences of any of the driver's four literals (`RENDER TIMEOUT`, `GPU wedged`, `V3D-binner-wedge`, `BIN wedge`) across **~136 800 GPU frames in 32 GPU-bearing logs** (3 six-app gates + 16 STK bench runs); control — those same 32 logs all carry `v3d-winsys: flipstat`, so the absence is not a silent detector. ⭑ **AND the documented reproducer was finally run**, which that bulk exposure did *not* cover: the gates launch **`q3dm1`**, not `q3dm7`. A dedicated `quake3 +map q3dm7` cycle (2026-09-25 09:14) — asserted to have loaded it (`Server: q3dm7`, `trying to load maps/q3dm7.aas`) — rendered **8 655 frames, 0 faults, 0 wedge signatures**. ⚠ One clean run does not clear an intermittent, but this is the first time the *named* reproducer has been exercised on a current shipping build rather than the window being extended with maps that were never implicated. Diagnostics are armed, so the next occurrence self-explains. |
| C5 | **PWM audio DMA stalls at startup on some boots**, which used to hang Quake II inside `SDL_OpenAudio()`. Captured three times: the channel is alive but parked for a DREQ that never comes, ring full, 0 samples moved. Mechanism open. Blast radius is contained — the driver now grades the channel by progress, re-arms, and degrades to a paced null sink. | **Mechanism unfixed; symptom quiet.** 25/25 Quake II runs since 2026-09-18 reached `SDL audio initialized` and drew frames; 0 stall aborts in any log 09-19 → 09-22. ⚠ Timing-sensitive: the rate has moved across functionally neutral relinks, so short clean runs bound it rather than clear it. | ✔ **2026-09-25 — +3 clean Quake II runs, graded with the driver's own literals** (copied from `audio/`, not composed): all three gate q2 runs show `SDL audio initialized` **present** (so the audio path was genuinely reached — the context is valid, not a silent log), with **0** `write STALLED`, **0** `engine RECOVERED after the stall`, **0** `paced null sink`. Running tally **28/28** reaching audio init with 0 stall aborts. ⚠ Unchanged conclusion: the rate moves across functionally neutral relinks, so clean runs **bound** this rather than clear it.
| C6 | **psh forks `ntpclient` fire-and-forget on session start**, so a ~56-year wall-clock step can land anywhere in a ~2-minute window. Any app that measures a duration in *wall-clock* time across its own startup sees nonsense. | **The one app that ever failed this way is fixed; the systemic exposure remains.** ✅ quake3's `Sys_Milliseconds` now uses `CLOCK_MONOTONIC` (`ports fd34552`) — it was the only port glue still using `gettimeofday`; quakespasm, vkquake and sdl2 already used monotonic. The property is measured, not assumed: `tools/clockstep` steps the wall clock 56 years on hardware and monotonic moves by exactly the `sleep(1)` (`mono-delta 1000 ms` vs `real-delta 1766015999858 ms`). ⏸ Still open and owner-gated: making psh *wait* for the clock is what its comment says was tried and reverted, and a bounded wait delays the prompt on every cold boot on every target since `pshapp.c` is shared. Historical rate of a visible failure: ~1 in 174 quake3 runs. |
| C8 | **A name on the NFS root intermittently stops resolving for a few operations, then starts again.** On 2026-09-23 three consecutive cases in the libc `stat_nlink_size_blk_tim` group each failed at their **first** resolution of the same relative path by name — `stat()`, `truncate()` and `lstat()`, all `Expected 0 Was -1` — while the two cases in between that touch a *different* name passed, and the group recovered by `st_dev_ino`. The file is created by the group's `TEST_SETUP` immediately after four `remove()` calls. | **~1%, and NOT a regression** — 1 firing in 90 archived runs of the group (89 of them on the netboot NFS root); the same binary then passed 212/0 **twice** on the next boot. The earlier "regression since 2026-09-22" reading was a 1-vs-1 comparison against a single archived run and is withdrawn. **Two** nfs-fs mechanisms are eliminated **by reading**: the attribute cache is positive-only (`fe3979d`), so it cannot hide a file that exists, and `nfs_node_detachPath()` unbinds the name, so a detached node cannot shadow a fresh lookup. A **third is open** — `nfs_ops_unlink()` does drop a cached node for the path, but only *after* a `nfs_lstat64()` that, on **any** failure including a transient RPC error, returns `-ENOENT` at `nfs_ops.c:1206` and never reaches that code. The group's `TEST_SETUP` calls `remove()` on three names that are expected not to exist, so it takes that early return every time; whether a node bound to the path can survive it with a lazily-cached `fh` (the idle LRU keeps one at `refs==0`) is unread and is the next thing to check. Since 2026-09-23 the next firing is self-explaining: the setup's `open()` is asserted with its errno, and the failing case reports whether `fstat()` on that same descriptor still works — `fd` valid + `fstat` OK + by-name `ENOENT` would mean the inode is fine and the **name** is what went missing, the only outcome that points back into nfs-fs. Instrument verified against real syscalls on the host, both failure shapes and the healthy one. | ✔ **2026-09-25 — +1 clean run.** The `stat_nlink_size_blk_tim` group lives in `test-libc-misc` (`libc/misc/stat.c`), which ran in tonight's full sweep: **212 Tests 0 Failures**, the group's cases present (9 occurrences). Tally now **1 firing in 91** archived runs of the group.

## 2. Graphics and UI

| # | Issue | Last measured |
|---|---|---|
| G1 | **Windowed GL in X11 is bandwidth-bound, ~10 fps.** A GL app drawing into a window copies pixels GPU → CPU → socket → CPU → GPU every frame: 96 ms/frame at 640×480, of which `XPutImage` is 58 ms. No DRI3/DMA-BUF equivalent exists, so a client can only hand the server pixels, never a buffer reference. Full-screen GL is unaffected (the games bypass X). | Architectural. All cheaper mitigations measured and closed; a point fix was half-proven on hardware with a ceiling of only ~1.7×. ✔ **2026-09-25 — the headline numbers are STALE. Re-measured at the SAME 640×480** (`gl-x11: start (640x480, …)`), agreeing across two independent showcase gates hours apart: **70.6 ms/frame, `put` 33.9 ms, 14.2 fps** — against the row's 96 ms / 58 ms / ~10 fps. So frame time is **1.36×** better and `put` **1.71×** better than written. ⭑ That 1.71× is exactly the *ceiling* this row attributes to the "half-proven" point fix, which suggests it landed and the row was never updated — **causation NOT verified**, only the coincidence is reported. Current breakdown: `put` 33.8 (48 %), `read` 12.5 (18 %), `pack` 6.4 (9 %), `draw` 1.6 (2 %), and **~16 ms (23 %) unaccounted** by the app's own counters — presumably server-side/socket, and the obvious next measurement if anyone revisits this. ➡ Plan against **14 fps, not 10**. |
| G2 | **xterm resize artefacts while dragging** on the GPU X desktop (owner-reported). | **Unreproduced.** No new evidence since 2026-09-14; resize *correctness* is verified once settled. The missing measurement is a deliberate drag test with the mouse released. |
| G3 | **A stale Mesa shader-cache blob renders as green speckle** over an otherwise-valid frame. Phoenix has no ELF build-id, so the cache keys on shader source only and a host toolchain change invalidates nothing. | **Mitigated, mechanism intact.** Both invalidation controls verified present 2026-09-22, and the `rsync --delete` hole that made the host-side one inert was closed 2026-09-16. No speckle report since. ⚠ The SD image ships **no** warm cache, so the first GL app on a freshly flashed card pays the full recompile. ✔ **2026-09-25 — the headline understates the protection, and it matters because it changes what work is worth doing.** The host-side control in `sync-netboot-tree.sh:99-115` does **not** rely on the hand-maintained `V3D_PHX_CACHE_VERSION`: it fingerprints the **sha256 of the three compiled Mesa archives** (`libv3d-phoenix.a`, `libGL-phoenix.a`, `libv3dv-phoenix.a`) and wipes the cache whenever that changes. That is **content-based**, so it catches exactly the case the headline calls unprotected — a host toolchain or sysroot change that alters the compiled driver without touching shader source — and it does not depend on anyone remembering to bump anything. ⛔ **Do not build a git-history or version-bump checker for this**: I wrote one, it reported "65 driver commits since the last bump" as a warning, and that was a **false alarm** — none of those 65 touched Mesa compiler sources (they are winsys/power/GPU-server glue), and the fingerprint would have caught it regardless. The tool was deleted rather than shipped. |

## 3. Platform limitations

| # | Issue | Last measured |
|---|---|---|
| P1 | **Only the 4 GB Pi 4B is validated, and 2/8 GB boards are known to be mis-mapped** — plo's syspage memory map is still hardcoded. The DTB parser also assumes a single interrupt controller. (TD-06, TD-15 remainder) | Open. Do not simply try an 8 GB board. Also in TD-15: VC4 quiesce + mailbox-move (low value), and drivers use identity va2pa (⚠ do not naively wire `dtb_armToBus` — it would break GENET). ✅ **2026-09-25 — two 2/8 GB-board holes closed in passing** (devices `ec654c0`): both in-process VideoCore mailbox callers (`v3d_phoenix_power.c`, which is linked into **every GPU application**, and `v3d_gpu.c`) resolved the message page with `va2pa`, checked only the unresolvable case, then cast to `uint32_t`. A PA above 4 GiB was silently truncated, aiming the firmware at the wrong physical page — which it then writes its response word into at +4 — while still reporting success. `rpi4-vcmbox` and `rpi4-wifi` already guarded this; these two did not. Latent on the validated 4 GB board (highest PA in the whole log archive ~`0x451f0000`, and vcmbox's guard has never fired), real on 2/8 GB. |
| P2 | **Asynchronous SError is masked in early kernel paths** because a live PCIe/VL805 USB external-abort SError is not root-caused; unmasking regresses boot. A dump-and-halt handler is implemented and armed. ⚠ This is why an MMIO read of an unclocked block **hangs silently** instead of aborting. (TD-10) | Open, HW-gated. |
| P3 | **`dc zva` fast path disabled in `hal_memset` on the Cortex-A72.** Performance-only, correctness-safe. (TD-20) | Open — but **no longer blocked on the EL2 question, which is now ANSWERED**. `tools/dczid-probe/` read `DCZID_EL0` on the real Pi 4: `raw=0x4`, **DZP=0 (dc zva PERMITTED)**, `BS=4` (64-byte block), and executing it zeroed exactly 64 bytes with the neighbouring byte intact. **`HCR_EL2.TDZ` is not set**, so the trap this row waited on does not exist. ⛔ Do not re-run that probe. ⚠ **The gate still stands for a better reason:** `hal_memset`'s own header says it *"may not work for uncached memory"*, and the recorded failure is a **hang with no exception output** in `_log_init`'s first large zeroing right after the D-cache is enabled — which fits `dc zva` on memory that is not Normal cacheable at that moment, not a trap (a trap would have raised an exception). ➡ Lifting it now requires showing **no caller of `hal_memset` can pass uncached memory** — a whole-kernel argument, not a measurement. Enabling it without that trades correctness for a perf-only gain, so it was deliberately left alone. ⚠ **2026-09-25 — the stated blocker may be the wrong frame for the recorded failure, which narrows the question but does NOT clear the gate.** The failure this row rests on is a hang in `_log_init`'s first large zeroing. That zeroing is `hal_memset(&log_common, 0, sizeof(log_common))` (`log/log.c:522`), and `log_common` is a **file-scope struct in ordinary kernel `.bss`** (`log/log.c:76-83`, dominated by `char buf[KERNEL_LOG_SIZE]`) — Normal **cacheable** memory once the kernel image is mapped, not Device and not Normal-NC. So "a caller passed uncached memory" does not explain *that* hang; the remaining suspects are specific to the moment it happens, i.e. the window immediately after the D-cache is enabled. ⛔ This is an observation from reading, **not** a reproduction and **not** a licence to lift the gate: the whole-kernel argument over all 108 `hal_memset` call sites (39 posix, 30 hal, 23 proc, 6 test, 4 vm, 3 log, 2 lib, 1 syscalls) is still owed, and the item stays perf-only with correctness risk. It does mean whoever picks this up should start by re-testing the `_log_init` window rather than auditing callers for uncached memory. |
| P4 | **The generic aarch64 early-console path hardcodes the UART at VA `0xffffffffffe00000`**, because it must print before the DTB-discovered base is available. On the Pi 4B this alias equals the discovered pl011 base. Matters only when porting to another aarch64 board. | Open for a future cross-board port; inert on this board. |
| P5 | **Netboot NFS-root: one narrow boot-order race.** plo launches psh as a sibling of the NFS takeover without gating on it, so a command issued at the prompt can still hit the pre-takeover RAM root and report `not found`. | Open; invisible in practice because the test harness waits for the takeover marker. ⚠ **2026-09-25 — the stated fix ("a plo boot-order gate") does not exist as described, and the obvious replacement is contraindicated. Read this before attempting either.** (a) plo's `app` lines only *register* programs in the syspage; `main.c:83-120` then walks that list calling `proc_syspageSpawn` back to back with **no wait between entries**, so `app` order is spawn order and nothing more. There is no bootloader-time wait that could gate it — `cmds/wait.c` runs before `go!`, i.e. before any program starts. (b) A real gate *is* constructible: `-x` sets `flagSyspageExec` (`plo/cmds/app.c:199`), which becomes the `X` the kernel tests, so dropping `-x` from the nfsroot psh line would load psh without running it, and the takeover server could then start it by name via `spawnSyspage()` (`libphoenix/include/sys/threads.h:60` → `syscalls_spawnSyspage` → `proc_syspageSpawnName`). ⛔ **But that makes the shell conditional on the takeover succeeding, and this project has already measured that class of change**: the `/etc/rc.nfsroot` (`psh -i`) indirection tried exactly that shape and was reverted because no-prompt boots went from **0 of 134 to 5 of 88** (rationale kept in `user.plo.yaml`). A boot with no shell costs far more than this race does. ➡ So the remaining options are an owner decision, not an autonomous change: accept the race, or gate psh and accept that a failed takeover leaves no shell. |
| P7 | **libphoenix has no UTF-8 support: the multibyte layer is C/POSIX byte-identity, so any text above U+00FF cannot be encoded at all.** Every conversion function maps one byte to one `wchar_t` (documented in a comment above each one in `wchar/wchar.c`). Measured on the host against glibc by `tools/libwchar-hosttest/` (4083 comparisons): `wcstombs` of a string containing U+1F600 returns **-1**; `mbstowcs("\xe4\xbd\xa0\xe5\xa5\xbd")` yields **6 wide chars, one per byte**, where UTF-8 would give 2; `wcwidth` reports **1 column** for East Asian Wide characters and emoji where glibc reports 2, so terminal alignment drifts. | Open, and **newly written down rather than newly broken** — this is long-standing deliberate behaviour, not a regression. ✔ **What is NOT affected:** the same harness proves all 17 locale-independent functions (`wcslen wcscmp wcsncmp wcschr wcsrchr wcsstr wcsspn wcscspn wcspbrk wmemchr wmemcmp wmemset wcscpy wcsncpy wcscat wcstol`) match glibc **exactly, 0 differences** — the wide-string core is correct. ⚠ **Exposure is NOT audited per port.** It matters only where a port asks *libc* to convert; a port carrying its own codecs (CPython does) is unaffected. ✔ **`%ls` behaviour MEASURED end-to-end 2026-09-25** (not inferred), driving libphoenix's own `format.c` against its own `wchar.c`: `L"caf\u00e9"` emits `63 61 66 **e9**` — Latin-1, where UTF-8 would be `c3 a9` — U+00FF emits `ff`, and **U+0100 and above return rc=-1**. So the failure above U+00FF is LOUD (the whole `printf` call fails, which is correct C for an encoding error) rather than silent mojibake; below U+0100 it is silently Latin-1. That asymmetry is the thing to know before relying on `%ls` for UI text. Related: D3 (STK's wide-printf shim). ➡ Fixing means a real UTF-8 encoder/decoder in the mb* family plus a Unicode width table; both are self-contained and now testable on the host in ~1 s, so this is a well-bounded piece of work if the owner wants it. |

## 4. Incomplete features

| # | Issue | Last measured |
|---|---|---|
| F1 | **WiFi has no userspace in the image at all.** ↩ *Corrected 2026-09-23 — this row said the netif was wired up and only the join was manual, "start the `rpi4-wifi` daemon". You cannot: nothing builds it.* `rpi4-wifi` lives in `devices/wifi/rpi4-wifi/` with its own `build-standalone.sh` (deliberately outside the project build, since the BCM43455 firmware/NVRAM/CLM blobs are compiled-in C arrays under a Cypress EULA and are gitignored). It is **not** in any target program list, and no `rpi4-wifi` or `wifi` binary exists in `prog/`. What IS in the image is the lwip side: `wifi43455` registers a netif on every boot and then prints `waiting for /dev/wifidata`. The proven results (WPA2 join, 4-way key, full DHCP lease) came from hand-built binaries, not from anything shipped. | Open. ✅ Partly unblocked 2026-09-23: `build-standalone.sh` run against today's tree, **0 undefined symbols**, and `rpi4-wifi` + `wifi` staged to the NFS root — so WiFi is now actually runnable on the netboot lane (`rpi4-wifi &` then `wifi scan`). ⚠ The previously staged copy dated from **2026-09-04**, i.e. linked against a months-old libphoenix — the known "a tool left over from before a core rebuild measures the old libc" trap. ⚠ The SD image has no WiFi userspace either. Deciding this means choosing whether to bring the driver into the project build, which needs an answer on shipping the EULA'd blobs. ★ **2026-09-23 — WiFi re-verified end to end against a host AP, on today's tree.** `radio-ap-up.sh` brings up a WPA2 AP (`PhoenixNet`, ch6, `10.43.0.1/24`) on the host's spare `wlp3s0` via NetworkManager AP mode, on a subnet separate from netboot — both `10.42.0.1` and `10.43.0.1` and both dnsmasq instances verified coexisting. Rebuilt `rpi4-wifi`/`wifi` with `build-standalone.sh` first, because the staged copies dated 04:04 and libphoenix changed four times today (the "tool built before a core rebuild measures the old libc" trap). Result: `join ssid=PhoenixNet ... link=1`, then `DHCP-RESULT offer=1 ack=1 bound_ip=10.43.0.89 => BOUND`. ⚠ **But throughput still cannot be measured, and that is the real gap** — the driver does DHCP itself from raw frames; nothing binds an IP stack to the WiFi netif. `WIFISTATS frames tx_ok=0 rx_ok=3`: the Pi never answers the ARP for its own lease. `glom descs=0 supers=0 subframes=0` in that session is expected, not a defect — three RX frames give aggregation nothing to aggregate. So the landed `0.14 → 3.0 MB/s` claim remains **unverified by measurement**, and what it needs is the `/dev/wifidata` lwip seam carrying bulk traffic, not an AP. |
| F2 | **Bluetooth is driver-level only.** `/dev/hci0` comes up, patchram loads (323/323), a real BD_ADDR is read and an HCI Inquiry completes. There is **no host stack** — no L2CAP, SDP, GATT or RFCOMM anywhere in the tree, so no pairing, profiles or audio. | Open. Last `bt/` commit 2026-09-04 was packaging. |
| F3 | **No I²C, SPI, general-purpose PWM, camera (CSI-2) or DSI display drivers for the BCM2711.** ⚠ *Corrected 2026-09-22 — USB mass storage was on this list and works*: `/dev/umass0`/`umass1` enumerate at SuperSpeed and mount ext2, `e2fsck`-clean. `rpi4-audio`'s internal PWM1 use is a driver-internal path, not a general-purpose PWM driver. | Open, not started. |
| F4 | **PWM audio has had no audible sign-off on real headphones.** The path is verified end to end in software (DMA ring, mixer backend, self-test feeds 8960 samples). | Open; needs an owner at the hardware. The support matrix agrees. |

## 5. Debt and cleanup

| # | Issue | Last measured |
|---|---|---|
| D1 | **`RPI4AUDIO_ARMTRIALS` is a permanent diagnostic ABI in a published header** — an ioctl plus struct in `rpi4-audio.h` for a facility that can block the driver's only message thread. (TD-23) | Open by design; removal was tied to C6 closing, which has not happened. Still present at `rpi4-audio.h:65`. |
| D2 | **Three TD-14 fallbacks still soften Pi-4 boot, and the slowness they were written for is gone.** All three say "restore the fatal path once the underlying IPC slowness is rooted out": `pl011-tty.c:1271` treats `/dev/tty0` registration failure as non-fatal; `pl011-tty.c:1290` keeps an extra `portRegister()` alias for `/dev/console` beside `create_dev()`; `pshapp.c:1701` lets psh continue on inherited stdio when `/dev/console` cannot be opened (upstream does `return err`). ↩ *Corrected 2026-09-23:* the registry also listed a fourth ID, `TD-14-psh-ttyopen-errno`, as separately active — it has **no marker in source**; what it described is the errno print at `pshapp.c:1710`, part of the third item. And the retry budget was misquoted as a tolerance extension: **upstream is 5 × 100 ms and ours is 50 × 10 ms — the same 500 ms total**, differing only in poll granularity, so ours comes up sooner and is arguably the better default. The real deviation is the non-fatal treatment, not the count. | **Measured across 7 067 boot logs: `tty0 register failed` 0, `failed to register /dev/console` 0, `ttyopen … failed` 1.** (Sound evidence — these messages are unconditional on their failure paths.) So restoring the fatal paths is now a decision about what a rare failure should do — die, or boot degraded — rather than a wait for latency to improve. |
| D3 | **SuperTuxKart still carries its own wide-printf shim**, because Irrlicht passes `wchar_t*` to `%s` — which the standard defines as a **multibyte** string (`%ls` is the wide one). ✅ *Half of this closed 2026-09-23: libphoenix now implements conforming `swprintf`/`vswprintf`* (`libphoenix 07a0a29`, 11 target tests in `test-libc-printf`), so any other port needing wide formatted output just works. | Open, and narrower than before. What remains is only STK's own non-conforming format strings: handing those call sites the conforming `swprintf` would reinterpret every wide string, so the shim now redirects by macro (`ports 6257d19`) instead of colliding with the real symbol. Closing it means changing Irrlicht's `%s` → `%ls`, not deleting the patch. ✅ **2026-09-24 — the printf half is now DONE too: `%ls`, `%lc`, `%S` and `%C` implemented** (libphoenix `21d3b32`, tests `42b9a0a`). `format.c` handled only `'s'`/`'c'`, and `l` before them merely set the 64-bit flag — so `%ls` read a `wchar_t*` as `char*` and printed `"L"` for `L"Lorem"`, stopping at the first zero byte. Silent wrong output for a standard conversion. New `FLAG_WIDE` + `format_printWide()`, converting via `wcrtomb()` in two passes (size, then emit) because the field padding must be known before the first byte and a wide string cannot be staged in the one contiguous buffer `format_printBuffer()` needs. Precision counts BYTES and never splits a character; an encoding error fails the call rather than truncating. **Gated both ways on hardware: 4/4 red without the change, 4/4 green with it; printf suite 129 → 133 tests, 0 failures; full libc sweep 1214 tests, 0 failures, 0 faults.** ⚠ **This does NOT let SuperTuxKart's shim go** — re-read the defect above: Irrlicht passes `wchar_t*` to **`%s`**, which is a *caller* bug, so a correct `%ls` does not help it. The shim stays until Irrlicht's call sites are fixed; what closes here is the libc gap, so any other port needing wide formatted output now just works. ⚠ The upstream `stdio_printf_cspn` `ls`/`S`/`C` cases stay ignored: they expect `L"\4399\0ns"` → `"#99"`, which needs a locale whose `wcrtomb()` can represent U+011B, and Phoenix's C locale is 1:1 onto bytes with `EILSEQ` above 0xff — a conforming implementation cannot produce that string. New standard-conforming cases were added instead. |
| D4 | **plo shipped a debug exception vector table whose every slot printed one tag character and halted** (`plo/hal/aarch64/generic/_init.S`), with `VBAR_EL3`, `VBAR_EL2` and `VBAR_EL1` all pointing at it — so any exception inside the bootloader gave a single letter instead of a register dump. | **RESOLVED 2026-09-25, plo `b5bfbea`.** The 16 slots now share a `CurrentEL`-aware `_exc_dump` that reads `ESR`/`ELR`/`FAR`/`SPSR`/`SCTLR` from the faulting EL's own register bank, over the same polled UART, keeping the slot tag. This was the blocker: the pre-existing dispatcher (`hal/aarch64/_exceptions.S`) hardcodes the `_el3` names while the one table is installed at three ELs, so at EL1 the dumper itself trapped. **Graded by making it fire.** Throwaway builds with a deliberate `brk #0` after the `vbar_el1` and `vbar_el2` installs each dumped correctly — slot `E` (offset 0x200), the right EL digit, `ESR=f2000000` (EC 0x3C, BRK), and an `ELR` equal to the `brk` address (`0x2001a8` / `0x200170`), every field matching a prediction taken from the disassembly *beforehand*, and the two runs reading different register banks proves the dispatch selects rather than falls through. ⚠ **The EL3 arm is unproven on this board:** a third build put `brk #0` after the `vbar_el3` install and booted clean through to psh, showing `start_el3` is never entered on the Pi 4 firmware handoff (plo starts at EL2). That arm differs from the proven two only in register names. Reverted and re-verified: 0 `brk`, 16 slots at exact 0x80 spacing, clean boot with the loader self-reporting `plo b5bfbeadc124`, 0 faults, 0 `EXC` dumps. |
| D5 | **VL805 firmware-load race workarounds in xhci** — config-space/BAR0 access races the boot ROM (the bridge returns `0xdead…`), so the code waits, capped at 300 ms; plus a pmap workaround pre-creating the xhci MMIO mapping because a late `mmap` empirically reads `0xdead`. (TD-USB, TD-USB-pmap, 4 sites) | Open; real workarounds, not cosmetic. |
| ~~D6~~ | ✅ **RESOLVED 2026-09-25 — `vkCmdSetDepthBias` calls the real implementation again** (ports `vk_trampolines.c`). The hazard was the **route**, not the recording: `vkGetDeviceProcAddr` returns mesa's `vk_common_CmdSetDepthBias`, which forwards through `dispatch_table.CmdSetDepthBias2EXT` — a slot V3DV's *generated* table leaves unpopulated (it does not advertise `VK_EXT_depth_bias_control`), so the indirect call jumped to garbage (PC = ASCII `"_emit_li"`) and PC-alignment-faulted on the first world brush-draw. ⭑ **The tracked fix — populate V3DV's dispatch — was unnecessary:** `vk_common_CmdSetDepthBias2EXT` is a defined symbol in `libv3dv-phoenix.a` and the driver is linked in-process, so the trampoline calls it **directly** and never touches the empty slot; the change stays in the port's glue rather than V3DV's generated code. Gated: gate's own `--only vkq` arm, rc=0, **0 faults**, 7975 frames, #67 torch ROI **PRESENT**, frame unchanged. ⚠ **No visible difference was demonstrated** — polygon-offset z-fighting is subtle here; the justification is removing a known-broken path, not a proven visual win. |
| D7 | **Three intentional upstream deviations are still carried**: `TD-14-devfs-direct` (devfs fast-path predicate), `TD-14-console-open-fastpath` (strdup short-circuit for `/dev/console`), `TD-14-tiocspgrp-pgrp` (correct semantics, upstreamable as-is). | Open only in the sense that they are deviations; all three work as designed. ✔ **2026-09-25 — verified against source, and two things are off.** (1) **`TD-14-tiocspgrp-pgrp` has NO marker in the tree.** The TD-14 IDs actually present are `TD-14` (×6), `-console-alias`, `-console-open-fastpath`, `-devfs-direct` (×2), `-psh-retry`, `-tty`, `-ttyopen-nonfatal`. The TIOCSPGRP code exists (`devices/tty/libtty/libtty.c:576-580`) but carries no marker and no deviation comment — it reads as ordinary code, with the same `FIXME: check permissions` style upstream uses on the neighbouring `TIOCSCTTY`. So this row lists an ID that does not exist, the **same staleness class D2's row already had to correct**. The other two are live (`-devfs-direct` `kernel/proc/name.c:32` ×2, `-console-open-fastpath` `libphoenix/unistd/file.c:371`). (2) ⚠ **The framing is off-policy.** Owner, 2026-09-08 (direct quote): *"We don't send anything upstream. We get changes from upstream to our fork and we maintain our fork."* Under fork-only, carrying a deviation that works as designed is the **normal state**, not debt, and "upstreamable as-is" is not a pending decision. ➡ Suggest closing this row, or re-scoping it to the two live markers as ordinary documented behaviour — the owner's call. |
| D8 | **Five unmerged local branches hold work that was never landed or discarded.** ★ `devices/wip/rpi4-wifi-glom` triaged 2026-09-23 and it is **landable**: SDPCM glom de-aggregation (the firmware packs several subframes into one SDIO superframe; without de-aggregation you get one per read, which is the 0.14 MB/s), **one file, +201/−14, zero leftover diagnostics**, it **compiles clean against today's tree**, and `diag_wifiFrameRx` puts it in the **real RX path** feeding the `/dev/wifidata` lwip seam — so the claimed 0.14 → 3.0 MB/s is about networking, not a probe. The others: `lwip/agent/rpi4-genet` (+115), `lwip/full-history-backup` (+178), `lwip/rpi4-port-clean` (+6, drops unused Cypress WHD source), `lwip/wifi-wip` (+146). ✅ *22 merged branches deleted 2026-09-23, each verified in `master` first.* | Open — a keep/land/discard call per branch. **`rpi4-wifi-glom` needs only an AP + credentials to verify**, which is yours; before landing, check its file-scope `g_glom_*` state against however many consumers the driver has. ↩ *Correction to my own note: `syntax-check.sh` cannot check this file not because of a tooling gap but because the driver is **not in the project build at all** — it is standalone by design. The right instrument is its own `build-standalone.sh`, and under that **both master and this branch link with 0 undefined symbols** against today's tree.* ↩ **2026-09-23 — `rpi4-wifi-glom` never needed an AP: it was already landed.** Master carries `4e8a7e9 rpi4-wifi: de-aggregate SDPCM glom frames -- RX 0.14 -> 3.0 MB/s`, and master's `rpi4-wifi.c` has 69 `glom` mentions. Diffed master→branch: **+2/−16**, and the "+2" are merely the *pre-improvement* versions of lines master later fixed (`843d193`, mailbox-failure reporting) — the branch is strictly behind master with nothing unique. **Deleted.** ⚠ My earlier `master..branch` reading of "93 files, −10150 lines" was the wrong comparison: the branch forks from 2026-09-02, so that diff shows master's newer work as deletions. The branch's own change is the `+201/−14` the row already claimed. **Four `lwip/*` branches remain** (`agent/rpi4-genet`, `full-history-backup`, `rpi4-port-clean`, `wifi-wip`) and are still a keep/land/discard call. | ✔ **2026-09-25 — the four `lwip/*` branches are NOT redundant, and the WHD question is now a MEASURED fact.** Each has real unique content vs master (`agent/rpi4-genet` +8696 lines/19 files incl. `xhci-rig-handoff.h`; `wifi-wip` +2152/17; `rpi4-port-clean` and `full-history-backup` both **−49085 lines**), so none can simply be deleted — the commit counts quoted above are commits, not lines. ⭑ **What the two big ones remove is verified dead weight:** `sources/phoenix-rtos-lwip/wi-fi/whd/` is **1.6 MB / 75 files** of vendored Cypress WHD, and (a) its build is gated behind `ifeq (${LWIP_WIFI_BUILD},yes)`, which **every** project in the tree sets to `no` (`aarch64a72-generic-rpi4b/build.project:23`), so it compiles for nothing; (b) **no file outside `wi-fi/` references any `whd_` symbol**; (c) our actual WiFi driver `devices/wifi/rpi4-wifi/rpi4-wifi.c` has **0** `whd_` references — it is an independent implementation, not built on WHD. ➡ So "unused" is now established rather than assumed. ⚠ **Publication angle:** it is third-party code (Cypress/Infineon, Apache-2.0) that ships in a repo about to be published; keeping it means carrying its attribution, dropping it removes 49k lines that nothing builds. **Still the owner's keep/land/discard call** — not deleted unilaterally.
| D9 | **Every boot log opened with ~20 unlabelled characters of bring-up scaffolding**: `1111444422225555AS0` then `TR0`–`TR3`. Decoded 2026-09-23 — `1 4 2 5` are armstub milestones (errata 859971+1319367, L2 prefetch policy, SMPEN, L2CTLR timing) printed by **each of the 4 cores**; `AS0` ends the armstub; `TR0`–`TR3` come from `phoenix-kernel8-reloc.S`, one per core; plo's `hal_exitToEL1` adds `A` plus a digit for the EL it exited. Each is commented in source but opaque on the wire. | **RESOLVED 2026-09-25** — project `37421ec`, plo `217f288`. Gated behind **`RPI4_EARLY_MARKERS`** in the target's `board_config.h`, default **0**: a stock boot log now opens with `hal: console_init done`. **Gated, not deleted** — when a boot hangs before that line these characters are the only evidence that exists, and `summarize-rpi4b-uart-log.py` uses them to separate a firmware-handoff failure from a trampoline one. They stay single characters because four cores share one unlocked polled UART, so strings would interleave. One source of truth: plo reads the macro directly, while the armstub and trampoline (assembled standalone, no `board_config.h` on their include path) get a `-DRPI4_EARLY_MARKERS` that `build.project` greps from the same header — the mechanism `RPI4_LOG_TO_FILE` already uses. **Proven both ways**: disassembly counts of the macro's TXFF spin go armstub 0→9, trampoline 0→20, plo 92→100, each matching the source character for character; on hardware the markers-on build reproduces `1111444422225555AS0`/`TR0`–`TR3`/`A1` exactly, and the markers-off build is **3/3 trials** to psh with 0 of those literals, 0 faults, and the 4-core evidence (`smp release-2`, `r0p3 x4`) intact. ⚠ **Trap recorded in the header**: editing `board_config.h` does not invalidate plo's cached objects, so flipping this macro also needs a touch of plo's `_init.S` or it lands in the armstub and trampoline but not plo — a property of `board_config.h` generally (`PLO_SMP_ENABLE` too). `uart-summary.sh`'s stale `132AS0\|^132` pattern (which never matched the real 4-core output) was fixed and a trampoline stage added. |
