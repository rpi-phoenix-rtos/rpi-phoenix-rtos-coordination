# Week 2026-W36 (Mon 2026-08-31 → Sun 2026-09-06)

Phoenix-RTOS RPi4 — weekly working log. **This file is the whole picture.**
History lives in `docs/done/`, one-off analyses in `docs/misc/`. Convention +
week-rollover: `docs/inprogress/README.md`.

---

## 1. DECISIONS — ALL ANSWERED except D4 (D6-D9 answered 2026-09-02)

**Settled facts (not decisions — never re-ask):** the toolchain is **GCC 16.2.0 + binutils 2.47** and has been the default since 2026-08-28 (owner-approved); the whole system is gcc-16-built and HW-verified.


| # | Decision | Answer → status |
|---|----------|-----------------|
| D2 | move the V3D driver `tools/` → `phoenix-rtos-devices` | **YES** → queued (last item of the ports migration; §2). |
| D3 | SD SDMA-write gate | **"do whatever you suggest"** → ✅ **gate stays OFF** (PIO writes). Correctness is proven cache-cold; the one gate-ON boot damaged a file on the card and the quirk is documented. Closed — I'll stop poking it. |
| D5 | make the SD card non-bootable | **YES** (you'll reflash when you want SD-boot tests) → queued as a small, optional determinism win (§2). |
| D4 | "genuine tool" boundary | *You'll decide later* — nothing blocked. |
| D9 | how much of the V3D driver can actually move into `phoenix-rtos-devices` | **APPROVED (option A) → implemented 2026-09-02.** Glue + build scripts now live in `sources/phoenix-rtos-devices/gpu/rpi4-v3d/mesa/` on the `wifi/rpi4-wifi` model; Mesa and `tools/.gpu-libs/` stay put. 4 repos committed, build-verified, archives symbol-identical. Stage-3 Pi cycle (GLQuake + X11 GPU desktop) running now. |
| D7 | fork + publish the Quake II / Quake III port work | **YES → DONE, then corrected.** Reviewed both (11 leftover `YQ2DIAG` prints discarded), committed (`yquake2 d0c064f9`, `quake3e acdf34a`) and pushed. First attempt created **plain repos, not forks**; owner caught it. Now real GitHub forks (`parent=yquake2/yquake2`, `parent=ec-/Quake3e`), branch `phoenix-rpi4-port` pushed + set as default, so the compare-to-upstream view works. `quakespasm` default branch aligned too. ⚠️ **One owner action left:** the two mis-created repos are renamed `*-nonfork-obsolete` and I cannot delete them (token lacks `delete_repo`) — run `gh auth refresh -h github.com -s delete_repo` then `gh repo delete rpi-phoenix-rtos/yquake2-nonfork-obsolete --yes` (same for `quake3e-`), or delete them in the web UI. |
| D8 | quakespasm pinned 16 commits behind what we test | **"remove the pin, always use latest" → DONE.** All four game forks are now tracked by **branch** (`phoenix-rpi4-port`) in `bootstrap-linux-host.sh`, so a clean build takes our fork HEAD instead of a stale sha (`8e47fc0b0`). Mesa and `PI_FW_REF` stay pinned deliberately (exact-sha reproducibility). ⚠️ **Scope correction:** branch-tracking only affects the `external/` dev clones. Those are a build input for **quakespasm and vkquake only** (`build-showcase-apps.sh:337,381` → `libquakespasm.a`/`libvkquake.a`); for yquake2/quake3e nothing builds from them. See the new §1 D10. |
| D10 | two sources of truth per game (owner-found) | **Owner: "one way to build; no parts of the patches lost."** → **(b) DONE, (a) prepared.** The **fork is now the single source** and each ports patch is generated from it (`scripts/game-port-patch.sh --check/--regen`, wired into `publication-audit.sh`); verified for all four that pinned tarball + generated patch == the fork tree **byte-identically** (`diff -rq` 0 differences) and still applies with plain `patch -p1`. ports `0d9de9a` + fork `ee181885` pushed. **(a) DONE 2026-09-03:** all five game ports are `if: true` in `ports.yaml` (plus a new `supertuxkart` entry — it had never been registered), the `_user/rpi4-quake` / `_user/rpi4-vkquake` wrappers and their `loader.disk` `app` line are deleted, and the ad-hoc `libquakespasm.a`/`libvkquake.a` archive steps are gone. All five engines now install into the rootfs and are HW-verified from the clean image (STK: GPU UI only — see the "Verification matrix, clean image" progress entry). |
| D6 | **NEW** — purge ~55 MB of build-output ELFs from coord **git history**? Removing them from HEAD is done; shrinking the history needs a **force-push of coord `main`**. | *Needs you.* Harmless to leave (clones just carry the weight). I won't force-push a published branch on my own. |

## 2. PLAN FOR THE NEXT DAYS

**★★★ OWNER PRIORITY (2026-09-03, overnight): ONE COMPLETE SD IMAGE WITH EVERY GAME, AND HONEST DOCS.**
Owner's words, and his corrections of *our* claims — treat these as facts, not open questions:
- `README.md` states three things that are **false/outdated** and must go: (a) "the GLQuake binary is too large to run from the ext2/NFS loader" — **the loader was fixed long ago; big binaries run from NFS and ext2**; (b) that only ONE of GLQuake / vkQuake can be on an SD image; (c) that Q2 / Q3 / STK are "not included / not yet supported" on SD.
- **All the games already rendered correctly on Pi 4** — yQuake2 on **gl3/GLES3** (mipmap generation had to be off), Quake III on **both** renderers (OpenGL and Vulkan), QuakeSpasm, and vkQuake (which also had working mouse + keyboard). My "unresolved whether the gl3 build ever rendered" note was wrong; delete it.
**Owner escalation (2026-09-03): CLEAN-REBUILD EVERYTHING, then test.** "We did important changes to ALL the subprojects — merging upstream, rearranging, fixing build scripts, generating patches, managing forks. All these changes could break some ports (incl. game ports). So we need to cleanly rebuild (no reuse of old modules, .o or .a files, binaries etc), relink, repackage everything and do a clean test. Only then we know... And fix all scripts that potentially reuse old stuff or do not clean correctly." Also: "Never hand-copy old binaries" — done, `make-pristine-nfs-export.sh` no longer carries executables forward (coord `996db622e`).

**Owner addition (2026-09-03): prove DETERMINISM with a clean chain, no reused binaries.**
9. Recreate the **NFS rootfs fully clean from the build scripts** — reusing none of the old hand-staged binaries — so every port is proven to build, link, be present, **and run**.
10. Then a **full clean SD build** to an image.
11. Then **compare the key binaries**: new NFS rootfs vs new SD image.
12. Then flash the SD card, and **re-run the ports from SD boot** on the Pi. That chain is the final verification of the recently modified ports and of build determinism.

**★★ OWNER RULE (2026-09-03) — ONE BUILD PATH PER PORT; `tools/` IS NOT A PORTS DIRECTORY.** Stated as policy, not a question:
- `tools/` in the coordination repo is **only** for our own tools needed to build the project that cannot easily live anywhere else. "Use `tools/` if no other place makes sense."
- A **normal port** — take a versioned artifact from the internet, apply our patches, build — belongs in `phoenix-rtos-ports` as a framework port. `nano` is the model case.
- Improvements to the system itself belong in `kernel` / `devices` / `libphoenix` / etc.
- The **only** acceptable reason to keep a hand-written script is a build too complex for the framework. Owner expects that to be "very rare and exceptional".
- The problem to eliminate: ports that exist **twice** (framework port *and* legacy `tools/ports/build-*.sh`), where the legacy script is sometimes the one the image actually uses. "We don't need multiple ways to build mv or nano or quake or sdl or python." To be done **carefully, in a controlled, tested way** — one port at a time, not a sweep. Audit running now; owner asked not to interrupt the in-flight image work for it.

**OWNER TASK (2026-09-03, scheduled — not now): ship a MODERN nano (target 9.2), not 2.2.6.** The port pins nano 2.2.x *specifically* to dodge gnulib, and that reason is now obsolete: the **coreutils** port ships gnulib-based software by renaming the clashing symbols in a patch — `sources/phoenix-rtos-ports/coreutils/patches/0001-rename-gnulib-gettime-settime.patch` is exactly that technique, and `0002`/`0003` handle the gnulib↔Phoenix stdio internals. Owner: "we can follow the same approach as in coreutils, and ship a modern, up-to-date nano". So the work is: bump to the newest stable nano, port gnulib the coreutils way, drop the `nano-phoenix-shim.h` pieces that a modern nano no longer needs, and retire the "2.2.x deliberately, no gnulib" rationale from both the port and the build script. Pairs naturally with the deduplication above — do the version bump **as** the framework-port-only move, so nano ends up modern *and* single-pathed in one step.

**OWNER TASK (2026-09-03, scheduled): hunt the nano pattern across ALL ports — legacy pins whose blocker we later removed.** Generalizing the nano finding, in the owner's words: *"review our ports and look for similar situations in which we based on a legacy, old version due to some porting difficulties. Maybe in other, later ports (or general system extensions) we actually solved or improved the situation to a point that a newer version can now be easily ported."* The nano case is the template: pinned at 2.2.6 to avoid gnulib, then `coreutils` defeated gnulib by renaming the clashing symbols in a patch, and nobody revisited nano. Audit running now (read-only): every `port.def.sh` / `tools` version pin, the *stated reason* for each old one, whether that reason still holds given what libphoenix/kernel gained since, classified UNBLOCKED / STILL BLOCKED / PINNED BY POLICY / UNKNOWN-no-recorded-reason, ranked by value-over-effort. A pin with no recorded reason is itself a finding.

Work items, in order:
1. Fix the build scripts/settings so a **complete rootfs** builds with ALL games (all four quakes + STK) in one image.
2. Keep **as little ported software in `loader.disk` as possible** — games live on the rootfs, not the boot blob.
3. Prove there is **no** problem launching big binaries from **ext2 and NFS** (explicit test, not an assumption).
4. Make all the games build and run **deterministically**.
5. Test every game binary **over NFS** and confirm each renders (HDMI evidence per game).
6. Then build the **SD image** and verify the game binaries on it are **byte-identical** to the ones tested over NFS (rule out build differences).
7. Tell the owner to flash **only at the very end**.
8. Finally, make **all** docs reflect the true state of the ports — no outdated limitations anywhere.


0. **AF_UNIX: the remaining bug is a ZEROED MEMORY REGION in the forked child — instrumented, waiting for a hit.** ⚠️ **Correction to yesterday's claim: the id fix is NOT the cause of the cross-delivery.** The mismatch reproduced with it in place (`DATA-MISMATCH ... data[]=INTACT`, `recv bytes appear nowhere in data[]`), and the logic never fit: `unix_transfer` uses **`socketpair`**, so no pathname is ever resolved and stale-name/id-reuse cannot apply. The id fix stays (a real hazard, ECONNREFUSED semantics now pinned by tests) but it is not this bug. Also: the earlier "read-overrun eliminated" came from a **passing** run, so it eliminated nothing. **What the crashes now say, precisely:** two different child faults have the SAME shape — a pointer that must be valid reads NULL. `fwrite` -> `mutexLock(stream->lock)` at `far=0x30` (a NULL `FILE *`; stdio globals are heap pointers in `.data` from `_file_init`) and `_malloc_chunkRemove` -> `lib_listRemove` at `far=0x10` (a NULL free-list link, `esr` = level-3 **write** fault). One region of the child's memory going to zeros explains both; a COW fault installing a fresh zero page instead of a copy is the obvious producer, and this path has had a COW/PROT_USER bug before. **Instrumented (tests `83eda31`, pushed):** the child now receives into a **stack** buffer (private by construction — separates "socket delivered foreign bytes" from "the page we read back is not the page the kernel wrote"), and a 64 KiB canary filled in the PARENT is verified in the child (inherited COW, so a zeroing fault shows as a run of zeros, reported with offset, offset-within-page, run length and whether it is zeros). Both reports use raw `write(2)` + `_exit` because stdio and malloc are the suspects. **Rates so far, honestly:** with `.bss` recv 1 mismatch in 12 runs; with stack recv **0 mismatches in 9** (too few to conclude) and 1 crash-family run. **Next: keep running trials until a canary fires, then read the offset/run to confirm or kill the zero-page theory.**

0a. ~~Wire clock sync at boot~~ **DONE for nfsroot** (see progress). **netboot: N/A, not pending** — its `/` is a `dummyfs` RAM root (the boot script has to `mkdir /dev`), so nothing populates `/etc` and there is no script for `psh -i` to read. It would need either a file server mounted before psh (netboot does mount the export at `/mnt`, so `psh -i /mnt/etc/…` is possible but couples the shell to that mount) or the clock set by a syspage *program*, which `ntpclient` is not (it is a psh applet). Left alone deliberately. **sd** would work — it has a real ext2 root and an `/etc/rc.psh` already — but its rc script starts no networking, and SD boot is not testable right now (card deliberately wiped, reader detached), so it waits.
0b. **posix/vm audit thread: CLOSED.** It produced 6 kernel fixes (open() construction ref, flags-write guard, port-0 sentinel, fault-dump narrowing + its own no-deref fix, storm canary, putUnusedFile symmetry) plus a regression test. The heap corruption that started it is **still open and still not reproducing** — instrumented so the next sighting names the block. Remaining known-but-unfixed items are listed at the end of `docs/misc/2026-09-02-kernel-heap-corruption-workorder.md`; the `unix_close` double-put is the only one with real teeth and it is balanced by design today.

1. **WiFi performance — SOLVED for now: RX 0.14 → 3.0 MB/s, TX → 3.3 MB/s.** The firmware aggregates inbound frames into SDPCM channel-3 superframes and we dropped every one (~18% of all RX); it **cannot be disabled** (`bus:rxglom=0` accepted and ignored), so it is now **de-aggregated**. The key was dumping a superframe and decoding it instead of guessing the stride: subframes sit in the **descriptor's slots measured from the start of the superframe** (slot 0 also holds the superframe's 12-byte header — headers at 12/96/192/288, gaps 84/96/96, slots summing to exactly the superframe length). Measured, 3 runs each: **TX 0.22–0.66 → 3.27 MB/s, RX 0.13–0.29 → 3.00 MB/s**, and the wild variance is gone — it was frame loss driving retransmit storms, not the radio. 740 superframes → 3595 subframes, `bad=0`, `rx_err=0`. devices `4e8a7e9`. Next (optional): per-frame overhead is the remaining ceiling; wired genet is ~30/20 for scale.
2. **V3D driver → `phoenix-rtos-devices`** (D2/D9 option A) — **Stages 0-2 DONE 2026-09-02**; the glue + build scripts now live in `sources/phoenix-rtos-devices/gpu/rpi4-v3d/mesa/` and every consumer is repointed. Build-verified (GPU archives rebuilt from the new location, symbol-identical; `--scope core --variant nfsroot --with-tests` rc=0); **the Stage-3 Pi cycle proving GLQuake AND the X11 GPU desktop is still owed** (the UART was locked by another task). Key finding: *"move it all" is not achievable* — the three archives are ~99% compiled Mesa and their build needs meson + `ninja` codegen + a host `compile_commands.json` + the 1.2 GB `external/mesa` clone, none of which a devices framework component can do. What we own is ~3.7k lines of glue, and **half the driver already lives in `devices/gpu/rpi4-v3d/`** (the GPU server + client lib + the same UAPI headers `tools/` duplicates). Achievable scope = move the glue + build scripts on the `wifi/rpi4-wifi` model (source in devices, standalone script, gitignored outputs), Mesa and `tools/.gpu-libs/` stay put. Plan + hazards: `docs/misc/2026-09-02-v3d-migration-plan.md`.
3. **Sync all four game forks with upstream** (owner-requested 2026-09-02, near future) — now trackable because they are real forks. Drift today: **vkquake 212 behind**, **yquake2 85**, **quakespasm 12**, **quake3e 11**. Do them one at a time: merge upstream, rebuild, one Pi cycle per game (HDMI-verified), push only what renders. Our own delta is small in every case (see progress), so conflicts should be few — vkquake is the one with real churn upstream.
4. **Pi firmware pin — worth revisiting, 1 cycle** (owner-raised): analysis in `docs/misc/2026-09-02-pi-firmware-pin-revisit.md`. The recorded reason for the pin **does not hold up** (see progress), so bumping it is cheap to try; no EEPROM reflash needed.
5. **NFS-over-WiFi** (optional, closes the perf ask fully) — TCP numbers are in (3.3 TX / 3.0 RX); an NFS workload over `wl2` would need a second mount alongside the NFS root, so it is deliberately deferred rather than risk the netboot rig.

---

## 3. IN PROGRESS

**Rebuilding nfsroot with the full stage list** so a deterministic torch capture can run. Two script defects fixed en route (below). Previous flashable image `bec273cc…` is **withdrawn — it shipped a broken Quake III**; no new SHA until it passes the completeness gate, byte-identity, and a Pi run.

**Next, in order:** (1) re-sync the NFS export from the fresh tree + `check-rootfs-complete.sh`; (2) **the first deterministic torch capture** — `quakespasm +map start` then `vkquake +map start`, one Pi cycle each, graded by `scripts/check-torch-rois.py` instead of by eye; (3) `nano`/`mc` + `import curses` on HW; (4) rebuild the `sd` image, byte-verify, hand the owner a SHA to flash.

**⏸ OWNER-BLOCKED:** flashing (card reader detached — it shares the USB-C port with power/eth) and the SD-boot port re-run, which is also the right way to test SuperTuxKart (assets local).

**Remaining known gaps:** (1) vkQuake start-map torches **absent** — fix ships intact, never worked (see progress); (2) SuperTuxKart not yet in-game; (3) X11 root window black (cosmetic `wmsetbg`); (4) `nano`/`mc` built + staged but never driven interactively.

## 4. PROGRESS THIS WEEK (terse; detail in `docs/done/`)

- **★★★★★ vkQUAKE TORCHES ROOT-CAUSED AND REPRODUCED ON DEMAND — after five re-diagnoses, it is now a one-line hypothesis with a measured confirmation.** `vkquake +r_lerpmodels 2 +map start` renders the flames: **397–599** and **363–562** lit px across **7/7** frames at the reference viewpoint, against **0 and 0** with the default cvar in the identical scene minutes earlier (quakespasm scores 401–548 for comparison). Evidence: `docs/misc/torch-archaeology/vkquake-TORCHES-WITH-lerpmodels2-2026-09-03.png`.
  - **Root cause = the unported #67 V3D alias vertex-fetch defect.** `MOD_NOLERP` forces `blend = 0` while the model is multi-pose, so both position bindings land on the *same* offset and V3D mishandles the draw. **quakespasm needed two fixes for exactly this, on exactly this GPU** (`4ef0a42` "don't bind Pose2 at blend==0", `3d742a3` "mis-fetches once the VBO spans a second page"), and **vkQuake has neither**. `r_lerpmodels 2` bypasses the `MOD_NOLERP` exclusion → two *distinct* offsets → correct fetch → flames appear. So it was never alpha, never scanout, never a regression: **a known, already-solved V3D bug that was fixed in one engine and never carried to the other.**
  - **Immediate workaround exists** (`r_lerpmodels 2`, no rebuild). The permanent fix is to port quakespasm's two fixes into the vkquake fork — next.
  - Two instrument lessons paid for here: `+devstats 1` is not a vkQuake cvar, and the resulting console overlay made the gate report UNUSABLE — the viewpoint check refused a frame it could not grade instead of scoring a covered screen, which is exactly the behaviour that was missing in August. And the staged quakespasm-specific `id1/config.cfg` spams vkQuake with `Unknown command "vid_width"` every boot; worth splitting per engine.

- **★★★ `import curses` WORKS ON HARDWARE — end-to-end, machine-verified.** `CURSES-IMPORT-OK b'2.2'` / `CURSES-SETUPTERM-OK cols=240` / `CURSES-PY-PASS`. Getting there found a bug **far bigger than curses**: `dlopen: unresolved symbol: _PyLong_UnsignedInt_Converter`. The symbol is a global `T` in `Objects/longobject.o` and that object *is* linked — but `-ffunction-sections` + `--gc-sections` drops functions the static interpreter never calls. Exactly three were missing (`_PyLong_UnsignedInt_Converter`, `_PyLong_Size_t_Converter`, `_PyLong_UInt8_Converter`) while their referenced neighbours **from the same object** survived. Phoenix resolves `dlopen` against the **symtab** (a static ELF has no `.dynsym` — which is why this port installs python3 non-stripped), so **any** extension module needing a private C-API helper the interpreter happens not to call was broken. Fix needed overriding LDFLAGS in **two** places: configure alone was not enough because CPython's Makefile computes `PY_LDFLAGS = $(CONFIGURE_LDFLAGS) $(LDFLAGS)` and takes `$(LDFLAGS)` from the **environment at make time**, putting the flag back on the final link. Costs +1.5 MB on 57 MB (+2.7%); the alternative is a growing list of `-u` flags each found by a crash. ports `7ea80dd`. Also: `nano 2.2.6` and `GNU Midnight Commander 4.8.31 (ncurses 6.4)` both **confirmed running on HW** — the last "built but never executed" gap is closed. Smoke test now ships in the overlay (project `591dc62`) because psh mangles nested quotes, which made `python3 -c '…'` die with a `SyntaxError` that reads like a curses failure and is not one.
- **★★ vkQuake torches: alpha theory REFUTED, and `d3e329c` was a no-op from day one.** Its premise is false in this codebase: vkQuake 1.34's nobright palette sets **alpha=255 on every index** (`gl_texmgr.c:573-588`), so the "present opaque models with alpha=1" override writes 1.0 over 1.0. The flag fires exactly as designed and changes nothing — which resolves the archaeology's open ambiguity in favour of **never worked**, with no V3D/Mesa regression to hypothesise. Refuted six ways, most decisively: `lavaball.mdl` is **88.2% fullbright** (4× the flame's 23.3%) and renders fine; the torch's **non-fullbright holder is missing too**; and alias pipeline 0 has `blendEnable=VK_FALSE`, so any fragment writes colour unconditionally — yet the wall's brick pattern runs **unbroken** through the ROI. So there are **no fragments at all**, not dark ones. New measurement: ROI stats byte-identical across 7 frames over 2.5 min while the flame cycles 6 poses at 10 Hz — nothing contributed in *any* pose. **My `+r_showtris 1` probe was inert**, as suspected: V3DV *does* advertise `fillModeNonSolid`, but the Phoenix shim hardcodes `vulkan_globals.non_solid_fill = false` (`glue/pl_phoenix_vk_vid.c:876`), so `R_ShowTris` early-returns. Leading candidate **R1**: the unported **#67 V3D alias vertex-fetch defect** — quakespasm needed *two* fixes for exactly this on this GPU (`4ef0a42`, `3d742a3`) and **vkQuake has neither**. Next: `vkquake +devstats 1 +r_lerpmodels 2 +map start`, which splits R1 from R2 in one cycle.

- **vkQuake torch probe #1 (`+r_showtris 1`): INCONCLUSIVE, and said so rather than over-read.** The ROI crops show bare wall with no torch-shaped wireframe, which *looks* like "the flame is never drawn" — but `gl_rmain.c:763` gates showtris on `vulkan_globals.non_solid_fill` (`VK_POLYGON_MODE_LINE` / `fillModeNonSolid`), which V3D may not expose, so absence of wireframe may mean the probe was inert. Also noted: "bare wall" does **not** discriminate — a flame drawn with alpha=0 under an alpha-keyed scanout produces exactly bare wall too, so it is consistent with *both* hypotheses. Static analysis of the alias-fullbright vs present paths delegated; the Pi stays with me for verification cycles. Side finding: vkQuake logs `Unknown command "vid_width"…` — the staged `id1/config.cfg` is quakespasm-specific (harmless, worth splitting later).

- **★★★ vkQuake torches: MEASURED ABSENT — the archaeology's inference is now a fact.** Same `cmdline`-cvar fix applied to the vkquake fork (`65f599e`, patch regenerated ports `9ddc08d`), rebuilt, restaged — vkQuake had the identical upstream defect, so `+map start` never worked there either. With it, `vkquake +map start` holds the **same reference viewpoint as quakespasm** (mae 3.4–3.6) and `check-torch-rois.py` scores **0 and 0 lit pixels across 7 frames**, where quakespasm at that identical viewpoint scores **401–548**. Same map, same scene, same GPU, one engine renders the flames and the other does not. That is a clean A/B instead of an argument, and it is the first time this bug has been stated as a number. Evidence: `docs/misc/torch-archaeology/vkquake-MEASURED-ABSENT-mapstart-2026-09-03.png` (vs `…quakespasm-VERIFIED-mapstart-…png`). `d3e329c` (present opaque models with alpha=1) ships intact in fork, patch and compiled SPIR-V — so **the shipped fix does not fix it**, and the alpha-scanout theory needs re-testing rather than re-asserting. Next probe, now possible because `+commands` work: `+r_showtris 1` discriminates "geometry submitted but shaded invisible" from "never drawn".

- **★★★★ ROOT CAUSE OF THE WHOLE TORCH SAGA: `+command` NEVER WORKED ON SHAREWARE DATA — so no Quake I test ever had a controlled viewpoint.** `Cmd_StuffCmds_f()` iterates the **`cmdline` cvar**, and upstream sets that cvar only on the **registered** path, *after* the `gfx/pop.lmp` checksum — the shareware branch `return`s first. All our Quake I testing runs on the free shareware pak, so every `+map …` was **silently ignored**: `argc=3` and `Command line: quakespasm +map start` both echoed correctly, then `quake.rc`'s `startdemos` ran and the **attract-mode demo loop** played instead. That is why "torches present" and "torches absent" were screenshots of *different places in the map*, and why five fixes could each look right and none stick. Fixed in the fork (`b2a47ae`, moved the assignment above the early return; costs nothing on registered data), patch regenerated (ports `d959fb1`), rebuilt, restaged.
- **★★★ GLQuake/quakespasm torches: MACHINE-VERIFIED PRESENT — the first deterministic visual verification in this project.** With the fix, `quakespasm +map start` loads the map (no `Playing demo from demo1.dem` line any more) and the viewpoint is stable at the reference (mae **3.4–3.5**, vs 12–24 for demo frames). `check-torch-rois.py` scores **401–468** and **417–548** lit pixels against a threshold of **8** — 5 of 8 frames pass, the rest are menu/loading. Not "looks right to me": a number, reproducible, with the lavaball decoy excluded. Evidence frame kept as `docs/misc/torch-archaeology/quakespasm-VERIFIED-mapstart-2026-09-03.png`. **vkQuake next** — same method, and it is expected to FAIL, which will convert the archaeology's conclusion into a measured fact.

- **Two build-script defects fixed, both silent-failure class.** (a) `--scope project` now runs `fs` (coord `7358af67c`): `prepare-buildroot.sh` deletes `_fs/root` with `rsync --delete` on every run, so `project image` left a rootfs with no `/etc` and **no ~300 MB of game data** — and no stage failed. `core` already included `fs` and documented why; `project` never got it. (b) The content-keyed patch marker is now **self-healing** (build `fc3de5c`): it correctly refused to build yquake2 whose work directory still held the *pre-regen* patch's hunks, but refused *permanently* when the remedy was always the same mechanical `rm -rf`. It now clears the stale tree as part of failing, so the next run re-extracts — one loud failure, then automatic recovery, instead of a wasted cycle in the unattended loop. `PHOENIX_PORT_KEEP_STALE_WORKDIR=1` preserves local edits.

- **⚠️ FOUND: `--scope project` (and `core`) silently leave an INCOMPLETE rootfs.** `prepare-buildroot.sh` rsyncs `_fs/<target>/root` with `--delete`, but those scopes run only `project image` — the `fs` stage never re-applies `root-skel` or the `rootfs-overlay`. Caught it because `_fs/…/root/usr/share/` had **no game data at all** after an `--scope project` run, while `.buildroot/_projects/…/rootfs-overlay/` correctly held `pak0.pk3` + `pak1.pk3` + `q3key`. So the tree looked built and would have produced a dataless image. `--scope auto` picks the full `host fs core ports project image` list and is fine; the new ext2 completeness gate catches it for `sd`, but **nfsroot has no such gate**, which is how it slipped past. Fix queued (add `fs` whenever prepare ran) — deliberately NOT applied yet because `rebuild-rpi4b-fast.sh` is executing right now, and editing a running bash script corrupts the run (that has already cost this project one build).

- **★★★ TORCH GATE SHIPPED — a checker that would have caught the false closure** (coord `35d86c151`, `scripts/check-torch-rois.py`). Scores the two archway torch ROIs, exits non-zero when dark, and explicitly ignores the lavaball ROI that caused the 2026-08-22 false positive. Verified on **all four** cases: host reference **PASSES**, the Aug-22 "proof" frame **FAILS**, today's vkQuake grabs **FAIL**, demo frames report **INCONCLUSIVE**. Two defects in the handed-over spec, both found by *running* it instead of trusting it: (1) its pass rule was **unachievable** — `R>180` "orange flame core" counts **zero** pixels in the host reference itself, because at the start map's light level the flame renders pale warm grey peaking near R=123; a gate that fails known-good frames gets switched off, so it is recalibrated against measurements (reference 17/11 lit px vs vkQuake 0/0 peaking R=63–74, ~2× separation). (2) Fixed ROIs are meaningless off the reference viewpoint and would report "torches absent" about a wall not in shot — the *mirror* of the original false positive — so the checker now thumbnail-compares to the reference first and reports **UNUSABLE**. That guard paid off immediately: **every idle quakespasm tick frame is Quake's DEMO ATTRACT MODE** wandering the map (mae 14–17 vs 3.5 for a spawn-point frame), so the archaeology's "quakespasm torches visually confirmed" rested on eyeballed frames from arbitrary viewpoints. **Nobody ever controlled the viewpoint — that is the deeper reason this bug survived five re-diagnoses.** Grading either engine now needs an explicit `+map start` capture, which is the next step.

- **★★★ TORCHES ANSWERED: it was never fixed, and never regressed — the frame that "proved" it showed a LAVABALL.** Full archaeology in `docs/misc/2026-09-03-quake-torch-regression-archaeology.md` (coord `046852f41`). Owner's "solved 5 times" is **two unrelated bugs sharing a word**, plus one real delivery loss: (a) *quakespasm — mangled geometry* (#28→#67), genuinely fixed after two band-aids and two refuted hypotheses, and **visually re-confirmed rendering torches today**; (b) *vkQuake — invisible models* (`d3e329c`, fb0 scanout keys on colour-buffer alpha; `flame.mdl` is nearly all fullbright texels with alpha≈0) — a different symptom, different code, **zero shared root**. The 2026-08-22 grab that closed (b) shows a moving `misc_fireball` **lavaball projectile**, not a wall torch; the archway torches are absent in that frame *and* in today's, identically. **So: false closure, not regression.** `d3e329c` ships intact in fork HEAD, in the generated patch, *and* in the pre-compiled SPIR-V (`spirv-dis`-verified) — and the torches are still gone. So the owner's "both engines" premise doesn't hold: this is **vkQuake-only**, and the real defect is a **verification** failure, not a lost fix. Amplifiers named: the symptom-name collision (every vkQuake report got re-diagnosed against quakespasm's history), a determinism metric that scores a consistently-broken render as 0.0% and *passes*, and a KNOWN-ISSUES entry that overwrote its own "these RESOLVED claims were FALSE POSITIVES" correction and was then deleted 17 days before the owner re-reported it. Honest caveat kept: the 2026-08-03/04 "7/7 verified" claim is **unverifiable** (artifacts gone, no vkQuake capture harness), so "the fix never worked" is the parsimonious reading, not a proven one.
  - **Acted on the live landmines already:** `external/vkquake`'s local `phoenix-rpi4-port` was one commit behind `master` and the missing commit **was `d3e329c`** — a checkout + `--regen` would have silently deleted the torch fix while `--check` reported OK; fast-forwarded (remote was already fine). `game-port-patch.sh --check` was **red** (`yquake2 STALE`) because I committed the GLES3 discard to the fork and never regenerated its patch — so that fix would not have shipped either; regenerated (ports `e9a39f4`). Three false "FIXED/HW-validated" torch claims corrected in the support matrix (coord `80aaa43e7`) — a doc asserting a visual bug is fixed is *how* it survived five re-diagnoses.
- **★ My own `--scope full-clean` invalidation broke coreutils; fixed and verified.** `b_port_invalidate_stale_configure` deletes autoconf outputs when the libc API changes, and its own comment warned recipes guard on different markers — the list still missed one: **coreutils, python and openssl111 guard on `Makefile`**. Leaving it made the recipe skip configure, then the stale Makefile tried to regenerate the deleted `config.status` → `make: *** [Makefile:8896: config.status] Error 127`. Now also removed, but only when `Makefile.in` proves it generated (a port shipping its Makefile as source must not be wrecked). Verified: coreutils rebuilds, **105 tools installed**. build `24bf9c1`.
- **`_curses` VERIFIED BUILDING** — `_curses.cpython-314.so` compiled and installed into the rootfs in the nfsroot build, so the unbuilt Tier-0 change works; `import curses` still needs the on-HW check.

- **★★★ CAUGHT THE IMAGE SHIPPING A BROKEN QUAKE III — before the owner flashed it.** The full-clean image was byte-perfect in all 12 compared binaries and still had **`pak1.pk3` and `q3key` absent**, so Quake III would die at startup with `User Interface is version 3, expected 6` — the exact failure "fixed" earlier the same day. Cause: the pak was hand-staged into the **live NFS export**, never into the `rootfs-overlay`, so recreating the export from the build tree correctly discarded it. Nothing noticed because the only completeness check **printed `MISS` and exited 0** (indistinguishable from success in a build log) and its list didn't include `pak1.pk3` anyway. Root hole: **local builds never run `stage-game-data.sh`** — only the Dockerfile does — so a stale overlay persists indefinitely; ours was frozen at 00:54, predating the 07:13 asset. Fixed the data, then the hole: `scripts/check-rootfs-complete.sh` now **owns** the required/optional lists (game **data** is required, not just binaries) and exits non-zero; the export path reports loudly and the **ext2 image path REFUSES to build** — an image is what someone flashes and boots. One list, two consumers. coord `03b6653e6`.
- **★★ A stub header in one port was silently breaking a different port — build-order dependent.** The nfsroot rebuild failed compiling CPython: `_localemodule.c:596: error: 'DAY_2' undeclared here ... did you mean 'DAY_1'?`. `tools/ports/build-mc.sh` copied its own `<langinfo.h>` over the **shared** `$SYSROOT/usr/include/langinfo.h`; that stub declares only `DAY_1`/`ABDAY_1` where libphoenix defines the full POSIX item set, and `_localemodule.c` enumerates all of them. Invisible for months precisely *because* of build order — python is a framework port (ports stage), mc a showcase step after it, so a full build compiled python **before** the pollution landed and merely left the poisoned header behind for next time. Removed (libphoenix has `nl_langinfo` too, so the stub object went as well), sysroot header restored. coord `1a201ffd8`. **This is the strongest evidence yet for the owner's one-build-path rule**: a port that writes into a shared sysroot can break an unrelated port, and the damage stays hidden until build order changes.

- **Tier-0 #2 written: `import curses` becomes true of the image, not of a hand-run script** (ports `95079f8` — **committed, UNBUILT, UNVERIFIED**; it lands in the next build). The framework `python` port had no `_curses` path at all; the module existed only via `tools/python-port/build-curses.sh`, which the build never calls — so curses "worked" on 2026-09-01 because a human ran that script, and any plain build shipped a python3 without it. Now built by the port itself as a dlopen'd `.so` (the HW-proven shape — folding `libncurses.a` into the interpreter would change the proven link for every user), gated on `PyInit__curses` actually being present. `ncurses` added to `depends=`; no `ports.yaml` change needed. The `-fPIC` worry was already handled — the ncurses port has configured `-fPIC` all along, so the risk is someone "cleaning up" that flag, and the consuming site now says so. **Watch on the next build:** ncurses is now built in *every* image and mirrors `curses.h`/`term.h`/`termcap.h` into the shared `PREFIX_H`, so other ports' configure may newly *detect* curses (bash is immune — its cache pins `bash_cv_termcap_lib`).
- **Tier-0 #1 completed properly.** Retiring mc's stub library left `mc.cache` still force-answering `ac_cv_func_hasmntopt=no`, which half-undid it: `mountlist.c` then defines its own no-op `hasmntopt()->NULL` and silently loses bind-mount detection. Fixed to `yes` (ports `a33541b`) — libphoenix both declares and implements it.
- **`compare-rootfs-binaries.sh` now verifies the artifact that gets flashed** (coord `fbaf9dc38`), via `RPI4B_SD_IMAGE=<exported .img>`, resolving partition 2's offset from the partition table. It previously read only a build intermediate, which a later `--variant` build overwrites — so it could not answer the owner's actual question. Also documented a trap I walked into: run it mid-build and `prepare-buildroot`'s `rsync --delete` makes most paths read MISSING, indistinguishable from the staging bug the script exists to catch.

- **Tier-0 #1 done: the framework `mc` port's stub library is retired** (ports `319130f`). libphoenix now implements the **whole** mntent family *and* `nl_langinfo`, so `libmcsupport.a` was entirely obsolete — both halves, not just the mntent one. It was also actively harmful: it staged its own `<mntent.h>`/`<langinfo.h>` into the **shared** `PREFIX_H`, i.e. one port mutating headers every other port sees, which is precisely how the sibling `hasmntopt` failure happened. Two deliberate behaviour changes recorded honestly: mc now does real `setmntent(MOUNTED)` — but Phoenix maintains no `/etc/mtab`, so it still lists **no mounts today**; the gain is it will list them once something writes that file, which a hardcoded stub never could. And CODESET moves from the stub's `"UTF-8"` to libphoenix's `"ANSI_X3.4-1968"`, which is *deliberate* (its multibyte layer maps bytes 1:1 with no UTF-8 decoder), so mc takes the 8-bit path like ncurses — wants an on-HW look at box-drawing. Without this fix, flipping mc to the framework port would have shipped a **worse** mc than we have.

- **★★★ PORT DEDUPLICATION AUDIT DONE — the owner's suspicion was an understatement.** `docs/misc/2026-09-03-port-build-deduplication-audit.md`. **81** third-party items, **24 dual-personality**, **39 divergences**. Classification: 76 `PORT` (45 already framework-only), **only 2 `TOOLS-JUSTIFIED`**, 11 genuine host tools — so the owner's "very rare and exceptional" is right, and almost everything in `tools/ports` should not exist.
  - **The mechanism, finally named:** `build-showcase-apps.sh --phase stage` runs *after* `build.sh`'s ports stage and writes the same `_fs/<target>/root`, so **on any path collision the `tools/` copy wins because it is written last**. That is the nano trap, and it answers "which one ships" for every overlap. Second trap: **`if: false` does NOT mean unbuilt** — it only drops a port from the *user-requested* set, so `sdl2` still builds via `depends=` while `nano`/`mc`/`ffmpeg` genuinely don't.
  - **Worst divergence: `tools/ports/build-libiconv.sh` does not build libiconv at all.** It compiles a hand-written ASCII/UTF-8 identity stub, while the framework port is real GNU 1.18 — two "ports" of one name that are *different software*. The stub ships inside `/bin/mc` and is installed into the **shared dev sysroot**, where it can shadow GNU 1.18 for any port whose include order favours the sysroot. Runners-up: **libffi 3.3 vs 3.4.6** and **zlib 1.3.1 vs 1.2.11**, each linked into different binaries of the *same image*.
  - **Two live regressions found (verified myself, not taken on trust):** (1) **`import curses` is broken in the shipped image** — the framework `python` port has no `_curses` path at all, and `build-showcase-apps.sh` never calls `tools/python-port/build-curses.sh`, so curses exists only if a human runs that script by hand (which is how it "worked" on 2026-09-01). (2) the shipping `vkquake` glue is **stale versus `tools/`** on the `copysign` removal — the one case where `tools/` holds the newer fix.
  - **Migration order:** `nano` first (leaf, patch sets already unified today → the reference migration: flip `if: true`, delete the showcase step, one Pi cycle) → `mc`, **but only after** fixing the framework mc port's retired `mntent` stub, or the flip would ship a *worse* mc than we have → then delete the four legacy lib steps (`libiconv`/`libffi`/`ncurses`/`glib2`) in one commit, which kills most of the shared-sysroot pollution at once. A **Tier 0** of framework-side fixes precedes every flip, precisely because two of them would otherwise turn a migration into a regression.
  - **Deletion trap to respect:** `build-showcase-apps.sh:185-198` `die`s if `tools/{v3d-driver-port,quakespasm-port,vkquake-port}` is missing — that list must be edited *before* any of those directories go, or every `--phase gpu` build breaks.

- **SuperTuxKart "clean-build link failure" was a FALSE ALARM — and the log now says so.** A monitor flagged `bin/supertuxkart] Error 1` with a wall of undefined GL/zlib/mbedtls symbols. That link is **meant** to fail: the recipe lets CMake reach its own link (`make -k … || true`) because the GLES entrypoints only resolve against `libGL-phoenix.a` at the stage-4 group-link — which then succeeded, `M3 complete`, installed in 399 s. The reason was in the comments but not in the log, so anyone skimming for "why is my image incomplete" would misread it, as my own monitor did. Added four lines that state the expectation *before* the failure and name the line that means a real one (the `no ELF` b_die). ports `ebe520d`. Lesson recorded rather than the near-miss repeated: check whether a failure is the one the recipe plans for before root-causing it.

- **★ V3D bandwidth win implemented for Quake II GLES3 — and the "cherry-pick" turned out to be a trap.** The sync plan called upstream yquake2 `9b891e59` (GLES3 `glInvalidateFramebuffer`) independently cherry-pickable. Tested it: **conflicts in 2 of 4 files**, not from our delta — it sits on upstream's later draw-batching/stats rework (its `GL3_EndFrame` uses `gl3_show_draw_stats`/`GL3_DrawCurrent2Dbatch` and has moved to `gl3_draw.c`). Worse, **its call is wrong for our case**: it passes `GL_*_ATTACHMENT` enums, which are for user FBOs, on the *default* framebuffer where GLES wants `GL_DEPTH`/`GL_STENCIL` — and it discards **colour** right before `SDL_GL_SwapWindow`, i.e. the frame being presented. So the idea was implemented directly instead (fork `9d423b6b`, 31 lines, in the file our tree actually has): discard depth+stencil before the swap so V3D's tile store skips resolving them to memory. **Both** enums are named on purpose — with a packed depth/stencil buffer Mesa drops the discard unless both appear (`fbobject.c discard_attachments()`: `mask &= ~zsmask`), so the obvious depth-only version would run and silently do nothing. `gl3_discardfb` (default on) makes it A/B-able **in one boot** rather than two builds. Also fixed `external/yquake2`'s detached HEAD onto a named `phoenix-rpi4-port` branch (step 0 of that sync). **Not yet run on hardware** — needs the patch regen + one cycle, both queued behind the image build.

- **Fork↔upstream sync is now a plan, not a guess** — `docs/misc/2026-09-03-game-fork-upstream-sync-plan.md` (coord `08b64d1ed`). Drift recomputed: quake3e **11** behind, quakespasm **12**, yquake2 **85**, vkquake **216**. In all four `merge-base(HEAD, upstream) == pin`, so the patch-generation contract holds and every sync is a **merge, never a rebase** (all four fork HEADs are published). The number that matters is the **conflict surface**, not the drift: quake3e **0 files**, quakespasm **1 (orthogonal)**, yquake2 **3 (all trivial/orthogonal)**, vkquake **8 of our 9, 3 of them hard** — `gl_rmisc.c` rewritten (+2326, every anchor moved), `sys_sdl.c` where upstream now owns the same handle table and partly *supersedes* our fix, and `host_cmd.c` editing our exact line range. Order: quake3e → quakespasm → yquake2 → **vkquake last, as its own step** (it is a re-port: TU list hand-transcribed from upstream `meson.build`, vendored SPIR-V to regenerate).
- **★ One upstream commit is worth cherry-picking ahead of any sync: yquake2 `9b891e59` "GLES3: Use glInvalidateFramebuffer()".** 4 files, +56/−21, guarded by `YQ2_GL3_GLES` — precisely the renderer this port ships. It tells a tile-based GPU to *discard* colour/depth/stencil at end-of-frame instead of resolving them to memory, which is V3D 4.2's actual bottleneck. Independent of the other 84 commits. Queued behind the image work; needs a rebuild + one HDMI cycle. (Riskiest to avoid for now: vkquake `e1abec5` SDL3-by-default, `b1b9c2f` MBOIT renderer; and quake3e `d0b2021e` flips VM memory RWX→RW, which is exactly where our `vm_aarch64.c` workaround lives — read it before merging.)

- **★★ Quake III QVMs are REPRODUCIBLE — the last hand-staged game artifact is explained bit-for-bit.** `tools/quake3-vm/build-quake3-vms.sh` pins ioquake3 (`58839361`, no upstream tags exist so a sha is the only immutable pin), builds the QVM toolchain with host gcc (`lburg`→`q3rcc`, `q3cpp`, `q3lcc`, `q3asm`) and compiles the three modules with source lists transcribed from ioquake3's own `basegame.cmake`. Result vs the pak we have shipped since 2026-08-05: `cgame.qvm` and `ui.qvm` **byte-identical**, `qagame.qvm` differs in **4 bytes** — the `__DATE__` string baked into the `gamedate` cvar. So the recipe does not merely replace the artifact, it *accounts for* it. Correction to our own docs, found in the process: `external/quake3e` does **not** carry the VM sources (only the four interface headers) — both sources and compiler come from ioquake3, which the bit-for-bit match proves is how the original pak was made. API compatibility is asserted by preprocessing `UI_API_VERSION`/`GAME_API_VERSION`/`CGAME_IMPORT_API_VERSION` out of *both* trees (6/8/4). The committed pak is deliberately **left in place**: identical for two modules, and for the third it would swap an HDMI-proven binary for a date-churned unverified one. `--install` exists but is opt-in. Not yet run on the Pi. coord `6ddde003c`, `c70c8ffde`, `7fbf04583`.

- **★ `nano` BUILDS AGAIN — the last soft failure is gone.** One line: nano 2.2.6 has `bool edit_refresh_needed = NULL;`, and its `bool` resolves to ncurses' `NCURSES_BOOL` (an int), so the initializer is an int-from-pointer conversion — a warning for a decade, an **error** since GCC 14. Fixed the initializer (`FALSE`) rather than demoting the diagnostic, which would hide real pointer/int mistakes. Upstream nano corrected this in later releases. Also **unified the two build paths**: `tools/ports/build-nano.sh` built the tarball *unpatched* while the framework port applies `patches/`, and the image takes the `tools/` one — so a fix landing in the port dir would silently never reach the shipped binary. Both now apply the same patch dir. Verified from a clean extract: static aarch64 ELF, 0 undefined. ports `a732d02`, coord `eb0eec1bf`.
- **★ The colima/macOS quakespasm linker failure is ROOT-CAUSED and fixed** (owner-reported). A clean Docker build reproduced it on Linux: `multiple definition of 'strtok_r'` — libphoenix's `libm.a(string.o)` vs our `libGL-phoenix.a(gl_stubs.c.o)`, which defined its own. Only visible in a clean build because a warm tree links a stale archive. The third clean run went green end-to-end (~25 min) and exported an image. The diagnostic that made it findable is worth keeping: the old code filtered linker output to lines matching `error|cannot`, capped at 10, which reduced this to a bare `collect2: error: ld returned 1 exit status` with **no cause** — exactly what the owner saw. It now prints the complete linker output.
- **★ CSD wait committed + honestly labelled.** `ioc_submit_csd` spun 8M times for `INT_CSDDONE` — fine for the probe's small dispatches, not for V3DV's real ones (vkQuake lightmap jobs, CFG0 ~0x00570000), which timed out hundreds of times per run and returned `num_completed=0`, so the caller rendered without lightmaps → black world, intermittently by scene. Raised 10×, still bounded. A fixed spin count is the wrong shape for a timeout (it means something different per core clock), so this is a **correctly-sized mitigation**; the CNTVCT_EL0 deadline is a separate commit needing its own Pi cycle. devices `1010570`.
- **Named the last reuse hole in `--scope full-clean`** (coord `1770ed5fd`): it wipes the buildroot, GPU archives and /tmp prefixes but **not** the extracted port source trees under `tools/{ports,x11-port}/src`, which keep their `.o` files *and* their `config.status` — and every `tools/ports` script skips configure when that exists. `RPI4B_CLEAN_PORT_SOURCES=1` closes it (keeps tarballs, so no re-download); off by default because it re-runs ~40 configures and the Docker `--no-cache` build already covers from-nothing by cloning fresh. Documented rather than silent, so a report can say which kind of clean it was.

- **★ `mc` BUILDS AGAIN (rc=0, static aarch64)** — coord `8d722cb3a`, pushed. Two stale-artifact bugs of the same family we have been clearing all night. (1) The failure looked like a missing libc function (`mountlist.c:728: implicit declaration of 'hasmntopt'`) but was the **opposite**: libphoenix implements the whole mntent family now (`29f5373`), and `build-mc.sh` was **copying its own older stub header over `$SYSROOT/usr/include/mntent.h`** — so `configure`, reading the real header first, correctly set `HAVE_HASMNTOPT` while the compile then saw the stub with no such declaration. That copy also mutated a **shared** sysroot header from inside one port's build, so any other port needing mntent silently got the stub. (2) Removing it exposed the next layer: `ar rcs` only replaces the members you name, so `libmcsupport.a` kept the retired `mc-mntent.o` forever and it collided with libphoenix (`multiple definition of setmntent`); the archive is now created fresh. Bonus: mc gets the real mount table instead of a hardcoded "no mounts". **`nano` still fails** — next.

- **★★ DOCS NOW STATE THE TRUE STATE (owner's final requirement).** coord `34f685ea0`+`48937284f`+`0f9094b13`, ports `aba93d2`, all pushed. Deleted the false limitations: "the ~18 MB GLQuake binary is too large to exec from the ext2/NFS loader" (it is 4.5 MB of `loader.disk` with **zero** game bytes; 18-38 MB binaries exec fine), "loader.disk fits a single large GL/VK binary" so you get GLQuake **or** vkQuake (both ship), and Q2/Q3/STK "not yet on the card" (all five are `if: true` and install into the rootfs). Commands corrected everywhere (`rpi4-quake` no longer exists → `quakespasm`); `mc`/`nano` removed from every "ships" list since they fail to build; **Quake III documented as running on the FREE DEMO data** with the `pak1.pk3`/`q3key` explanation and the QVM-recipe caveat. Files: `README.md`, `docs/BUILD.md`, `docs/KNOWN-ISSUES.md`, `docs/pi4-hardware-support-matrix.md`, `TUTORIAL.md`, `TUTORIAL-NETBOOT.md`, `LICENSING.md`, plus a `SHIPPING STATE:` block in each of the four game recipes.
- **Two things worth noting from that pass:** (1) **`assets/quake3-qvm/pak1-ioq3-vms.pk3` was undeclared in `LICENSING.md`** — now listed as GPL-2.0 ioquake3-built QVMs containing no id content, which matters because the image is to be published. (2) My own task brief was stale: it told the agent to fix `if:false` wording in the four recipe headers that **does not exist** at HEAD or anywhere in that repo's history — it verified, refused to fabricate a correction, and recorded the real state instead.

- **★★ CSD: measured, and it inverts my "compute got slower" inference.** Built the matmul benchmark against the CURRENT archives and ran it beside the Aug-14 (baseline-era) binary: current compute is **numerically correct and much faster** — `max_rel_err=0.000e+00`, 0.3658 ms/matmul at N=256 (18.2x faster than CPU), 36.81 ms at N=2048, empty-dispatch floor **6.5 us**; the old binary managed only 12.19 ms/matmul. So nothing regressed in compute throughput. **What the numbers actually show:** a single large dispatch can legitimately run tens of milliseconds (36.8 ms measured), and the wait was a fixed **8M-iteration** count whose wall-clock duration is shorter than that — so big V3DV lightmap dispatches (`cfg0` up to `0x00570000`) tripped it while the baseline's smaller ones did not. The 80M raise is therefore correct but still the wrong *shape*: **the proper fix is a monotonic-clock deadline instead of a spin count**, which removes the whole class. Left as a follow-up rather than another same-night rebuild. Diagnostic binaries removed from the export.

- **★★ CSD root cause narrowed to ONE remaining candidate, by A/B on hardware.** Ran the **baseline-built** vkQuake (from the isolated rollback tree, with its own 8M budget compiled in) on the **current** kernel/driver: **`CSD_TIMEOUT=0`, renders** (`20260903-061656-vkq-baseline-tick.png`). Current-built vkQuake with the same 8M budget: 211-257 timeouts. So the environment (HW, kernel, `rpi4-v3d` driver) is exonerated and the difference is in the **built userspace archives**. Then eliminated the obvious suspects by diff, not guesswork: the glue sources (`v3dv_libdrm_shim.c`, `v3d_phoenix_winsys.c`) are **functionally identical** (comments only; the winsys' 2 real lines are my own spin change); the D9 `build-*.py` changes are cosmetic plus one `-I` include; and the **libvcmbox de-dup guard is unreachable** — there is exactly one `vcmbox_call` site (`GET_VIRTUAL_WH, 8u`) and 8 bytes passes (limit 48, 4-aligned). **Surviving hypothesis:** the GPU archives compile and link with **no `--sysroot`**, against the hand-maintained `.toolchain` bundle, whose libphoenix the clean build refreshed (+23 commits) — while the rollback tree used a private baseline-era bundle. A libphoenix change affecting BO/uncached mapping or cache maintenance would mean the GPU does not observe the job promptly, which is exactly `num_completed=0`. **Clean next test:** rebuild the current archives against the BASELINE libphoenix bundle (and the converse) — a 2-way swap that settles it without touching anything else.

- **★★★ ROLLBACK ANALYSIS DONE — and it reframes the premise. The summer demo was never a reproducible build.** Baseline (`2026-08-28-gcc16-promoted-default`) rebuilt in an isolated workspace: **rc=0**, image verified, 17/17 sibling SHAs matched. Findings that matter more than the build: (1) **at the demo state, `tools/` had engine recipes for only quakespasm and vkQuake** — `yquake2-port`/`quake3-port`/`supertuxkart-port` contained *only launchers*. **Q2, Q3 and STK were built standalone and hand-copied onto the NFS export**, with hand-staged game data (baseline `rootfs-overlay` = one file, `etc/rc.psh`) and hand-staged fonts. So "roll back and verify" is literally possible only for GLQuake + vkQuake + X. (2) The manifest is **not** the demo state: the "all 5 games + X desktop" verification is **35 coord commits later**. (3) **The baseline cannot build the glamor X daemon from a clean tree at all** (`missing libglamor.a`) — coord `d0b151111` (mine, last night) is the fix, so that soft failure was pre-existing, not introduced. **Conclusion: we did not break a working reproducible system — we converted a hand-assembled one into a reproducible one, and every "regression" was a gap that hand-staging had been hiding.** That is why the fixes were staging/config/data restorations, not code reverts. Detail: `docs/misc/2026-09-03-rollback-baseline-analysis.md`.
- ⚠️ **One genuine open question the analysis flags:** the CSD spin budget is **not** a baseline defect — coord `c5968445f` records vkQuake rendering clean on the baseline with the 8M budget. So my 80M raise is a **mitigation**, and *what changed the dispatch timing* remains a prime bisect candidate (rank: ports patch regeneration > port flips > D9 glue changes > port_manager re-patch timing > export staging).

Older bullets from this week are in [docs/done/2026-09-03-w36-progress-archive.md](../done/2026-09-03-w36-progress-archive.md).

## 5. BLOCKED / WAITING (no action from me possible)


- **SD host card-reader** — shares the USB-C port with power/eth, so it's detached. I'll **ask** when I need it (e.g. to re-flash the card if its filesystem is damaged).
- **bash `-i`** interactive — needs a live terminal (psh automation sees EOF).
- **ffmpeg audio/player** — needs the attended `/dev/audio0` sign-off.
- **gcc-16 promotion**, **v3d placement** — D1/D2 above.
