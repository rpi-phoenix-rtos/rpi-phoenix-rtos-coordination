# Week 2026-W36 (Mon 2026-08-31 → Sun 2026-09-06)

Phoenix-RTOS RPi4 — weekly working log. **This file is the whole picture.**
History lives in `docs/done/`, one-off analyses in `docs/misc/`. Convention +
week-rollover: `docs/inprogress/README.md`.

---

## 1. DECISIONS — ALL ANSWERED except D4 (D6-D9 answered 2026-09-02)

**Settled facts (not decisions — never re-ask):** the toolchain is **GCC 16.2.0 + binutils 2.47** and has been the default since 2026-08-28 (owner-approved); the whole system is gcc-16-built and HW-verified.


| # | Decision | Answer → status |
|---|----------|-----------------|
| D2 | move the V3D driver `tools/` → `phoenix-rtos-devices` | **YES** → queued (last item of the ports migration; §2). |
| D3 | SD SDMA-write gate | **"do whatever you suggest"** → ✅ **gate stays OFF** (PIO writes). Correctness is proven cache-cold; the one gate-ON boot damaged a file on the card and the quirk is documented. Closed — I'll stop poking it. |
| D5 | make the SD card non-bootable | **YES** (you'll reflash when you want SD-boot tests) → queued as a small, optional determinism win (§2). |
| D4 | "genuine tool" boundary | *You'll decide later* — nothing blocked. |
| D9 | how much of the V3D driver can actually move into `phoenix-rtos-devices` | **APPROVED (option A) → implemented 2026-09-02.** Glue + build scripts now live in `sources/phoenix-rtos-devices/gpu/rpi4-v3d/mesa/` on the `wifi/rpi4-wifi` model; Mesa and `tools/.gpu-libs/` stay put. 4 repos committed, build-verified, archives symbol-identical. Stage-3 Pi cycle (GLQuake + X11 GPU desktop) running now. |
| D7 | fork + publish the Quake II / Quake III port work | **YES → DONE, then corrected.** Reviewed both (11 leftover `YQ2DIAG` prints discarded), committed (`yquake2 d0c064f9`, `quake3e acdf34a`) and pushed. First attempt created **plain repos, not forks**; owner caught it. Now real GitHub forks (`parent=yquake2/yquake2`, `parent=ec-/Quake3e`), branch `phoenix-rpi4-port` pushed + set as default, so the compare-to-upstream view works. `quakespasm` default branch aligned too. ⚠️ **One owner action left:** the two mis-created repos are renamed `*-nonfork-obsolete` and I cannot delete them (token lacks `delete_repo`) — run `gh auth refresh -h github.com -s delete_repo` then `gh repo delete rpi-phoenix-rtos/yquake2-nonfork-obsolete --yes` (same for `quake3e-`), or delete them in the web UI. |
| D8 | quakespasm pinned 16 commits behind what we test | **"remove the pin, always use latest" → DONE.** All four game forks are now tracked by **branch** (`phoenix-rpi4-port`) in `bootstrap-linux-host.sh`, so a clean build takes our fork HEAD instead of a stale sha (`8e47fc0b0`). Mesa and `PI_FW_REF` stay pinned deliberately (exact-sha reproducibility). ⚠️ **Scope correction:** branch-tracking only affects the `external/` dev clones. Those are a build input for **quakespasm and vkquake only** (`build-showcase-apps.sh:337,381` → `libquakespasm.a`/`libvkquake.a`); for yquake2/quake3e nothing builds from them. See the new §1 D10. |
| D10 | two sources of truth per game (owner-found) | **Owner: "one way to build; no parts of the patches lost."** → **(b) DONE, (a) prepared.** The **fork is now the single source** and each ports patch is generated from it (`scripts/game-port-patch.sh --check/--regen`, wired into `publication-audit.sh`); verified for all four that pinned tarball + generated patch == the fork tree **byte-identically** (`diff -rq` 0 differences) and still applies with plain `patch -p1`. ports `0d9de9a` + fork `ee181885` pushed. **(a) DONE 2026-09-03:** all five game ports are `if: true` in `ports.yaml` (plus a new `supertuxkart` entry — it had never been registered), the `_user/rpi4-quake` / `_user/rpi4-vkquake` wrappers and their `loader.disk` `app` line are deleted, and the ad-hoc `libquakespasm.a`/`libvkquake.a` archive steps are gone. All five engines now install into the rootfs and are HW-verified from the clean image (STK: GPU UI only — see the "Verification matrix, clean image" progress entry). |
| D6 | **NEW** — purge ~55 MB of build-output ELFs from coord **git history**? Removing them from HEAD is done; shrinking the history needs a **force-push of coord `main`**. | *Needs you.* Harmless to leave (clones just carry the weight). I won't force-push a published branch on my own. |

## 2. PLAN FOR THE NEXT DAYS

**★★★ OWNER PRIORITY (2026-09-03, overnight): ONE COMPLETE SD IMAGE WITH EVERY GAME, AND HONEST DOCS.**
Owner's words, and his corrections of *our* claims — treat these as facts, not open questions:
- `README.md` states three things that are **false/outdated** and must go: (a) "the GLQuake binary is too large to run from the ext2/NFS loader" — **the loader was fixed long ago; big binaries run from NFS and ext2**; (b) that only ONE of GLQuake / vkQuake can be on an SD image; (c) that Q2 / Q3 / STK are "not included / not yet supported" on SD.
- **All the games already rendered correctly on Pi 4** — yQuake2 on **gl3/GLES3** (mipmap generation had to be off), Quake III on **both** renderers (OpenGL and Vulkan), QuakeSpasm, and vkQuake (which also had working mouse + keyboard). My "unresolved whether the gl3 build ever rendered" note was wrong; delete it.
**Owner escalation (2026-09-03): CLEAN-REBUILD EVERYTHING, then test.** "We did important changes to ALL the subprojects — merging upstream, rearranging, fixing build scripts, generating patches, managing forks. All these changes could break some ports (incl. game ports). So we need to cleanly rebuild (no reuse of old modules, .o or .a files, binaries etc), relink, repackage everything and do a clean test. Only then we know... And fix all scripts that potentially reuse old stuff or do not clean correctly." Also: "Never hand-copy old binaries" — done, `make-pristine-nfs-export.sh` no longer carries executables forward (coord `996db622e`).

**Owner addition (2026-09-03): prove DETERMINISM with a clean chain, no reused binaries.**
9. Recreate the **NFS rootfs fully clean from the build scripts** — reusing none of the old hand-staged binaries — so every port is proven to build, link, be present, **and run**.
10. Then a **full clean SD build** to an image.
11. Then **compare the key binaries**: new NFS rootfs vs new SD image.
12. Then flash the SD card, and **re-run the ports from SD boot** on the Pi. That chain is the final verification of the recently modified ports and of build determinism.

**★★ OWNER RULE (2026-09-03) — ONE BUILD PATH PER PORT; `tools/` IS NOT A PORTS DIRECTORY.** Stated as policy, not a question:
- `tools/` in the coordination repo is **only** for our own tools needed to build the project that cannot easily live anywhere else. "Use `tools/` if no other place makes sense."
- A **normal port** — take a versioned artifact from the internet, apply our patches, build — belongs in `phoenix-rtos-ports` as a framework port. `nano` is the model case.
- Improvements to the system itself belong in `kernel` / `devices` / `libphoenix` / etc.
- The **only** acceptable reason to keep a hand-written script is a build too complex for the framework. Owner expects that to be "very rare and exceptional".
- The problem to eliminate: ports that exist **twice** (framework port *and* legacy `tools/ports/build-*.sh`), where the legacy script is sometimes the one the image actually uses. "We don't need multiple ways to build mv or nano or quake or sdl or python." To be done **carefully, in a controlled, tested way** — one port at a time, not a sweep. Audit running now; owner asked not to interrupt the in-flight image work for it.

**OWNER TASK (2026-09-03, scheduled — not now): ship a MODERN nano (target 9.2), not 2.2.6.** The port pins nano 2.2.x *specifically* to dodge gnulib, and that reason is now obsolete: the **coreutils** port ships gnulib-based software by renaming the clashing symbols in a patch — `sources/phoenix-rtos-ports/coreutils/patches/0001-rename-gnulib-gettime-settime.patch` is exactly that technique, and `0002`/`0003` handle the gnulib↔Phoenix stdio internals. Owner: "we can follow the same approach as in coreutils, and ship a modern, up-to-date nano". So the work is: bump to the newest stable nano, port gnulib the coreutils way, drop the `nano-phoenix-shim.h` pieces that a modern nano no longer needs, and retire the "2.2.x deliberately, no gnulib" rationale from both the port and the build script. Pairs naturally with the deduplication above — do the version bump **as** the framework-port-only move, so nano ends up modern *and* single-pathed in one step.

**OWNER TASK (2026-09-03, scheduled): hunt the nano pattern across ALL ports — legacy pins whose blocker we later removed.** Generalizing the nano finding, in the owner's words: *"review our ports and look for similar situations in which we based on a legacy, old version due to some porting difficulties. Maybe in other, later ports (or general system extensions) we actually solved or improved the situation to a point that a newer version can now be easily ported."* The nano case is the template: pinned at 2.2.6 to avoid gnulib, then `coreutils` defeated gnulib by renaming the clashing symbols in a patch, and nobody revisited nano. Audit running now (read-only): every `port.def.sh` / `tools` version pin, the *stated reason* for each old one, whether that reason still holds given what libphoenix/kernel gained since, classified UNBLOCKED / STILL BLOCKED / PINNED BY POLICY / UNKNOWN-no-recorded-reason, ranked by value-over-effort. A pin with no recorded reason is itself a finding.

**★★★ OWNER TASK (2026-09-03, near future): use LINUX/Pi4 as the reference implementation for #67.** Owner: *"maybe the long chased torches bug in vkQuake is actually a bug on Linux as well? … If yes - we can simply document this as a known GPU issue on Pi4 and stop working on this. If no - we can try to debug, analyze what Linux does differently."* Plan: `docs/misc/2026-09-03-linux-reference-comparison-plan.md`.
- **Not owner-blocked and needs no card:** `artifacts/linux-netboot/` already holds a Raspberry Pi OS Lite arm64 NFS-root netboot image, so this runs unattended.
- **Prior checked online:** Igalia's V3DV dev log documents vkQuake 1/2/3 working on the Pi 4 and there is **no** public report of missing alias/flame models on V3DV — so "it also breaks on Linux" is the least likely branch. A prior, not a conclusion.
- **Three outcomes, not two:** (1) reproduces → document as a known Pi4 GPU issue and stop, per the owner; (2) doesn't reproduce and `dmesg` shows **no** v3d timeouts → the wedge is ours, diff our submit path against `v3d_sched`/`v3d_submit_cl_ioctl`; (3) doesn't reproduce **but** `dmesg` shows `v3d_sched: job timed out` → the wedge is **shared** and the difference is that **Linux reports and recovers while we silently drop**. (3) fits the evidence best (a *valid* CL wedging; `drmSyncobjWait` stubbed to always return signalled) and is the branch no amount of Phoenix-only debugging would have found.
- **The instrument that makes it decisive:** `V3D_DEBUG=cl` / `clif` (`external/mesa/.../v3d_debug.c:43,47`) dumps the real control lists on Linux — **ground truth for what the ~98-byte meta-copy RCL should contain**, which is exactly the open question. `v3d_debug` is not compiled into our archives yet (0 symbols), so aligning both sides to the same dumper turns interpretation into a byte diff.
- **Reuse:** `check-torch-rois.py` grades a Linux frame unchanged (the reference is a host quakespasm render), so the Linux result is directly comparable to our 0/8 and 1/8 — n≥8 and `--rate`, never a screenshot.

**★★★ TASK ORDER (set 2026-09-03 at the owner's request: "make sure none of the in-progress tasks is forgotten or broken and figure out yourself what should be the task order"). Principle: no half-migrations, then a deliverable that matches the tree, then proof it rebuilds from git, then pay verification debt, then new investigation, then improvements.**

**Phase 1 — integrity: leave nothing half-done.**
1. ✅ `gl-x11-window` libiconv link fix **built + verified** (it had been committed at 17:13 but last built at 17:10 — the one real gap this audit found; now 0 undefined, 15 iconv symbols = the real GNU 1.18, not the 1,974-byte stub).
2. **Delete the four legacy lib steps** (`libiconv`/`libffi`/`ncurses`/`glib2`) + full build + gate. Consumer check is complete and this is the **stable point** for the dedup work — half-migrated is the worst state to stop in.

**Phase 2 — make the deliverable match the tree.**
3. Full-clean `--variant sd` → completeness gate → `RPI4B_SD_IMAGE=` byte-identity → **new SHA for the owner**. The current `a15e1a4a` predates the nano+mc migrations and the libiconv fix; it is good, but stale.

**Phase 3 — prove it rebuilds from git.**
4. Docker `--no-cache` clean build (the owner's original ask). ~30 commits have landed since the last green run.

**Phase 4 — verification debt (committed but unproven).**
5. ~~**yquake2 GLES3 depth/stencil discard**~~ — **verified 2026-09-04** as part of the yquake2 sync A/B: 4 HDMI runs, in-game 4/4, both arms identical. (The sync also brought upstream's *own* discard-before-present, which is inert for us — it needs `YQ2_GL1_GLES`, which our gl1 variant does not define.)
6. ~~**`+map start` capture flake**~~ — **FIXED in the harness 2026-09-04** (coord, `test-cycle-psh-interact.sh` + `psh-interact.py`). It was never a render fault: on a slow start every 15 s HDMI tick landed during loading, so the grader had no frame to judge and the run read as a failure of whatever it was testing. Two knobs now:
   - `--ready-line ERE` — until the marker appears, `--max-cmd-secs` is only the deadline for *reaching* it; once it does, capture is guaranteed `--ready-extra-secs` longer. A run that never matches now says so explicitly instead of looking like a failure.
   - `--hdmi-dense-on ERE` — snapshot every 5 s instead of 15 s once ready.
   Measured on the case that flaked twice: **20 frames, 13 at the reference viewpoint** (was 13–16 frames, 6–9 at the viewpoint). Use `--ready-line 'present 30' --hdmi-dense-on 'present 30'` for vkQuake.

**Phase 5 — the owner's new angle on #67.**
7. Linux/Pi4 reference comparison — `docs/misc/2026-09-03-linux-reference-comparison-plan.md`. Runs unattended (netboot Linux image already staged).

**Phase 6 — scheduled improvements.**
8. Legacy version pins: ~~zlib~~ **DONE** (1.3.1, 2 CVEs, 9 dependents validated). **Remaining: openssl / mbedtls in-branch** — must touch both paths or they re-drift.
9. ~~**Modern nano (9.x)**~~ — **DONE**: nano 9.2 builds and runs interactively on hardware.
10. ~~Fork upstream syncs~~ — **ALL FOUR DONE, HW-verified** (§3 item 3). The ordering guess here was wrong on both counts: quake3e does not have 0 conflicting files (it has the **JIT**), and the overlap counts came from a `comm` on unsorted input. vkquake-last was right, and for the reason given.

**⏸ Owner-blocked, not forgotten:** flash the image; SD-boot port re-run + **SuperTuxKart in-game** (needs local assets); delete the two obsolete `*-nonfork-obsolete` GitHub repos (my token lacks `delete_repo`).

**Also open, tracked, not lost:** ~~#67~~ **SOLVED** (§3 — kernel `5d8645f6`); X11 root window black (cosmetic `wmsetbg`); **TD-21** syscall-order revert due at the next full rebuild; mc's `/etc/mc/sfs.ini` not staged.

## 3. IN PROGRESS

**★★★★★ FLASH THIS — `artifacts/rpi4b/rpi4b-sd-2part-cachefix-20260904.img`**, SHA256 `7316f82a6e78332680a2ef89d31fc12ef68ea192b3dc241ff0b32f96ed31f3db` (1,680,867,328 B). Clean-room `docker build --no-cache` from the published Dockerfile, `BUILD_RC=0` / `EXPORT_RC=0`, contents gate **PASSES**.
- **This is the first image containing the #67 fix** — the kernel stale-cache-line repair — plus both driver defects, nano 9.2, and the `autoexec.cfg` staging change.
- `sudo dd if=artifacts/rpi4b/rpi4b-sd-2part-cachefix-20260904.img of=/dev/sda bs=4M conv=fsync status=progress` (`/dev/sda` is always the card on this host). ⏸ SD-boot port re-run + STK in-game still need the card, which is detached.
- **The games on this image are one sync older than the tree** — it was built before the four fork syncs (2026-09-04). Nothing on it is broken; the next clean build picks them up. Not a reason to delay flashing.
- The superseded `…-cleanbuild-20260903b.img` is **deleted**, not left beside it — a near-identical 1.68 GB file predating every fix is a mis-flash waiting to happen.

**#67 — SOLVED (2026-09-04).** Torches **7/7 (100%)** after **0/15**; **0 wedges across all 8 boots** (0 corrupt lists, 0 dropped jobs, 0 render timeouts), presents 3,600–3,780. Fix: `dc civac` + `dsb` over a BO's range at create (devices `9bac0f7`, pushed).
- **Cause:** pages handed out for an **uncached** BO still held the **previous owner's dirty cache lines**, evicted later on top of Mesa's uncached writes — corrupting control lists at **64-byte granularity in both directions**. The GPU then executed a half-written list and parked at the first invalid byte: the long-misdiagnosed "wedge". The hardware was never at fault.
- **Why five earlier fixes failed:** every one targeted **BO identity** (handle recycling, address recycling, VA reuse, mappings). The buffer was always correct; its *contents* were being eaten afterwards.

**★★★ THE KERNEL FIX IS LANDED** (kernel `5d8645f6`, §4) and the local winsys workaround is removed, so it is proven to stand alone. Pages mapped uncached must have the previous owner's dirty lines retired first. **Every uncached DMA mapping in the system has this exposure — SD, genet, USB, WiFi** — and the standing history of intermittent, address-dependent, "HW-marginal" faults in those subsystems is exactly what this bug class produces. The winsys `dc civac` is a local patch over a system-wide defect.

**Next, in order:**
0. ~~Regression-test the kernel cache change against the libc/driver suite~~ — **DONE, clean: 574 tests, 0 failures, 0 faults on hardware.**
1. **Test the mailbox-spin hypothesis** on `rpi4-wifi` / `rpi4-hci` / `rpi4-audio` — the three drivers the exposure list (§4) shows allocating uncached RAM **per operation**, i.e. the ones the #67 defect could have been corrupting all along. The claim to check is that "unbounded mailbox spin" incidents disappear with the kernel fix in. (The old item here — "finish the three-way test" — was a #67 diagnostic to tell candidate mechanisms apart; the root cause is found, so it is retired, not deferred.)
2. ~~**zlib 1.2.11 → 1.3.1**~~ — **DONE** (2 CVEs closed; all **9** dependents rebuilt and validated).
3. ~~**Fork upstream syncs**~~ — **ALL FOUR DONE 2026-09-04, every one HW-verified.** Ports `a38880c`; manifest `manifests/2026-09-04-game-fork-syncs.md`; `game-port-patch.sh --check` green on all four.
   | fork | behind | overlap | conflicts | pin | HW result |
   |---|---|---|---|---|---|
   | quakespasm | 12 | 2 | 0 | `f5fe1786` | torches 9/9 frames, 0 faults/wedges |
   | quake3e | 12 | 1 (**the aarch64 JIT**) | 0 | `8dd27a54` | q3dm1 fully lit + HUD, 0 faults |
   | yquake2 | 85 | 3 | 0 | `a9e88f6c` | 2-per-arm A/B: both arms 2/2 in-game, identical |
   | vkquake | **217** | 9 | **3** | `1aa13a56` | **7/7 conclusive trials, torches in EVERY frame**, presents 3,510–3,690 |
   - **vkQuake is the one that earned its place last in the risk order.** It is also the case against treating a clean merge as a result: after the three conflicts were resolved it still failed to compile, then failed to link (40 missing `*_spv` symbols), then died twice on hardware — `out of handles`, then `Double free detected`. Both crashes were **real defects**, not integration noise:
     - upstream added `Sys_FileInit()` (it marks the handle table free); `sys_sdl_unix.c` calls it first thing in `Sys_Init` and our shim, which *replaces* that TU, did not;
     - `COM_FindFile` now duplicates a pak's handle for **every** file opened from it, so our owned pak-slurp buffer was reachable from many handles that each freed it — now reference-counted (fork `cc7c390`).
   - **The embedded SPIR-V generator was the hidden landmine.** Upstream consolidated 14 shader files into `-D` variants of 34; our generator scanned `Shaders/` and compiled each file with no defines, so it produced 34 modules where the link needs **67**. It now **parses upstream's own `shaders` + `shader_variants` matrix out of meson.build** (coord `a75921b15`) — a hand-kept copy of that table is exactly what went stale.
   - **Budget note for future benches:** 34 → 67 shader modules means first frame arrives later. `test-cycle-bench.sh`'s fixed 150 s `max-cmd-secs` is no longer enough for vkQuake (2 trials ended mid-texture-upload — counted INCONCLUSIVE, not failed: 0 faults, 0 wedges). Use 195 s.
   - **Two further defects found by READING the new code, not by running it** (ports `a85d217`): the pak-slurp refcount was non-atomic although the duplication it guards is concurrent by design (task-worker `COM_FindFile`) — a race no torch bench can see; and `Sys_FindFirst` compared the extension raw where upstream strips an optional leading dot, so a caller passing `.sav` would have got an empty savegame list with nothing to explain it. Latent today, both fixed, re-verified 8/8 torch frames.
   - Three lessons the syncs paid for: bump the pin **before** regenerating the patch (regen against the old pin folds upstream's commits into *our* delta — 38 files/+821 instead of 13/+545); launch a game through its **`/usr/bin/<game>` launcher**, never by hand (my hand-rolled yQuake2 line set 640×480, then double-loaded the map — both looked exactly like a render regression); and `git push` rejecting a fork is worth reading, not forcing — vkQuake's published branch held a commit local lacked (the Pose2-hack revert), which touches `r_alias.c`, the torch path, so it got its own 2 trials.
4. **Linux/Pi4 comparison** — owner says run it when needed and suggests grabbing a full **Raspberry Pi OS 64-bit** image rather than adding Vulkan to our Debian rootfs piecemeal. Deprioritised on purpose: the bug is now localised to our own userspace winsys/Mesa integration, which Linux does not share, so the comparison would confirm rather than localise.

**Still unexplained, tracked:** a boot can degrade to ~200 presents with **zero** CSD timeouts, so there is a third degradation path beyond the freeze already root-caused. **Q2's ~1 wedge/run is gone** (kernel fix, §4), so it is no longer the cheap reproducer — what replaced it as Q2's open item is a **first-frame no-render, seen 1 run in 4** on the synced binary: the map loads, the screen goes black and no 3D frame is ever presented (4.5 min). Not attributable to the sync (the pre-sync arm has 3/3, too few to exclude the same rate). Detector + A/B method are in ports `687c16a`; it needs a rate, not another single run.

## 4. PROGRESS THIS WEEK (terse; detail in `docs/done/`)

- **★★★★ EXPOSURE LIST NOW GROUNDED IN CODE, NOT SPECULATION — and it names three drivers plus a candidate symptom.** Applying the criterion the genet A/B produced (the defect needs uncached RAM taken **repeatedly from the free pool**, not merely uncached DMA), every non-V3D `MAP_UNCACHED` site was read:
  | driver | site | allocated | exposed? |
  |---|---|---|---|
  | `rpi4-wifi` | `:132` in `diag_mboxPower()` | **per mailbox call** | **YES** |
  | `rpi4-hci` (BT) | `:118` in `hci_mbox()` | **per mailbox command** | **YES** |
  | `rpi4-audio` | `:365` in `audio_dmaStart()` | **per playback start** | **YES** |
  | `genet` | DMA rings | once at init | no — **measured clean** in the A/B |
  | everything else | `MAP_DEVICE\|MAP_PHYSMEM` | fixed MMIO windows | no — Device memory holds no cache lines |
  - **Candidate symptom, and it matches an existing complaint.** Those mailbox helpers allocate a buffer, write a request, and let VideoCore DMA-read it — so stale dirty lines corrupt the *request*, and the reply never arrives. The Pi4 driver code review already lists **"recurring = unbounded mailbox spins"** as a known issue. That is now a specific, testable link rather than a hunch: the kernel fix already covers these paths, so the test is whether mailbox-spin incidents disappear.
  - Restored fixed kernel **verified on hardware**: `0` corrupt lists, `0` render timeouts, `0` wedges, `0` faults, torches present (10/10 frames), **4,320 presents** — the highest of the night.

- **⚠️★★★ A/B DONE — MY BLAST-RADIUS PREDICTION IS REFUTED FOR genet. The pre-fix kernel's CRCs are ALSO perfectly clean.** Same 115 MB × 2 check, pre-fix kernel `f35d6d54`: q3 `940b6e0e`, q2 `cdd535b6`, q1 `74e3b084` — all matching host, 0 faults. Identical to the fixed kernel. **So genet/NFS was never affected by the stale-cache defect**, and the "SD/genet/USB/WiFi are probably the same bug" line I carried all night is **wrong for the one subsystem I could actually test**.
  - **And the negative is more useful than the guess was, because it explains WHICH drivers are exposed.** V3D churns BOs **per frame** — pages are constantly recycled through the free pool, so a fresh uncached mapping regularly lands on a page still holding another process's dirty lines. genet allocates its DMA rings **once at driver init** and keeps them for the process lifetime, so it never receives a recycled page. **The defect matters where uncached DMA memory is allocated repeatedly from the free pool — not where it is allocated once.** That is a far sharper statement than "every DMA driver is at risk", and it is now backed by a measurement rather than a mechanism story.
  - Revised exposure list, by that criterion: any per-operation uncached allocation. SD/USB/WiFi should be judged on whether they allocate per transfer or once at init — **still untested**, but now with a concrete test to apply rather than a hunch.
  - **Fix restored** (`git checkout HEAD -- pmap.c`, verified 3× `MAIR_IDX_NONCACHED` and both flush call sites present) and the fixed kernel is rebuilding. The tree must not be left on the reverted build.

- **★★ CORRUPTION test (the right axis) on genet/NFS: 230 MB transferred, CRCs match host EXACTLY, 0 faults.** Two runs of a staged `verify-nfs.py` over 115 MB — q3 `940b6e0e`, q2 `cdd535b6`, q1 `74e3b084` — all identical to host references. The earlier throughput number measured the wrong thing: the predicted failure mode is **silent data corruption**, not slowness, so a CRC mismatch is the signal.
  - **And now the A/B that makes it a measurement instead of a one-sided result:** the kernel fix is temporarily reverted (`git revert --no-commit 5d8645f6`, verified: the `hal_cpuFlushDataCache` call is gone) and a pre-fix `--scope core` kernel is building. Same CRC check will run on it.
    - **pre-fix CRCs also clean** ⇒ genet was **never** affected, and the blast-radius claim **narrows honestly** to "V3D only, so far".
    - **pre-fix CRCs corrupt** ⇒ the claim is **proven** for a second subsystem.
  - Low risk: the revert returns to the previously known-good kernel, and the manifest records the fixed state for one-command restore.

- **First attempt to MEASURE the blast-radius prediction (genet/NFS) — and the honest answer is "no signal, not a valid comparison".** Two large NFS reads over genet under the new kernel: 46.9 MB in 2.200 s (**20.3 MB/s**) and 50.0 MB in 2.376 s (**20.1 MB/s**), **0 faults**, stable across both.
  - **I am not calling this a regression against the recorded 29.9 MB/s.** That figure came from `nfs-read-bench`, a purpose-built tool; this is busybox `dd` with 64 KiB blocks through the libc path, single-threaded. Different tool, different method — the numbers are not comparable, and reading a 30% "drop" into them would be exactly the kind of inference this bug punished five times.
  - **What it does establish:** genet/NFS runs clean under the kernel cache change — no faults, no instability, consistent throughput across two 50 MB transfers. A real A/B would need `nfs-read-bench` staged on target **and** a run against the pre-fix kernel; without both, the SD/genet/USB/WiFi claim stays where it has been all night: **predicted, not measured**.

- **★★★ KERNEL CACHE FIX REGRESSION-TESTED BEYOND THE GPU PATH: 574 tests, 0 failures, 0 faults.** `string` 207 · `stdio` 80 · `pthread` 24 · `semaphore` 3 · `mprotect` 3 · `sys-mutex` 12 · `dirent` 38 · `misc` 207. Suites chosen for exposure to a cache-maintenance change — `mprotect` (page-attribute changes) and `pthread`/`semaphore`/`sys-mutex` (concurrency and shared memory) — not just whatever was cheap. Built via `--scope core --with-tests`, which also sidesteps the host-local lighttpd failure by skipping the ports stage.
  - **What this does and does not show:** the fix does not regress libc, memory-protection or concurrency behaviour on target. It still does **not** directly measure the SD/genet/USB/WiFi prediction — those need their own historical flakes re-run, and that stays on the predicted-not-measured list.
  - **Sixth loose-check misread of the night, and it nearly inverted this result:** my waiter used `pgrep -f "psh-interact\.py"` and exited *instantly* because the process had not spawned yet, so I read a 5 KB partial log, saw no test output, and began diagnosing "the tests did not run" — when they were merely still booting. Correct form: match the actual script name via `ps -eo cmd` and exclude `bash -c`.

- **Process note that keeps costing time: `pgrep -f <pattern>` matches its own shell.** It reported a build "STILL RUNNING" that had stopped two minutes earlier; `ps -eo cmd | grep -F … | grep -v "bash -c"` (or checking the log mtime) is the reliable form. That is the **fifth** loose-check misread tonight — four silent-empty false negatives (a probe before the submit `dsb`, `objdump` not on `PATH`, a git compare against a nonexistent `origin/master`, `ls … | awk … || echo`) and now one false positive. Same root cause each time: a convenient pattern standing in for an actual test.
- **⚠️ lighttpd: I misdiagnosed this twice, and the evidence says it is HOST-LOCAL, not a port or upstream bug.** It fails on this host with `undefined reference to chunkqueue_chunk_file_viewadj` — a symbol `chunk.c` defines only under `#ifdef HAVE_MMAP` while `mod_webdav.c` uses it unconditionally, and the recipe passes `--disable-mmap`. That looked like a clean upstream bug. **But lighttpd installs fine in the clean Docker build (16.44 s), same recipe, same flags** — so the port is correct in the authoritative path. The plausible mechanism is the recipe's `./autogen.sh`, which regenerates `configure` using *this host's* autotools rather than the clean-room's. **No recipe change made**: changing a port that builds correctly in CI to satisfy one dev host would be the wrong fix.
  - Consequences: lighttpd is **not** in the completeness gate's required set and is present in the shipped image, so nothing is at risk. Only the local `--scope project` build is blocked, and with it local `autoexec.cfg` parity — cosmetic, since the clean image (what actually ships) already has it via the Dockerfile's `stage-game-data.sh` run.
  - My first two readings — "stale workdir state", then "upstream lighttpd bug" — were both wrong. Recorded so the next person does not re-derive them.

- **★★ zlib 1.2.11 → 1.3.1 DONE (2 CVEs closed), and the local patch DELETED rather than rebased** (ports `3c51ce4`). CVE-2018-25032 (`memLevel=1` deflate) and CVE-2022-37434 (`inflateGetHeader` OOB read) are both fixed upstream. Same `fossils/` URL, so version + sha256 + size was the entire source change.
  - The port's `01-cmake.patch` failed 3 of 6 hunks — because **upstream adopted what it added**: `cmake_minimum_required` is now a *version range* (`2.4.4...3.15.0`) that modern CMake accepts, examples have an upstream toggle (`ZLIB_BUILD_EXAMPLES`), and `SKIP_INSTALL_MAN` is honoured directly. So the patch is **gone** and `p_build` passes the upstream option name. Fewer local deltas is the point of a bump.
  - Verified: builds clean, `libz.a` reports **"deflate 1.3.1"**, and `deflate`/`deflateEnd`/`deflateInit_`/`inflate` are all present; libpng-1.6.40 rebuilt against it in the same run. **Validation in progress.** A `--scope project` rebuild failed on **lighttpd** — `undefined reference to chunkqueue_chunk_file_viewadj`, an *internal lighttpd symbol* with no zlib involvement. Clean-building lighttpd alone **succeeds**, so that was **stale workdir state**, not a zlib regression: port workdirs are keyed on the tarball and `--scope project` reuses them (the documented hazard). Now clean-building zlib's real dependents instead — measured as **9**, not the 18 I estimated from a looser grep: `dropbear python libpng lighttpd glib2 xorg_fonts curl xorg_server supertuxkart`. **ALL 9 dependents clean-built against 1.3.1, zero failures** — `dropbear`, `libpng`, `lighttpd`, `glib2`, `xorg_fonts`, `curl`, `python-3.14.4`, `xorg_server-1.20.14`, `supertuxkart-1.4`. **Pushed** (ports `3c51ce4`).

- **⚠️ CORRECTION: `autoexec.cfg` still has not reached the target, so that change remains UNEXERCISED on hardware.** I said a Pi cycle would close it; that was wrong. Staging writes to the *project overlay*, which only reaches `.buildroot/_fs/<target>/root` during a build's **fs stage**, and `sync-netboot-tree.sh` syncs `_fs/root` → export — not the overlay. Verified with a real test: **ABSENT from export, ABSENT from buildroot rootfs**. So every Pi run tonight, including the one I labelled a validation, read the old `config.cfg`. Harmless (identical settings, and no GPU finding depends on it) but it means the staging fix is proven only in the *clean* build path, where the Dockerfile does run `stage-game-data.sh` before the build. Local parity needs a `--scope project` build; queued behind the image build, not skipped.
  - That cycle *was* still useful as a third consecutive clean engine run on the new kernel: **0 faults, 0 wedges, torches present 8/8 frames** at the reference viewpoint.
  - **Tooling lesson, now four times in one night:** a check that returns *silent empty* reads exactly like a clean pass. Tonight: a probe reading before the submit `dsb`; `aarch64-phoenix-objdump` not on `PATH`; a git compare against a nonexistent `origin/master`; and just now `ls … | awk … || echo ABSENT`, where the `||` bound to `awk` (which succeeds) instead of `ls`. Every one of them would have been read as good news. Use `[ -f ]`/explicit exit codes for existence checks, never a pipeline's tail.

- **The final image build FAILED — on my own inconsistency, caught by my own gate.** Not the linker (that red herring cost me a few minutes: CMake's STK link is *expected* to fail and the port's `--start-group` relink then succeeds, and supertuxkart did install). The real stop was:
  ```
  ABSENT usr/share/quake/id1/config.cfg   <-- REQUIRED
  INCOMPLETE: 1 required path(s) missing, 0 optional.
  ```
  Earlier tonight I moved Quake's shipped settings from `config.cfg` to **`autoexec.cfg`** (to fix a staging guard that silently skipped shipped changes) and did not update the gate's required-path list. Fixed (coord `ebd4609a4`); build restarted.
  - **A second, quieter inconsistency found while fixing the first:** neither the local buildroot rootfs nor the NFS export had `autoexec.cfg` — only the old `config.cfg` — because `stage-game-data.sh` runs in the Docker build and only on demand locally. So the local gate would have failed too, and every Pi test tonight was reading the *old* config. Ran `stage-game-data.sh q1`; the overlay now carries `autoexec.cfg` (479 B, always refreshed) alongside the legacy `config.cfg`, which is the game's own file and correctly left alone.
  - **The gate did exactly its job**: everything else was correct — all five engines, all game data, STK linked and installed — and it still refused to package an image whose contents did not match what the project claims to ship. That is the same gate that caught the `gl3_discardfb` image earlier; it has now blocked two bad artifacts in one night, one of them mine.

- **★★★ BLAST-RADIUS CONFIRMED IN A SECOND ENGINE: yQuake2's wedge is gone too.** It previously logged **~1 binner wedge per run** even after the GLES3 discard was reverted, and I had recorded it as "the same wedge family as #67, now reproducible in a second engine". With the kernel fix: **0 wedges, 0 bin timeouts, 0 render timeouts, 0 dropped jobs, 0 faults**, game confirmed running. Different engine, different renderer path (gl3/GLES3 rather than V3DV) — so the cache-alias defect was never vkQuake-specific.
  - **Kept honest — what is now MEASURED vs still PREDICTED.** Measured: #67 (7/7 from 0/15), zero wedges across 8 vkQuake boots, the GL/gallium path clean, yQuake2's wedge eliminated, nano 9.2 on the new kernel, clean boot with 0 faults. **Still only predicted:** that the same defect explains intermittents in **SD, genet, USB, WiFi** (the mechanism implies it; nobody has re-run those specific flakes), ~~and that gallium's long-disabled cacheable-BO mode is this bug from the other side~~ — **RETRACTED, by reading the code rather than running the experiment.** `v3d_resource.c`'s comments are layered: the hang was later root-caused (`dc ivac` is EL1-only and traps at EL0; `dc civac` works) and the mode is now disabled for a **measured no-benefit** reason — readpx is identical cached vs uncached (~23 ms), because the cost is the detile's scattered tiled access pattern, not the uncached read. So it is not this defect, and re-enabling it would gain nothing. Saved a build and a bench. After a root cause this satisfying the pull to retro-attribute every open intermittent is strong, and that pull is exactly what produced five false closures here.

- **Kernel fix passes a GL-path regression check too.** `nano --version` → **GNU nano 9.2** on the new kernel, and `quakespasm +map start` renders its torches (**401 / 417** lit px, 8/8 frames at the reference viewpoint — its historical range is 401–548) with **0 faults, 0 wedges, 0 dropped jobs**. That path matters because gallium GL allocates BOs differently from V3DV and is the stack whose *cacheable* BO mode was disabled years ago for hanging the GPU "after the first cacheable readback" — plausibly the same defect from the other side, and now worth re-testing.

- **Final image rebuilding with everything** (`docker build --no-cache`, all repos verified pushed first: kernel `5d8645f6`, devices `6cdf597`, ports `6356a70`, project `2642335`). The current flashable image `384c0a1e…` **predates every fix from this session** — the kernel cache fix, both driver defects, and nano 9.2 — so it should be replaced before anyone flashes it.

- **★★★★★★ THE KERNEL FIX ALONE CARRIES IT — the local workaround was correctly removed, and both are now pushed.** With the winsys `dc civac` **gone**, the kernel change delivers **4/4 (100%) torches** at the identical 599/553 lit px, `badentry=0` and `RENDER TIMEOUT=0` on every trial, presents 3,660–3,720. So the general fix does the work; there is no second, silently load-bearing copy.
  - Pushed: kernel `5d8645f6` (`cc9a3544..5d8645f6`) and devices `6cdf597` (`9bac0f7..6cdf597`).
  - **Manifest snapshotted** — `manifests/2026-09-04-uncached-page-cache-fix.md` — because core integration changed at the kernel level and this is exactly the point rollback discipline exists for.
  - **Recap of the whole defect in one line:** `_pmap_destroy()` released a process's pages with **no cache maintenance**, so dirty lines survived into the free pool and were evicted later on top of uncached DMA writes; the kernel now retires them whenever a page is mapped `MAIR_IDX_NONCACHED`.

- **★★★★★ KERNEL FIX BOOTS CLEAN AND IS THE BEST RESULT OF THE NIGHT.** `kfix-boot`: psh reached, **0 corrupt lists, 0 render timeouts, 0 faults** (no Exception/Data Abort/Fatal/panic), and **4,290 presents** — higher than the winsys-only runs (3,600–3,780). A kernel change was the one category where a regression meant dropping everything to restore the Pi; it didn't regress.
- **Local winsys `dc civac` REMOVED** (devices `db3c1a4`-class change, rebuilding) so the kernel fix is tested *alone*. Two overlapping fixes for one defect means nobody can tell which is load-bearing, and a redundant workaround would mask a regression in the general one. **The next bench is exactly that test:** rate stays 0 ⇒ the kernel fix does the work and the local patch is correctly gone; corruption returns ⇒ the kernel fix is incomplete and the workaround comes back. Either way the answer is measured, not assumed. **FINAL (kernel fix alone, n=8): 0 corrupt lists and 0 render timeouts on every boot, presents 3,660–3,870, torches 7/7 (100%)** — identical to the winsys-workaround result, so the kernel fix fully replaces it. (T8 ran fine at 3,660 presents but did not reach the reference viewpoint: the known ~1-in-8 capture flake, excluded rather than failed.)

- **★★★★★ THE KERNEL-SIDE FIX IS WRITTEN — and the gap is exactly where predicted** (kernel `5d8645f6`, `--scope core` building). The kernel *already* protects against this: `_pmap_cacheOpBeforeChange()` flushes when a mapping goes cached→non-cached/unmapped. But its **only two callers are `_pmap_enter()` and the unmap path** — **`_pmap_destroy()` releases a whole process's pages with no cache maintenance at all.** So on process exit, dirty lines survive into the free pool, and nothing retires them before the pages are handed to the next owner.
  - **Fix placed where the guarantee is needed:** `_pmap_cacheOpAfterChange()` now clean+invalidates when the *new* mapping is `MAIR_IDX_NONCACHED` — the data-cache counterpart of the instruction-cache handling already there. Restricted to Normal-NC (real RAM mapped uncached); Device mappings hold no lines and are untouched. Cost is one page flush per uncached mapping — rare (DMA buffers) — versus flushing every page at teardown.
  - **Blast radius is the point:** this covers **every uncached DMA mapping in the system** — SD, genet, USB, WiFi — not just V3D. The V3D `dc civac` remains as the local, already-verified repair; this is the general one.
  - Next: `--scope core` build → netboot boot test (kernel change: **restoring the Pi is priority #1 if it regresses**) → re-run the vkQuake bench and confirm the winsys workaround is no longer load-bearing.

- **★★★★★★ #67 IS FIXED — THE TORCHES RENDER, 4/4 (100%), AFTER 0/15.** Graded by the standing rate harness, never a screenshot:
  ```
  T1..T7 all PRESENT at 599/553 lit px;  T8 INCONCLUSIVE (never reached the viewpoint -- the known capture flake, excluded not failed)
  RATE: 7/7 gradeable trials rendered torches (100%)   [full 8-trial bench]
  ```
  Baseline on this arm was **0/15**, and **0/8 in every arm tested tonight**. The lit-pixel counts (**599 / 553**) sit *above* quakespasm's reference range of 401–548, i.e. the flames are fully rendered rather than marginally present. Across **all 8** boots the GPU wedges vanished completely: **0 corrupt lists, 0 dropped jobs, 0 render timeouts**, with presents 3,600–3,780 (the healthy early-August range), so the game is running normally rather than not running at all.
  - **Root cause, confirmed by the fix:** pages handed out for an **uncached** BO can still hold the **previous owner's dirty cache lines**; those get evicted later, on top of the uncached writes Mesa makes into the buffer. That corrupts control lists at **64-byte granularity, in both directions**, with either zeros or old texels — which is exactly what the byte dumps showed, and exactly why every BO-identity fix changed nothing. Retiring the lines at BO create (`dc civac` + `dsb`) removes the class. Devices `9bac0f7`, **pushed**.
  - **The real fix belongs one layer down, in the kernel.** Every uncached DMA mapping in the system has this exposure — SD, genet, USB, WiFi all map DMA buffers this way — and a long history of intermittent, address-dependent, "HW-marginal" faults across those subsystems is exactly what this bug class produces. The winsys `dc civac` is a local patch over a system-wide defect. **This is the highest-value follow-up in the tree.**
  - Five earlier "fixes" for this bug were false closures. This one is graded by pass **rate** with a run-control (presents > 0) and a mechanism that predicts the observed byte patterns.

- **★★★★★ THE `dc civac` FIX WORKS — THE GPU STOPS WEDGING ENTIRELY (interim, n=3 of 8).** Per trial, corrupt lists / dropped jobs / render timeouts:
  | | T1 | T2 | T3 |
  |---|---|---|---|
  | corrupt lists | **0** | **0** | **0** |
  | dropped jobs | **0** | **0** | **0** |
  | render timeouts | **0** | **0** | **0** |
  | presents | 3,660 | 3,750 | 2,400 |
  Baseline was **1–4 corrupt lists and 2–4 render timeouts on every boot**. **Control checked first, because zero drops is also what a boot that never ran the game looks like:** vkQuake ran in all three, at 2,400–3,750 presents — the healthy early-August range. So this is a real elimination, not an absence of testing.
  - **What this confirms:** the corruption was **stale dirty cache lines from a page's previous owner**, evicted on top of the uncached writes Mesa makes into a freshly handed-out BO. Retiring those lines at BO create (`dc civac` + `dsb`, devices `9bac0f7`) removes the whole failure class — which is why *every* BO-identity fix before it changed nothing.
  - **Not yet claimed: #67 itself.** The torch rate is graded at the end of the 8 trials against the standing 0/8 baseline. Wedges gone ≠ flames back, and conflating those two is precisely the error that produced five earlier false closures.
  - **The general fix belongs in the kernel**, not here: pages handed to an *uncached* mapping must have the previous owner's dirty lines retired first. Every uncached DMA mapping in the system has this exposure, not just V3D — SD, genet, USB and WiFi all map DMA buffers this way.


Older bullets from this week are in [docs/done/2026-09-03-w36-progress-archive.md](../done/2026-09-03-w36-progress-archive.md).

## 5. BLOCKED / WAITING (no action from me possible)


- **SD host card-reader** — shares the USB-C port with power/eth, so it's detached. I'll **ask** when I need it (e.g. to re-flash the card if its filesystem is damaged).
- **bash `-i`** interactive — needs a live terminal (psh automation sees EOF).
- **ffmpeg audio/player** — needs the attended `/dev/audio0` sign-off.
- **gcc-16 promotion**, **v3d placement** — D1/D2 above.
