# E10 — gcc 14.2.0 → 16.2.0 toolchain rebase: assessment + plan (2026-08-21)

Owner-greenlit "big achievement" goal. Current Phoenix cross-toolchain =
**gcc-14.2.0 + binutils-2.43** (`sources/phoenix-rtos-build/toolchain/build-toolchain.sh`
`GCC=gcc-14.2.0`, line 66), with 11 gcc + 4 binutils Phoenix patches.

## Feasibility: TRACTABLE (patch-port) but the revalidation is the real cost

- **gcc 16.2.0 EXISTS** on the GNU ftp (16.1.0 + 16.2.0 both present). binutils
  stays 2.43 (unchanged) → its 4 patches are unaffected.
- **Patch-port scope (gcc-14.2.0-* → gcc-16.2.0-*):** most patches are small and
  touch **target-definition files** that are additive + stable across gcc majors:
  - `11-aarch64-phoenix` (57L: `gcc/config.gcc` + `libgcc/config.host`) — the key
    one for RPi4; small, stable files.
  - `09-fix-libc-spec` (15L: `gcc/config/phoenix.h`) — small, phoenix OS header.
  - `04-arm-pic_crtstuff` (30L: `libgcc/config.host` + arm t-phoenix) — config.host
    is shared; arm t-fragment.
  - The arm/i386/riscv64/sparc patches (01/02/03/06/07/08/10) are for **other
    Phoenix targets** — needed only if building those; irrelevant to the aarch64
    RPi4 toolchain.
  - **`05-libstdcpp` (265L: `libstdc++-v3/configure`) = the RISK.** `configure` is
    auto-generated and regenerates between majors, so this patch will likely
    reject and need re-derivation (find the phoenix host cases in gcc-16.2.0's
    configure + re-add). Alternatively re-derive from `configure.ac` if the
    upstream generator is available.
- **Build is SAFE to attempt:** `build-toolchain.sh <target> <install-path>` takes
  the install prefix as `$2` → build to a **separate prefix** (e.g.
  `.toolchain-gcc16`), never touching the working `.toolchain`. Guards: aborts if
  `${TARGET}-gcc` is in PATH or CC/CFLAGS/... are set (run with a clean env).

## The real cost / risk (why this is multi-week + partly attended)

The patch-port + toolchain build is bounded. The EXPENSIVE + RISKY part is
**rebuilding all of Phoenix (kernel + libphoenix + ports) with gcc 16.2.0 and
revalidating** — a new gcc major routinely surfaces new `-Werror` warnings,
stricter UB handling, and codegen differences that can break subtle things across
the whole system. That revalidation needs owner judgment on breakages and is best
done attended (or at least owner-aware). **Do NOT swap gcc-16 into `.toolchain`
unattended** — everything depends on the working gcc-14.2.0.

## Plan (staged)

- **E10-M0 (unattended, SAFE — in progress):** port the aarch64-relevant patches
  (11/09/04 + the shared config.host bits; defer/skip the other-arch ones) to
  gcc-16.2.0, build a gcc-16.2.0 aarch64-phoenix cross-toolchain to a **separate
  prefix**, and compile-test a hello-world. Success = the toolchain builds + emits
  a working aarch64-phoenix binary. Resolve `05-libstdcpp` reject (or build
  `--without-libstdcxx`-style C-only first if C++ is not needed for the smoke).
- **E10-M1 (attended/owner-aware):** rebuild Phoenix (kernel/libphoenix/ports)
  with the gcc-16 toolchain, triage every new warning/error/codegen diff, HW-boot,
  full regression. Only then swap `.toolchain`.

## M0 RESULT (2026-08-21) — SUCCESS, exceeded (full C toolchain builds + links)

Built a gcc-16.2.0 aarch64-phoenix cross-toolchain to the separate prefix
`.toolchain-gcc16` (working `.toolchain`/gcc-14.2.0 verified UNTOUCHED — 0 files
changed; siblings clean; no commit/push; no Phoenix rebuild).

- **Builds:** binutils-2.43 ✓, gcc-16.2.0 stage1 (C/C++) ✓, libgcc/stage2 ✓ (after
  the fix below), build_libc (libphoenix/libc/libm/libpthread) ✓. **libstdc++ ✗**
  (the one remaining issue — M1, see below).
- **Smokes:** `aarch64-phoenix-gcc (GCC) 16.2.0`, `-dumpmachine=aarch64-phoenix`;
  `-c hello.c` → aarch64 ELF object ✓; **full static link** `hello_full.c -o
  hello_full.elf` → `ELF aarch64, statically linked` executable ✓. A complete,
  runnable C toolchain.

### gcc-16 patch deltas vs gcc-14 (the port)
- Rename all `gcc-14.2.0-*.patch` → `gcc-16.2.0-*.patch` (binutils-2.43 patches
  UNCHANGED — apply clean).
- **`11-aarch64-phoenix`: two deltas.** (a) DROP its Hunk #3 (the
  `aarch64_multilibs="lp64"` override) — now redundant: gcc-16's
  `aarch64*-*-elf|…` config.gcc branch already defaults to `lp64`. (b) ADD
  `md_unwind_def_header=aarch64/aarch64-unwind-def.h` to the `aarch64*-*-phoenix*`
  block in `libgcc/config.host` — NEW gcc-16 requirement (the var didn't exist in
  gcc-14): gcc-16's PAC/GCS unwind refactor declares `arch_fs` only under
  `MD_ARCH_FRAME_STATE_T`, which that header defines (aarch64-linux/elf set it).
- `04-arm-pic_crtstuff`, `09-fix-libc-spec`, **`05-libstdcpp`** — apply CLEAN.
  ★ `05-libstdcpp` was the plan's feared reject-risk but is a NON-ISSUE (all 11
  hunks slot into gcc-16's regenerated configure at offset +399).
- Other-arch patches (01/02/03/06/07/08/10) not needed for aarch64; set aside. For
  a full MULTI-TARGET gcc-16 toolchain later: 01/02/03/06/10 apply clean, but
  **07-sparc-mno-pditr** (rejects on autogen `gcc/configure`) and
  **08-sparc-pic_crtstuff** (rejects on `libgcc/config.host`) need porting —
  sparc-only, irrelevant to RPi4.
- Ported patches live in the build subagent's scratch `~/.claude/jobs/*/tmp/tc16/`
  (volatile) — the deltas above fully reproduce them.

### libstdc++ — ✅ FIXED (2026-08-21), gcc-16 is now a FULL C++ toolchain
The libstdc++ blocker below is RESOLVED. Fix = libphoenix `sys/types.h` (94df683):
in the C++ branch, `_ATOMIC(type)` → plain `type` (was `std::atomic<type>`, whose
deleted copy ctor broke `<ext/concurrence.h>`) AND drop `#include <atomic>` (reached
by libstdc++'s own -std=gnu++98 TUs, where `<atomic>` hard-errors). C branch
unchanged (C build/ABI untouched); layout-compatible; verified no C++ TU atomic-ops
these fields. VALIDATED: gcc-16.2.0 libstdc++ builds to completion + a C++
hello-world (std::string + std::atomic) statically links to a runnable aarch64 ELF.
⇒ gcc-16.2.0 aarch64-phoenix = full C + C++ toolchain (M0-full). Remaining M1:
(1) C++23 `import std;` — libstdc++'s std named module falls back to empty (build
still succeeds) because Phoenix `<cmath>`/`<cstdlib>` don't export the long-double
math family + several C99 names into `std::`; classic `#include` C++ works fully.
A libphoenix header-completeness task. (2) the attended full-Phoenix rebuild+
revalidate + `.toolchain` swap. (3) ABI note: C sees `initialized` as `_Atomic int`,
C++ as plain int — compile+link-validated + layout-sound, but runtime C++-touching-
pthread not yet HW-exercised (M1/HW).

### (historical) The mis-predicted vs real blocker
Not the libstdc++ *configure* patch (that's fine). It's **libstdc++ compilation**:
gcc-16's C++ resolves Phoenix's C11 `_Atomic int` inside `pthread_mutex_t`/
`pthread_cond_t` (via `PTHREAD_MUTEX_INITIALIZER`) to `std::atomic<int>` (deleted
copy ctor), and `ext/concurrence.h` (`__GTHREAD_MUTEX_INIT`/`_COND_INIT`, used by
`gthr-default.h:77/90`) copy-initializes it → `error: use of deleted function
'std::atomic<int>::atomic(const std::atomic<int>&)'`. FIX (M1, libphoenix side): a
pthread-header / gthr adjustment so the mutex/cond static initializers don't
invoke a copy under gcc-16 C++ (e.g. brace/`_Atomic`-init shape, or a Phoenix
`__GTHREAD_*_INIT` that constructs in place). This is the concrete next E10 step.

## Recommendation

The rebase is worth doing (owner-greenlit) and M0 is safely attemptable now. M1
(the system revalidation) should be attended given the breakage surface. This turn
kicks off M0 (build the gcc-16.2.0 toolchain to a separate prefix).
