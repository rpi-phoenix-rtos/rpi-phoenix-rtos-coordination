# Week 2026-W37 (Mon 2026-09-07 → Sun 2026-09-13)

Phoenix-RTOS RPi4 — weekly working log. Kept **short on purpose**: this is the file you read.
Detail: `docs/done/2026-09-10-w37-detail-snapshot-{2,3,4,5}.md`. Analyses: `docs/misc/`.
W36: `docs/done/WEEK-2026-W36.md`.

**★ OWNER GOAL (2026-09-06):** a stable build with the games and X11 good enough for a public
presentation / a screen recording published online. Everything below is ranked against that.

---

## 1. DECISIONS NEEDED FROM WITOLD — none open

- **✅ The POSIX option macros: you said "follow your recommendations and move forward", and it is
  done** (§3). Nothing left for you on it.
- **D4** (the "genuine tool" boundary) and **D2** (moving Mesa itself into `phoenix-rtos-devices`):
  optional, nothing blocked. ⓘ The Mesa fork has only the upstream `origin`, so its local commits are
  unpushed — resolving that is part of D2.

**✅ Decided / done, do not re-raise:** D7 buffer sharing **CLOSED by you** · Quake II underwater
stopgap **LEAVE AS IS** · `std::chrono` **FIXED, HW-verified, promoted** (it was ~30 libstdc++
features off, not one) · your C-side question **answered** (3 ports, 2 systemic causes, both halves
fixed) · the reel is intact and **not** being re-cut (its captions are true readings of different
moments, not understatements).

**Settled, never re-ask:** toolchain is **GCC 16.2.0 + binutils 2.47**. **Fork-only, never
upstream.** **Game forks: if ours works, leave it.** **D6** (purge build-output ELFs from coord
history): leave as is.

## 2. ⏸ YOUR ONE ACTION — flash the gated image

`artifacts/rpi4b/rpi4b-sd-2part-gated-1afa724b.img` · SHA256
`1afa724bb1fd0d97b48e7adf06dad5bf503a9452301ecad9770e40f118cfad30` · manifest
`manifests/2026-09-10-demo-image-overnight-fixes.md`

```
udisksctl unmount -b /dev/sda1 2>/dev/null || true
sudo dd if=artifacts/rpi4b/rpi4b-sd-2part-gated-1afa724b.img of=/dev/sda bs=4M conv=fsync status=progress && sync
```

ⓘ It is now under an explicit `-gated-` name so that a later `--variant sd` build cannot overwrite
the one artifact you have a pending action on. `rpi4b-sd-2part.img` is the rolling build output;
don't flash `rpi4b-sd.img` (FAT-boot-only).

**Gates — all green.** Contents + FAT **PASS** ("safe to flash") · QEMU structural **PASS** · boot
reliability **47/47** to the `(psh)%` prompt on this build, 0 fault-bearing, 0 wedge-suspect ·
provenance read out of **this image's own** ext2 root (not the build tree).

✅ **App gate on THIS image's own binaries: 6/6, 0 faults each** — the export was re-synced from the
image's rootfs and all six binaries verified sha256-identical to the ones packed in, so these
describe the code in the image. Judged from the HDMI frames, not the log.
ⓘ **This table is about the gated image `1afa724b` only.** Today's later gate, on rebuilt binaries
with a widened fault detector, reads **5/6 clean plus STK reporting 27 contained heap-corruption
events** (§3d). STK showed 0 faults in every earlier gate because the detector could not see that
class, **not** because STK got worse today.

| app | evidence on `1afa724b`'s own binaries |
|---|---|
| X desktop (`startx_gpu action`) | Window Maker + xlogo, xbill, xclock, Python GoL, xterm |
| QuakeSpasm | in-game demo playback, **48 FPS** on screen |
| Quake III (`+map q3dm1`) | in-game, full HUD (100/100, ammo 20/5/0), bot visible, **25 fps** |
| Quake II | in-game, weapon + HUD, **38.78 fps** |
| vkQuake | rendering, demo2 playback, console overlay |
| SuperTuxKart (`--track=hacienda --numkarts=4 --profile-laps=2`) | **in-race**, 4 karts, lap 1/2, `FPS: 6/7/9 · 50 KTris` |

⚠ **You are the first to boot this from a card** — the host reader is detached (shares the USB-C port
with power/eth), so SD boot cannot be exercised here. The contents gate compensates: it asserts the
six firmware files, `arm_64bit=1`, and that every file `config.txt` names exists (validated against a
known-bad copy).
↩ Fallback: `rpi4b-sd-2part.KNOWN-GOOD-e0768b87.img`.

## 2b. ✅ 6-APP GATE ON THE strncmp-FIXED BUILD — all six run

Mechanical: **6/6 `rc=0`, prompt reached, 0 faults each.** Content-graded from the HDMI frames, not
the logs:

| app | frames / fps | screen |
|---|---|---|
| X desktop | — | 89.6% non-black, 8856 colours ✅ |
| QuakeSpasm | 9624 @ ~35 fps *(re-run)* | ✅ demo playback with pickups |
| Quake III | ~21–26 fps, bots fighting | 62.5% non-black, 21444 colours ✅ |
| Quake II | **10593 @ 38.8 fps** | grader says SUSPECT at 14.8% non-black — that is its small viewport, 6141 colours ✅ |
| vkQuake | — | 91.4% non-black ✅ |
| SuperTuxKart | **1893 frames to the 300 s cap** | 99.9% non-black, 47868 colours ✅ |

★ **STK ran the whole 300 s window with ZERO corruption events** — where a cycle on the *same*
binaries hit 13 events and a fault. So the defect is intermittent (as the ~40% history said), and
**nothing here is attributable to the heap-release guard, which is not built yet.**

### 2c. ⚠ NEW, MEASURED: an app hangs BEFORE `main()` about 1 run in 7

QuakeSpasm's gate cycle produced **no output at all** after the command echo. I first called it a
regression, then "1 failure in 2" — both wrong, and both were 2-sample readings. Measured properly
with a 5-trial bench: **5/5 launched**, ~5150 frames each (35 fps). Across all 7 post-`strncmp`
QuakeSpasm runs the rate is **1 failure in 7 (~14%)**.

What it is, from three cheap observations:

* A passing run prints `quakespasm: main() entered` on the **very next line** after the echo. The
  failing run printed **nothing** → it never reached `main()`.
* The failing run had a **longer** window (gate: `idle 240 / max 300`) than the five passing trials
  (`120 / 150`) → **not** slow-exec of the 18.5 MB ELF, a genuine hang.
* No `(psh)%` prompt ever returns after the echo → psh is still blocked, so the process **hung**
  rather than exiting silently.

So the hang is in libphoenix's pre-`main` startup: `crt0-common.c:_startc()` runs `_libc_init()`,
then `_init_array()` (every static constructor), then `main`. Two candidates, no third.

✅ **TRIGGER FOUND — it races the NFS root takeover, and the logs say so exactly.** The Pi's own
`rpi4-sysinfo` measures the window: `/etc/build-versions appeared after 5000 ms (root filesystem
mounted late)`. So for ~5 s after the nfs-fs root takeover the root is not yet usable, and exec'ing
a large NFS-resident binary in that window **hangs before `main()`**. The correlation in the logs is
exact — **1** line between `registered / (takeover)` and the command echo ⇒ hung; **23** lines ⇒ ran
(both passing runs):

| harness | `--inter-cmd-secs` | takeover→command gap | reached `main()` |
|---|---|---|---|
| `run-showcase-gate.sh` | *not passed* → inherited **3** | 1 line | ✗ |
| `test-cycle-bench.sh` | hardcoded **8** | 23 lines | ✓ 5/5 |

**Harness fixed:** the gate now passes `--inter-cmd-secs 8` (`fc781b1c1`+, script `run-showcase-gate.sh`),
which clears the measured 5 s window. That removes the failure from the gate **and from the drive
commands a live demo would use** — the practical win.

⚠ **The underlying defect is still open and is mine to fix:** an exec that races the root mount
should **fail cleanly, not wedge**. It produces no diagnostic at all and leaves psh without its
prompt. Localised to `crt0-common.c:_startc()`'s pre-`main` steps (`_libc_init()`, then
`_init_array()`); note `debug()` is a raw syscall (`ID(debug)`) and so is usable that early, while
**psh cannot set env vars**, so any trace must be compile-time gated.

## 3. ✅ Settled today — the POSIX option macros (your go-ahead)

libphoenix advertised `_POSIX_VERSION 200809L` with **no option macro defined**, so portable
`#if _POSIX_TIMERS > 0 && ...` guards were always false. **Eleven now claimed** (`070ceda`),
contract-tested both ways (`98e7014`, `03cbc97`), **HW 4/4, suite 212/0**, ports rebuilt, showcase
re-gated. Detail: [`docs/done/2026-09-10-w37-detail-snapshot-6.md`](../done/2026-09-10-w37-detail-snapshot-6.md).

## 3d. ★★★ NOT AUDIO — a HEAP BASE is getting into a free bin

The widened report ran on hardware and **overturned my own audio hypothesis**. The one field that
settles it is `heap = 0x0`: `malloc_chunkInit()` sets `->heap` on every chunk it makes, so an entry
with a NULL heap **is not a chunk**. Reading the same bytes as a `heap_t` header plus its first
chunk makes every field consistent — `size = 0xd000` is the heap size (an exact page multiple),
`heap = 0` is `freesz` (nothing free), `next = 0x8003` is the first chunk's size with its
`CUSED|PUSED` bits, and `prev == chunk` is that chunk's `->heap` back-pointer, not a self-link.
`32768 + 20464 = 53232 = 0xd000 − 16`, both allocated, which is exactly why `freesz` is 0.

So the 53248 figure was the **heap size, never a request size** — which retires the ~53 kB audio
buffer search, the `sfx_buffer.cpp:189` candidate, and the "302 ms of 16-bit stereo" reading (that
was numerology on a page multiple). `--no-sound` giving 0 events still holds, but now reads as the
audio path *driving the large-allocation path*, not writing into freed memory.

The same run still faults, now **inside the allocator** — `malloc_chunkSize` (malloc_dl.c:101) on
`far=0x4423400051392000`, a wild address. 13 bad bin entries are caught and their bins abandoned;
one gets past and faults. Two of the four fault PCs already recorded in the STK-crash row were
inside the allocator, so this looks like that row's actual mechanism.

✅ **MECHANISM FOUND (containment fix committed, NOT yet built/tested).** `free()` releases a heap
on `freesz == size - sizeof(heap_t)`, which says every byte is free but **not** that the heap is one
chunk. Two reachable paths break that invariant — `_malloc_chunkJoin()` *breaks* out on a neighbour
that fails validation (the `stopping coalesce` message), and `_malloc_chunkRemove()` can take its
abandon path mid-merge leaving a chunk free but not unlinked. The release then `munmap`s the heap
while every free chunk in it **except the first** is still threaded into a free bin. `mmap` reuses
the region for the next same-size heap, and the dangling entry reads as a sane header whose fields
are the **new** heap's — which is exactly the observed report, `heap` included (it is really
`freesz`). It is **self-feeding**: each abandon can leave another chunk unremoved. libphoenix
**`cdea6dc`** releases the heap only when it genuinely is one chunk, and prints when it is not.

⚠ **Containment, not first cause** — the invariant only breaks after some earlier problem, so the
new diagnostic is the point: if it fires, this path is real and is what turns one fault into 13.
✅ **Built and boot-tested. Result: INCONCLUSIVE, and I am not calling it a fix.** 3 STK trials with
the guard: **0 corruption events, 0 faults, 1632 / 1679 / 1704 frames** (bench complete). But the guard's own diagnostic
(`heap fully free but not one chunk`) **never fired**, which means the path it defends was not taken
— so the guard did nothing in these runs and the clean result is not attributable to it. Pre-guard
the split was 1 bad run in 2; now 1 bad in 5, against a ~40% historical rate. Not enough to claim
anything. `cdea6dc` is at least harmless (no regression, STK races 1600+ frames).

⚠ For the record: I pushed `cdea6dc` before building it, breaking my own "push once verified" rule.
It is verified now.

★ **So the next occurrence self-diagnoses instead of costing another manual decode.** libphoenix
**`3e78cbf`** computes the heap-base decode at the report site and prints `hbase?=1/0` — page-aligned,
`size` a whole number of pages, and the chunk that would sit at `heap + sizeof(heap_t)` pointing its
`->heap` back at the address. No new state; validated on the host against the exact captured bytes
(→1) and against a healthy free chunk and a page-aligned real chunk (→0).
Three **refuted** first-cause candidates (runt splits, hand-out while binned, `freesz` mis-accounting)
are recorded so they are not re-walked.
Decode, arithmetic and the ruled-out candidates:
[`docs/misc/2026-09-10-stk-audio-heap-corruption-source-sweep.md`](../misc/2026-09-10-stk-audio-heap-corruption-source-sweep.md)..

### 3e. ★★★ ROOT-CAUSED A REAL libc BUG — `strncmp` read one byte past `n`

STK faulted early, and the fault symbolised to **libphoenix `strncmp`**, not to STK:
`esr=0x92000007` (WnR clear ⇒ a **read**), `n=1`, `s1=0x0cf0dfff` — the **last byte of a mapped
page** — from `asCTokenizer::IsKeyWord()`. Both `strncmp` and `strncasecmp` looped on `*p && k < n`,
and C evaluates left to right, so the byte at index `n` is dereferenced **before** the bound is
tested. Invisible until that byte begins an unmapped page, hence "intermittent".
`strncpy`/`stpncpy`/`strncat` already ordered the bound first; **`strncasecmp` had no test at all.**
The relink did not cause this, it **exposed** it (shifted sizes moved a buffer onto a page edge).

✅ **HW-verified:** new page-edge test passes (`7 Tests 0 Failures`; `strcmp`/`memcmp` 6/0), and
**STK now reaches the race**. Negative control: the same pattern faulted on this hardware with the
old code, so the test is not vacuous. libphoenix **`f2d2789`**, tests **`a82e0e4`** + **`7c80324`**,
pushed. Manifest `manifests/2026-09-10-strncmp-over-read-fix.md`.

### 3f. The allocator instrument is widened; the corruption itself is still open

Re-reading the 27 reports corrected two of my own claims: the trigger is the **8-byte alignment**
test on `next`, not the heap-range test, and `prev` is a *correct* self-link — so exactly **8 bytes
at payload offset 0** were overwritten, by a single integer near `0x8000` with its top six bytes
zero. That is header-shaped, so "PCM written into a freed block" was wrong. The report now also
prints `size` (its `CHUNK_CUSED` bit separates a duplicate hand-out from a write-after-free), `heap`
and `pay2`/`pay3` — libphoenix **`7bc30ed`**. The next STK cycle reads those fields.

⚠ **A trap that cost a build:** `--scope core --skip-prepare` is a **silent no-op** for a sibling
*source* edit. `.buildroot/` holds COPIES of the core repos too, not just `phoenix-rtos-{build,ports}`
and `_projects/` as the `--skip-prepare` help says, so the build rebuilt 10:39 sources and reported
`rc=0`. The script's own drift report did flag it (`libphoenix.a identical`).

- **✅ The games are back on one libphoenix.** They were still linked against `070ceda` while core had
  moved to `1ee441a` (two libc changes today) — an integration smell in the build the owner will
  present, and it also kept the repaired allocator diagnostic out of the apps that actually showed
  corruption. All five game ports rebuilt against current libphoenix, then relinked against the new
  GPU archives; the coverage gap noted last turn is closed rather than deferred.
- **Blocked on hardware/attendance:** SD host card reader detached · bash `-i` needs a live terminal ·
  ffmpeg audio needs the attended `/dev/audio0` sign-off.

## 5. Upstream sync — nothing outstanding

- **2026-09-10 13:0x — nothing behind.** All 16 Phoenix siblings **0 behind** `origin/master`;
  yquake2 (12 ahead), quakespasm (26), quake3e (9, tracks `origin/**main**` not master) all **0
  behind**; all four generated port patches **OK, no drift**. Nothing merged, nothing to verify.
- **2026-09-10 01:20** — merged 10 upstream commits into the yquake2 fork (the one that mattered:
  `gl3: fix GL3_Draw_TileClear for scrap images`), regenerated its patch, re-verified Quake II
  in-game, pushed.
- **vkQuake: 39 behind, staying behind by your rule.** Measured: **6 of our 10 patched files are also
  changed upstream** (the MBOIT-pipeline and MD5-skinning rewrites moved every anchor we rely on), and
  the 39 commits are ray-tracing, SSAO/XeGTAO, MoltenVK-on-Apple, Ironwail imports and CI — nothing
  we need. That is a re-port, not a merge; it deserves its own session.

## 6. Also settled this week

One line each, with full detail in `docs/done/2026-09-10-w37-detail-snapshot-{4,5,6}.md`: the
libstdc++/`std::chrono` fix (~30 features off, not one) and its promotion · libphoenix
`clock_getres` · the allocator large-bin abandon fix · the readlink clamp · the nfs-fs
attribute-cache fix · CPython's restored `dir_fd`/`sched_*` · the Window Maker `nice()` collision
that had made the X11 showcase unbuildable, **and the stale docs that would have re-introduced it**
· the yquake2 upstream sync · MicroPython's `ticks_ms`/`ticks_us` were the wall clock, now monotonic
· a sweep of all 16 port shims against libphoenix's 994 symbols (one overlap, removed) · the public
docs re-synced · your four reported bugs, all addressed.
