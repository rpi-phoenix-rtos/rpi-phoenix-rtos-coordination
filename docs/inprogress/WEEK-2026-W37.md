# Week 2026-W37 (Mon 2026-09-07 → Sun 2026-09-13)

Phoenix-RTOS RPi4 — weekly working log. Kept **short on purpose**: this is the file you read.
Full detail for this week: `docs/done/2026-09-09-w37-detail-snapshot.md` and
`docs/done/2026-09-08-w37-archived-detail.md`. Analyses: `docs/misc/`. W36: `docs/done/WEEK-2026-W36.md`.

---

## 1. DECISIONS NEEDED FROM WITOLD

- **★ D7 (new) — the GL window's speed: spend a rewrite on it, or ship as is?** Your fourth X11 point
  and the only one still open. Fixable, design proven in isolation, but **not** small: the client cannot
  *export* its buffer (Mesa here has `resource_from_handle`, no `resource_get_handle`), so the fix
  inverts allocation — the client mints the buffer through the raw daemon RPC and becomes an
  **st/gallium** client instead of a GL one — and the X server gains a **new GPU-blit path in its damage
  timer**. That is a rewrite of the two components the demo depends on. Measured ceiling **~1.7×**
  (96 → ~55-60 ms/frame, 10.4 → ~17 fps), not the ~5× the subtraction suggests, because ~35 ms/frame is
  the DDX present, which sharing does not remove. Design + numbers:
  `docs/misc/2026-09-09-gl-window-buffer-sharing-work-order.md`. **Not started.**
- **Quake II underwater Y-mirror stopgap → real fix** (#2 below): declare the scanout FBO `FlipY` and
  delete the `st_atom_framebuffer.c` size gate, which also removes glamor's hand-rolled flips. Needs one
  build + a re-verify pass over 5 games + the desktop. `docs/misc/2026-09-08-flipy-scanout-gate-work-order.md`.
- **libstdc++ `std::chrono` is 1-second granularity system-wide** (a toolchain defect; it capped STK at
  1 fps until a per-port `CLOCK_MONOTONIC` patch). General fix = rebuild libstdc++ with
  `--enable-libstdcxx-time=rt` — a whole-system C++ rebuild, so not started unattended.
- **D4** (the "genuine tool" boundary) and **D2** (moving Mesa itself into `phoenix-rtos-devices`):
  optional, nothing blocked.

**Settled, never re-ask:** toolchain is **GCC 16.2.0 + binutils 2.47**. **Fork-only, never upstream** —
fix bugs the best way for the Pi 4 without breaking other arches. **Game forks: if ours works, leave it**
(yquake2 10 behind, vkquake 30 — settled, not deferred). **D6** (purge build-output ELFs from coord
history, needs a force-push): leave as is.

**★ OWNER GOAL (2026-09-06):** a stable build with the games and X11 good enough for a public
presentation / a screen recording published online. Everything below is ranked against that.

## 2. STATE — the goal is met for *running* the demo; one exit-path bug found

Five games + the X desktop HW-gated at **0 faults** with frames checked, on a flashable image that also
passes a contents gate and a QEMU structural boot gate. ⚠ One caveat found this turn: **quitting** the
desktop wedges the board roughly 1 in 6 times (§2c). Running it does not.

**⏸ OWNER ACTION — ★ FLASH THIS:** `artifacts/rpi4b/rpi4b-sd-2part.img` · SHA256
`5ace5d4b59c830c40f3d012cd6543ce655bfabeaaf0d1684c21c5de4a64e5782` · manifest
`manifests/2026-09-09-x11-utils-merge-demo-image.md`.
```
udisksctl unmount -b /dev/sda1 2>/dev/null || true
sudo dd if=artifacts/rpi4b/rpi4b-sd-2part.img of=/dev/sda bs=4M conv=fsync status=progress && sync
```
(`/dev/sda` is the SD card here; do **not** flash `rpi4b-sd.img` — FAT-boot-only.)

⚠ **Being re-cut now to carry the wedge fix** (kernel `f0e94a9b`). The five games have been re-gated
on that tree first — **all 5: 0 faults, no wedge, verified by CONTENT not just fault count**:

| app | evidence on the fixed kernel |
|---|---|
| QuakeSpasm | textured dungeon, demo playing, HUD, **37 FPS** on screen |
| Quake III | textured 3D scene, HUD 100/100, **34 fps** on screen |
| Quake II | animating, motion 20–25 across every frame |
| vkQuake | animated (motion 13–27), ran a demo to completion, ended on a static console |
| SuperTuxKart | in-race: kart, track, minimap, lap 1/2, timer, `FPS: 5/6/6` (matches your ~6 fps) |

The SHA above is the **old** image until the re-cut finishes and its gates pass; this section will name
the new one.

**★ WATCH THIS ONE, NOT `x-bulkupload.mp4`:**
`artifacts/hdmi-video/20260909-122231-x-shipped-image.mp4` — 120 s of the desktop, recorded from the
exact binaries in the image above. Three of your four X11 points are fixed and **the file you reviewed
predates all three**: it is from 01:32, the mirror fix landed 03:24 and the xbill layout fix 08:55. Same
detector on both files:

| | mirrored Clip | xbill hidden | GoL bands |
|---|---|---|---|
| your `x-bulkupload.mp4` | **326/442 frames**, MAD 0.00 (exact mirror) | **354/442** | — (old layout) |
| new clip, shipped image | **0/360**, MAD 71.5 | **0/360**, 91% visible | **0/360**, 48 px (AA only) |

Full 11-segment reel: `artifacts/hdmi-video/20260909-084929-phoenix-rtos-rpi4-showcase.mp4`.

**Gates on this image — all PASS:**

| gate | result |
|---|---|
| image contents (paths, provenance, config-CONTENT markers, **FAT boot partition**) | **PASS — safe to flash** |
| QEMU structural boot | **PASS** (`bcm2711-emmc refs=4, nfs refs=0` → SD variant) |
| desktop-**exit** soak | ⚠ **5/6 clean — 1 run WEDGED the board**, see §2c |

| app | evidence (HW, this image's binaries) |
|---|---|
| X desktop (`startx_gpu action`) | 0 faults · mirror MAD 71.5 · xbill 91% visible · GoL 48 px · **10.4 fps** |
| QuakeSpasm (`quakespasm +playdemo demo1`) | textured 3D gameplay, **37 FPS** overlay, 0 faults |
| Quake III (`quake3 +devmap q3dm1`) | 1390 files in 2 pk3s, animating (motion 8.7–17.3), 0 faults |
| Quake II (`quake2 +demomap q2demo1.dm2`) | animating (motion 16–31), 0 faults |
| vkQuake (`vkquake +map start`) | animating (motion 11–25), 0 faults |
| SuperTuxKart (`stk --profile-laps=2 --track=hacienda --numkarts=4`) | racing, animating (motion 22–43); ⚠ faults in ~40% of runs, see §3 |

⚠ **Harness rules that cost me cycles:** games launch through their RAM-staging wrappers (`quake3`,
`quake2`), never the raw `quake3e`/`yquake2` (bare `quake3e` finds no data). **One game per Pi cycle** —
they do not exit, so a second command is typed into the running game.

## 2a. ⚠ SD boot cannot be tested on this bench — so the FAT partition is now gated

**You are the first person to boot this image from a card.** The host card reader is detached (it shares
the USB-C port with power/eth) so `/dev/sda` is absent, and the Pi has no card either
(`sdcard: no card present in slot 0`). Everything gated above is the ext2 rootfs, QEMU's structural
boot, and netboot.

Nothing was checking the *first* thing the firmware reads: `verify-rpi4b-sdimg.sh` only checks size and
sha256. The contents gate now asserts the six files the firmware needs, `arm_64bit=1`, and — the check
that catches the realistic failure — that **every file `config.txt` names actually exists**. It is
**validated against a known-bad copy** (delete `kernel8.img` → MISS + cross-check FAIL + "do not
flash"), and it now **runs inside `--variant sd`** under `set -e`; before this turn nothing invoked it.

## 2c. ⚠ NEW, and you should know before you flash: quitting the desktop wedges the Pi ~1 in 6

The exit soak had only ever been run on the **pre-relink** build. Re-run on the shipped binaries:
**1 of 6 observed session exits hung the board.** In the bad run the kernel printed
`vm: page fault with a corrupt process pointer` (`vm/map.c:892`) and then produced **no further
output at all** — the shell prompt never came back and two later commands went unanswered. The other
5 exits shut down cleanly. Gameplay and the desktop *while running* are unaffected.

What it is: `thread->process` pointing at a **freed** `process_t`. `process_isLive()` only tests
`magic == PROCESS_MAGIC`, and `process_destroy()` clears that magic immediately before `vm_kfree(p)`
(`proc/process.c:122`) — so this fires on a plain **use-after-free**, not on the "wild-written" memory
the code comments assume. It is **not new today**: the scheduler carries the *same* check at
`proc/threads.c:534`, so the condition has been hit before and was papered over with a printf and a
fallback to the kernel map in two places rather than fixed. Killing six clients at once (what
quitting Window Maker does) is what makes it likely.

**★ The WEDGE is now root-caused, and the cause is the kernel's own safety net.** Two proven defects,
both independent of whatever corrupts the pointer:

1. The scheduler's "name the victim" diagnostic (`proc/threads.c:539`) calls `lib_printf` **while
   holding `threads_common.spinlock`** — and `lib_printf` → `lib_putch` → `log_write` takes
   `log_common.lock`, whose acquisition re-enters `hal_spinlockSet(&threads_common.spinlock)`
   (`threads.c:1833`), a **non-recursive** spinlock, with the global scheduler lock held and
   interrupts masked. **The first character deadlocks the CPU**, then every other core wedges at its
   next scheduler entry. That is why no `proc: thread …` line ever appears, and why the `vm:` message
   stops mid-word: a timer tick lands inside the byte-at-a-time print. Two runs truncated at 8 and 34
   characters — a fixed bug would truncate in the same place.
2. The `proc = NULL` fallback **guarantees a panic**: `vm_mapForce(kmap, <user addr>)` can only fail,
   and the failure branch is `LIB_ASSERT_ALWAYS(proc != NULL, "exception in kernel")` — with `proc`
   just set to NULL. Under `NDEBUG` that reaches `hal_cpuReboot()`, which on BCM2711 is
   `for (;;) hal_cpuHalt()`. **No reboot, permanent halt.**

Reproduced 2 of ~12 exits. The dangling pointer itself is **not** a `process_t` refcount hole — a
full census of all 13 `refs` sites and every thread-exit path found the accounting symmetric; the
leading candidate is a stray write into a recycled `thread_t` (there is a root-caused precedent on
this board at `threads.c:343`). `thread->process` is also written **once** (`threads.c:700`) and never
cleared, so any freed process leaves it dangling forever.

**✅ Landed this turn (kernel `ac40dcf1`, pushed, HW-verified):** the same fault class kills the kernel
at `_kmalloc_free` → `lib_listRemove`. All three zone `LIST_REMOVE` sites now check both links point
into kernel space and **strand** the zone instead of faulting. Boots, runs the desktop, exits cleanly,
and **does not false-positive**. Manifest `manifests/2026-09-09-kmalloc-zone-link-guard.md`.
⚠ My first version of that patch had a deadlock of exactly the kind described above (printing under
the allocator's own non-recursive mutex, which `log_write` re-enters via `vm_kmalloc`); it now uses
`hal_consolePrint`, the HAL's documented leaf lock.

**✅ FIXED (kernel `ce445804`, pushed):** all three diagnostics now format with `lib_sprintf` and emit
with `hal_consolePrint` (the HAL's leaf lock, allocation-free), capped at 4 lines; the fallback retires
just the faulting thread instead of asserting into a permanent halt; both detection sites clear
`thread->process`; and `process_dumpException` no longer follows a non-live pointer. The scheduler line
now also prints `magic`/`refs`, which tells us **which** cause it is next time it fires (`magic==0` +
`refs==0` = a real `process_t` use-after-free; arbitrary bits = a stray write).

**Soak: 20/20 clean session exits, 0 wedges, 0 faults** (5 cycles × 4 exits). At the observed ~1-in-6
base rate, 20 clean exits by luck is `(5/6)^20` ≈ **2.6%**, so the wedge is gone with ~97% confidence.

⚠ **But one honest gap, and it is the reason I am not calling this closed:** **no diagnostic fired in
any of the 20 runs**, so the *corruption* did not happen either — which means the new recovery path
(retire the thread, keep the board alive) is **still unexercised on hardware**. And a null result right
after a code change is exactly the trap this project already hit once: the 2026-09-02 pass found that
adding a `memset` perturbed the timing enough to stop the corruption reproducing. So "20/20 green"
may partly be "the bug hid again".

**★ Recovery path now DEMONSTRATED, not inferred** (test-only injection, compiled out of shipped
builds — verified 0 `INJECT` strings in `loader.disk`). Pointing a thread at a `process_t` with `magic`
cleared to 0 (exactly what `process_destroy` leaves) shows:

- the diagnostic printed **complete** — both real wedges truncated it mid-word, at 8 and 34 characters.
  That is the direct evidence the print path no longer deadlocks the CPU.
- the discriminator works: **`magic=00000000 refs=0`**, so the next real occurrence will say whether it
  is a genuine use-after-free or a stray write.
- the kernel ran **well past where it used to die** — a full ~300-line exception dump after the fault,
  where the old code asserted and halted forever on the spot.

⚠ **What it does not show:** that the system stays *usable*. The hook fires on the first abort after
boot, so it retired a thread of an essential early process and that run never reached a prompt. That is
my test's targeting, not a failure of the recovery.

It also exposed a real defect in my own previous commit: the "NOT LIVE" line went only to `posix_write`,
and fd 2 of a process whose `process_t` is gone goes nowhere — so the one line explaining why the dump
stops never reached the UART. Fixed. Kernel `f0e94a9b`, manifest
`manifests/2026-09-09-wedge-fix-verified.md`.

Aside worth knowing: `map_pageFault` is entered **fewer than 5 times** across a whole boot + desktop +
libc test on this port, which is why four runs were spent on wrong thresholds first.

**This does not block the demo:** it needs you to *quit* the desktop, which a presentation or a
recording does not have to do. But it is a real kernel bug on a path you reported as #4, so it is not
being written off.

## 2d. 🔎 OPEN, newly seen: one AF_UNIX liveness iteration in 50 fails with an unexplained code

On the final verification run, `test-libc-unix-socket` reported **27 Tests 1 Failures**:
`unix-socket.c:1729`, `WEXITSTATUS(status)` **Expected 0 Was 70` — a forked child exited 70. The test
is a 25-iteration × 2-socket-type liveness loop, so this is roughly **1 failure in 50** iterations.

Two things make it worth a proper look rather than a guess:
- **No code path in that test exits 70.** Every child failure path is `exit(1)`, the success path is
  `exit(0)`, and `TEST_ASSERT(WIFEXITED(status))` on the line above **passed** — so it was a normal
  exit with a code the test cannot produce.
- It is **not plausibly** from today's kernel work: every one of those changes is inert unless a
  corrupt pointer is detected, and that run reported `fault_pattern_matches: 0`, so none fired.

But I have **no clean baseline** for this suite in recent logs, so I am not claiming it is pre-existing
either. Needs: a few repeat runs to get a rate, and the child's stderr (`CHILD-FAIL` prints errno)
captured to find where 70 comes from. Not chased further this turn.

## 2b. ✅ What was fixed on X11 this week (all HW-verified, all in the image)

| fix | effect |
|---|---|
| glamor mirror artefacts — Mesa's ≥1024×768 `Y_0_TOP` gate applied to a non-scanout pixmap | your mirrored Clip **and** the GoL/`top` band bleed were **one** bug; MAD 6.85 → 74.98 |
| AF_UNIX sockets got a **one-page (4 kB)** ring, so a 1.2 MB `XPutImage` crossed in ~512 blocking round-trips | frame **395.9 → 112.6 ms** |
| the damage path was **dead** (`glamor_init` ran after `shadowSetup`, so glamor's `CreateGC` never chained) | HDMI **2.3 → 25.6** updates/s |
| xbill was hidden: it ignores its `-geometry` *position* (Xt sets PPosition, so Window Maker auto-places it) | layout rebuilt **around** where it lands; 91% visible |
| desktop-exit Data Abort — glamor's `DestroyPixmap` never chained | 6/6 crashed before, **0/18** after |

The three artefact detectors are now **`scripts/grade-x-desktop-video.py`** (run with `.venv/bin/python`),
validated FAIL-on-your-video / PASS-on-current — not shell history. Two of them were wrong while I wrote
it, one of them failing *open* (a NaN that compared False and reported a clean PASS on 326 mirrored
frames); both are now impossible by construction.

## 3. THE FOUR BUGS YOU REPORTED — status

| # | bug | status |
|---|-----|--------|
| #4 | Data Abort on exiting Window Maker | ✅ **FIXED** — kernel half + glamor `DestroyPixmap` chain. 6/6 → 0/18. |
| #3 | Quake III glitches early in gameplay | ✅ fixed — Mesa `u_vbuf` dropped-draw fix, in the image |
| #2 | Quake II underwater view Y-mirrored | ✅ fixed, but a **stopgap** (over-corrects at `viewsize <= 71`). Real fix is in §1. |
| #1 | GPU X11 xterm resize artefacts | **not reproduced** by a sound measurement; resize *correctness* verified. Likely intermittent frames during a fast drag (no compositing here). Did they persist *after* you released the mouse? If so it needs a different hypothesis. |

**STK, per your call ("at least STK renders correctly"):** races correctly, its own overlay reads
`FPS: 4/4/6 – 55 KTris` (matching your ~6 fps). Faults in **~40% of runs at varying sites** (allocator
coalesce path; a near-NULL read in its font manager during *startup*). libphoenix's allocator is
**exonerated** by a 4.8 M-op harness, so this is corruption in STK's own code — an upstream-app bug.
Not a regression, not investigated further. My earlier "teardown crash" and "deterministic" claims were
both wrong and are corrected here.

## 4. ALSO DONE THIS WEEK (short)

STK 1 → 5.84 fps (the libstdc++ clock defect in §1); root CAs; libphoenix stdio partial-write fix; V3D BO
leak proven bounded; libphoenix allocator hardened + a regression test; buffer-sharing steps 0 and 2
proven on HW (see D7). **Upstream sweep #9 (2026-09-09): nothing to do** — all 16 siblings 0 behind
`origin/master`, all four game-port patches `--check` OK.

## 5. BLOCKED / WAITING
- **SD host card reader** — detached (shares the USB-C port with power/eth). Blocks flashing/SD-boot here.
- **bash `-i`** interactive — needs a live terminal (psh automation sees EOF).
- **ffmpeg audio/player** — needs the attended `/dev/audio0` sign-off.
