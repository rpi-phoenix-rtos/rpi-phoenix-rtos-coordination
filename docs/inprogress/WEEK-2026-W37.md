# Week 2026-W37 (Mon 2026-09-07 → Sun 2026-09-13)

Phoenix-RTOS RPi4 — weekly working log. Kept **short on purpose**: this is the file you read.
Detail: `docs/done/2026-09-10-w37-detail-snapshot-{3,4,5,6,7}.md` and
`docs/done/2026-09-10-w37-allocator-investigation.md`. Analyses: `docs/misc/`. Analyses: `docs/misc/`.
W36: `docs/done/WEEK-2026-W36.md`.

**★ OWNER GOAL (2026-09-06):** a stable build with the games and X11 good enough for a public
presentation / a screen recording published online. Everything below is ranked against that.

---

## 1. DECISIONS NEEDED FROM WITOLD — none open

Optional only: **D4** (the "genuine tool" boundary) and **D2** (moving Mesa into
`phoenix-rtos-devices`; the Mesa fork has only upstream `origin`, so its local commits are unpushed
— resolving that is part of D2). Nothing is blocked on either.

**Decided, do not re-raise:** D7 buffer sharing · Quake II underwater stopgap **leave as is** ·
`std::chrono` **fixed + promoted** · the reel is **not** being re-cut · toolchain is **GCC 16.2.0 +
binutils 2.47** · **fork-only, never upstream** · game forks: if ours works, leave it · **D6** leave
as is.

## 2. ⏸ YOUR ONE ACTION — flash the gated image (a newer one is being cut now)

🐞 **I broke three games and the gate caught it — fixed the same session.** The `released[]` test I
added to `malloc_chunkValid()` was a **stale blacklist**: the ring is written on release and was
**never cleared**, but `mmap` reuses addresses, so once a region came back as a new heap **every valid
chunk in it was rejected**. quakespasm, quake3e and yquake2 all faulted (**163641** allocator messages
in one run; near-NULL `far=0x200`/`far=0x30` where callers dereferenced a NULL `malloc()`).
✅ Heap allocation now drops any `released[]` record overlapping the new region. Verified: stdlib
93/0, misc 212/0, **0 faults, 0 allocator messages**, and **quakespasm back to 7561 frames @ 40.6 fps**
— the same game that was failing. ⇒ Lesson: *a "known bad" test is only safe while the badness lasts.*

⏹ **Refresh attempted, and I decided NOT to cut it yet.** Re-gate on the fixed build: **5 of 6 apps
clean** (X, quakespasm, quake3e, Quake II, vkQuake — all 0 faults, 0 allocator messages), but **STK
faulted once**, the same allocator class, with no STK guard firing. That is the pre-existing ~1-in-10
rate, so the new image would be **no worse** than `1f493117` and carries real fixes — but I have been
changing the allocator all day and broke three games once already, so shipping a same-day build with
a non-clean gate is not a trade worth making while you are away. **`1f493117` stays the flash
target**; the fixes keep soaking.
★★ **INSERTION IS EXCLUDED (10 trials, 25 abandons, 0 catches).** Across a full bench —
**1 crashed run, 2 faults, 25 abandons, 0 insertions** — `_malloc_chunkAdd` never once received a
heap base. With 25 genuine corruption events and the probe silent throughout, the bad entry is **not
inserted**; it appears in a bin some other way. Crash rate unchanged at ~1 in 10.
ⓘ The entry decodes as `heap_t{size=0xd000, freesz=0x4ff0}` — a **LIVE** heap's header, matching the
recorded `freed?=0` readings. So it is not a stale pointer into a *released* heap either.
ⓘ Structurally ruled out too: `live[]` sits **after** the bins in `malloc_common`, and both ring
writes are index-masked (`&255`, `&7`), so no intra-struct overflow can spill heap bases into a bin.
⇒ What remains: a chunk's `next`/`prev` being **overwritten after insertion** (they live in the freed
chunk's payload), which `LIST_REMOVE` then propagates into the bin head.
★★★ **ROOT SHAPE FOUND, and it is a 4-byte write — not an allocator bug.** Every genuine event
carries a **corrupted high 32 bits with the low half intact**, measured on a LIVE heap header
(`it->heap == refheap`): `heapsz = 0x80000001_0000d000` where the real size is `0xd000`. The
process then dies computing `heap + heap->size`, and `heapEnd - heapsz` is **exactly the heap base**,
which proves the wild pointer is that arithmetic and nothing else. ⇒ the allocator is the **victim**.
ⓘ It is a marker of the REAL events: present in 3/9, 3/3 and 2/5 records in the three faulting runs,
and in **1 of 18471** records from the build I had broken. Same value across two boots and two builds
⇒ deterministic, not random.
✅ **The poison bench closes the last allocator-internal route: 8 trials, 0 fires.** T1 genuinely
corrupted (7 abandons, 1 bad-neighbour, 1 fault, 3 high-word records) and the poison **still never
fired**, so nothing wrote into a freed small-bin chunk between insert and removal. The other 7 ran
clean to ~1700 frames (crash rate 1/8, in line with ~10%). ⚠ Bounded claim: abandoned entries are
heap BASES, which are never poisoned, so this zero cannot speak for those.
✅ **Contained, HW-VERIFIED and PUSHED** (libphoenix `11286c0`, manifest
`2026-09-12-heap-size-guard.md`): `heap->size` had only an alignment and a LOWER bound, so a
page-aligned huge value passed every check. Now bounded above by `heapHi` + a wrap check; the
coalesce walk answers "last" and **stops** instead of dereferencing. HW: **stdlib 93/0, misc 212/0,
0 faults, 0 allocator messages — and 0 guard fires**, the negative control that makes a future fire
meaningful.
ⓘ Two traps caught by checking instead of trusting `rc=0`: `--scope core` left the guard **out of
every port binary** (`psh`=1, `supertuxkart`=0) until a `--ports-only` relink; and STK's build prints
`collect2: error` + `Error 1` **by design** — the real signal is `M3 complete`.
⚠ **Two corrections of mine.** (1) The `st_bind_ubos` attribution is **withdrawn** — that pc came from
a binary I had already proven does not match the fault (`lr` landed after no `bl`). (2) I called a
chunk full of sequential `uint16` "the origin"; that record's own probe reads `hbase?=0`, so the
memory is legitimately an index buffer and the allocator merely read a **stale pointer** into it —
a different signature from the one that kills us. Detail:
[`docs/misc/2026-09-12-two-corruption-signatures.md`](../misc/2026-09-12-two-corruption-signatures.md).
⏭ Open: who performs the 4-byte write at `(8-aligned)+4`. **Lead 1 confirmed in the source and
patched** (ports `838b1f5`, patch `0019`, unpushed): in `FontWithFace::render()` `indices`/`offsets`
are compact but `fallback` was indexed by the glyph loop counter, and the `GLF_NEWLINE` `continue`
makes them diverge — so for multi-line text `fallback[n]` is the wrong glyph, and the `sprite_id`
bounds check is **skipped entirely** when it reads true, indexing the smaller fallback bank through
an `operator[]` with no check under NDEBUG. The wild `ITexture*` then reaches `grab()`/`drop()`,
which are 4-byte read-modify-writes — the right shape. ⚠ A `++` on a zeroed half gives `1`, not
`0x80000001`, so this is a strong lead, **not** a proven cause; it is a real defect either way.
✅ **Both rival leads CLOSED without Pi time, leaving `0019` the only live candidate.** The
`m_cached_gls` dangling reference cannot fire in-race: the clear inside `getCachedLayouts()` is gated
on `getGameState() != GAME`, and its only other caller is the language settings screen — while every
observed crash is in-race (72–1361 frames). (`m_cached_gls` is a `std::map`, so an insert does not
invalidate other nodes either.) Latent in menus only. And the Mesa `vbo_save_api.c` underflow is
**dead code here** — every `glNewList`/`glCallList` hit across the four games is a declaration in a
bundled `SDL_opengl.h`, never a call.
⚠ **The planned guard-first order did not survive contact: the ports build APPLIES `patches/*`
automatically**, so the relink pulled `0019` in with it (6 log mentions; the staged source carries it).
The 12-trial bench now running therefore measures **guard + 0019 together and cannot attribute
between them**. Caught before reading the result, not after.
★ **Better experiment than a guard-only arm — `0019` now PROVES ITS OWN RELEVANCE.** The new bounds
test reports once (`STK-FONT-OOB`) when it is the **fallback** bank that is out of range, which is
exactly the access the old code performed with no check at all. **One fire = the path is live**, a
direct observation instead of a rate difference needing many trials. ⓘ Reachability is not
hypothetical: `BoldFace::init()` calls `setFallbackFont(regular)`, so any bold glyph missing from the
bold TTF takes the fallback branch — and the HUD uses bold text. Patch amended (ports `8f7a972`,
reverse-dry-run clean, unpushed).

ⓘ **Two leads closed without Pi time.** The Mesa `vbo_save_api.c` underflow is **dead code here**
— every `glNewList`/`glCallList` hit across all four games is a *declaration* in a bundled
`SDL_opengl.h`, never a call — so it is a real upstream bug we are not exposed to; not patching it.
And the guard's own invariant is verified: `heapHi` tracks `max(heap + heapSize)` (not max base) and
is updated **before** the heap is first used, so `base + size <= heapHi` holds by construction and
the guard cannot false-reject. Existing `malloc_fragment_and_drain` + `malloc_heap_recycle_churn`
already cover the coalescing it could have broken (93/0 with it live), so no new test was added —
a near-duplicate would be noise.

### 📊 First arm: guard + `0019` — 12/12 clean, but NOT yet a fix

**0 faults, 0 abandons, 0 bad-neighbours, 0 high-word records, 0 guard fires** across 12 trials,
11 of them reaching ~1630–1785 frames.
⚠ **This does not establish anything yet.** At the 9.3% baseline only **1.12** crashes were expected
in 12 trials, so observing 0 gives **Fisher p = 0.586** — unremarkable. Calling this fixed is exactly
the mistake that produced five false closures on the vkQuake torch issue.
⚠ The `STK-FONT-OOB` count of 0 is **vacuous**: the detector was added to the patch *after* this
build, and `strings` on the benched binary confirms it was not in it.
ⓘ T3 produced only 3 frames — **not a crash**: its log ends mid shader-compilation, i.e. it spent the
whole window rebuilding the shader cache after today's port rebuild. Real presentation risk, and the
reason the "warm each GPU app once" note exists.
★★★ **PROVEN PORTING BUG FOUND — Mesa's GL path munmaps the WINSYS's own BO mapping** (mesa
`274ee5abea9`, unbuilt). `bo->map` is not a libc `mmap()` result on this port: the winsys hands out
its own `b->cpu` as the MMAP_BO offset. `v3d_bo_free()` `munmap`s it anyway, returning pages to the
kernel while `W.bos[slot]` still has `used=1`, a live `->cpu` and **valid GPU PTEs** — so malloc can
take the same pages for a heap. That is a concrete route for a 4-byte store to land on allocator
metadata.
ⓘ **Verified, not taken on trust:** the identical hazard is already guarded in **two of three**
places (`v3dv_bo_unmap`, `ioc_close_bo`) — the **GL path STK uses was missed**. And it silently
defeated the W36 winsys fix that stopped recycling a closed BO's address: `V3D_UNMAP_CLOSED_BO` is
set **nowhere**, so this was the ONLY path still returning BO pages to the kernel.
⚠ Sharper for imported BOs: the size is a caller-supplied `stride*height`, **not page aligned**, so
the munmap tore a partial hole out of the HEAD of a mapping the winsys still owned. Scanout BOs
bypass the BO cache and every RT ≥1024x768 is scanout ⇒ STK's deferred pipeline hit this constantly,
which also explains **why STK and not the GL1-style Quakes**.
❌ **My fence hypothesis is REFUTED, with evidence** — not merely unconfirmed. No seqno/fence value
is written anywhere in the port: every syncobj entry point is a constant-returning stub
(`*handle = 1`, `*first_signaled = 0`). Dropped.

⏭ **Second hypothesis prepared, in case the detector never fires.** Read as a set, the corrupting
values — `0x80000001`, `0x80000000`, `0x00000002` — are small counters, some with bit 31 set: the
shape of a **fence / sequence number**. A stray 4-byte fence or query-result writeback into memory
`mmap` recycled from a freed GPU buffer would explain the 4-byte width, the arbitrary +4 offset, the
intermittency and the process-wide damage. ★ It also fits the sharpest discriminator we have:
corruption is **STK-only**, yet the three Quakes are GPU apps too — but they are GL1-style, while STK
is the only app using GLES 3.1 queries/writeback. Subagent auditing the V3D fence + BO-lifetime paths.

⏳ **Detector arm RUNNING** (12 trials, `det19`): detector + guard both confirmed present in the
**live export** binary this time, not just the staged one. Window widened to 300 s so a cold
shader-compile cannot truncate a trial the way it truncated T3. One `STK-FONT-OOB` fire proves
`0019`'s path is live; none across corrupting runs retires it.

### ✅ Your question: is this a regression? **No — measured, three ways**

STK-attributed Data Aborts per STK run (the kernel dump has existed all along, so days compare):

| 09-07 | 09-08 | 09-09 | 09-10 | 09-11 | 09-12 |
|---|---|---|---|---|---|
| **33.3%** (12/36) | 31.4% (11/35) | 19.4% (6/31) | 11.3% (6/53) | **1.9%** (1/53) | 9.3% (7/75) |

- **Down 3.5× from the peak.** "Relatively OK a couple of days ago" lands on 09-11, the *best* day;
  09-10 was 11.3%, worse than today. Today vs yesterday at **identical settings** (hacienda, 4 karts,
  2 laps): 1/47 vs 7/75, **Fisher exact p = 0.151** — not significant. 09-11 was luck, not a better build.
- **Nothing changed on 09-09**, where the corruption messages first appear — both detectors were
  *added* that day (`2d55b1e`, `bd6ae05`). It was invisible before, not absent. I nearly filed that
  date as the regression.
- **Not cumulative / not "after long play".** Frames rendered at each of the 8 crashes:
  `72 152 234 239 318 318 1316 1361` — half under 320, spread 19×. A constant hazard per unit of work,
  not degradation. ⚠ All runs are a fixed 2-lap bench, so a genuinely long session is untested:
  one `--profile-laps=8` bench would confirm the hazard stays flat.
- Frame rate is steady `~8.1–8.7 fps` in every run including the crashing ones — no pre-crash drift.
  The slowness is a separate, known ceiling.



`artifacts/rpi4b/rpi4b-sd-2part-gated-1f493117.img` · SHA256
`1f493117cfc9de4ca28ac9a5e266a8075d137fe355f7599885f9785d42d388aa` · manifest
`manifests/2026-09-11-demo-image-1f493117.md`

```
udisksctl unmount -b /dev/sda1 2>/dev/null || true
sudo dd if=artifacts/rpi4b/rpi4b-sd-2part-gated-1f493117.img of=/dev/sda bs=4M conv=fsync status=progress && sync
```

**Why this supersedes `2940cc70`:** it carries the **V3D clock-race fix**. That race intermittently
left the GPU clock off, and the next V3D MMIO read then **never completes** — a silent, unkillable
hang at GPU-app startup. On a live demo that is a game that simply never appears. `2940cc70` predates
the fix.

**Gates — three, all passing.** Contents + FAT **"safe to flash"** · structural **OK** · and
**provenance read from this image's own ext2 root**: all five games carry **both** V3D fixes
(`refusing to read V3D MMIO`, `TRUE reset FAILED`) and the diagnostic startup trace is **absent** from
every binary including `psh`.

**Evidence behind it:** six-app showcase gate **6/6, 0 faults**, pixel-checked (not just clean logs) ·
**85/85** quakespasm launches with `clk=on`, 0 retries, 0 faults · Quake II **10588 frames @ 38.8 fps**
· and the cold-state probe's bogus `MBOX_FAIL` readings went **~9% → 0%**, which is direct measured
evidence the mailbox race is real and now serialized away.

★ **Before presenting or recording: run each GPU app once to warm up** (a cold first run costs ~35 s),
and don't rebuild GPU archives right beforehand.
↩ Fallbacks, newest first: `-gated-2940cc70.img`, `-gated-aa176f38.img`, `-gated-80a1cb88.img`,
`-gated-1afa724b.img`, `rpi4b-sd-2part.KNOWN-GOOD-e0768b87.img`.
ⓘ Don't flash `rpi4b-sd.img` (FAT-boot-only) or the rolling `rpi4b-sd-2part.img`.
⚠ **You are the first to boot this from a card** — the host SD reader is absent (it shares the USB-C
port with power/eth), so SD boot cannot be exercised here. The contents gate compensates.
⚠ The TFTP `loader.disk` is now the **SD** blob, so netboot testing needs an `--variant nfsroot`
rebuild first.


## 2b. ✅ Demo repeatability, the V3D clock race, and STK's AI crash — all settled

**3 full gates, 18 cycles, 17 clean**, all six apps rendering every time (content-graded from HDMI).
The one blemish was `/bin/ntpclient`, not the app. The intermittent GPU-app start stall was a **V3D
clock race** — fixed and shipped in `1f493117` (`MBOX_FAIL` ~9% → 0%). SuperTuxKart's AI crash was
an `assert()` compiled out under NDEBUG — guarded by port patches 0014/0015, **0 crashes in 12
trials** against a 1-in-3 baseline. ⚠ `premain-hang` stays **reopened**: the V3D fix does not explain
a launch that printed nothing.
Full text: [`docs/done/2026-09-12-w37-settled-sections.md`](../done/2026-09-12-w37-settled-sections.md).

## 3. ✅ Settled today — the POSIX option macros (your go-ahead)

libphoenix advertised `_POSIX_VERSION 200809L` with **no option macro defined**, so portable
`#if _POSIX_TIMERS > 0 && ...` guards were always false. **Eleven now claimed** (`070ceda`),
contract-tested both ways (`98e7014`, `03cbc97`), **HW 4/4, suite 212/0**, ports rebuilt, showcase
re-gated. Detail: [`docs/done/2026-09-10-w37-detail-snapshot-6.md`](../done/2026-09-10-w37-detail-snapshot-6.md).

## 4. ✅ libc bugs found and fixed this week — all HW-verified, all with tests

- **`strncmp`/`strncasecmp` read one byte past `n`** (libphoenix **`f2d2789`**, tests **`a82e0e4`**
  + **`7c80324`**). The bound was tested *after* `*p`, so a string ending exactly at a page boundary
  faulted — it was killing SuperTuxKart in AngelScript's tokenizer. `strncasecmp` had **no** test
  coverage at all. Negative control: the same pattern faulted on the old code, so the test is not
  vacuous. Manifest `2026-09-10-strncmp-over-read-fix.md`.
- **`getaddrinfo` leaked and lied** (**`75bf050`**, test **`fc3dd0f`**): on a failed `realloc` it
  leaked the old buffer *and* returned 0 with `*res == NULL`. It had zero coverage; the new test is
  DNS-free so it runs anywhere.
- **`_atexit_init` dereferenced NULL** (**`160e916`**): the list head came from a file-scope compound
  literal, leaving no named storage to recover to; it is now a named static. This was the "2070
  exceptions in ntpclient" report — ntpclient was innocent (it is `psh` under an applet name).
  ⚠ The ELF is *correct* (`.data` holds `0x4373e0`), so a runtime NULL meant **`.data` did not reach
  the process intact** — a load-time failure that would hit **any** statically initialised pointer.
  ✅ **2026-09-12: the recovery no longer masks it, and it is not happening now.** The fix recovered
  **silently**, hiding the only evidence the corruption still occurred; `_atexit_init` now reports it
  (libphoenix **`94a2d8e`**). Measured with the reporter live: **~3645 processes across three boots**
  (2500 + 1000 + 85 launches plus boot processes), every child reaching `main()`, **0 reports, 0
  faults**. ⚠ Not proof it is fixed — the check covers **one word**, and kernel `6cd3adec`'s
  premature-EOF diagnostic never fired either — but it bounds the current rate, and a recurrence can
  no longer pass unnoticed. Tracked as `atexit-null-head`.
- ✅ **Regression-checked twice: 541 libc tests, 0 failures**, with zero spurious instrument fires.


## 5. Allocator free-bin corruption — SUPERSEDED by §2; allocator is the victim

Kept only as a pointer: the hardening (validator cannot fault, release never abandons a linked
chunk, coalesce boundary from the trusted heap) is committed and HW-clean, and the free-bin entries
are **stale pointers into `mmap`-recycled regions**. Insertion is excluded (25 abandons, 0 catches)
and the poison bench closed the last internal route (8 trials, 0 fires). The live thread is §2.
Full text: [`docs/done/2026-09-12-w37-settled-sections.md`](../done/2026-09-12-w37-settled-sections.md).

## 5c. ✅ The banked GPU wedge did not reproduce on the shipped build

`q3dm7` is the documented reproducer for the intermittent V3D binner wedge. **3 runs, 27605 frames,
0 wedges, 0 faults** — plus 12 six-app gate runs at 0. Last actually observed **2026-08-22** (2
events); every sampled run since 2026-08-27 is clean. A root cause was found and fixed in between:
`MAP_CONTIGUOUS` returns **non-zeroed** DRAM, so unpopulated binner-overflow-pool slots held cold-boot
garbage that CT1 followed as a wild pointer. ⚠ **Kept open, not closed** — clean runs also predate
that fix, so for an intermittent this bounds the rate rather than proving it gone. The recovery
reports loudly (`RENDER TIMEOUT` / `DROPPED job`), so any recurrence is visible in any gate log.

## 5b. ✅ X11 re-verified on the current build

The desktop had not been exercised since two allocator commits landed, and X is the heaviest AF_UNIX
**and** malloc consumer here — a workload shape the 541-test suite does not cover. One cycle:
**0 exceptions, 0 corruption events, 2430 frames at a steady 10.3 fps**, content-graded **89.5%
non-black / 8679 colours** against the 90.2% / 8856 baseline. No regression.

## 7. ⏸ UPSTREAM: one merge taken, two deferred — kernel conflict is your call

**2026-09-11 sweep:** posixsrv **merged, verified, pushed** (`211d49a`; the one commit was
`2d2a193 tmpfile: forward mtGetAttrAll`, 1 line, clean) — boot-tested together with the kernel fix
above: banner + psh, `test-libc-stdlib` **93/0**, `test-libc-misc` **212/0**, **0 exceptions**.
Manifest `2026-09-11-vmfix-posixsrv-verified.md`. Game forks all **0 behind**; all four generated port
patches **OK**.
⏸ **`phoenix-rtos-project` deferred on purpose:** its only new commit is `29e6c64 update submodules`,
which bumps the pointers for **kernel and tests** — the two repos whose merge is blocked below. Taking
it would make `project` claim a kernel we do not have.

- **2026-09-12 sweep (4th).** Unchanged, nothing merged, no Pi time: kernel **5**, project **1**,
  tests **2** — the AF_UNIX cluster, still your call. Forks measured against real upstream refs
  (three sit on `phoenix-rpi4-port` with no tracking branch, so a bare `@{upstream}` check reports a
  false `behind=0`): quakespasm **0/26 ahead**, yquake2 **0/12**, quake3e **0/9**, vkquake **42
  behind** by your rule. All four generated port patches **OK**.
- **2026-09-12 sweep (3rd).** Unchanged and still deferred to you: kernel **5**, project **1**, tests
  **2** — the AF_UNIX cluster. Forks: quake3e/quakespasm/yquake2 **0 behind**; **vkquake 40 → 42**
  (upstream moved two more, same ray-tracing/SSAO character, staying behind by your rule). All four
  generated port patches **OK**. **Nothing merged, no Pi time.**
- **2026-09-12 sweep (2nd).** Unchanged: kernel **5**, project **1**, tests **2** — all the AF_UNIX
  cluster, deferred to you. Forks: quake3e/quakespasm/yquake2 **0 behind**, vkquake **40** by your
  rule. All four port patches **OK**. **Nothing merged, nothing to verify.**
- **2026-09-12 sweep.** Game forks: **quake3e / quakespasm / yquake2 all 0 behind**; **vkquake 40
  behind, staying behind by your rule**. All four generated port patches **OK, no drift**.
  ★ **New evidence for the AF_UNIX decision: the `tests` merge is COUPLED to the deferred kernel
  rewrite and cannot be taken on its own.** I merged it (conflict was mechanical — both sides append
  test bodies and runner entries) and it **built clean**, then **failed on hardware: 6 EL0 Data
  Aborts and only 10 of the suite's tests ran, against a baseline of 27 tests / 0 faults / `OK`.**
  Those upstream tests target upstream's **rewritten** AF_UNIX (the 5 kernel commits), which we do
  not have. **Reverted** (never pushed), export rebuilt, and re-verified back at **27/27, 0 faults**.
  ⓘ So the AF_UNIX re-port is now a **two-repo** job — kernel + tests together — not kernel alone.
  Kernel stays **5 behind**, project **1 behind** (it only bumps those two pointers).

**2026-09-11:** upstream moved for the first time in days — **kernel 5 commits, tests 2**. Both
merges **conflict** and the kernel one is **not mechanical**, so per your standing rule I **left both
repos untouched** (verified clean: kernel `0c768551`, tests `c91eede`, 0 modified).
**Why:** upstream **deletes `posix/unix.c` outright** — it rebuilds UNIX sockets on a new
endpoint/channel model — while we have local changes to that file (git reports `UD`), plus 8 conflict
hunks in `posix/posix.c`. **AF_UNIX is what X11 runs on**, i.e. the demo path, so this is a **re-port
decision, not a merge** and it needs a session of its own. Nothing is blocked: the delivered image and
every fix are unaffected. Full scope, including which of our AF_UNIX fixes are at stake:
[`docs/misc/2026-09-11-afunix-upstream-report-scope.md`](../misc/2026-09-11-afunix-upstream-report-scope.md).

- **2026-09-10 13:0x — nothing behind.** All 16 Phoenix siblings **0 behind** `origin/master`;
  yquake2 (12 ahead), quakespasm (26), quake3e (9, tracks `origin/**main**` not master) all **0
  behind**; all four generated port patches **OK, no drift**. Nothing merged, nothing to verify.
- **2026-09-10 01:20** — merged 10 upstream commits into the yquake2 fork (the one that mattered:
  `gl3: fix GL3_Draw_TileClear for scrap images`), regenerated its patch, re-verified Quake II
  in-game, pushed.
- **vkQuake: 39 behind, staying behind by your rule.** Measured: **6 of our 10 patched files are also
  changed upstream** (the MBOIT-pipeline and MD5-skinning rewrites moved every anchor we rely on), and
  the 39 commits are ray-tracing, SSAO/XeGTAO, MoltenVK-on-Apple, Ironwail imports and CI — nothing
  we need. That is a re-port, not a merge; it deserves its own session.

## 8. Also settled this week

One line each, with full detail in `docs/done/2026-09-10-w37-detail-snapshot-{3,4,5,6,7}.md`: the
libstdc++/`std::chrono` fix (~30 features off, not one) and its promotion · libphoenix
`clock_getres` · the allocator large-bin abandon fix · the readlink clamp · the nfs-fs
attribute-cache fix · CPython's restored `dir_fd`/`sched_*` · the Window Maker `nice()` collision
that had made the X11 showcase unbuildable, **and the stale docs that would have re-introduced it**
· the yquake2 upstream sync · MicroPython's `ticks_ms`/`ticks_us` were the wall clock, now monotonic
· a sweep of all 16 port shims against libphoenix's 994 symbols (one overlap, removed) · the public
docs re-synced · your four reported bugs, all addressed.
