# Week 2026-W37 (Mon 2026-09-07 → Sun 2026-09-13)

Phoenix-RTOS RPi4 — weekly working log. **This file is the whole picture.**
History lives in `docs/done/` (W36: `docs/done/WEEK-2026-W36.md`), one-off
analyses in `docs/misc/`. Convention: `docs/inprogress/README.md`.

---

## 1. DECISIONS NEEDED FROM WITOLD

| # | Decision | Status |
|---|----------|--------|
| D4 | "genuine tool" boundary | you'll decide later — nothing blocked |
| D2 | final move of **Mesa itself** into `phoenix-rtos-devices` (the glue already moved) | optional, queued |

**Nothing else is waiting on the owner.** All three open questions were answered 2026-09-08.

**Settled, never re-ask:**
- Toolchain is **GCC 16.2.0 + binutils 2.47** (owner-approved 2026-08-28).
- **★ FORK-ONLY, never upstream (owner, 2026-09-08).** "We don't send anything upstream. We get
  changes from upstream to our fork and we maintain our fork. In our fork we fix all the bugs we
  can in the best possible way - to provide good solution for the Pi4 and if possible, not to
  break other architectures." So: do not propose upstreaming anything, and do not treat it as a
  pending decision. Keep the standard it implies — best fix for the Pi 4, root-caused, designed
  so other arches still build (per-arch define + fallback, best-effort `mprotect`).
- **Game forks: if our version works, leave it.** yquake2 (10 behind) and vkquake (30) stay as
  they are — not a decision, settled. Stop re-listing them as deferred each sweep; only revisit
  if one of those games actually breaks.
- **D6** (purge build-output ELFs from coord git history, needs a force-push): **leave as is.**

**★ OWNER GOAL (2026-09-06):** a **stable build with the games and X11 good enough for a
public presentation / a screen recording published online**. Everything below is ranked
against that.

## 1b. 🔄 full-project change study for Phoenix-RTOS maintainers — acting on your 5 remarks

**[`docs/PHOENIX-RTOS-RPI4-CHANGES.md`](../PHOENIX-RTOS-RPI4-CHANGES.md)** — 14-row ★ shortlist of
defects this port found in *upstream's* code, then per-repository sections. Method:
`docs/done/2026-09-08-fork-vs-upstream-study.md`. Your review, 2026-09-08:

| # | your remark | status |
|---|---|---|
| 1 | no mention of HW video decode | ✅ added (`14480b662`) |
| 2 | no mention of the ML CPU-vs-GPU work | ✅ added — framed **parity-first** (end-to-end GPU MLP is 0.90×; the 4.4×/11× microbench ratios rested on an inflated CPU baseline and are recorded as superseded) |
| 3 | no mention of AXI-PMU | ✅ added |
| 4 | study the old archive docs for other uncredited work | ✅ **done** — 35 gaps found, **33 integrated** (13 ★★ + 16 ★ + 4 marginal); doc 885 → **1427 lines**. Two were **corrections to claims the doc made**: the caveats section opened with the X desktop-exit crash as *open* when it is fixed (6/6 crashes before → 0/18 after, and the root cause is an *upstream glamor* bug), and the ffmpeg entry read as if nothing used it. Scope corrected for a 15th patched tree (`external/mesa`). Several sweep claims were **dropped or corrected on verification** — a cited file that no longer exists, a fontconfig figure that disagreed with its own commit, and an inference presented as a measurement. |
| 5 | point to it from `README.md` | ✅ first entry under Documentation (`14480b662`) |

All three new areas live in one new final section, **"Hardware experiments outside the Phoenix
repos"** — they are programs *built on* Phoenix in `tools/`, not changes to it (verified: `git log
--all --grep` for hevc/argon/rpivid/axi across kernel, libphoenix and devices returns nothing), so
the per-repository structure had no place for them. Each carries an explicit limits block.

## 2. STATE / WHAT'S NEXT

**★ The standing goal looks MET.** Stable build; all five games + the X desktop HW-gated with
0 faults and frames visually checked; a flashable image that also passes a contents gate and a
QEMU structural boot check; and one publishable reel with real gameplay motion.

**⏸ OWNER ACTION — ★ FLASH THIS:** `artifacts/rpi4b/rpi4b-sd-2part.img`
SHA256 `ce9ae9f0764b4879f4cff00612ae83df3418911587d427381660f035f96a741c`
manifest `manifests/2026-09-08-reel-capable-demo-image.md`.

```
udisksctl unmount -b /dev/sda1 2>/dev/null || true
sudo dd if=artifacts/rpi4b/rpi4b-sd-2part.img of=/dev/sda bs=4M conv=fsync status=progress
sync
```

(`/dev/sda` is always the SD card on this host. Do **not** flash `rpi4b-sd.img` — FAT-boot-only.)
It carries the STK clock fix, the vkQuake demo hook and `startx_gpu browse`, so it can reproduce
every reel segment. Fully gated on its own binaries: contents PASS · QEMU structural boot PASS ·
six HW gates 0 faults with frames confirmed.
⚠️ **SD boot itself is still unverified** — no card in the host reader; all HW evidence is netboot
on the image's own binaries. The image also now lags core by one STM32-only plo commit
(irrelevant to Pi4).

**✅ STABILITY, quantified from today's logs (no extra Pi cycles):** **132 boots, 132 reached the
`(psh)%` prompt — 100.0%.** 17 of 132 logs contain a fault pattern and **all 17 are attributed,
0 unexplained**: 11 are SuperTuxKart's known `--profile-time` per-kart-report crash (benchmark
path only, after the race; `--race-now` is clean) and 6 are your bug #4 being deliberately
reproduced *before* it was fixed. **Bug #4's fix quantified: 6/6 X sessions crashed before it,
0/18 after.** Caveat: all 132 are **netboot** — SD boot remains unverified.
`docs/misc/2026-09-08-boot-stability-tally.md`.

**✅ SUSTAINED-LOAD THERMALS — no throttling.** The stability numbers were all *short* runs, so
a long presentation was untested. New `bin/thermal-soak` (forks, execs the game, polls
`/dev/thermal` + `/dev/throttled`) under QuakeSpasm for **6.3 min**: 35.0 °C → plateau **53–55 °C**
(flat from t=240 s), **`throttle=0x00000000` on all 19 samples** — no under-voltage, no ARM
capping, no sticky bits. 0 faults. ~5 °C of headroom to the Pi 4's first soft cap (60 °C), ~25 °C
to real throttling. Limits: 6.3 min not 30, one board, open-air bench.
`docs/misc/2026-09-08-thermal-soak.md`.

🔄 **SHOWCASE v5 IN PROGRESS — acting on your review (2026-09-08).** Your points and status:

| your point | status |
|---|---|
| X11 segment totally static | ✅ **fixed** — new `startx_gpu action`: WM + **GL-accelerated window** (rotating, via v3d-server) + xterm running a **Python/curses Game of Life** + xbill + xclock. 6/6 distinct frames 1 s apart, 0 faults. |
| STK shows a frozen frame | ✅ **root-caused: my error, not a grabber/game bug.** The reel used a clip recorded at **10:32**, before the clock fix landed at **12:12** — i.e. 1 fps footage. Re-recorded post-fix, 6/6 distinct frames. |
| on-screen FPS figures | ✅ **DONE — all four Quakes + STK**, read off *unfiltered native-res* frames: **QuakeSpasm 35 FPS · vkQuake 73 FPS · Quake II 27.75 fps · Quake III 43–46 fps · STK 5/6/6**. The cvar alone was not enough — `scr_showfps` draws at `glwidth/vid.conwidth` = 1.0 at 1920 wide, i.e. **8-pixel text**; `scr_conscale 4` in `id1/autoexec.cfg` (shared by both Quake engines) gives ~32 px. STK needed the `<Video>` group attribute + `ports 31e6097` (`NO_TRUST_COUNT` 200→20). |
| Quake III no demo playing | ✅ **fixed — and the cause was a PHOENIX bug, not the game.** Q3's shipped demos really are the unloadable 1999 `.dm3` protocol, so the segment is now a 5-bot deathmatch with an **orbiting third-person camera** — live gameplay, **43–46 fps** on screen, scoreboard showing the bots fragging each other. Two cycles were lost first to **psh silently truncating the launch line at 128 chars** (see §2d): `+devmap q3dm1` was in the discarded tail, so the engine sat in its menu / stuck at "awaiting snapshot" and it read like a game bug. |
| boot sequence + shell action (bash, fs, nano/mc) | ⏳ not yet captured |
| Python doing something visible | ✅ `life.py` written + staged to `/usr/share/demo/life.py` |
| HW-accelerated video playback | 🔄 **your three questions answered, code written, not yet run on HW** — see §2e |
| Dillo segment | ✅ keeping it — you liked it |

**Captured so far for v5:** `x-action2`, `stk-fps3`, `qs-fps2` (35 FPS), `vkq-fps` (73 FPS),
`q2-fps` (27.75 fps). **Still to capture:** Q3 (running), boot+shell, video playback. Then
reassemble in your order: boot → shell → X11+GL → video → games.
⚠️ Cut the X segment from the **first ~90 s**: `life.py` freezes after ~80 s while the rest of the
desktop stays live (GoL canvas 0.09 % changed t=160→235 s vs GL window 49.9 %). Unresolved, no
fault in the log — but a frozen Game of Life is exactly the "totally static" complaint again.
Also: `+playdemo demo1` plays **one** demo then drops to the console, so each Quake take has a
~50 s usable window; use `+startdemos demo1 demo2 demo3` for a longer one.

## 2b. 🐞 NEW BUG YOU FOUND (#5) — glamor paints screen content at its exact vertical MIRROR

Both artefacts you spotted in `x-action2` are **one bug**, and it is measured, not eyeballed
(`docs/misc/2026-09-08-glamor-screen-mirror-and-gl-window-rate.md`):

| you saw | measured |
|---|---|
| white blocks in the GoL xterm | rows 192–232 are a **pixel-exact vflip of rows 861–901** (the `top` window's text) — mean&#124;diff&#124; **4.62** vs 20.97 for the next-best offset |
| mirrored clippy icon bottom-left | rows 1008–1076 are a vflip of rows 6–74 (the real Clip) — **5.03** vs 16.94 |

`dst_y + src_y ≈ 1080` in both, so the mirror axis is the **full screen height** — which rules out
any window-local coordinate (one instance pulls pixels out of a *different window*, the other
reaches across 1000 px of desktop) and puts the faulty coordinate in the **screen pixmap's texture
space**. The clip mirror is painted once at Window Maker startup and never repaired; the xterm one
is transient (GoL bright-pixel share 26.0 % at t=112 vs 4–8 % either side).

**First suspect tried and REFUTED — by its own built-in diagnostic.** The fork's screen-pixmap
Y-flip was hand-rolled into `glamor_transfer.c` only, and `glamor_spans.c:234`/`:345` index the FBO
row straight from the X row. That patch shipped with a one-shot `ErrorF` per direction so it could
fail loudly, and it did: **0 hits** in a cycle whose daemon binary provably contained both markers.
Neither span path runs on the screen pixmap, so the flip could not have moved a pixel. **Reverted.**
(The diagnostic channel was validated *before* trusting its silence.)

**Where it actually is:** `glamor_copy.c`. The geometry said so all along — both offsets are
copy-shaped (13 px = one xterm text row = a one-line scroll; 2 px = a frame inset), and
`glamor_copy_fbo_fbo_draw`'s shader samples texel row = pixmap row with **no** screen-pixmap flip
while the destination gets its flip from the rasterizer. One open objection: frequency — a
systematically broken CopyArea should corrupt *every* scroll, and it doesn't, so something gates
which copies take that route. Next step is to instrument its four routes the same way, **not** to
patch first.

## 2b-bis. ⚠️ NEW HAZARD: the X server cannot be relinked onto current Mesa

Fell out of the same cycle and matters more than the mirror.

| daemon | Mesa in the binary | result |
|---|---|---|
| the one that has been shipping (built 02:12) | `git-e4be116324` | works, 0 faults |
| relinked from the current tree (21:38) | `git-aa916f2f06` | **SIGILL — X server dies** |

`aa916f2f060` is **our own** `u_vbuf: do not silently drop draws on the index-unrolling path` — the
fix for your bug #3 (Quake III glitches). The *games* link Mesa in-process and have it; the X server
was simply never relinked after it landed, so glamor has been running on the previous Mesa all
along. Failure shape: two `v3d-winsys` MMU violations → `signal 4` → `server exited (status=0x300)`.

**✅ RESTORED AND RE-VERIFIED — 0 faults, six clients up, desktop identical to before.**
`build-xfbdev.sh` writes `Xphoenix-glamor` and staging renames it, so the known-good binary was
never overwritten; put back into both `_fs` and the live export (sha `8e003ab45ef41009…`,
`git-e4be116324`) and confirmed on hardware. **X-desktop gate intact.**

⚠️ **Budget for this:** the next change to the glamor X server needs a relink, and a relink now
needs this fixed first — either find what in `aa916f2f060` upsets glamor's draw path (the same
index-unrolling code its copy and composite paths lean on) or pin the X server's Mesa link.

**★ Why this matters more than the cosmetics:** it is the *same root-cause family* as your bug #2
(the Quake II underwater Y-mirror), which currently ships a documented stopgap. Real fix for both:
declare the scanout FBO `FlipY` and delete the `st_atom_framebuffer.c:137` size gate, which removes
**all three** hand-rolled flips. That is owner-gated (invalidates the 6-app gate).

## 2e. ✅ VIDEO PLAYBACK — your three questions

**(a) Is there a windowed playback version?** Yes, but it is **software** decode: `startx_gpu
wmmedia` runs `e4-x11-play` (ffmpeg **CPU** H.264) in a Window Maker window. The **hardware**
decoder (`hevc-play`, rpivid H.265) was full-screen only. Which leads to —

**(c) Video overlapping the fbcon terminal.** ✅ **Built** (`0ad4f10d6`):
`hevc-play --window 960x540+480+270 <clip>` scales the decode into that rectangle with a 2 px white
border and writes **only inside it**, so the terminal stays visible all around. `WxH` alone centres
it; omitting `--window` keeps the old full-screen blit; a geometry running off the edge is clamped,
not refused. Nearest-neighbour with a 16.16 fixed-point step (one multiply-shift per pixel) — this
is a CPU loop per frame and the point is to show the decode is ours, not to resample well.
⏳ Not yet run on hardware.

**(b) Send an iPhone clip.** ✅ **Yes — please do.** One command turns it into something the
hardware decodes: `./scripts/../tools/hevc-decode/transcode-for-phoenix.sh <clip>`
(`60e6eb5bc`), verified end-to-end on a real 1080p30 mp4. It does **not** stream-copy the iPhone's
own HEVC even though that would be the obvious move: our decoder covers the tools x265 enables by
default and rejects or mismatches on the rest (tiles, non-zero deblock offsets, AMP,
emulation-prevention bytes in headers), Apple may use any of them, and there is no way to tell from
outside — so it re-encodes through the exact parameter set the conformance harness exercises.
Rotation (phones store landscape + a matrix), frame rate (240 fps slow-mo is decimated — `hevc-play`
has no clock) and chroma/bit-depth are handled; **HDR is not**, so record with HDR off if you can.

## 2d. 🐞 ROOT-CAUSED + FIXED: psh silently truncated any command past 128 chars

Cost two Pi cycles on a Quake III "bug". `pshapp` had `#define CMDSZ 128` and dropped every
character past it with **no bell, no message and no refusal**, then executed the prefix. The UART
echo of the line looks complete, so the run reads as evidence about the *program*. A 167-char
launch line lost `+devmap q3dm1`, and Quake III sat in its menu.

**Fixed properly, both halves** (`phoenix-rtos-utils 02d733d`): `CMDSZ` is 1024 on 64-bit MMU
targets (aarch64/x86_64/riscv64) and the historical 128 elsewhere — it backs one malloc plus a
same-size static clipboard, and the NOMMU/MCU targets shouldn't pay ~2 KiB for a Pi 4 problem
(overridable with `-DCMDSZ`); and hitting the limit now **rings the bell** so the refusal is
perceptible at any size. The harness warns against the old limit too (`0c89ccf88`), since an
older psh binary can still be on the target.
⏳ Needs a **`--scope core`** rebuild to reach the Pi. Not blocking anything — the Q3 cvars moved
into `demoq3/autoexec.cfg`, so its launch line is 21 chars.

## 2c. ✅ ANSWERED: the GL window really is slow — ~1 fps, not the capture

You asked whether the slow-updating GL window was the grabber or the stack. **It is the stack.**
120 consecutive captured frames (4.00 s at 30 fps), GL-window crop: **4 changed pairs → ~1.0 update/s**,
one every ~0.73 s. Not a grabber limit — the same frames show `top` and `xbill` changing far more
often, and the same card recorded 35 / 73 fps game counters that evening. So **windowed GL through
`/dev/v3d-srv` + glamor + X ≈ 1 fps vs 35 fps for full-screen in-process** (~30×). From the same
frame's `top`: `/sbin/rpi4-v3d` **76.9 % CPU**, CPU0 **100 %**, `Xphoenix-glamor-daemon` 17.9 % — the
windowed path is **CPU-bound in the V3D daemon**, not GPU-bound, consistent with a per-frame CPU
readback instead of a page flip. Not yet root-caused; next session starts from a number.

**★ SHOWCASE REEL — ready to publish, not published.** `artifacts/hdmi-video/20260908-162023-phoenix-rtos-rpi4-showcase.mp4`
(140 s, 7 segments): QuakeSpasm `demo1` · Quake II `q2demo1` · vkQuake `demo2` · Quake III bot
deathmatch · SuperTuxKart AI race · Dillo web browser · X desktop. Rebuild:
`./scripts/make-demo-reel.sh`. Four segments are real gameplay motion; browser and desktop are
static by nature.

**Next, in priority order:**
1. **SD-boot verification** — ⏸ blocked on the card reader (shares the USB-C port with power/eth).
2. **Needs you present** (both invalidate the 6-app gate): the libstdc++ `std::chrono` rebuild
   (§4) and bug #2's real `FlipY` fix (§3).
3. **Unattended-safe, still open:** #67 vkQuake torches (16/16 boots clean, 95% UB 17%, does not
   reproduce); q3dm7 GPU wedge banked — **use `q3dm1` for demo takes**.
4. Delete the two `*-nonfork-obsolete` GitHub repos (my token lacks `delete_repo`).

⚠️ **Harness note:** cutting a `--variant sd` image replaces the TFTP `loader.disk`, so netboot
refuses until restored with
`./scripts/rebuild-rpi4b-fast.sh --scope project --variant nfsroot --skip-prepare`. It also
relinks the games, which expires their HW gate — budget 6 cycles after any cut.

## 3. THE FOUR BUGS YOU REPORTED — status

| # | bug | status |
|---|-----|--------|
| #4 | Data Abort on exiting Window Maker | ✅ **FIXED** — kernel half + the glamor `DestroyPixmap` chain fix (§4e). Soak-tested. |
| #3 | Quake III glitches early in gameplay | ✅ fixed — Mesa `u_vbuf` dropped-draw fix, in the shipped image |
| #2 | Quake II underwater view Y-mirrored | ✅ fixed (`83235581` + ports `a196466`). ⚠️ documented **stopgap**: a conditional correction on a conditional bug — over-corrects at `viewsize <= 71`. Real fix = declare the scanout FBO `FlipY` and delete the `st_atom_framebuffer.c:137` size gate, which also removes glamor's three hand-rolled flips. Queued: needs one build + a re-verification pass over 5 games + the desktop. |
| #1 | GPU X11 xterm resize artefacts | **not reproduced** by a sound measurement. Resize *correctness* verified (server and client agree once settled). Most likely what you saw is intermittent frames during a fast drag — this stack has no compositing and presents by GPU readback. Worth confirming whether the artefacts persisted *after* you let go of the mouse; if so it needs a different hypothesis. |

Full detail for all four: `docs/done/2026-09-08-w37-archived-detail.md`.

## 4. WHAT GOT DONE THIS WEEK (short)

**★★ STK was capped at exactly 1 fps by a broken C++ clock — fixed, now 5.84 fps (5.8×).**
libstdc++ for aarch64-phoenix is built with **none** of its time backends, so
`std::chrono::steady_clock` falls back to `std::time()` and ticks in whole **seconds**; STK's frame
loop sat in `while (dt == 0) { StkTime::sleep(1); … }` waiting for it. Patch reads
`CLOCK_MONOTONIC` (`ports 6f08c26`). STK is now GPU-bound (~171 ms/frame, ~88% CL submits).
Found by *precision, not magnitude* — 1000.0 ms/frame within 0.08% across two tracks and settings
is a clock, not a workload. Detail: `docs/misc/2026-09-08-stk-frame-budget.md`.

**⚠️ YOUR CALL — the general fix.** That is a **toolchain** defect: any C++ code here timing with
`std::chrono` gets 1-second granularity, and `sleep_for`/`yield` are degraded too. Elsewhere it
fails *silently*. Proper fix = rebuild libstdc++ with `--enable-libstdcxx-time=rt`; note
`steady_clock::now()` lives in `libstdc++.a`, so a header define changes nothing. Whole-system C++
rebuild, so **not** started unattended.

**Your four bugs:** #4, #3, #2 fixed (#2 with a documented stopgap), #1 not reproduced — see §3.

**Also fixed / added:** trusted root CAs (121, real HTTPS) · libphoenix stdio partial-write +
regression test · V3D per-session BO leak (15.7 MB → 2.3 MB; leak proven **bounded**: per-session
cost decays +60 MB → +2.2 → +1.5 over three X lifecycles) · `stk-launcher` now honours caller
options · vkQuake demo playback via `id1/phoenix-demo.cfg` (`ports d7de9aa`) · `startx_gpu browse`
= Window Maker + Dillo · ext2 volume sized from content · `scripts/make-demo-reel.sh`,
`qemu-boot-sdimage.sh`, `record-hdmi.sh`, `tools/v3dmemprobe`.

**Measurement traps found the hard way** (all in `docs/misc/`): `fault_pattern_matches: 0` is
vacuously true when the program never ran · an ffmpeg `blackframe` check passes on a text console ·
mpdecimate measures scene change, not frame rate · an in-boot A/B needs the arms **swapped** ·
RAM-staging STK's assets is a measured **loss** (73 s of copying to save 2.7 s).

**QEMU ceiling:** plo-only. The kernel never starts — plo needs a firmware DTB in x0 and QEMU
supplies none, so it faults in early hal init. `-dtb` does not help.
`docs/misc/2026-09-08-qemu-boot-ceiling.md`.

## 4e. STK time-to-race — one line for a demo take

Best-looking: keep the deferred pipeline, accept a one-time ~50 s before the first lap after a
boot (cut in editing; steady-state fps is the same or better). Quick take: add
`--disable-dynamic-lights --shadows=0` → same lap in 22 s on any boot. The ~27 s first-run cost is
**not** shader compilation, asset reads, or steady-state rendering — all measured and excluded;
leading hypothesis is the kernel's contiguous allocator. RAM-staging STK's assets is a measured
**loss** (73 s of copying to save 2.7 s; keep using `ram-stage-play` for Quake).
Full workings: `docs/misc/2026-09-08-stk-time-to-race.md`.

## 4f. Upstream sync

**8th sweep 2026-09-08 — 1 commit in, verified, pushed.** plo `953bba7` (upstream: include
`board_config.h` in stm32 `peripherals.h`; touches only `hal/armv7m/stm32/*` + `hal/armv8m/stm32/*`,
no aarch64). Merged clean, **0 conflicts** across all 16 siblings. plo is a core repo so the rule
was followed regardless of it looking STM32-only: `--scope core` rebuild (plo.elf confirmed
rebuilt by mtime) → Pi boot test **0 faults**, psh + lwip up, `test-libc-exit` **31 Tests 0
Failures OK** → pushed. All four game-port patches `--check` **OK**. Now **0 behind everywhere**.
7th sweep: libphoenix `7218adc` (`execve("")`→ENOENT), same treatment. Earlier:
`docs/done/2026-09-08-w37-archived-detail.md`.

## 5. BLOCKED / WAITING

- **SD host card-reader** shares the USB-C port with power/eth, so it is detached. I'll ask when I need it.
- **bash `-i`** interactive — needs a live terminal (psh automation sees EOF).
- **ffmpeg audio/player** — needs the attended `/dev/audio0` sign-off.
