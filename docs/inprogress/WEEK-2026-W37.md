# Week 2026-W37 (Mon 2026-09-07 → Sun 2026-09-13)

Phoenix-RTOS RPi4 — weekly working log. Kept **short on purpose**: this is the file you read.
Detail: `docs/done/2026-09-10-w37-detail-snapshot-{3,4,5,6,7}.md` and
`docs/done/2026-09-10-w37-allocator-investigation.md`. Analyses: `docs/misc/`. Analyses: `docs/misc/`.
W36: `docs/done/WEEK-2026-W36.md`.

**★ OWNER GOAL (2026-09-06):** a stable build with the games and X11 good enough for a public
presentation / a screen recording published online. Everything below is ranked against that.

---

## 1. DECISIONS NEEDED FROM WITOLD — none open

Optional only: **D4** (the "genuine tool" boundary) and **D2** (moving Mesa into
`phoenix-rtos-devices`; the Mesa fork has only upstream `origin`, so its local commits are unpushed
— resolving that is part of D2). Nothing is blocked on either.

**Decided, do not re-raise:** D7 buffer sharing · Quake II underwater stopgap **leave as is** ·
`std::chrono` **fixed + promoted** · the reel is **not** being re-cut · toolchain is **GCC 16.2.0 +
binutils 2.47** · **fork-only, never upstream** · game forks: if ours works, leave it · **D6** leave
as is.

## 2. ⏸ YOUR ONE ACTION — flash the REFRESHED gated image

`artifacts/rpi4b/rpi4b-sd-2part-gated-1f493117.img` · SHA256
`1f493117cfc9de4ca28ac9a5e266a8075d137fe355f7599885f9785d42d388aa` · manifest
`manifests/2026-09-11-demo-image-1f493117.md`

```
udisksctl unmount -b /dev/sda1 2>/dev/null || true
sudo dd if=artifacts/rpi4b/rpi4b-sd-2part-gated-1f493117.img of=/dev/sda bs=4M conv=fsync status=progress && sync
```

**Why this supersedes `2940cc70`:** it carries the **V3D clock-race fix**. That race intermittently
left the GPU clock off, and the next V3D MMIO read then **never completes** — a silent, unkillable
hang at GPU-app startup. On a live demo that is a game that simply never appears. `2940cc70` predates
the fix.

**Gates — three, all passing.** Contents + FAT **"safe to flash"** · structural **OK** · and
**provenance read from this image's own ext2 root**: all five games carry **both** V3D fixes
(`refusing to read V3D MMIO`, `TRUE reset FAILED`) and the diagnostic startup trace is **absent** from
every binary including `psh`.

**Evidence behind it:** six-app showcase gate **6/6, 0 faults**, pixel-checked (not just clean logs) ·
**85/85** quakespasm launches with `clk=on`, 0 retries, 0 faults · Quake II **10588 frames @ 38.8 fps**
· and the cold-state probe's bogus `MBOX_FAIL` readings went **~9% → 0%**, which is direct measured
evidence the mailbox race is real and now serialized away.

★ **Before presenting or recording: run each GPU app once to warm up** (a cold first run costs ~35 s),
and don't rebuild GPU archives right beforehand.
↩ Fallbacks, newest first: `-gated-2940cc70.img`, `-gated-aa176f38.img`, `-gated-80a1cb88.img`,
`-gated-1afa724b.img`, `rpi4b-sd-2part.KNOWN-GOOD-e0768b87.img`.
ⓘ Don't flash `rpi4b-sd.img` (FAT-boot-only) or the rolling `rpi4b-sd-2part.img`.
⚠ **You are the first to boot this from a card** — the host SD reader is absent (it shares the USB-C
port with power/eth), so SD boot cannot be exercised here. The contents gate compensates.
⚠ The TFTP `loader.disk` is now the **SD** blob, so netboot testing needs an `--variant nfsroot`
rebuild first.


## 2b. ✅ DEMO REPEATABILITY ON THE DELIVERED CODE — 3 full gates, 18 cycles

**17 of 18 app cycles clean**, all six apps rendering every time (content-graded from HDMI, not
logs). The one blemish was **not the app**: `/bin/ntpclient` entered a NULL-write crash loop and
flooded the UART with **2070 Data Aborts**, which cost that Quake II cycle its flipstat output while
it was still on screen. New `ntpclient-null-loop` row in `docs/KNOWN-ISSUES.md`; ~1 in 60 boots, no
rendering impact observed, not root-caused.
★ Worth the gates: they caught a defect in a *background* process that every app-focused test had
missed — and the lesson is to attribute an exception storm via the dump's process name before
blaming whatever was in the foreground.

## 2b2. ✅ Full 6-app gate detail — 6/6, zero faults

The gap this closes: every check since the STK guards landed had been piecemeal (STK benches, X11
alone, the libc suite). This is the **complete showcase sequence the owner would drive**, in one
pass, on the same ports SHA the image carries (`13d4a89`).

**6/6 `rc=0`, prompt reached, launched — and 0 exceptions AND 0 corruption events on every app**
(post-banner counted, so no stale-buffer artifact). Content-graded from the HDMI frames:
X desktop 89.4% non-black · QuakeSpasm 36.4% · Quake III 29.2% (11537 colours) ·
Quake II **10591 frames @ 38.8 fps** (its 14.8% grade is the small viewport, 6178 colours) ·
vkQuake 91.4% · SuperTuxKart **99.9%, 47152 colours**.

### 2c. The intermittent app-start stall — ROOT-CAUSED AND FIXED (it was never a libc bug)

How the hunt got here, in four lines: a **launch** is a far cheaper unit of exposure than a **boot**,
so `/bin/spawn-storm` (tests **`8d00cda`**) storms one program per boot. ~4200 launches of
`bash`/`python3`/`cxxprobe` (C and C++, sequential and 4-way concurrent) stayed **clean**, which said
the fault is **binary-specific**; storming the real `/usr/bin/quakespasm -loadbench` then reproduced
it, and the committed startup trace — deployed in a port binary for the first time — named the phase.
ⓘ Byproducts kept: `vfork`+`execv` is solid over ~4200 cycles (process spawn is not a demo risk), and
**0 `object EOF at`** across all of them, so kernel `6cd3adec`'s premature-EOF path does not fire even
under heavy demand-paging load. Detail:
[`docs/misc/2026-09-11-premain-hang-hunt.md`](../misc/2026-09-11-premain-hang-hunt.md).


### ★★★ ROOT-CAUSED — it was never a libc hang. It is a **V3D clock race**, and it is FIXED

Caught a stalled launch **with the startup trace live**, and the trace settles it: **all 8 libc
initialisers completed — including `file`, the `isatty`/`tcgetattr` suspect — and `main() entered`
printed.** The process then loaded the game and died in **V3D bring-up**, last line
`v3d-coldstate: … meas=0 Hz … clkstate=0x0` — the clock **off**. (A stall, not a cutoff: 23 completed
launches at ≤3095 ms = ≤71 s, yet the command was alive at the harness's 300 s cap ⇒ **≥229 s hung**.)

**Mechanism.** The next statement reads **V3D core MMIO**. A read of an *unclocked* block never
completes, and SError is masked here (**TD-10**), so there is no abort to take — the process hangs
forever, silently. The clock was off because `v3d_phoenix_powerOn()` deliberately toggles it OFF/ON
around the reset deassert via the **direct mailbox FIFO with the result discarded** (`(void)` cast).
That FIFO has **no hardware arbitration**, so a concurrent client (vcmbox server, thermal, usb,
genet) can pop our response — the established rule here is to use the serialized `/dev/vcmbox`, and
this path was still violating it. The **caller discarded the return too**, so a failed power-on walked
straight into the hanging read.
**The evidence that picks out the clock:** of 7 anomalous cold-state readings in that run, six were
`0xffffffff` = `MBOX_FAIL` (the *query* failed) and **none stalled**; the one reading `clkstate=0x0`
(the query *succeeded* and said "off") is the one that hung.

**Fix** (`phoenix-rtos-devices` **`9f9ec65`**): enable the clock through `/dev/vcmbox` and **read the
state back** with bounded retry; **check `v3d_phoenix_powerOn()`'s return** and bail `-ENODEV` rather
than touching MMIO; report the retry count. The second half turns *any* future power-on failure into
a clean error instead of an unkillable hang, whatever the cause.

| | launches | stalls | `clk=on` | retries needed | racy direct-FIFO GET failures |
|---|---|---|---|---|---|
| before | ~220 | **1** | — | — | 7 in 24 |
| after | **170** | **0** | 170/170 | **0** | 8 in 85 (**~9%**) |

⚠ **Well-supported, not yet proven:** 170 clean launches is only ~0.8 expected events at the old rate.
What strengthens it — the *diagnostic* queries still use the racy FIFO and **still fail ~9% in the
very same runs**, so contention is demonstrably live, yet the vcmbox-routed enable has never needed a
retry. Contention real + serialized path unaffected is exactly what the fix predicts.
✅ **Propagated to ALL FIVE games** (quakespasm · yquake2 · quake3e · vkquake · supertuxkart), each
verified by content — a GPU-archive change does **not** invalidate port state, so every port had to be
forced. ✅ **Regression-checked on a second game that actually renders:** Quake II ran **4743 frames at
38.8 fps, `clk=on`, 0 refusals, 0 retries, 0 faults** — the fix does not break GPU bring-up.
✅ **Also fixed on the RUNTIME path** (`phoenix-rtos-devices`): both in-job reset call sites
discarded `v3d_phoenix_reset()`'s result and then wrote V3D MMIO via `apply_core_regs()` — the same
forever-hang, but striking **mid-render** instead of at startup. Dormant in normal use (it only runs
on a binner wedge), so low risk, but it could not have been worse than hanging.
✅ **Rebuilt CLEAN** (no trace) and re-verified by content: **trace gone from every binary** (games +
psh) and the V3D fix present in **all five games**. ⚠ Turning a `-D` knob *off* does not invalidate
objects any more than turning it on does — a dropped flag would have relinked the traced `init.o` and
exited 0, i.e. **silently shipped a diagnostic build**. `rebuild-rpi4b-fast.sh` now stamps the knob's
state and forces the rebuild on **any** transition.
✅ **Six-app gate PASSED on the clean build: 6/6, rc=0, prompt reached, 0 faults each**, every app
reporting `clk=on`, 0 "refusing" bails. Pixel check (`check-hdmi-content.py`, not just the log):
**stk 100% nonblack / 46152 colours · x 89.6% · vkq 91.4% · q3 42.1% · qspasm 30.5% — CONTENT**;
`q2` grades SUSPECT at 14.6%/5983/14.7, but that is **its known signature** — three earlier *passing*
q2 runs scored 14.5%/5964/14.9 — and its UART shows **38.81 fps, 0 faults**. Not a regression.
✅ **SHIPPED as `rpi4b-sd-2part-gated-1f493117.img`** (§2). Re-validated with the three follow-up
commits in: **85/85** quakespasm launches `clk=on`, 0 retries, 0 faults · Quake II **10588 frames @
38.8 fps** · and the cold-state probe's bogus `MBOX_FAIL` readings **~9% → 0%** — measured proof the
mailbox race was real and the serialized path removes it.
⚠ One scare worth recording: a `/usr/bin/quake2` cycle produced **no output at all** — the exact
"looks like the hang" signature. It was a **too-short window**; the same command with `--idle-secs
240` rendered 10588 frames. The harness had already said so ("Re-run with a longer --idle-secs before
bisecting").
Detail: [`docs/misc/2026-09-11-v3d-clock-race-mmio-hang.md`](../misc/2026-09-11-v3d-clock-race-mmio-hang.md).

Detail, traps and the unapplied candidate fix:
[`docs/misc/2026-09-11-premain-hang-hunt.md`](../misc/2026-09-11-premain-hang-hunt.md).

## 2d. ★ DEMO REPEATABILITY, and SuperTuxKart's crash is FIXED

**3 consecutive 6-app gates, 18 cycles: 17 clean.** Zero allocator corruption on every app, and the
armed instruments produced no output at all, so nothing was contained-but-hidden. No launch hit the
pre-`main` hang.

✅ **The one failure was SuperTuxKart's own AI, and it is now fixed.** Root cause in STK's code:
`DriveGraph::getNode()` bounds-checks only with an `assert()` **compiled out under `NDEBUG`**, so a
`-1` sector indexes ~32 GiB past `m_all_nodes` and `dynamic_cast` dereferences the garbage vptr.
**Four routes** reached it (two unsigned-conversion boundaries, two direct array indexes with
`m_track_node`); all four guarded by port patches **0014 + 0015** (phoenix-rtos-ports `13d4a89`).

**Verified: 0 crashes in 12 trials at `--numkarts=4`** against a **1-in-3** baseline (~0.8% by luck),
corroborated beforehand by `--numkarts=1` (which removes the AI) giving 0 in 7.
★ **So your kart-count trade-off is gone** — four karts *and* stability, and `--no-sound` is moot for
this crash. STK is GPL-3, so the fix lives in the port patches, never a core repo.
Full analysis: [`docs/misc/2026-09-11-stk-ai-pathfinding-crash-rootcause.md`](../misc/2026-09-11-stk-ai-pathfinding-crash-rootcause.md).

★ **Before presenting or recording: run each GPU app once to warm up** — a cold first run costs ~35 s
(55.5 s vs 20.0 s per profile lap, measured by the sync script), and don't rebuild GPU archives
right beforehand.

## 3. ✅ Settled today — the POSIX option macros (your go-ahead)

libphoenix advertised `_POSIX_VERSION 200809L` with **no option macro defined**, so portable
`#if _POSIX_TIMERS > 0 && ...` guards were always false. **Eleven now claimed** (`070ceda`),
contract-tested both ways (`98e7014`, `03cbc97`), **HW 4/4, suite 212/0**, ports rebuilt, showcase
re-gated. Detail: [`docs/done/2026-09-10-w37-detail-snapshot-6.md`](../done/2026-09-10-w37-detail-snapshot-6.md).

## 4. ✅ libc bugs found and fixed this week — all HW-verified, all with tests

- **`strncmp`/`strncasecmp` read one byte past `n`** (libphoenix **`f2d2789`**, tests **`a82e0e4`**
  + **`7c80324`**). The bound was tested *after* `*p`, so a string ending exactly at a page boundary
  faulted — it was killing SuperTuxKart in AngelScript's tokenizer. `strncasecmp` had **no** test
  coverage at all. Negative control: the same pattern faulted on the old code, so the test is not
  vacuous. Manifest `2026-09-10-strncmp-over-read-fix.md`.
- **`getaddrinfo` leaked and lied** (**`75bf050`**, test **`fc3dd0f`**): on a failed `realloc` it
  leaked the old buffer *and* returned 0 with `*res == NULL`. It had zero coverage; the new test is
  DNS-free so it runs anywhere.
- **`_atexit_init` dereferenced NULL** (**`160e916`**): the list head came from a file-scope compound
  literal, leaving no named storage to recover to; it is now a named static. This was the "2070
  exceptions in ntpclient" report — ntpclient was innocent (it is `psh` under an applet name).
  ⚠ **Still open, and worth more than the crash it caused:** the ELF is *correct* (`.data` holds
  `0x4373e0`), so a runtime NULL means **`.data` did not reach the process intact** — a load-time
  failure that would hit **any** statically initialised pointer. Kernel **`6cd3adec`** closed the
  known *mechanism* (a premature EOF silently zero-filling a demand-paged page) but the first cause
  is open. Tracked as `atexit-null-head`.
- ✅ **Regression-checked twice: 541 libc tests, 0 failures**, with zero spurious instrument fires.


## 5. Allocator bin corruption — CONTAINED, first cause open, hunting stopped

A free bin ends up holding a pointer to memory that is not what the bin thinks it is. Proven by
in-allocator verdicts rather than inference (`hbase?=1` on 24/25, 34/34, 19/19 and 3/3 events).

✅ **Contained on both hand-out paths** (`0eab703`, `9aaacdf`): a **22-event run finishes with 0
exceptions and 1301 frames**, where this used to end in a Data Abort inside `malloc_chunkSize()`.
That is the part that matters for a demo — the defect no longer ends runs.
✅ **Regression-tested twice**: 541 libc tests, 0 failures, and **zero spurious instrument fires**.

⏹ **Hunting stopped.** Recent guarded builds: 1 event-run in 22 trials, down from ~1 in 3 — but I am
**not** claiming the rate dropped, because two confounds are uncontrolled (the STK guards changed how
runs end; shader-cache state varies). Twice today an apparent improvement turned out to be test
conditions. At ~1 in 22 the marginal value per Pi-hour is low and the instruments are permanent, so
the next occurrence in **any** app reports everything without another bench.

⚠ **Eleven candidates eliminated, five of them my own hypotheses** — listed in the `STK-crash` row of
`docs/KNOWN-ISSUES.md` so they are not re-walked. Three claims I made and retracted the same day: an
audio write-after-free, a shader-compilation trigger, and `rm -rf` of the cache dir disabling
caching.
⏭ Armed for the next occurrence: `hbase?=` · `freed?=` · `lheap?=` (+ `lovfl` capacity flag, after I
sized the first ring too small to discriminate) · a bounded large-bin tree audit.
Evidence trail: [`docs/misc/2026-09-10-stk-audio-heap-corruption-source-sweep.md`](../misc/2026-09-10-stk-audio-heap-corruption-source-sweep.md).

## 5b. ✅ X11 re-verified on the current build

The desktop had not been exercised since two allocator commits landed, and X is the heaviest AF_UNIX
**and** malloc consumer here — a workload shape the 541-test suite does not cover. One cycle:
**0 exceptions, 0 corruption events, 2430 frames at a steady 10.3 fps**, content-graded **89.5%
non-black / 8679 colours** against the 90.2% / 8856 baseline. No regression.

## 7. ⏸ UPSTREAM: one merge taken, two deferred — kernel conflict is your call

**2026-09-11 sweep:** posixsrv **merged, verified, pushed** (`211d49a`; the one commit was
`2d2a193 tmpfile: forward mtGetAttrAll`, 1 line, clean) — boot-tested together with the kernel fix
above: banner + psh, `test-libc-stdlib` **93/0**, `test-libc-misc` **212/0**, **0 exceptions**.
Manifest `2026-09-11-vmfix-posixsrv-verified.md`. Game forks all **0 behind**; all four generated port
patches **OK**.
⏸ **`phoenix-rtos-project` deferred on purpose:** its only new commit is `29e6c64 update submodules`,
which bumps the pointers for **kernel and tests** — the two repos whose merge is blocked below. Taking
it would make `project` claim a kernel we do not have.

**2026-09-11:** upstream moved for the first time in days — **kernel 5 commits, tests 2**. Both
merges **conflict** and the kernel one is **not mechanical**, so per your standing rule I **left both
repos untouched** (verified clean: kernel `0c768551`, tests `c91eede`, 0 modified).
**Why:** upstream **deletes `posix/unix.c` outright** — it rebuilds UNIX sockets on a new
endpoint/channel model — while we have local changes to that file (git reports `UD`), plus 8 conflict
hunks in `posix/posix.c`. **AF_UNIX is what X11 runs on**, i.e. the demo path, so this is a **re-port
decision, not a merge** and it needs a session of its own. Nothing is blocked: the delivered image and
every fix are unaffected. Full scope, including which of our AF_UNIX fixes are at stake:
[`docs/misc/2026-09-11-afunix-upstream-report-scope.md`](../misc/2026-09-11-afunix-upstream-report-scope.md).

- **2026-09-10 13:0x — nothing behind.** All 16 Phoenix siblings **0 behind** `origin/master`;
  yquake2 (12 ahead), quakespasm (26), quake3e (9, tracks `origin/**main**` not master) all **0
  behind**; all four generated port patches **OK, no drift**. Nothing merged, nothing to verify.
- **2026-09-10 01:20** — merged 10 upstream commits into the yquake2 fork (the one that mattered:
  `gl3: fix GL3_Draw_TileClear for scrap images`), regenerated its patch, re-verified Quake II
  in-game, pushed.
- **vkQuake: 39 behind, staying behind by your rule.** Measured: **6 of our 10 patched files are also
  changed upstream** (the MBOIT-pipeline and MD5-skinning rewrites moved every anchor we rely on), and
  the 39 commits are ray-tracing, SSAO/XeGTAO, MoltenVK-on-Apple, Ironwail imports and CI — nothing
  we need. That is a re-port, not a merge; it deserves its own session.

## 8. Also settled this week

One line each, with full detail in `docs/done/2026-09-10-w37-detail-snapshot-{3,4,5,6,7}.md`: the
libstdc++/`std::chrono` fix (~30 features off, not one) and its promotion · libphoenix
`clock_getres` · the allocator large-bin abandon fix · the readlink clamp · the nfs-fs
attribute-cache fix · CPython's restored `dir_fd`/`sched_*` · the Window Maker `nice()` collision
that had made the X11 showcase unbuildable, **and the stale docs that would have re-introduced it**
· the yquake2 upstream sync · MicroPython's `ticks_ms`/`ticks_us` were the wall clock, now monotonic
· a sweep of all 16 port shims against libphoenix's 994 symbols (one overlap, removed) · the public
docs re-synced · your four reported bugs, all addressed.
