# Week 2026-W37 (Mon 2026-09-07 → Sun 2026-09-13)

Phoenix-RTOS RPi4 — weekly working log. Kept **short on purpose**: this is the file you read.
Detail: `docs/done/2026-09-10-w37-detail-snapshot-{2,3,4,5}.md`. Analyses: `docs/misc/`.
W36: `docs/done/WEEK-2026-W36.md`.

**★ OWNER GOAL (2026-09-06):** a stable build with the games and X11 good enough for a public
presentation / a screen recording published online. Everything below is ranked against that.

---

## 1. DECISIONS NEEDED FROM WITOLD — none open

- **✅ The POSIX option macros: you said "follow your recommendations and move forward", and it is
  done** (§3). Nothing left for you on it.
- **D4** (the "genuine tool" boundary) and **D2** (moving Mesa itself into `phoenix-rtos-devices`):
  optional, nothing blocked. ⓘ The Mesa fork has only the upstream `origin`, so its local commits are
  unpushed — resolving that is part of D2.

**✅ Decided / done, do not re-raise:** D7 buffer sharing **CLOSED by you** · Quake II underwater
stopgap **LEAVE AS IS** · `std::chrono` **FIXED, HW-verified, promoted** (it was ~30 libstdc++
features off, not one) · your C-side question **answered** (3 ports, 2 systemic causes, both halves
fixed) · the reel is intact and **not** being re-cut (its captions are true readings of different
moments, not understatements).

**Settled, never re-ask:** toolchain is **GCC 16.2.0 + binutils 2.47**. **Fork-only, never
upstream.** **Game forks: if ours works, leave it.** **D6** (purge build-output ELFs from coord
history): leave as is.

## 2. ⏸ YOUR ONE ACTION — flash the gated image

`artifacts/rpi4b/rpi4b-sd-2part-gated-1afa724b.img` · SHA256
`1afa724bb1fd0d97b48e7adf06dad5bf503a9452301ecad9770e40f118cfad30` · manifest
`manifests/2026-09-10-demo-image-overnight-fixes.md`

```
udisksctl unmount -b /dev/sda1 2>/dev/null || true
sudo dd if=artifacts/rpi4b/rpi4b-sd-2part-gated-1afa724b.img of=/dev/sda bs=4M conv=fsync status=progress && sync
```

ⓘ It is now under an explicit `-gated-` name so that a later `--variant sd` build cannot overwrite
the one artifact you have a pending action on. `rpi4b-sd-2part.img` is the rolling build output;
don't flash `rpi4b-sd.img` (FAT-boot-only).

**Gates — all green.** Contents + FAT **PASS** ("safe to flash") · QEMU structural **PASS** · boot
reliability **47/47** to the `(psh)%` prompt on this build, 0 fault-bearing, 0 wedge-suspect ·
provenance read out of **this image's own** ext2 root (not the build tree).

✅ **App gate on THIS image's own binaries: 6/6, 0 faults each** — the export was re-synced from the
image's rootfs and all six binaries verified sha256-identical to the ones packed in, so these
describe the code in the image. Judged from the HDMI frames, not the log.

| app | evidence on `1afa724b`'s own binaries |
|---|---|
| X desktop (`startx_gpu action`) | Window Maker + xlogo, xbill, xclock, Python GoL, xterm |
| QuakeSpasm | in-game demo playback, **48 FPS** on screen |
| Quake III (`+map q3dm1`) | in-game, full HUD (100/100, ammo 20/5/0), bot visible, **25 fps** |
| Quake II | in-game, weapon + HUD, **38.78 fps** |
| vkQuake | rendering, demo2 playback, console overlay |
| SuperTuxKart (`--track=hacienda --numkarts=4 --profile-laps=2`) | **in-race**, 4 karts, lap 1/2, `FPS: 6/7/9 · 50 KTris` |

⚠ **You are the first to boot this from a card** — the host reader is detached (shares the USB-C port
with power/eth), so SD boot cannot be exercised here. The contents gate compensates: it asserts the
six firmware files, `arm_64bit=1`, and that every file `config.txt` names exists (validated against a
known-bad copy).
↩ Fallback: `rpi4b-sd-2part.KNOWN-GOOD-e0768b87.img`.

## 3. ✅ DONE THIS TURN — libphoenix now advertises the POSIX option macros

Your call, executed. libphoenix declared `_POSIX_VERSION 200809L` and then defined **no option macro
at all**, so the portable way to ask for a monotonic clock —
`#if _POSIX_TIMERS > 0 && defined(_POSIX_MONOTONIC_CLOCK)` — was **always false** and portable code
silently took its fallback. Eleven macros are now claimed (libphoenix `070ceda`).

- **Values are positive (`200809L`), never `0`/`-1`** — a lot of real code tests these with a bare
  `#ifdef`, to which POSIX's `-1` "not supported" convention reads as *supported*.
- **Contract test, both directions** (`phoenix-rtos-tests` `98e7014`, `libc/misc/posix_options`):
  every claim is positive, the portable gate is true, every claim is exercised through a real call,
  and the seven groups we do **not** implement are asserted still unclaimed. **HW 4/4**;
  `test-libc-misc` 208 → **212 Tests, 0 Failures**.
- **`_POSIX_SEMAPHORES` stays unclaimed on purpose** — libphoenix has no `sem_*` and no
  `semaphore.h`; claiming it makes CPython `#include <semaphore.h>` and fail to build. The test pins
  that.
- **`_POSIX_TIMERS` is claimed with a documented shortfall**: clocks complete, `timer_create` family
  absent. Claimed because it is the gate every portable monotonic test uses, and a `timer_create`
  caller now gets a loud link error instead of silent mistiming.
- **The one risk I flagged for you did not materialise.** `_POSIX_THREAD_ATTR_STACKSIZE` was supposed
  to make CPython set a thread stack size; `THREAD_STACK_SIZE` is 0, so `pthread_attr_setstacksize`
  is **never called** at default settings. CPython does now pass `&attrs` where it passed `NULL` —
  also a no-op, because libphoenix's `pthread_attr_init` copies `pthread_attr_default` and
  `pthread_create(NULL)` uses `&pthread_attr_default`, byte-identical. Net gain:
  `threading.stack_size(n)` goes from unsupported to working.
- **openssl's wall-clock entropy timing is fixed as a side effect** — `rand_unix.c:858` picked
  `CLOCK_REALTIME` for exactly this reason. Last night I recorded it as "not worth a wide rebuild";
  your go-ahead makes it free.

## 3a. ⚠ The trap that nearly made this a fake result — worth knowing about

Deleting a port's build-state json does **not** force a rebuild, and it fails *convincingly*:
port_manager cleans only when the state has **changed**, so with the file absent it goes straight to
the build, `make` finds every object newer than its source, and the port is merely **re-linked from
stale objects**. The log prints `BUILD: python-3.14.4` and the binary gets a fresh timestamp.

Caught by checking the thing itself rather than the log: `Python/thread.o` was **8 hours old** after
a 4-second "rebuild", and openssl **never compiled `rand_unix.c`** — the one file the change was for.
Four of the five ports silently did not get it.

✅ Fixed properly, not worked around: **`scripts/force-port-rebuild.sh <port>…`** keeps the state file
and poisons only its recipe digest, so port_manager sees a real change and cleans; the correct digest
is written back on success, so it is self-healing. The ports framework tracks use flags, dependency
versions and the recipe digest but **not the sysroot headers**, so a libc header change invalidates
nothing on its own — this is now the documented way to land one.

**Verification rule, recorded:** a `BUILD:` line is not evidence. Require both a
`Build state changed for <port>, cleaning` line **and** a compile line for a file you expect to change.

## 4. Open / not blocking the goal

- **🔎 Allocator double-free — OPEN, now self-diagnosing.** Broader than this log used to say: **21
  logs, 2026-06-28 → 09-09**, across xcalc (×7), Dillo, vkQuake (×2), the glamor desktop / Window
  Maker (×5) and the liveness test (×4) — a generic heap-corruption symptom with several causes, some
  already separately fixed. Block class established **from evidence** (`size = 0x410` = PATH_MAX +
  overhead, un-coalesced, so both neighbours were in use). All three named mechanisms eliminated.
  `free()` now prints its **caller** (libphoenix `d875dc9`), verified on hardware via `addr2line`,
  at zero cost on the normal path — the next occurrence will say which code freed the block.
- **⚠ STK fps 9 → 7: both mechanisms I could name are refuted** (the `hardware_concurrency` thread
  pool — capping it changed nothing; a second `steady_clock` — inert on the GLES/SP path). What is
  left is a confound this instrument cannot separate: scene complexity differs systematically between
  the samples (26–65 vs 45–78 KTris). The right instrument is a flip counter over a fixed window, not
  the HUD readout. Not image-blocking; STK races correctly and is in the 6/6 gate.
- **Blocked on hardware/attendance:** SD host card reader detached · bash `-i` needs a live terminal ·
  ffmpeg audio needs the attended `/dev/audio0` sign-off.

## 5. Upstream sync — nothing outstanding

- **2026-09-10 07:00 — nothing behind.** All 16 Phoenix siblings **0 behind** `origin/master`;
  yquake2, quake3e, quakespasm **0 behind**; all four generated port patches OK.
- **2026-09-10 01:20** — merged 10 upstream commits into the yquake2 fork (the one that mattered:
  `gl3: fix GL3_Draw_TileClear for scrap images`), regenerated its patch, re-verified Quake II
  in-game, pushed.
- **vkQuake: 39 behind, staying behind by your rule.** Measured: **6 of our 10 patched files are also
  changed upstream** (the MBOIT-pipeline and MD5-skinning rewrites moved every anchor we rely on), and
  the 39 commits are ray-tracing, SSAO/XeGTAO, MoltenVK-on-Apple, Ironwail imports and CI — nothing
  we need. That is a re-port, not a merge; it deserves its own session.

## 6. Also settled this week — one line each

Full detail in `docs/done/2026-09-10-w37-detail-snapshot-{4,5}.md`: the libstdc++/`std::chrono` fix
(~30 features) and its promotion · libphoenix `clock_getres` · the allocator large-bin abandon fix ·
the readlink clamp · the **nfs-fs attribute-cache fix** (intermittent stale `st_nlink`) · CPython's
restored `dir_fd`/`sched_*` · the Window Maker `nice()` collision that had made the X11 showcase
unbuildable, **and the stale docs that would have re-introduced it** · the yquake2 upstream sync ·
**MicroPython's `ticks_ms`/`ticks_us` were the wall clock** (now monotonic: reads 17315 ms since boot,
`ticks_diff` over a 200 ms sleep returns exactly 200) · a sweep of all 16 port shims against
libphoenix's 994 symbols, finding exactly one overlap (`copysign` in vkQuake's glue, removed) · the
public docs re-synced (they still listed `std::chrono` as open and recommended `-lrt`, which this
target does not have) · your four reported bugs, all addressed.
