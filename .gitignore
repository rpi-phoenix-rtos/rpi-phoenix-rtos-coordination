# Local upstream working copies
sources/
buildroots/

# Build outputs and external clones (large, machine-local)
artifacts/
external/

# Staged non-free firmware blobs (Cypress EULA, redistributable but
# kept out of git — populate via scripts/stage-bcm43455-firmware.sh)
.firmware/
.bootblobs/
.buildroot/
.toolchain/
.env.local

# Claude Code session state (worktrees, per-session settings)
.claude/

# macOS filesystem noise
.DS_Store

# Personal notes and incidental files
gemini-findings.md
*.mov
Screenshot *.png
/4

# libnfs study clone (own git repo; not tracked here) — see docs/research/2026-06-07-nfs-rootfs-feasibility/
research/libnfs/

# X11 port build outputs + logs (host-side build noise; sources/launchers are
# fetched/compiled, the .c sources stay tracked). See tools/x11-port/build-x11-phoenix.sh
tools/x11-port/src/
/tools/x11-port/launcher/pl_phoenix_xlaunch
/tools/x11-port/launcher/*-build.log
/tools/x11-port/xkb/xkbcomp.warn
/history.txt

# Python bytecode caches
__pycache__/
*.pyc

# Docker build outputs (SD image etc. — large, machine-local)
docker-out/

# Pre-publication review scratch: file inventories + internal notes, not for
# the public release.
docs/review/2026-07-06-pre-publication/

# Claude Code session-id scratch file
session-id-cl

# Local-only preservation of the unpublished WiFi WIP (Cypress WHD/HAL subtree
# removed from the lwip fork tip; kept here + on the lwip `wifi-wip` branch so WiFi
# work can resume). NOT for public release. See docs/inprogress/2026-07-22-consolidation-and-release-prep.md
.wifi-wip/

# git-filter-repo tool venv (release tooling)
.venv-filter/
