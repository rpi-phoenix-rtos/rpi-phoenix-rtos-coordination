# 2026-03-22: Pi 4 first-trial classification aid scope

## Scope

Close `STEP-0398` by defining the smallest interpretation aid after the first
trial checklist exists.

## Decision

The next bounded operator-facing addition should be one small mapping from the
observed result class to the next implementation direction.

That mapping should stay:

- documentation-only
- narrow
- consistent with the current step-by-step workflow

It should not prescribe broad debugging or broad code changes.
