# BCM2711 Link Bring-Up Scope

Date: `2026-03-21`

## Step

- `STEP-0317` Scope the smallest BCM2711 link-bring-up step

## Repositories

- coordination repo

## Summary

- reviewed the gap between the early BCM2711 host-bridge preparation hook and a
  meaningful link-up state
- selected the next smallest link slice as:
  `PERST` release, post-release wait, link-state sampling, and RC-mode
  sampling
- explicitly kept outbound windows, root-bridge class shaping, downstream
  enumeration, MSI, and xHCI out of scope

## Why This Comes Next

- Circle releases `PERST`, waits 100 ms, then checks both link-up and RC mode
  before it treats downstream configuration as meaningful
- that makes `PERST` release plus bounded status sampling the smallest next move
  after early reset and `MISC_CTRL` preparation
- it also gives Phoenix a safe way to gate downstream config-space accesses
  without overclaiming that PCIe is already fully live

## References

- `external/circle/lib/bcmpciehostbridge.cpp`
- `sources/phoenix-rtos-devices/pcie/server/pcie.c`

## Validation

- source review only
- no code changes in this step

## Next Logical Step

- implement `PERST` release, link-state sampling, and RC-mode sampling in the
  BCM2711 backend, then use that state to gate downstream config-space access
