# Manifest: Polling PL011 TTY Driver Scaffold

- Date: `2026-03-20`
- Step: `STEP-0081`
- Result: `completed`

## Scope

- add the first reusable polling `pl011-tty` driver scaffold in `phoenix-rtos-devices`
- keep the first version single-instance and polling-based
- validate the new driver directly on the generic target in `phoenix-dev`

## Upstream Repositories

### `phoenix-rtos-devices`

- Commit: `62d1e08`

## Files

- `phoenix-rtos-devices/tty/pl011-tty/Makefile`
- `phoenix-rtos-devices/tty/pl011-tty/pl011-tty.c`

## Validation

- refreshed the VM-local copied buildroot in `phoenix-dev`
- validated the driver directly with:
  `TARGET=aarch64a53-generic-qemu PROJECT_PATH=/home/witoldbolt.guest/phoenix-buildroots/phoenix-rtos-project-copy/_projects/aarch64a53-generic-qemu make -C phoenix-rtos-devices pl011-tty`

## Validation Evidence

- the new `pl011-tty` component now compiles and links successfully for `aarch64a53-generic-qemu`
- the first scaffold includes:
  - a single-instance polling RX/TX loop
  - `libtty`
  - `libklog`
  - `/dev/tty0`
  - `/dev/console`
- the first scaffold keeps interrupt support, DT probing, and generic target integration out of scope

## Notes

- the driver uses a board-config contract first, with safe defaults that keep the scaffold buildable before target integration
- the next smallest step is to integrate `pl011-tty` into the generic devices target default component set

## Selected Next Step

- define the first generic PL011 target-integration step
