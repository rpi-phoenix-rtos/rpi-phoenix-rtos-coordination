# vkQuake aarch64-phoenix whole-archive link — undefined-symbol closure snapshot.
#
# 2026-06-24: LINK OK — 0 undefined symbols.
#   The whole-archive link of libvkquake.a against the V3DV ICD (libv3dv-phoenix.a +
#   libv3d-phoenix.a) + the trampolines + the Phoenix platform shims now closes
#   completely to a statically-linked aarch64 ELF (/tmp/vkquake-phoenix).
#
# Progression:
#   174 (2026-06-23, initial whole-archive drive)
#   109 (after the Sys_*/IN_*/SNDDMA_*/SDL-threading platform shims landed)
#    26 (after the build-infra bucket: 80 *_spv shader arrays via
#        gen-vkquake-shaders.py + 3 vkquake_pak* via mkpak+bintoc)
#     0 (after the Vulkan vid shim pl_phoenix_vk_vid.c: VID_*/GL_*/vid+modestate+
#        r_usesops+prev_end_rendering_task + the 7 vid_* cvars + menu/screenshot stubs)
#
# Re-generate this snapshot with: build-vkquake-phoenix.py --link
# (prints "=== LINK OK -> /tmp/vkquake-phoenix ===" when the closure is empty.)
