# Minimal Redis config for Phoenix-RTOS bring-up: foreground, no persistence
# (so the fork()-based BGSAVE/AOF paths are never exercised), open to the
# netboot network for host-side verification.
daemonize no
save ""
appendonly no
protected-mode no
bind 0.0.0.0
port 6379
logfile ""
loglevel notice
