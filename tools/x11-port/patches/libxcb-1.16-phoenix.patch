--- a/src/xcb_conn.c
+++ b/src/xcb_conn.c
@@ -36,6 +36,7 @@
 #include <fcntl.h>
 #include <errno.h>
 #include <limits.h>
+#include <arpa/inet.h> /* Phoenix: htonl is a macro here; not pulled transitively */
 
 #include "xcb.h"
 #include "xcbint.h"
--- a/src/xcb_in.c
+++ b/src/xcb_in.c
@@ -52,6 +52,16 @@
 #include "xcbext.h"
 #include "xcbint.h"
 
+/* Phoenix: sys/socket.h lacks MSG_TRUNC/MSG_CTRUNC; recvmsg does not report these
+ * flags, so define them as no-op (0) — the control-data truncation check below just
+ * never trips (SCM_RIGHTS fd-passing is unused by the software X server anyway). */
+#ifndef MSG_TRUNC
+#define MSG_TRUNC 0
+#endif
+#ifndef MSG_CTRUNC
+#define MSG_CTRUNC 0
+#endif
+
 #define XCB_ERROR 0
 #define XCB_REPLY 1
 #define XCB_XGE_EVENT 35
