diff --git a/include/renderdoc_app.h b/include/renderdoc_app.h
index b2d10c5e2d2..c19305b198f 100644
--- a/include/renderdoc_app.h
+++ b/include/renderdoc_app.h
@@ -39,6 +39,8 @@
 #define RENDERDOC_CC
 #elif defined(__APPLE__)
 #define RENDERDOC_CC
+#elif defined(__phoenix__) || defined(__unix__)
+#define RENDERDOC_CC
 #else
 #error "Unknown platform"
 #endif
diff --git a/src/broadcom/compiler/v3d_shader_dump.c b/src/broadcom/compiler/v3d_shader_dump.c
new file mode 100644
index 00000000000..26e8f82315c
--- /dev/null
+++ b/src/broadcom/compiler/v3d_shader_dump.c
@@ -0,0 +1,474 @@
+/*
+ * v3d_shader_dump — host tool: compile a trivial shader through Mesa's v3d
+ * NIR->QPU compiler and print the QPU bytecode (u64 words). Validate the output
+ * with the built qpu_disasm. Lets us derive correct, validated V3D-4.2 shaders
+ * off-device for the Phoenix bare-metal triangle (GLQuake Path A).
+ *
+ * NOT upstream Mesa — a local harness in the clone. Build as an in-tree meson
+ * executable linked against the v3d compiler libs.
+ */
+#include <stdio.h>
+#include <stdlib.h>
+#include "broadcom/common/v3d_device_info.h"
+#include "broadcom/compiler/v3d_compiler.h"
+#include "compiler/nir/nir_builder.h"
+#include "compiler/glsl_types.h"
+#include "qpu/qpu_disasm.h"
+#include "util/ralloc.h"
+
+static void dbg_out(const char *msg, void *data) { (void)data; fprintf(stderr, "%s", msg); }
+
+static unsigned v3d_type_size(const struct glsl_type *type, bool bindless)
+{ (void)bindless; return glsl_count_attribute_slots(type, false); }
+
+/* Replicate the driver's pre-compile NIR finalization so I/O is wired correctly
+ * (gather_info from the actual IR; nir_lower_io for FS; v3d does VS I/O itself). */
+static void finalize_nir(nir_shader *s)
+{
+	nir_shader_gather_info(s, nir_shader_get_entrypoint(s));
+	if (s->info.stage == MESA_SHADER_FRAGMENT) {
+		nir_lower_fragcolor(s, 1); /* gl_FragColor -> per-RT outputs (max_rb=1) */
+		nir_lower_io(s, nir_var_shader_in | nir_var_shader_out, v3d_type_size, 0);
+	}
+	v3d_optimize_nir(NULL, s);
+	nir_lower_var_copies(s);
+}
+
+/* v3d NIR compiler options — copied verbatim from gallium/drivers/v3d/v3d_screen.c
+ * v3d_screen_get_compiler_options() (lower_fsat set for ver<71 = true at v4.2). */
+static nir_shader_compiler_options v3d_options =
+{
+                .compact_arrays = true,
+                .lower_uadd_sat = true,
+                .lower_usub_sat = true,
+                .lower_iadd_sat = true,
+                .lower_extract_byte = true,
+                .lower_extract_word = true,
+                .lower_insert_byte = true,
+                .lower_insert_word = true,
+                .lower_bitfield_insert = true,
+                .lower_bitfield_extract = true,
+                .lower_bitfield_extract16 = true,
+                .lower_bitfield_extract8 = true,
+                .lower_bitfield_reverse = true,
+                .lower_bit_count = true,
+                .lower_cs_local_id_to_index = true,
+                .lower_ffract = true,
+                .lower_fmod = true,
+                .lower_pack_unorm_2x16 = true,
+                .lower_pack_snorm_2x16 = true,
+                .lower_pack_unorm_4x8 = true,
+                .lower_pack_snorm_4x8 = true,
+                .lower_unpack_unorm_4x8 = true,
+                .lower_unpack_snorm_4x8 = true,
+                .lower_pack_half_2x16 = true,
+                .lower_unpack_half_2x16 = true,
+                .lower_pack_32_2x16 = true,
+                .lower_pack_32_2x16_split = true,
+                .lower_unpack_32_2x16_split = true,
+                .lower_fdiv = true,
+                .lower_find_lsb = true,
+                .lower_flrp32 = true,
+                .lower_fpow = true,
+                .lower_fsqrt = true,
+                .lower_ifind_msb = true,
+                .lower_isign = true,
+                .lower_hadd = true,
+                .lower_fisnormal = true,
+                .lower_mul_high = true,
+                .lower_wpos_pntc = true,
+                .lower_to_scalar = true,
+                .lower_interpolate_at = true,
+                .lower_int64_options =
+                        nir_lower_bcsel64 |
+                        nir_lower_bit_count64 |
+                        nir_lower_conv64 |
+                        nir_lower_divmod64 |
+                        nir_lower_iabs64 |
+                        nir_lower_iadd64 |
+                        nir_lower_icmp64 |
+                        nir_lower_imul_2x32_64 |
+                        nir_lower_imul64 |
+                        nir_lower_ineg64 |
+                        nir_lower_logic64 |
+                        nir_lower_minmax64 |
+                        nir_lower_shift64 |
+                        nir_lower_ufind_msb64,
+                .lower_fquantize2f16 = true,
+                .lower_ufind_msb = true,
+                .has_fsub = true,
+                .has_isub = true,
+                .has_imul24 = true,
+                .has_umul24 = true,
+                .has_uclz = true,
+                .divergence_analysis_options =
+                       nir_divergence_multiple_workgroup_per_compute_subgroup,
+                /* This will enable loop unrolling in the state tracker so we won't
+                 * be able to selectively disable it in backend if it leads to
+                 * lower thread counts or TMU spills. Choose a conservative maximum to
+                 * limit register pressure impact.
+                 */
+                .max_unroll_iterations = 16,
+                .max_samples = 4,
+                .force_indirect_unrolling_sampler = true,
+                .scalarize_ddx = true,
+                .max_varying_expression_cost = 4,
+        };
+
+int main(int argc, char **argv)
+{
+	/* V3D 4.2 device info, computed from the real Pi4 core IDENT regs
+	 * (IDENT0=0x04443356, IDENT1=0x81001422) per v3d_get_device_info(). */
+	struct v3d_device_info devinfo = { 0 };
+	devinfo.ver = 42;                 /* major 4, minor 2 */
+	devinfo.rev = 0;
+	devinfo.vpm_size = 65536;         /* (IDENT1>>28 & 0xf)=8 * 8192 */
+	devinfo.qpu_count = 8;            /* nslc(2) * qups(4) */
+	devinfo.has_accumulators = true;  /* ver < 71 */
+	devinfo.page_size = 4096;
+	devinfo.clipper_xy_granularity = 256.0f; /* ver 42 */
+	devinfo.cle_readahead = 256u;            /* ver 42 */
+	v3d_options.lower_fsat = true; /* ver < 71 */
+
+	glsl_type_singleton_init_or_ref();
+
+	const struct v3d_compiler *compiler = v3d_compiler_init(&devinfo, 0);
+	if (!compiler) { fprintf(stderr, "v3d_compiler_init failed\n"); return 1; }
+
+	/* Minimal fragment shader: gl_FragData[0] = const vec4. */
+	nir_builder b = nir_builder_init_simple_shader(MESA_SHADER_FRAGMENT,
+		&v3d_options, "constfs");
+	/* Emit store_output directly (v3d_compile expects I/O intrinsics, not var
+	 * derefs — the driver runs nir_lower_io before compile; we skip that by
+	 * emitting the intrinsic). */
+	/* Output variable (drives c->outputs sizing in ntq_setup_outputs); the
+	 * value is written via the store_output intrinsic below (no deref). */
+	nir_variable *outc = nir_variable_create(b.shader, nir_var_shader_out,
+		glsl_vec4_type(), "gl_FragColor");
+	outc->data.location = FRAG_RESULT_DATA0;
+	nir_store_var(&b, outc, nir_imm_vec4(&b, 0.07, 0.13, 0.20, 1.0), 0xf);
+	finalize_nir(b.shader);
+
+	struct v3d_fs_key fs_key = { 0 };
+	fs_key.cbufs = 1;  /* RT0 present (bit mask) */
+
+	struct v3d_prog_data *prog_data = NULL;
+	uint32_t asm_size = 0;
+	uint64_t *insts = v3d_compile(compiler, &fs_key.base, &prog_data,
+		b.shader, dbg_out, NULL, 0, 0, &asm_size);
+	if (!insts) { fprintf(stderr, "v3d_compile FAILED\n"); return 2; }
+
+	uint32_t n = asm_size / sizeof(uint64_t);
+	printf("/* FS QPU: %u instructions (%u bytes) */\n", n, asm_size);
+	for (uint32_t i = 0; i < n; i++)
+		printf("0x%016llxull, /* %s */\n", (unsigned long long)insts[i],
+			v3d_qpu_disasm(&devinfo, insts[i]));
+
+	/* ---- Vertex shader (passthrough position) + its coordinate variant ----
+	 * Built deref-based like mesa's blit VS; v3d_compile lowers VS I/O itself. */
+	for (int coord = 0; coord <= 1; coord++) {
+		nir_builder vb = nir_builder_init_simple_shader(MESA_SHADER_VERTEX,
+			&v3d_options, coord ? "coordvs" : "rendervs");
+		nir_variable *pin = nir_variable_create(vb.shader, nir_var_shader_in,
+			glsl_vec4_type(), "pos");
+		pin->data.location = VERT_ATTRIB_GENERIC0;
+		pin->data.driver_location = 0;
+		nir_variable *pout = nir_variable_create(vb.shader, nir_var_shader_out,
+			glsl_vec4_type(), "gl_Position");
+		pout->data.location = VARYING_SLOT_POS;
+		pout->data.driver_location = 0;
+		nir_store_var(&vb, pout, nir_load_var(&vb, pin), 0xf);
+		finalize_nir(vb.shader);
+
+		struct v3d_vs_key vs_key = { 0 };
+		vs_key.is_coord = coord;
+		vs_key.num_used_outputs = 0;
+
+		struct v3d_prog_data *vpd = NULL;
+		uint32_t vsz = 0;
+		uint64_t *vi = v3d_compile(compiler, &vs_key.base, &vpd, vb.shader,
+			dbg_out, NULL, 0, 0, &vsz);
+		if (!vi) { fprintf(stderr, "VS(coord=%d) compile FAILED\n", coord); continue; }
+		uint32_t vn = vsz / sizeof(uint64_t);
+		printf("/* %s QPU: %u instructions */\n", coord ? "COORD-VS" : "RENDER-VS", vn);
+		for (uint32_t i = 0; i < vn; i++)
+			printf("0x%016llxull, /* %s */\n", (unsigned long long)vi[i],
+				v3d_qpu_disasm(&devinfo, vi[i]));
+	}
+
+	/* ---- Compute shader (CSD probe: out[gid] = gid) ----
+	 * Models gallium v3d's compute path (v3d_program.c:281): a zeroed base
+	 * v3d_key, plus nir_lower_compute_system_values to lower
+	 * gl_GlobalInvocationID. Output is written to SSBO 0 so an on-device CSD
+	 * harness can read it back and numerically verify the dispatch. */
+	{
+		nir_builder cb = nir_builder_init_simple_shader(MESA_SHADER_COMPUTE,
+			&v3d_options, "csprobe");
+		cb.shader->info.workgroup_size[0] = 16;
+		cb.shader->info.workgroup_size[1] = 1;
+		cb.shader->info.workgroup_size[2] = 1;
+		cb.shader->info.num_ssbos = 1;
+
+		nir_def *gid = nir_load_global_invocation_id(&cb, 32);
+		nir_def *idx = nir_channel(&cb, gid, 0);
+		nir_def *byteoff = nir_imul_imm(&cb, idx, 4);
+		nir_store_ssbo(&cb, idx, nir_imm_int(&cb, 0), byteoff,
+			.write_mask = 0x1, .access = 0, .align_mul = 4, .align_offset = 0);
+
+		nir_shader_gather_info(cb.shader, nir_shader_get_entrypoint(cb.shader));
+		struct nir_lower_compute_system_values_options cs_opts = { 0 };
+		nir_lower_compute_system_values(cb.shader, &cs_opts);
+		v3d_optimize_nir(NULL, cb.shader);
+		nir_lower_var_copies(cb.shader);
+
+		struct v3d_key ckey = { 0 };
+		struct v3d_prog_data *cpd = NULL;
+		uint32_t csz = 0;
+		uint64_t *ci = v3d_compile(compiler, &ckey, &cpd, cb.shader,
+			dbg_out, NULL, 0, 0, &csz);
+		if (!ci) {
+			fprintf(stderr, "CS compile FAILED\n");
+		}
+		else {
+			uint32_t cn = csz / sizeof(uint64_t);
+			struct v3d_compute_prog_data *cs =
+				(struct v3d_compute_prog_data *)cpd;
+			printf("/* CS QPU: %u instructions; local_size=%ux%ux%u shared=%u */\n",
+				cn, cs->local_size[0], cs->local_size[1], cs->local_size[2],
+				cs->shared_size);
+			for (uint32_t i = 0; i < cn; i++)
+				printf("0x%016llxull, /* %s */\n", (unsigned long long)ci[i],
+					v3d_qpu_disasm(&devinfo, ci[i]));
+			printf("/* CS prog_data: threads=%u single_seg=%d uniforms=%u */\n",
+				cpd->threads, (int)cpd->single_seg, cpd->uniforms.count);
+			for (uint32_t u = 0; u < cpd->uniforms.count; u++)
+				printf("/*   uniform[%u]: contents=%d data=0x%08x */\n",
+					u, (int)cpd->uniforms.contents[u], cpd->uniforms.data[u]);
+		}
+	}
+
+	/* ---- CSMATMUL: o[i] = sum_j w[i*N+j]*x[j], N RUNTIME via ssbo3[0] (microbench) ----
+	 * N is read from a 4th SSBO (binding 3, element 0) so it drives BOTH the loop
+	 * bound AND the row stride consistently — a compile-time MMN let the compiler
+	 * bake the stride as an immediate while only the bound tracked a uniform, which
+	 * corrupted results for N!=256 (HW-confirmed). One kernel now serves any N. */
+	{
+		nir_builder mb = nir_builder_init_simple_shader(MESA_SHADER_COMPUTE,
+			&v3d_options, "csmatmul");
+		mb.shader->info.workgroup_size[0] = 64;
+		mb.shader->info.workgroup_size[1] = 1;
+		mb.shader->info.workgroup_size[2] = 1;
+		mb.shader->info.num_ssbos = 4;
+
+		nir_variable *sumv = nir_local_variable_create(mb.impl, glsl_float_type(), "sum");
+		nir_variable *jvar = nir_local_variable_create(mb.impl, glsl_uint_type(), "j");
+		nir_def *ii = nir_channel(&mb, nir_load_global_invocation_id(&mb, 32), 0);
+		/* N = params[0] from ssbo binding 3 (hoisted out of the loop). */
+		nir_def *nrt = nir_load_ssbo(&mb, 1, 32, nir_imm_int(&mb, 3),
+			nir_imm_int(&mb, 0), .align_mul = 4, .align_offset = 0);
+		nir_store_var(&mb, sumv, nir_imm_float(&mb, 0.0f), 0x1);
+		nir_store_var(&mb, jvar, nir_imm_int(&mb, 0), 0x1);
+
+		nir_push_loop(&mb);
+		{
+			nir_def *jj = nir_load_var(&mb, jvar);
+			nir_break_if(&mb, nir_uge(&mb, jj, nrt));
+			nir_def *widx = nir_iadd(&mb, nir_imul(&mb, ii, nrt), jj);
+			nir_def *wval = nir_load_ssbo(&mb, 1, 32, nir_imm_int(&mb, 0),
+				nir_imul_imm(&mb, widx, 4), .align_mul = 4, .align_offset = 0);
+			nir_def *xval = nir_load_ssbo(&mb, 1, 32, nir_imm_int(&mb, 1),
+				nir_imul_imm(&mb, jj, 4), .align_mul = 4, .align_offset = 0);
+			nir_def *acc = nir_fadd(&mb, nir_load_var(&mb, sumv),
+				nir_fmul(&mb, wval, xval));
+			nir_store_var(&mb, sumv, acc, 0x1);
+			nir_store_var(&mb, jvar, nir_iadd_imm(&mb, jj, 1), 0x1);
+		}
+		nir_pop_loop(&mb, NULL);
+
+		nir_store_ssbo(&mb, nir_load_var(&mb, sumv), nir_imm_int(&mb, 2),
+			nir_imul_imm(&mb, ii, 4), .write_mask = 0x1, .access = 0,
+			.align_mul = 4, .align_offset = 0);
+
+		nir_shader_gather_info(mb.shader, nir_shader_get_entrypoint(mb.shader));
+		struct nir_lower_compute_system_values_options mo = { 0 };
+		nir_lower_compute_system_values(mb.shader, &mo);
+		nir_lower_vars_to_ssa(mb.shader);
+		v3d_optimize_nir(NULL, mb.shader);
+		nir_lower_var_copies(mb.shader);
+
+		struct v3d_key mk = { 0 };
+		struct v3d_prog_data *mpd = NULL;
+		uint32_t msz = 0;
+		uint64_t *mins = v3d_compile(compiler, &mk, &mpd, mb.shader,
+			dbg_out, NULL, 0, 0, &msz);
+		if (!mins) {
+			fprintf(stderr, "CSMATMUL compile FAILED\n");
+		}
+		else {
+			uint32_t mnn = msz / sizeof(uint64_t);
+			printf("/* CSMATMUL QPU: %u instructions; threads=%u single_seg=%d uniforms=%u */\n",
+				mnn, mpd->threads, (int)mpd->single_seg, mpd->uniforms.count);
+			for (uint32_t z = 0; z < mnn; z++)
+				printf("0x%016llxull, /* %s */\n", (unsigned long long)mins[z],
+					v3d_qpu_disasm(&devinfo, mins[z]));
+			for (uint32_t u = 0; u < mpd->uniforms.count; u++)
+				printf("/*   uniform[%u]: contents=%d data=0x%08x */\n",
+					u, (int)mpd->uniforms.contents[u], mpd->uniforms.data[u]);
+		}
+	}
+
+	/* ---- CSGEMM8: C[i*8+k] = sum_j W[i*N+j]*X[j*8+k], K=8 batch, N runtime (ssbo3[0]).
+	 * Each invocation computes 8 batch outputs holding 8 accumulators and loading each
+	 * weight-row element W[i*N+j] ONCE (reused across the 8 batch columns) — the
+	 * weight-reuse that a plain per-vector GEMV lacks. Same D=256 grid as CSMATMUL, so
+	 * this is a kernel-only change (no dispatch-grid reconfig). Tests whether batching
+	 * amortizes the per-vector cost enough for the GPU to BEAT the CPU (single-vector
+	 * was measured at parity). X is (N x 8) row-major (batch-minor); C is (256 x 8). */
+	{
+		const int GK = 8;
+		int k;
+		nir_builder gb = nir_builder_init_simple_shader(MESA_SHADER_COMPUTE,
+			&v3d_options, "csgemm8");
+		gb.shader->info.workgroup_size[0] = 64;
+		gb.shader->info.workgroup_size[1] = 1;
+		gb.shader->info.workgroup_size[2] = 1;
+		gb.shader->info.num_ssbos = 4;
+
+		nir_variable *acc[8];
+		nir_variable *gjv = nir_local_variable_create(gb.impl, glsl_uint_type(), "j");
+		for (k = 0; k < GK; k++) {
+			acc[k] = nir_local_variable_create(gb.impl, glsl_float_type(), "acc");
+			nir_store_var(&gb, acc[k], nir_imm_float(&gb, 0.0f), 0x1);
+		}
+		nir_def *gi = nir_channel(&gb, nir_load_global_invocation_id(&gb, 32), 0);
+		nir_def *gn = nir_load_ssbo(&gb, 1, 32, nir_imm_int(&gb, 3),
+			nir_imm_int(&gb, 0), .align_mul = 4, .align_offset = 0);
+		nir_store_var(&gb, gjv, nir_imm_int(&gb, 0), 0x1);
+
+		nir_push_loop(&gb);
+		{
+			nir_def *gj = nir_load_var(&gb, gjv);
+			nir_break_if(&gb, nir_uge(&gb, gj, gn));
+			nir_def *gwidx = nir_iadd(&gb, nir_imul(&gb, gi, gn), gj);
+			nir_def *gw = nir_load_ssbo(&gb, 1, 32, nir_imm_int(&gb, 0),
+				nir_imul_imm(&gb, gwidx, 4), .align_mul = 4, .align_offset = 0);
+			nir_def *gjb = nir_imul_imm(&gb, gj, GK);          /* j*8 */
+			for (k = 0; k < GK; k++) {
+				nir_def *gxi = nir_iadd_imm(&gb, gjb, k);       /* j*8+k */
+				nir_def *gx = nir_load_ssbo(&gb, 1, 32, nir_imm_int(&gb, 1),
+					nir_imul_imm(&gb, gxi, 4), .align_mul = 4, .align_offset = 0);
+				nir_def *ga = nir_fadd(&gb, nir_load_var(&gb, acc[k]),
+					nir_fmul(&gb, gw, gx));
+				nir_store_var(&gb, acc[k], ga, 0x1);
+			}
+			nir_store_var(&gb, gjv, nir_iadd_imm(&gb, gj, 1), 0x1);
+		}
+		nir_pop_loop(&gb, NULL);
+
+		nir_def *gcb = nir_imul_imm(&gb, gi, GK);              /* i*8 */
+		for (k = 0; k < GK; k++) {
+			nir_def *gci = nir_iadd_imm(&gb, gcb, k);
+			nir_store_ssbo(&gb, nir_load_var(&gb, acc[k]), nir_imm_int(&gb, 2),
+				nir_imul_imm(&gb, gci, 4), .write_mask = 0x1, .access = 0,
+				.align_mul = 4, .align_offset = 0);
+		}
+
+		nir_shader_gather_info(gb.shader, nir_shader_get_entrypoint(gb.shader));
+		struct nir_lower_compute_system_values_options go = { 0 };
+		nir_lower_compute_system_values(gb.shader, &go);
+		nir_lower_vars_to_ssa(gb.shader);
+		v3d_optimize_nir(NULL, gb.shader);
+		nir_lower_var_copies(gb.shader);
+
+		struct v3d_key gk2 = { 0 };
+		struct v3d_prog_data *gpd = NULL;
+		uint32_t gsz = 0;
+		uint64_t *gins = v3d_compile(compiler, &gk2, &gpd, gb.shader,
+			dbg_out, NULL, 0, 0, &gsz);
+		if (!gins) {
+			fprintf(stderr, "CSGEMM8 compile FAILED\n");
+		}
+		else {
+			uint32_t gnn = gsz / sizeof(uint64_t);
+			printf("/* CSGEMM8 QPU: %u instructions; threads=%u single_seg=%d uniforms=%u */\n",
+				gnn, gpd->threads, (int)gpd->single_seg, gpd->uniforms.count);
+			for (uint32_t z = 0; z < gnn; z++)
+				printf("0x%016llxull, /* %s */\n", (unsigned long long)gins[z],
+					v3d_qpu_disasm(&devinfo, gins[z]));
+			for (uint32_t u = 0; u < gpd->uniforms.count; u++)
+				printf("/*   uniform[%u]: contents=%d data=0x%08x */\n",
+					u, (int)gpd->uniforms.contents[u], gpd->uniforms.data[u]);
+		}
+	}
+
+	/* ---- CSCONST: store a constant to out[0] (TMU write, no gid math) — step 2 ---- */
+	{
+		nir_builder cb = nir_builder_init_simple_shader(MESA_SHADER_COMPUTE,
+			&v3d_options, "csconst");
+		cb.shader->info.workgroup_size[0] = 16;
+		cb.shader->info.workgroup_size[1] = 1;
+		cb.shader->info.workgroup_size[2] = 1;
+		cb.shader->info.num_ssbos = 1;
+		nir_store_ssbo(&cb, nir_imm_int(&cb, 0xC0DE1234), nir_imm_int(&cb, 0),
+			nir_imm_int(&cb, 0), .write_mask = 0x1, .access = 0,
+			.align_mul = 4, .align_offset = 0);
+		nir_shader_gather_info(cb.shader, nir_shader_get_entrypoint(cb.shader));
+		struct nir_lower_compute_system_values_options o = { 0 };
+		nir_lower_compute_system_values(cb.shader, &o);
+		v3d_optimize_nir(NULL, cb.shader);
+		nir_lower_var_copies(cb.shader);
+		struct v3d_key k = { 0 };
+		struct v3d_prog_data *pd = NULL;
+		uint32_t sz = 0;
+		uint64_t *ins = v3d_compile(compiler, &k, &pd, cb.shader,
+			dbg_out, NULL, 0, 0, &sz);
+		if (!ins) {
+			fprintf(stderr, "CSCONST compile FAILED\n");
+		}
+		else {
+			uint32_t nn = sz / sizeof(uint64_t);
+			printf("/* CSCONST QPU: %u instructions; threads=%u single_seg=%d uniforms=%u */\n",
+				nn, pd->threads, (int)pd->single_seg, pd->uniforms.count);
+			for (uint32_t i = 0; i < nn; i++)
+				printf("0x%016llxull, /* %s */\n", (unsigned long long)ins[i],
+					v3d_qpu_disasm(&devinfo, ins[i]));
+			for (uint32_t u = 0; u < pd->uniforms.count; u++)
+				printf("/*   uniform[%u]: contents=%d data=0x%08x */\n",
+					u, (int)pd->uniforms.contents[u], pd->uniforms.data[u]);
+		}
+	}
+
+	/* ---- CSNOP: empty compute kernel (thread-end only) for CSD liveness ---- */
+	{
+		nir_builder nb = nir_builder_init_simple_shader(MESA_SHADER_COMPUTE,
+			&v3d_options, "csnop");
+		nb.shader->info.workgroup_size[0] = 16;
+		nb.shader->info.workgroup_size[1] = 1;
+		nb.shader->info.workgroup_size[2] = 1;
+		/* no body: just thread-end */
+		nir_shader_gather_info(nb.shader, nir_shader_get_entrypoint(nb.shader));
+		struct nir_lower_compute_system_values_options o = { 0 };
+		nir_lower_compute_system_values(nb.shader, &o);
+		v3d_optimize_nir(NULL, nb.shader);
+		nir_lower_var_copies(nb.shader);
+
+		struct v3d_key k = { 0 };
+		struct v3d_prog_data *pd = NULL;
+		uint32_t sz = 0;
+		uint64_t *ins = v3d_compile(compiler, &k, &pd, nb.shader,
+			dbg_out, NULL, 0, 0, &sz);
+		if (!ins) {
+			fprintf(stderr, "CSNOP compile FAILED\n");
+		}
+		else {
+			uint32_t nn = sz / sizeof(uint64_t);
+			printf("/* CSNOP QPU: %u instructions; threads=%u single_seg=%d uniforms=%u */\n",
+				nn, pd->threads, (int)pd->single_seg, pd->uniforms.count);
+			for (uint32_t i = 0; i < nn; i++)
+				printf("0x%016llxull, /* %s */\n", (unsigned long long)ins[i],
+					v3d_qpu_disasm(&devinfo, ins[i]));
+		}
+	}
+
+	return 0;
+}
diff --git a/src/broadcom/meson.build b/src/broadcom/meson.build
index 33a92828513..f6888f35171 100644
--- a/src/broadcom/meson.build
+++ b/src/broadcom/meson.build
@@ -73,6 +73,14 @@ libbroadcom_v3d = static_library(
   dependencies: [dep_valgrind, dep_thread, idep_mesautil, dep_libdrm],
 )
 
+# Local off-device harness (NOT upstream): compile trivial shaders through the
+# v3d NIR->QPU compiler and dump QPU bytecode. See tools/v3d-shader-tool/.
+executable('v3d_shader_dump', files('compiler/v3d_shader_dump.c'),
+  include_directories : [inc_include, inc_src, inc_broadcom],
+  link_with : [libbroadcom_v3d],
+  dependencies : [idep_nir, idep_mesautil, dep_libdrm, dep_valgrind, dep_thread],
+  build_by_default : true)
+
 subdir('perfcntrs')
 
 if with_perfetto and (with_datasources.contains('v3d') or with_datasources.contains('auto'))
diff --git a/src/broadcom/vulkan/v3dv_bo.c b/src/broadcom/vulkan/v3dv_bo.c
index 1417dda7212..0b090e4100b 100644
--- a/src/broadcom/vulkan/v3dv_bo.c
+++ b/src/broadcom/vulkan/v3dv_bo.c
@@ -295,6 +295,13 @@ v3dv_bo_map_unsynchronized(struct v3dv_device *device,
       return false;
    }
 
+#if defined(__phoenix__)
+   /* Phoenix has no V3D kernel DRM driver: the in-process winsys (phoenix_v3d_ioctl in
+    * v3d_phoenix_winsys.c) backs every BO with uncached, flat-MMU-mapped host memory and
+    * returns the BO's already-mapped CPU virtual address as map.offset. There is no
+    * separate kernel mmap to perform — use the returned address directly. */
+   bo->map = (void *)(uintptr_t)map.offset;
+#else
    bo->map = mmap(NULL, size, PROT_READ | PROT_WRITE, MAP_SHARED,
                   device->pdevice->render_fd, map.offset);
    if (bo->map == MAP_FAILED) {
@@ -302,6 +309,7 @@ v3dv_bo_map_unsynchronized(struct v3dv_device *device,
                 bo->handle, (long long)map.offset, (uint32_t)bo->size);
       return false;
    }
+#endif
    VG(VALGRIND_MALLOCLIKE_BLOCK(bo->map, bo->size, 0, false));
 
    bo->map_size = size;
@@ -346,7 +354,12 @@ v3dv_bo_unmap(struct v3dv_device *device, struct v3dv_bo *bo)
 {
    assert(bo && bo->map && bo->map_size > 0);
 
+#if defined(__phoenix__)
+   /* The winsys owns the BO's mapping (it is freed by GEM_CLOSE -> ioc_close_bo); the va
+    * here was not produced by libc mmap(), so there is nothing to munmap. */
+#else
    munmap(bo->map, bo->map_size);
+#endif
    VG(VALGRIND_FREELIKE_BLOCK(bo->map, 0));
    bo->map = NULL;
    bo->map_size = 0;
diff --git a/src/broadcom/vulkan/v3dv_cmd_buffer.c b/src/broadcom/vulkan/v3dv_cmd_buffer.c
index 56643df63d2..becf42561f1 100644
--- a/src/broadcom/vulkan/v3dv_cmd_buffer.c
+++ b/src/broadcom/vulkan/v3dv_cmd_buffer.c
@@ -521,6 +521,22 @@ v3dv_job_start_frame(struct v3dv_job *job,
 {
    assert(job);
 
+#if defined(__phoenix__)
+   /* Diagnostic: a frame started with a zero dimension yields zero tiles, a
+    * zero-size tile-state BO (NULL), and a NULL deref at submit time. Name the
+    * source once so the offending op (render area vs meta copy/clear/blit) is
+    * identifiable from a boot log. cmd_buffer == NULL marks the device noop job. */
+   if (width == 0 || height == 0 || layers == 0) {
+      static bool warned_zero = false;
+      if (!warned_zero) {
+         mesa_logw("v3dv: v3dv_job_start_frame with zero dimension "
+                   "(w=%u h=%u layers=%u, cmd_buffer=%p) -> no tile state\n",
+                   width, height, layers, (void *)job->cmd_buffer);
+         warned_zero = true;
+      }
+   }
+#endif
+
    /* Start by computing frame tiling spec for this job assuming that
     * double-buffer mode is disabled.
     */
diff --git a/src/broadcom/vulkan/v3dv_device.c b/src/broadcom/vulkan/v3dv_device.c
index 2ee6acafc7f..26a0ce26109 100644
--- a/src/broadcom/vulkan/v3dv_device.c
+++ b/src/broadcom/vulkan/v3dv_device.c
@@ -693,6 +693,13 @@ v3dv_CreateInstance(const VkInstanceCreateInfo *pCreateInfo,
 
    v3d_process_debug_variable();
 
+   /* Phoenix no-WSI fb0 winsys: the TFU (texture formatting unit) buffer->image / image->image
+    * copy path is now implemented in the winsys (v3d_phoenix_winsys.c ioc_submit_tfu, the
+    * DRM_V3D_SUBMIT_TFU ioctl), so V3DV uses its preferred hardware TFU copy for TILED/OPTIMAL
+    * texture uploads, blits and mipmap generation — no DISABLE_TFU override is needed. (It was
+    * previously forced on because the winsys's ioctl dispatch ended `default: return 0`, making
+    * every TFU job a silent no-op, so every sampled texture rendered black — #29.) */
+
    instance->vk.physical_devices.enumerate = enumerate_devices;
    instance->vk.physical_devices.destroy = destroy_physical_device;
 
@@ -864,6 +871,19 @@ device_has_expected_features(struct v3dv_physical_device *device)
 static VkResult
 init_uuids(struct v3dv_physical_device *device)
 {
+#if defined(__phoenix__)
+   /* Phoenix first-light: no .note.gnu.build-id is resolvable on the statically-
+    * loaded image, and the upstream build_id_data()+blake3 path dereferences a
+    * synthetic note header -> wild pointer -> fault. These UUIDs only gate pipeline-
+    * cache validity and cross-process memory sharing, neither of which is used at
+    * first light (no persistent cache, single process). Use fixed dummy UUIDs so
+    * device-create succeeds; revisit for a real build-id when caching matters. */
+   memset(device->driver_build_sha1, 0x5a, sizeof(device->driver_build_sha1));
+   memset(device->pipeline_cache_uuid, 0xb3, VK_UUID_SIZE);
+   memset(device->driver_uuid, 0xd5, VK_UUID_SIZE);
+   memset(device->device_uuid, 0xde, VK_UUID_SIZE);
+   return VK_SUCCESS;
+#else
    const struct build_id_note *note =
       build_id_find_nhdr_for_addr(init_uuids);
    if (!note) {
@@ -915,6 +935,7 @@ init_uuids(struct v3dv_physical_device *device)
    memcpy(device->device_uuid, blake3, VK_UUID_SIZE);
 
    return VK_SUCCESS;
+#endif /* __phoenix__ */
 }
 
 static void
@@ -1400,6 +1421,12 @@ get_device_properties(const struct v3dv_physical_device *device,
 
 }
 
+#if defined(__phoenix__)
+/* Inert render-fd token for the Phoenix winsys (no /dev/dri node). Any value works;
+ * phoenix_v3d_ioctl ignores the fd. Nonzero so it never looks like a closed fd. */
+#define V3DV_PHOENIX_FAKE_RENDER_FD 0x763364 /* 'v3d' */
+#endif
+
 static VkResult
 create_physical_device(struct v3dv_instance *instance,
                        int32_t primary_fd, int32_t render_fd,
@@ -1426,6 +1453,14 @@ create_physical_device(struct v3dv_instance *instance,
    if (result != VK_SUCCESS)
       goto fail;
 
+#if defined(__phoenix__)
+   /* No /dev/dri nodes to stat on Phoenix; the render fd is an inert token routed to
+    * phoenix_v3d_ioctl. Synthesize device ids so init can proceed. */
+   (void)primary_fd;
+   device->has_primary = false;
+   device->has_render = true;
+   device->render_devid = 0;
+#else
    struct stat primary_stat = {0}, render_stat = {0};
    if (fstat(primary_fd, &primary_stat) != 0) {
       result = vk_errorf(instance, VK_ERROR_INITIALIZATION_FAILED,
@@ -1443,6 +1478,7 @@ create_physical_device(struct v3dv_instance *instance,
 
    device->has_render = true;
    device->render_devid = render_stat.st_rdev;
+#endif
 
 #if USE_V3D_SIMULATOR
    device->sim_file = v3d_simulator_init(render_fd);
@@ -1458,6 +1494,16 @@ create_physical_device(struct v3dv_instance *instance,
       drmFreeVersion(version);
    }
 
+#if defined(__phoenix__)
+   /* The Phoenix-RTOS winsys submits synchronously (it blocks on FLDONE/FRDONE per
+    * job) and provides only no-op DRM syncobjs — there is no real syncobj/timeline
+    * backing for a threaded submit. is_shim selects exactly the no-threaded-submit
+    * path (queue_init + CreateDevice both gate vk_*_enable_*submit_thread on !is_shim),
+    * so force it true. Without this, threaded submit is enabled and the device-create
+    * noop-job submit waits forever on a syncobj that never signals (hang). */
+   device->is_shim = true;
+#endif
+
    if (!v3d_get_device_info(device->render_fd, &device->devinfo, &v3d_ioctl)) {
       result = vk_errorf(instance, VK_ERROR_INITIALIZATION_FAILED,
                          "Failed to get info from device.");
@@ -1685,6 +1731,15 @@ enumerate_devices(struct vk_instance *vk_instance)
    struct v3dv_instance *instance =
       container_of(vk_instance, struct v3dv_instance, vk);
 
+#if defined(__phoenix__)
+   /* Phoenix-RTOS (RPi4 V3DV port): there is no /dev/dri node and no real libdrm
+    * device enumeration. The Phoenix winsys (tools/v3d-driver-port/) routes every
+    * v3d ioctl through phoenix_v3d_ioctl regardless of fd, so the render fd is an
+    * inert token. Bypass the drmGetDevices2 scan and create the physical device
+    * directly. primary/display fd = -1 (no KMS; the custom /dev/fb0 swapchain at
+    * Tier 4 handles present). See docs/inprogress/2026-06-17-vulkan-v3dv-tier0-progress.md. */
+   return create_physical_device(instance, -1, V3DV_PHOENIX_FAKE_RENDER_FD, -1);
+#else
    /* FIXME: Check for more devices? */
    drmDevicePtr devices[8];
    int max_devices;
@@ -1746,6 +1801,7 @@ enumerate_devices(struct vk_instance *vk_instance)
    drmFreeDevices(devices, max_devices);
 
    return result;
+#endif /* !__phoenix__ */
 }
 
 uint32_t
diff --git a/src/broadcom/vulkan/v3dv_meta_copy.c b/src/broadcom/vulkan/v3dv_meta_copy.c
index f43181cb394..11846cdf3ec 100644
--- a/src/broadcom/vulkan/v3dv_meta_copy.c
+++ b/src/broadcom/vulkan/v3dv_meta_copy.c
@@ -21,6 +21,8 @@
  * IN THE SOFTWARE.
  */
 
+#include <stdlib.h> /* getenv (V3DV_DBGCOPY trace gate) */
+
 #include "v3dv_device.h"
 #include "v3dv_cmd_buffer.h"
 #include "v3dv_image.h"
@@ -1943,6 +1945,10 @@ copy_buffer_image_tfu(struct v3dv_cmd_buffer *cmd_buffer,
     */
    if (cmd_buffer->device->devinfo.ver < 71 &&
        (to_buffer || !image->tiled)) {
+#if defined(__phoenix__)
+      fprintf(stderr, "v3dv: TFU declined (ver<71 %s)\n",
+              to_buffer ? "image->buffer" : "non-tiled dst");
+#endif
       return false;
    }
 
@@ -1961,8 +1967,12 @@ copy_buffer_image_tfu(struct v3dv_cmd_buffer *cmd_buffer,
    /* Region must include full slice */
    const uint32_t offset_x = region->imageOffset.x;
    const uint32_t offset_y = region->imageOffset.y;
-   if (offset_x != 0 || offset_y != 0)
+   if (offset_x != 0 || offset_y != 0) {
+#if defined(__phoenix__)
+      fprintf(stderr, "v3dv: TFU declined (imageOffset %u,%u != 0,0)\n", offset_x, offset_y);
+#endif
       return false;
+   }
 
    uint32_t width, height;
    if (region->bufferRowLength == 0)
@@ -1995,6 +2005,11 @@ copy_buffer_image_tfu(struct v3dv_cmd_buffer *cmd_buffer,
     */
    if (region->imageExtent.width != slice->width ||
        region->imageExtent.height != slice->height) {
+#if defined(__phoenix__)
+      fprintf(stderr, "v3dv: TFU declined (region %ux%u != slice %ux%u — not full slice)\n",
+              region->imageExtent.width, region->imageExtent.height,
+              slice->width, slice->height);
+#endif
       return false;
    }
 
@@ -3385,6 +3400,20 @@ copy_buffer_to_image_shader(struct v3dv_cmd_buffer *cmd_buffer,
    }
 }
 
+#if defined(__phoenix__)
+/* Per-region copy-path tracer (#29 striping investigation). Uploading a Quake map records
+ * hundreds of textures x 6-7 mip regions each; printing every region to the ~115200-baud UART
+ * dominated map-load wall time. Gate the trace behind V3DV_DBGCOPY (checked once) so it is
+ * silent — and fast — by default, and re-enableable when the striping work resumes. */
+static bool v3dv_trace_copy(void)
+{
+   static int on = -1;
+   if (on < 0)
+      on = getenv("V3DV_DBGCOPY") != NULL;
+   return on != 0;
+}
+#endif
+
 VKAPI_ATTR void VKAPI_CALL
 v3dv_CmdCopyBufferToImage2(VkCommandBuffer commandBuffer,
                               const VkCopyBufferToImageInfo2 *info)
@@ -3404,12 +3433,49 @@ v3dv_CmdCopyBufferToImage2(VkCommandBuffer commandBuffer,
        * where we are copying full images, since they should be the fastest.
        */
       uint32_t batch_size = 1;
+#if defined(__phoenix__)
+      /* Phoenix copy-path tracer (#29): the prior boot showed every region as 0x1 / 1x0
+       * (degenerate), so the copy uploads no pixels and textures sample black. Disambiguate
+       * a real zero-extent from a logging/struct artifact by printing ALL of: region extent
+       * (w,h,d), region imageOffset (x,y,z), and the DESTINATION IMAGE's own created extent
+       * (image->vk.extent) + mip0 slice dims. If the image is 128x128 but the region extent
+       * is 0 -> the VkBufferImageCopy region is built/marshalled wrong (vkQuake side or the
+       * legacy->2 trampoline); if the image itself is 0x1 -> an image-creation bug. */
+      {
+         const struct v3d_resource_slice *s0 = &image->planes[0].slices[
+            info->pRegions[r].imageSubresource.mipLevel];
+         if (v3dv_trace_copy())
+         fprintf(stderr, "v3dv: CmdCopyBufferToImage region %u/%u extent=%ux%ux%u off=%d,%d,%d "
+                 "buf(off=%llu rowlen=%u imgh=%u) | IMAGE create=%ux%ux%u type=%u fmt=%u tiled=%d "
+                 "slice0=%ux%u -> trying TFU\n",
+                 r, info->regionCount,
+                 info->pRegions[r].imageExtent.width, info->pRegions[r].imageExtent.height,
+                 info->pRegions[r].imageExtent.depth,
+                 info->pRegions[r].imageOffset.x, info->pRegions[r].imageOffset.y,
+                 info->pRegions[r].imageOffset.z,
+                 (unsigned long long)info->pRegions[r].bufferOffset,
+                 info->pRegions[r].bufferRowLength, info->pRegions[r].bufferImageHeight,
+                 image->vk.extent.width, image->vk.extent.height, image->vk.extent.depth,
+                 image->vk.image_type, image->vk.format, image->tiled,
+                 s0->width, s0->height);
+      }
+#endif
       if (copy_buffer_image_tfu(cmd_buffer, image, buffer, &info->pRegions[r],
-                                false /* to_buffer */))
+                                false /* to_buffer */)) {
+#if defined(__phoenix__)
+         if (v3dv_trace_copy())
+         fprintf(stderr, "v3dv: CmdCopyBufferToImage region %u -> path=TFU\n", r);
+#endif
          goto handled;
+      }
 
-      if (copy_buffer_to_image_tlb(cmd_buffer, image, buffer, &info->pRegions[r]))
+      if (copy_buffer_to_image_tlb(cmd_buffer, image, buffer, &info->pRegions[r])) {
+#if defined(__phoenix__)
+         if (v3dv_trace_copy())
+         fprintf(stderr, "v3dv: CmdCopyBufferToImage region %u -> path=TLB\n", r);
+#endif
          goto handled;
+      }
 
       /* Otherwise, we are copying subrects, so we fallback to copying
        * via shader and texel buffers and we try to batch the regions
@@ -3439,6 +3505,10 @@ v3dv_CmdCopyBufferToImage2(VkCommandBuffer commandBuffer,
 
       if (copy_buffer_to_image_shader(cmd_buffer, image, buffer,
                                       batch_size, &info->pRegions[r], true)) {
+#if defined(__phoenix__)
+         if (v3dv_trace_copy())
+         fprintf(stderr, "v3dv: CmdCopyBufferToImage region %u -> path=SHADER(texel)\n", r);
+#endif
          goto handled;
       }
 
@@ -3450,6 +3520,10 @@ v3dv_CmdCopyBufferToImage2(VkCommandBuffer commandBuffer,
        */
       if (copy_buffer_to_image_shader(cmd_buffer, image, buffer,
                                       batch_size, &info->pRegions[r], false)) {
+#if defined(__phoenix__)
+         if (v3dv_trace_copy())
+         fprintf(stderr, "v3dv: CmdCopyBufferToImage region %u -> path=SHADER(blit)\n", r);
+#endif
          goto handled;
       }
 
diff --git a/src/broadcom/vulkan/v3dv_queue.c b/src/broadcom/vulkan/v3dv_queue.c
index d1b2e4d0c97..fbce1015a3d 100644
--- a/src/broadcom/vulkan/v3dv_queue.c
+++ b/src/broadcom/vulkan/v3dv_queue.c
@@ -998,6 +998,30 @@ queue_handle_job(struct v3dv_queue *queue,
       job->needs_bcl_sync = job->type == V3DV_JOB_TYPE_GPU_CL;
    }
 
+#if defined(__phoenix__)
+   /* Skip a degenerate CL job that has no tile state. A CL job's tile state is
+    * allocated from the frame tiling (layers * tiles_x * tiles_y); when one of
+    * those dimensions is zero the tile-state size is zero and v3dv_bo_alloc()
+    * returns NULL (see v3dv_job_allocate_tile_state / v3d_tile_alloc_sizes).
+    * handle_cl_job() then dereferences job->tile_state->offset and faults. Such a
+    * job (a zero-extent meta copy/clear/blit, or an empty render area) has nothing
+    * for the binner or renderer to do, so dropping it is correct and lets the real
+    * render job in the same submit proceed. Without this guard the no-WSI vkQuake
+    * 2D frame path hit a NULL-tile_state CL job at the first vkQueueSubmit and
+    * Data-Aborted in handle_cl_job (far=0x24 == offsetof v3dv_bo, offset). */
+   if (job->type == V3DV_JOB_TYPE_GPU_CL && job->tile_state == NULL) {
+      static bool warned = false;
+      if (!warned) {
+         mesa_logw("v3dv: skipping degenerate CL job with no tile state "
+                   "(zero-extent frame: w=%u h=%u layers=%u draws=%u)\n",
+                   job->frame_tiling.width, job->frame_tiling.height,
+                   job->frame_tiling.layers, job->draw_count);
+         warned = true;
+      }
+      return VK_SUCCESS;
+   }
+#endif
+
    switch (job->type) {
    case V3DV_JOB_TYPE_GPU_CL:
       return handle_cl_job(queue, job, counter_pass_idx, sync_info);
diff --git a/src/gallium/auxiliary/util/u_vbuf.c b/src/gallium/auxiliary/util/u_vbuf.c
index 882f683e2a3..ae7dfe409e5 100644
--- a/src/gallium/auxiliary/util/u_vbuf.c
+++ b/src/gallium/auxiliary/util/u_vbuf.c
@@ -468,6 +468,18 @@ u_vbuf_translate_buffers(struct u_vbuf *mgr, struct translate_key *key,
    /* Get a translate object. */
    tr = translate_cache_find(mgr->translate_cache, key);
 
+   /* translate_cache_find() -> translate_create() -> translate_generic_create()
+    * can return NULL — e.g. for a pure-integer input format with an illegal
+    * int->output combo (is_legal_int_format_combo). Upstream never checks because
+    * on x86 translate_sse covers everything; on aarch64 (Phoenix-RTOS RPi4/V3D)
+    * only translate_generic exists, so an unsupported combo yields NULL and the
+    * unchecked tr->set_buffer()/tr->run() below dereferenced it -> crash (owner HW:
+    * quake3 SIGSEGV mid-combat, u_vbuf_translate_begin, far=0x3a0). Fail the
+    * translate cleanly instead; the caller (u_vbuf_translate_begin) treats a
+    * failure as "skip this draw's translate" rather than crashing. */
+   if (!tr)
+      return PIPE_ERROR;
+
    /* Map buffers we want to translate. */
    mask = vb_mask;
    while (mask) {
@@ -1669,6 +1681,20 @@ void u_vbuf_draw_vbo(struct pipe_context *pipe, const struct pipe_draw_info *inf
              * performance. */
             if (!indirect &&
                 !new_info.primitive_restart &&
+#if defined(__phoenix__)
+                /* Phoenix-RTOS RPi4: do NOT take the index-unrolling
+                 * optimisation.  On HW it is the ONLY reason u_vbuf enters the
+                 * translate path for Quake III's world surfaces (the report says
+                 * incompat_vb=0, incompat_elem=0, misaligned=0, unroll=1), and
+                 * u_vbuf_translate_begin() then fails and SKIPS pipe->draw_vbo
+                 * entirely -- the surface is silently dropped, which is why the
+                 * world rendered black.  Unrolling is a pure upload-ratio
+                 * optimisation, so declining it is always correct; the draw takes
+                 * the ordinary indexed path instead.  (Why translate_generic
+                 * cannot build that key here is still open -- aarch64 has no
+                 * translate_sse, so the generic path is the only one.) */
+                false &&
+#endif
                 util_is_vbo_upload_ratio_too_large(new_draw.count, num_vertices) &&
                 !u_vbuf_mapping_vertex_buffer_blocks(mgr, misaligned)) {
                unroll_indices = true;
@@ -1694,7 +1720,34 @@ void u_vbuf_draw_vbo(struct pipe_context *pipe, const struct pipe_draw_info *inf
          if (!u_vbuf_translate_begin(mgr, &new_info, &new_draw,
                                      start_vertex, num_vertices,
                                      min_index, unroll_indices, misaligned, &releasebuf)) {
-            debug_warn_once("u_vbuf_translate_begin() failed");
+            /* Phoenix-RTOS RPi4: this failure path SKIPS pipe->draw_vbo entirely,
+             * so the draw is silently dropped -- which on HW showed up as Quake
+             * III's world rendering BLACK during a bot match (the multitexture
+             * path adds a second texcoord element).  debug_warn_once is a no-op in
+             * release builds, so it failed with no trace at all.  Say so, once,
+             * unconditionally, and name the element that could not be translated. */
+            {
+               static bool phx_reported = false;
+
+               if (!phx_reported) {
+                  unsigned i;
+
+                  phx_reported = true;
+                  fprintf(stderr, "u_vbuf: TRANSLATE FAILED - DRAW DROPPED "
+                          "unroll=%d incompat_vb=0x%x incompat_elem=0x%x "
+                          "misaligned=0x%x nelem=%u\n",
+                          (int)unroll_indices, incompatible_vb_mask,
+                          mgr->ve->incompatible_elem_mask, misaligned,
+                          mgr->ve->count);
+                  for (i = 0; i < mgr->ve->count; i++) {
+                     fprintf(stderr, "u_vbuf:   ve[%u] fmt=%s off=%u stride=%u vb=%u\n",
+                             i, util_format_short_name(mgr->ve->ve[i].src_format),
+                             mgr->ve->ve[i].src_offset,
+                             mgr->ve->ve[i].src_stride,
+                             mgr->ve->ve[i].vertex_buffer_index);
+                  }
+               }
+            }
             goto out;
          }
 
diff --git a/src/gallium/drivers/v3d/v3d_blit.c b/src/gallium/drivers/v3d/v3d_blit.c
index c5c07d1deb0..68c38ad1092 100644
--- a/src/gallium/drivers/v3d/v3d_blit.c
+++ b/src/gallium/drivers/v3d/v3d_blit.c
@@ -26,6 +26,7 @@
 #include "util/perf/cpu_trace.h"
 #include "util/u_surface.h"
 #include "util/u_blitter.h"
+#include "util/bitscan.h"
 #include "compiler/nir/nir_builder.h"
 #include "v3d_context.h"
 #include "broadcom/common/v3d_tiling.h"
@@ -252,6 +253,21 @@ v3d_generate_mipmap(struct pipe_context *pctx,
         if (prsc->target == PIPE_TEXTURE_3D)
                 return false;
 
+        /* Phoenix/RPi4 workaround (Quake II NPOT model skins rendered scrambled
+         * at distance): the TFU multi-level mipmap generator supplies OPAD only
+         * for level 0 and INFERS the tiling of levels >=1 internally. That
+         * inference only matches what v3d_setup_slices/the TMU sampler
+         * reconstruct when the base dimensions are powers of two; for NPOT bases
+         * (which carry ub_pad and pot_width/pot_height padding on the small
+         * UBLINEAR/LINEARTILE levels) the generated layout diverges from the
+         * sampled layout, so those mip levels read as garbage. Decline NPOT here
+         * so st_generate_mipmap falls back to the render-based util_gen_mipmap
+         * path, which lays out each level via its own correctly-tiled surface.
+         * POT textures keep the fast TFU path (this guard is a no-op for them). */
+        if (!util_is_power_of_two_or_zero(prsc->width0) ||
+            !util_is_power_of_two_or_zero(prsc->height0))
+                return false;
+
         struct v3d_context *v3d = v3d_context(pctx);
         struct v3d_screen *screen = v3d->screen;
         struct v3d_device_info *devinfo = &screen->devinfo;
diff --git a/src/gallium/drivers/v3d/v3d_bufmgr.c b/src/gallium/drivers/v3d/v3d_bufmgr.c
index e82fd069b18..0ec757b6db0 100644
--- a/src/gallium/drivers/v3d/v3d_bufmgr.c
+++ b/src/gallium/drivers/v3d/v3d_bufmgr.c
@@ -117,9 +117,18 @@ v3d_bo_from_cache(struct v3d_screen *screen, uint32_t size, const char *name)
 
 struct v3d_bo *
 v3d_bo_alloc(struct v3d_screen *screen, uint32_t size, const char *name)
+{
+        return v3d_bo_alloc_flags(screen, size, name, 0);
+}
+
+struct v3d_bo *
+v3d_bo_alloc_flags(struct v3d_screen *screen, uint32_t size, const char *name,
+                   uint32_t create_flags)
 {
         struct v3d_bo *bo;
         int ret;
+        bool cacheable = (create_flags & V3D_CREATE_BO_CACHEABLE) != 0;
+        bool scanout = (create_flags & V3D_CREATE_BO_SCANOUT) != 0;
 
         /* The CLIF dumping requires that there is no whitespace in the name.
          */
@@ -127,7 +136,9 @@ v3d_bo_alloc(struct v3d_screen *screen, uint32_t size, const char *name)
 
         size = align(size, 4096);
 
-        bo = v3d_bo_from_cache(screen, size, name);
+        /* Cacheable BOs bypass the BO cache so they can never be reused as an
+         * uncached GPU-read buffer (which would silently read stale CPU lines). */
+        bo = (cacheable || scanout) ? NULL : v3d_bo_from_cache(screen, size, name);
         if (bo) {
                 if (dump_stats) {
                         mesa_logd("Allocated %s %dkb from cache:", name, size / 1024);
@@ -147,7 +158,8 @@ v3d_bo_alloc(struct v3d_screen *screen, uint32_t size, const char *name)
         bo->private = true;
 
         struct drm_v3d_create_bo create = {
-                .size = size
+                .size = size,
+                .flags = create_flags
         };
 
  retry:
@@ -167,6 +179,8 @@ v3d_bo_alloc(struct v3d_screen *screen, uint32_t size, const char *name)
 
         bo->handle = create.handle;
         bo->offset = create.offset;
+        bo->cacheable = cacheable;
+        bo->scanout = scanout;
 
         screen->bo_count++;
         screen->bo_size += bo->size;
@@ -203,7 +217,34 @@ v3d_bo_free(struct v3d_bo *bo)
                 } else
 #endif
                 {
+#if defined(__phoenix__)
+                        /* The winsys owns the BO's mapping (it is freed by
+                         * GEM_CLOSE -> ioc_close_bo); the address here was not
+                         * produced by libc mmap(), so there is nothing to
+                         * munmap. Same contract as v3dv_bo_unmap() in the
+                         * Vulkan driver, which already carries this guard.
+                         *
+                         * Unmapping it anyway returns the pages to the kernel
+                         * behind the winsys's back: W.bos[slot] still has
+                         * used=1, a non-NULL ->cpu and VALID GPU PTEs, and
+                         * gpuva_to_cpu() keeps handing that pointer out. malloc
+                         * can then take the same pages for a heap, which is how
+                         * a 4-byte store through the stale pointer lands on
+                         * allocator metadata.
+                         *
+                         * This also defeated the winsys fix that deliberately
+                         * stopped recycling a closed BO's CPU address: with
+                         * V3D_UNMAP_CLOSED_BO unset (it is set nowhere), this
+                         * was the ONLY path returning BO pages to the kernel.
+                         *
+                         * Worse for imported BOs, where bo->size is a
+                         * caller-supplied stride*height that is not page
+                         * aligned -- that unmapped a partial range out of the
+                         * HEAD of a mapping the winsys still owned.
+                         */
+#else
                         munmap(bo->map, bo->size);
+#endif
                         VG(VALGRIND_FREELIKE_BLOCK(bo->map, 0));
                 }
         }
@@ -277,7 +318,9 @@ v3d_bo_last_unreference_locked_timed(struct v3d_bo *bo, time_t time)
         struct v3d_bo_cache *cache = &screen->bo_cache;
         uint32_t page_index = bo->size / 4096 - 1;
 
-        if (!bo->private) {
+        if (!bo->private || bo->cacheable || bo->scanout) {
+                /* cacheable / scanout BOs must not enter the (uncached) BO cache
+                 * (scanout aliases a fixed framebuffer PA). */
                 v3d_bo_free(bo);
                 return;
         }
@@ -418,6 +461,47 @@ v3d_bo_open_dmabuf(struct v3d_screen *screen, int fd)
         return v3d_bo_open_handle(screen, handle, size);
 }
 
+#if defined(__phoenix__)
+/*
+ * Phoenix-RTOS: open a BO the CALLER did not create, by its daemon handle.
+ *
+ * Neither sibling above works on this port: there is no GEM_OPEN (flink names)
+ * and no dmabuf/PRIME. What Phoenix has instead is a v3d-srv daemon that owns
+ * every BO in one GLOBAL handle namespace -- `W.bos[]`, `handle == slot + 1`,
+ * looked up with no per-client scoping -- so a handle is already meaningful in
+ * any process, and no import ioctl is needed to make it so.
+ *
+ * Verified on hardware before this was written (tools/boshare-probe): a second
+ * process MMAP_BOs a handle it did not create and gets the SAME physical pages
+ * with writes visible both directions, and GET_BO_OFFSET on that foreign handle
+ * returns the SAME non-zero GPU VA. Those are the only two daemon calls the
+ * import path needs -- GET_BO_OFFSET here, and MMAP_BO later when the BO is
+ * first mapped -- so v3d_bo_open_handle() below works unchanged.
+ *
+ * Caller-supplied `size` because the daemon's size is not reachable through the
+ * DRM ioctl surface (`drm_v3d_mmap_bo` carries {handle, flags, offset} and no
+ * size) and `struct winsys_handle` has no size field. The importer passes
+ * stride*height, i.e. exactly the region it intends to use: if the real BO is
+ * larger, the only consequence is that v3d_resource_from_handle's
+ * offset-overflow check becomes stricter, which is the safe direction.
+ */
+struct v3d_bo *
+v3d_bo_open_phoenix_handle(struct v3d_screen *screen, uint32_t handle,
+                           uint32_t size)
+{
+        if (handle == 0 || size == 0) {
+                mesa_loge("Phoenix BO import: bad handle %u / size %u",
+                          handle, size);
+                return NULL;
+        }
+
+        /* v3d_bo_open_handle() requires this held and unlocks it on exit. */
+        mtx_lock(&screen->bo_handles_mutex);
+
+        return v3d_bo_open_handle(screen, handle, size);
+}
+#endif /* __phoenix__ */
+
 int
 v3d_bo_get_dmabuf(struct v3d_bo *bo)
 {
@@ -515,13 +599,10 @@ v3d_bo_map_unsynchronized(struct v3d_bo *bo)
                 abort();
         }
 
-        bo->map = mmap(NULL, bo->size, PROT_READ | PROT_WRITE, MAP_SHARED,
-                       bo->screen->fd, offset);
-        if (bo->map == MAP_FAILED) {
-                mesa_loge("mmap of bo %d (offset 0x%016llx, size %d) failed",
-                          bo->handle, (long long)offset, bo->size);
-                abort();
-        }
+        /* Phoenix winsys: BOs are already CPU-mapped (uncached, MAP_CONTIGUOUS) at
+         * CREATE_BO time, and MMAP_BO returns that CPU virtual address as `offset`.
+         * Use it directly — re-mmap()ing our non-device fd would fault/hang. */
+        bo->map = (void *)(uintptr_t)offset;
         VG(VALGRIND_MALLOCLIKE_BLOCK(bo->map, bo->size, 0, false));
 
         return bo->map;
diff --git a/src/gallium/drivers/v3d/v3d_bufmgr.h b/src/gallium/drivers/v3d/v3d_bufmgr.h
index 5363bf92fcb..b59ff2a1f6d 100644
--- a/src/gallium/drivers/v3d/v3d_bufmgr.h
+++ b/src/gallium/drivers/v3d/v3d_bufmgr.h
@@ -54,14 +54,37 @@ struct v3d_bo {
          * it's safe to reuse it in the BO cache).
          */
         bool private;
+
+        /* Phoenix: CPU mapping is cacheable (vs the default uncached DMA mapping).
+         * Used only for GPU-written / CPU-read-back render targets so the CPU
+         * readback (detile) hits cache instead of crawling uncached DRAM. Such BOs
+         * bypass the BO cache (they must not be reused as uncached GPU-read buffers)
+         * and are invalidated (dc ivac) before each CPU read. */
+        bool cacheable;
+
+        /* Phoenix: BO is backed by the HDMI scanout framebuffer's physical pages
+         * (render-to-scanout) — the GPU stores straight to the displayed surface, so
+         * there is no CPU readback/blit. Like cacheable, such a BO bypasses the BO
+         * cache (it aliases a fixed PA and must never be reused as a generic buffer). */
+        bool scanout;
 };
 
+/* Phoenix: drm_v3d_create_bo.flags bits. */
+#define V3D_CREATE_BO_CACHEABLE (1u << 0)
+#define V3D_CREATE_BO_SCANOUT   (1u << 1)
+
 struct v3d_bo *v3d_bo_alloc(struct v3d_screen *screen, uint32_t size,
                             const char *name);
+struct v3d_bo *v3d_bo_alloc_flags(struct v3d_screen *screen, uint32_t size,
+                                  const char *name, uint32_t create_flags);
 void v3d_bo_last_unreference(struct v3d_bo *bo);
 void v3d_bo_last_unreference_locked_timed(struct v3d_bo *bo, time_t time);
 struct v3d_bo *v3d_bo_open_name(struct v3d_screen *screen, uint32_t name);
 struct v3d_bo *v3d_bo_open_dmabuf(struct v3d_screen *screen, int fd);
+#if defined(__phoenix__)
+struct v3d_bo *v3d_bo_open_phoenix_handle(struct v3d_screen *screen,
+                                          uint32_t handle, uint32_t size);
+#endif
 bool v3d_bo_flink(struct v3d_bo *bo, uint32_t *name);
 int v3d_bo_get_dmabuf(struct v3d_bo *bo);
 
diff --git a/src/gallium/drivers/v3d/v3d_context.c b/src/gallium/drivers/v3d/v3d_context.c
index d639c688637..fefb9046cb9 100644
--- a/src/gallium/drivers/v3d/v3d_context.c
+++ b/src/gallium/drivers/v3d/v3d_context.c
@@ -69,7 +69,10 @@ v3d_pipe_flush(struct pipe_context *pctx, struct pipe_fence_handle **fence,
                  */
                 drmSyncobjExportSyncFile(v3d->fd, v3d->out_sync, &fd);
                 if (fd == -1) {
-                        mesa_loge("Export failed");
+                        /* Bare-metal Phoenix port: no DRM syncobj/sync-file FD export. The
+                         * synchronous winsys submit already guarantees GPU completion, so a
+                         * NULL fence is correct here. Do NOT log — this fires on every flush
+                         * and floods the UART with 100k+ lines, crippling the boot. */
                         *fence = NULL;
                         return;
                 }
diff --git a/src/gallium/drivers/v3d/v3d_resource.c b/src/gallium/drivers/v3d/v3d_resource.c
index 33df0d07be5..52d54969f93 100644
--- a/src/gallium/drivers/v3d/v3d_resource.c
+++ b/src/gallium/drivers/v3d/v3d_resource.c
@@ -110,8 +110,39 @@ v3d_resource_bo_alloc(struct v3d_resource *rsc)
          */
         uint32_t padding =
                 rsc->base.target == PIPE_BUFFER ? 4 : V3D_TFU_READAHEAD_SIZE;
-        bo = v3d_bo_alloc(v3d_screen(pscreen), rsc->size + padding,
-                          "resource");
+        /* Phoenix: a color render target that is NOT sampled is GPU-written and only
+         * ever CPU-read-back (glReadPixels) — never CPU-written and never GPU-read. Map
+         * it cacheable so the readback detile hits cache instead of crawling uncached
+         * DRAM (the dominant present-path cost). Excludes sampled textures + depth, which
+         * stay uncached so the GPU reads them coherently. Invalidated before CPU read in
+         * v3d_resource_transfer_map. */
+        /* Cacheable-readback for render targets (would cut the uncached glReadPixels
+         * detile, the dominant present-path cost) is DISABLED: making a GPU-rendered RT
+         * BO CPU-cacheable hangs the GPU/render after the first cacheable readback — a
+         * coherency wall (NOT contiguity: the BO is contiguous), pending a focused
+         * deep-dive. Re-enable by setting this to the RENDER_TARGET rule. See
+         * docs/inprogress/2026-06-15-td16-cache-enable-plan.md. */
+        /* Cacheable-readback for the tiled color RT is DISABLED: it works (with dc CIVAC;
+         * dc IVAC is EL1-only and traps at EL0) but gives NO speedup — measured readpx is
+         * identical cached vs uncached (~23ms). The cost is the detile's SCATTERED tiled
+         * read pattern (each output pixel from a non-contiguous tiled address -> no cache-
+         * line reuse), not the uncached read. The FPS lever must move the detile to the GPU
+         * (render-to-scanout / TFU-to-linear, after which a LINEAR CPU read would benefit
+         * from caching). The cacheable infra (v3d_bo_alloc_flags + civac) is kept and proven
+         * for that + the NFS GENET-RX path. See docs/inprogress/2026-06-15-td16-cache-enable-plan.md. */
+        /* Phoenix: the full-screen color RT is forced RASTER (see should_tile). RENDER-TO-
+         * SCANOUT: back it with the HDMI framebuffer's physical pages so the GPU raster-stores
+         * straight to the displayed surface — no per-frame glReadPixels/blit/fb0 CPU copies
+         * (those were the ~25ms readpx bottleneck at 1080p; the 8.3 MB RT exceeds the A72 L2 so
+         * even a cacheable readback is DRAM-bandwidth-bound). The winsys maps GPU VA -> scanout
+         * PA; coherency to the display is the existing per-submit SLCACTL + L2T flush. The
+         * cacheable-readback infra (v3d_bo_alloc_flags + civac) stays proven for the NFS GENET-RX
+         * path. See docs/inprogress/2026-06-15-td16-cache-enable-plan.md. */
+        uint32_t create_flags =
+                ((rsc->base.bind & PIPE_BIND_RENDER_TARGET) &&
+                 rsc->base.width0 >= 1024 && rsc->base.height0 >= 768) ? V3D_CREATE_BO_SCANOUT : 0;
+        bo = v3d_bo_alloc_flags(v3d_screen(pscreen), rsc->size + padding,
+                          "resource", create_flags);
         if (bo) {
                 v3d_bo_unreference(&rsc->bo);
                 rsc->bo = bo;
@@ -256,6 +287,25 @@ v3d_map_usage_prep(struct pipe_context *pctx,
         }
 }
 
+/* Phoenix: invalidate a cacheable BO's CPU cache lines so a following CPU read sees
+ * what the (non-coherent) GPU wrote to DRAM, not stale cache. The BO is page-aligned
+ * and page-sized, so the [base, base+size) range is cache-line aligned (no partial-line
+ * loss); and these BOs are CPU-read-only (never CPU-written), so invalidate is safe.
+ * Requires SCTLR_EL1.UCI=1 (EL0 cache maintenance), set by the kernel. */
+static void
+v3d_bo_invalidate_for_cpu_read(struct v3d_bo *bo)
+{
+        uintptr_t a = (uintptr_t)bo->map;
+        uintptr_t end = a + bo->size;
+        /* Use DC CIVAC (clean+invalidate), NOT DC IVAC: invalidate-only is EL1-privileged
+         * and traps at EL0 even with SCTLR.UCI=1 (UCI gates CVAU/CVAC/CVAP/CIVAC/IVAU, not
+         * IVAC). This BO is CPU-read-only so there are no dirty lines — the clean is a
+         * no-op and CIVAC's invalidate makes the next read fetch the GPU's DRAM data. */
+        for (; a < end; a += 64)
+                __asm__ volatile("dc civac, %0" :: "r"(a) : "memory");
+        __asm__ volatile("dsb ish" ::: "memory");
+}
+
 static void *
 v3d_resource_transfer_map(struct pipe_context *pctx,
                           struct pipe_resource *prsc,
@@ -316,6 +366,11 @@ v3d_resource_transfer_map(struct pipe_context *pctx,
                 goto fail;
         }
 
+        /* Phoenix: for a cacheable readback RT the GPU wrote DRAM non-coherently;
+         * invalidate stale CPU lines so the detile/copy below reads the new pixels. */
+        if ((usage & PIPE_MAP_READ) && rsc->bo->cacheable)
+                v3d_bo_invalidate_for_cpu_read(rsc->bo);
+
         *pptrans = ptrans;
 
         /* Our load/store routines work on entire compressed blocks. */
@@ -802,6 +857,11 @@ v3d_resource_setup(struct pipe_screen *pscreen,
         return rsc;
 }
 
+/* Phoenix winsys (v3d_phoenix_winsys.c, linked into libv3d-phoenix.a): is the NEXT BO the
+ * HDMI scanout surface? Used below to keep the scanout render target RASTER (the HVS display
+ * cannot scan a UIF-tiled surface). */
+int v3d_phoenix_peek_next_scanout(void);
+
 static struct pipe_resource *
 v3d_resource_create_with_modifiers(struct pipe_screen *pscreen,
                                    const struct pipe_resource *tmpl,
@@ -847,6 +907,34 @@ v3d_resource_create_with_modifiers(struct pipe_screen *pscreen,
         if (tmpl->bind & PIPE_BIND_SCANOUT)
                 should_tile = false;
 
+        /* Phoenix: force the full-screen color render target to RASTER (linear) so a CPU
+         * readback (glReadPixels) is a fast linear copy instead of the ~23ms scattered
+         * tiled detile. Targeted by display size so small SAMPLED render targets (e.g. the
+         * water warpimage) stay tiled (the TMU needs tiled to sample efficiently).
+         *
+         * MUST exclude SAMPLER_VIEW: Mesa marks every renderable (RGBA8) texture
+         * PIPE_BIND_RENDER_TARGET, so a *large sampled* texture (e.g. quake3's 1024x1024
+         * merged lightmap atlas) also matched the size gate and was forced RASTER — then the
+         * TMU sampled the linear data as if UIF and read garbage/black (quake3 lightmap-black,
+         * quake2 floor-speckle, vkQuake striping — all large sampled textures >=1024x768). A
+         * texture the TMU will sample must stay tiled; only a pure (non-sampled) RT may go
+         * RASTER for the readback fast-path.
+         *
+         * EXCEPTION -- the scanout render target: Mesa unconditionally adds
+         * PIPE_BIND_SAMPLER_VIEW to EVERY renderbuffer (main/renderbuffer.c), so the
+         * full-screen scanout RT (a GL renderbuffer the SDL2/GLX present layer
+         * render-to-scanouts and page-flips) also carries SAMPLER_VIEW and was newly caught by
+         * the exclusion above -- left UIF-tiled, so the HVS display scanned tiled memory as
+         * linear and produced horizontal-shred garbage on HDMI for all GL games
+         * (quakespasm/quake2/quake3). The scanout RT is never sampled by the TMU; it must be
+         * RASTER so the display reads it correctly. The winsys flagged it via set_next_scanout()
+         * immediately before this alloc, so peek that one-shot to force RASTER regardless of the
+         * spurious SAMPLER_VIEW bit -- sampled textures never set next_scanout, so they stay tiled. */
+        if ((tmpl->bind & PIPE_BIND_RENDER_TARGET) &&
+            tmpl->width0 >= 1024 && tmpl->height0 >= 768 &&
+            (v3d_phoenix_peek_next_scanout() || !(tmpl->bind & PIPE_BIND_SAMPLER_VIEW)))
+                should_tile = false;
+
         /* No user-specified modifier; determine our own. */
         if (count == 1 && modifiers[0] == DRM_FORMAT_MOD_INVALID) {
                 rsc->tiled = should_tile;
@@ -961,7 +1049,18 @@ v3d_resource_from_handle(struct pipe_screen *pscreen,
 
         switch (whandle->type) {
         case WINSYS_HANDLE_TYPE_SHARED:
+#if defined(__phoenix__)
+                /* Phoenix has no flink names, so v3d_bo_open_name()'s GEM_OPEN
+                 * would always fail and this case is dead as written. Repurpose
+                 * it for the thing "shared by handle" actually means here: the
+                 * v3d-srv daemon's global BO handle namespace. Size is not in
+                 * winsys_handle, so use the region the caller described. */
+                rsc->bo = v3d_bo_open_phoenix_handle(screen, whandle->handle,
+                                                     whandle->stride *
+                                                     tmpl->height0);
+#else
                 rsc->bo = v3d_bo_open_name(screen, whandle->handle);
+#endif
                 break;
         case WINSYS_HANDLE_TYPE_FD:
                 rsc->bo = v3d_bo_open_dmabuf(screen, whandle->handle);
diff --git a/src/gallium/drivers/v3d/v3dx_draw.c b/src/gallium/drivers/v3d/v3dx_draw.c
index 14fb2306026..b33e88f50b2 100644
--- a/src/gallium/drivers/v3d/v3dx_draw.c
+++ b/src/gallium/drivers/v3d/v3dx_draw.c
@@ -917,6 +917,21 @@ v3d_update_primitives_generated_counter(struct v3d_context *v3d,
 static void
 v3d_update_job_ez(struct v3d_context *v3d, struct v3d_job *job)
 {
+        /* Phoenix Pi4 (mesa 26.2): force Early-Z OFF for the whole job. On the 26.2 base
+         * this port's V3D 4.2 render pipeline hits the HW-marginal depth/fragment drain
+         * stall (fdbgs=EZTEST) on EVERY frame with EZ enabled — the winsys reset+drop
+         * mitigation then fires constantly, collapsing GLQuake to ~5 fps (337 render
+         * timeouts). The stall is EZ-triggered: forcing EZ off yields 0 wedges and full
+         * ~38 fps (HW-verified via netboot, 20260716-165718, triple-buffer, frames render
+         * correctly). EZ early-rejects overdraw (a perf lever) but at 1080p GLQuake is not
+         * overdraw-bound, so disabling it costs no measurable fps here while eliminating
+         * the wedge. (On the pre-26.2 base EZ was re-enabled after a winsys L2T-flush fix;
+         * 26.2's codegen re-triggers the stall, so keep EZ off on 26.2. Falls through to
+         * the existing first_ez_state==DISABLED early-return below.) TODO(v3d-26.2-ez):
+         * root-cause why 26.2 makes the EZTEST drain stall constant to safely re-enable EZ. */
+        job->first_ez_state = V3D_EZ_DISABLED;
+        job->ez_state = V3D_EZ_DISABLED;
+
         /* If first_ez_state is V3D_EZ_DISABLED it means that we have already
          * determined that we should disable EZ completely for all draw calls
          * in this job. This will cause us to disable EZ for the entire job in
@@ -1009,6 +1024,17 @@ v3d_update_job_ez(struct v3d_context *v3d, struct v3d_job *job)
                 job->ez_state = V3D_EZ_DISABLED;
         }
 
+        /* Early-Z (EZ) is ENABLED on this V3D 4.2 port (the normal mesa state machine above
+         * decides per-shader/per-direction; it already disables EZ for FS discard/writes-Z).
+         * EZ was previously force-disabled here because depth-tested steeply-tilted polygons
+         * hung the render — but that hang was a symptom of the L2T flush-completion race
+         * (GFXH-1897): the render fetched the binner's tile-list before the bin-phase L2T flush
+         * had drained. That race is now fixed in the winsys (l2t_flush_wait around the pre-bin
+         * and bin->render flushes, v3d_phoenix_winsys.c). With it fixed, the stalltest renders
+         * 3000 tilted depth-tested EZ-ON frames with 0 wedges, so EZ is re-enabled here — it
+         * early-rejects overdraw (FPS win) and lets stock GL_LEQUAL run without the depth-drain
+         * stall. See docs/inprogress/2026-06-21-v3d-early-z-reenable-plan.md. */
+
         if (job->first_ez_state == V3D_EZ_UNDECIDED &&
             (job->ez_state != V3D_EZ_DISABLED || job->draw_calls_queued == 0))
                 job->first_ez_state = job->ez_state;
diff --git a/src/gallium/drivers/v3d/v3dx_state.c b/src/gallium/drivers/v3d/v3dx_state.c
index 0ce11c17a7e..8ba2a4f64ac 100644
--- a/src/gallium/drivers/v3d/v3dx_state.c
+++ b/src/gallium/drivers/v3d/v3dx_state.c
@@ -558,6 +558,16 @@ v3d_set_framebuffer_state(struct pipe_context *pctx,
 
                 if (desc->swizzle[3] == PIPE_SWIZZLE_1)
                         v3d->blend_dst_alpha_one |= 1 << i;
+
+                /* Phoenix Pi4: do NOT swap R<->B. The plo HDMI framebuffer is configured with RGB
+                 * pixel order (mailbox SET_PIXEL_ORDER=1), which matches the V3D's native 32-bit
+                 * store order — so a color RT, whether the GPU scans it out directly (render-to-
+                 * scanout / page-flip) or it is glReadPixels'd for the CPU-present fallback (which
+                 * copies channels straight through, present_blit() in pl_phoenix_vid.c), already
+                 * lands in display order. An earlier render-to-scanout workaround swapped R<->B
+                 * here; on this fb config that was spurious and rendered the brown Quake world
+                 * blue (HW-verified). Leave swap_color_rb at 0. (A format-agnostic fb would derive
+                 * this from plo's actual pixel order rather than hardcoding it.) */
         }
 
         v3d->dirty |= V3D_DIRTY_FRAMEBUFFER;
diff --git a/src/mesa/state_tracker/st_atom_framebuffer.c b/src/mesa/state_tracker/st_atom_framebuffer.c
index cc5ad3d3cb7..463eb975322 100644
--- a/src/mesa/state_tracker/st_atom_framebuffer.c
+++ b/src/mesa/state_tracker/st_atom_framebuffer.c
@@ -104,6 +104,32 @@ framebuffer_quantize_num_samples(struct st_context *st, unsigned num_samples)
    return quantized_samples;
 }
 
+#if defined(__phoenix__)
+/* Phoenix-RTOS Pi4: opt-out for the render-to-scanout Y-flip heuristic below.
+ *
+ * The heuristic tests SIZE, not scanout-ness, so it also fires for any offscreen
+ * FBO >= 1024x768 that merely happens to be full-screen-sized. A platform layer
+ * whose present path is a CPU readback rather than a scanout-backed FBO must set
+ * this to 0 at context creation; its full-screen render target is then left
+ * Y_0_BOTTOM, i.e. exactly what upstream Mesa would do.
+ *
+ * The X server (glamor) is such a consumer: its screen pixmap is a plain GL
+ * texture that the DDX presents by glReadPixels into a shadow it write()s to
+ * /dev/fb0 -- nothing about it is scanout-backed. Forcing Y_0_TOP there put every
+ * glamor GL path into a flipped coordinate world and needed three hand-rolled
+ * compensating flips to stay upright; any path that missed one produced content
+ * mirrored to y' = H-1-y (a spurious Y-mirrored copy of the Window Maker Clip at
+ * the bottom of the screen was the reproducible case). With the gate off, all of
+ * glamor's paths agree and the compensators are deleted.
+ *
+ * Defaults to 1, so the SDL games -- which DO render into a genuinely
+ * scanout-backed full-screen FBO, and share this object file -- are unaffected.
+ * Replacing the size test with a real scanout predicate for everyone is the
+ * larger follow-up: docs/misc/2026-09-08-flipy-scanout-gate-work-order.md.
+ */
+int phx_scanout_flip_gate = 1;
+#endif
+
 /**
  * Update framebuffer state (color, depth, stencil, etc. buffers)
  */
@@ -124,6 +150,22 @@ st_update_framebuffer_state( struct st_context *st )
 
    st->state.fb_orientation = _mesa_fb_orientation(fb);
 
+
+#if defined(__phoenix__)
+   /* Phoenix-RTOS Pi4 render-to-scanout: the full-screen user FBO is backed by the HDMI
+    * framebuffer's physical pages (winsys V3D_CREATE_BO_SCANOUT, gated on >=1024x768), so the
+    * GPU stores straight to the displayed surface. That surface is scanned top-down (y-down),
+    * but a user FBO is Y_0_BOTTOM (y-up), which lands the whole frame upside down. Force Y_0_TOP
+    * for the full-screen FBO so Mesa negates the viewport Y (st_atom_viewport.c: scale[1]*=-1)
+    * and the frame renders upright directly into scanout — no extra blit/CPU copy. Gated on
+    * full-screen size (matching the scanout trigger) so small internal FBOs (mipmap generation,
+    * blit fallbacks) keep Y_0_BOTTOM and are not wrongly flipped. Winding is compensated by the
+    * platform layer's glFrontFace(GL_CCW) (Mesa does not auto-invert front-face for Y_0_TOP). */
+   if (st->state.fb_orientation == Y_0_BOTTOM && phx_scanout_flip_gate &&
+       fb->Width >= 1024 && fb->Height >= 768)
+      st->state.fb_orientation = Y_0_TOP;
+#endif
+
    /**
     * Quantize the derived default number of samples:
     *
diff --git a/src/util/blake3/blake3_impl.h b/src/util/blake3/blake3_impl.h
index 0dd387c7f77..31c44b49467 100644
--- a/src/util/blake3/blake3_impl.h
+++ b/src/util/blake3/blake3_impl.h
@@ -58,15 +58,22 @@ enum blake3_flags {
 #endif
 #endif
 
-#if !defined(BLAKE3_USE_NEON) 
+#if !defined(BLAKE3_USE_NEON)
   // If BLAKE3_USE_NEON not manually set, autodetect based on AArch64ness
-  #if defined(IS_AARCH64)
+  #if defined(IS_AARCH64) && !defined(__phoenix__)
     #if defined(__ARM_BIG_ENDIAN)
       #define BLAKE3_USE_NEON 0
     #else
       #define BLAKE3_USE_NEON 1
     #endif
   #else
+    // Phoenix-RTOS aarch64: the NEON blake3 backend (blake3_neon.c) is NOT
+    // compiled into the V3D/V3DV port, so the upstream auto-detect would route
+    // blake3_hash_many() to an undefined/stubbed blake3_hash_many_neon and
+    // silently mis-hash any multi-chunk (>4 KiB) input. Force the portable C
+    // backend so the static if/else dispatch in blake3_dispatch.c falls through
+    // to blake3_hash_many_portable. Correct over fast — Mesa only hashes SPIR-V
+    // shader blobs once at load. See tools/v3d-driver-port + the vkQuake port.
     #define BLAKE3_USE_NEON 0
   #endif
 #endif
diff --git a/src/util/os_memory_aligned.h b/src/util/os_memory_aligned.h
index 98b3cd6f8d8..16de49d8b28 100644
--- a/src/util/os_memory_aligned.h
+++ b/src/util/os_memory_aligned.h
@@ -37,6 +37,15 @@
 
 #include "util/u_overflow.h"
 
+/* PHOENIX PORT FIX: Phoenix has no real posix_memalign (the gl_stubs.c shim ignores
+ * the alignment and returns plain malloc, only 16-byte aligned). util_sparse_array
+ * (the texture-object hash) needs 64-byte-aligned nodes — it packs the tree level
+ * into the low 6 bits of the node pointer (NODE_ALLOC_ALIGN=64). Underaligned nodes
+ * corrupt that encoding -> util_sparse_array_get returns wild pointers -> memory
+ * corruption (clobbered glGenTextures/glBindTexture). Force the self-contained
+ * malloc+cubby-hole path below, which honors alignment and frees correctly. */
+#undef HAVE_POSIX_MEMALIGN
+
 #if defined(HAVE_POSIX_MEMALIGN)
 
 static inline void *
diff --git a/src/util/ralloc.c b/src/util/ralloc.c
index 5736dd6dd48..2707677e577 100644
--- a/src/util/ralloc.c
+++ b/src/util/ralloc.c
@@ -681,6 +681,56 @@ get_gc_header(const void *ptr)
 
    gc_block_header *info = (gc_block_header *)c_ptr;
    assert(info->canary == GC_CANARY);
+
+#ifdef __phoenix__
+   /* TODO(C1-hunt): Phoenix-only diagnostic, remove when C1 closes.
+    *
+    * Nothing above validates the result on a release build. The canary that
+    * would is behind NDEBUG, which this port defines, and the walk-back is
+    * steered by c_ptr[-1] -- one byte of whatever happens to precede the
+    * pointer -- so a call on anything that is not a live GC block lands the
+    * header up to 127 bytes off and the caller then writes through it.
+    * Phoenix's C1 is a 4-byte write into a malloc heap header, and the heap it
+    * lands in is one gc_zalloc_size() created, so report an implausible result
+    * here rather than wait for the ~2% of them that happen to be visible.
+    *
+    * The test must not false-positive or it is worse than nothing:
+    * gc_alloc_size()'s large path sets only ->bucket and never initialises
+    * ->slab_offset, so slab_offset is only meaningful for a slab-backed block.
+    * ->bucket == NUM_FREELIST_BUCKETS is the legal "allocated directly" marker.
+    *
+    * ret= is __builtin_return_address(0); this function is small enough to be
+    * inlined, so read it as "inside the caller", not as an exact call site.
+    */
+   /* POSITIVE CONTROL. Without this, "the implausible-header check never fired"
+    * is worthless: it reads identically whether the header is always sane or
+    * this function is simply never reached. That exact mistake -- banking a
+    * null from a detector that had no opportunity to fire -- is already on
+    * record in this hunt (the closed-BO quarantine reported "nothing written"
+    * across 5 runs in which the event never occurred). One line per process,
+    * so a reader can tell the two apart. */
+   {
+      static unsigned c1_live;
+
+      if (c1_live == 0u) {
+         c1_live = 1u;
+         printf("ralloc: C1-hunt: get_gc_block_header LIVE (first call in this process)\n");
+      }
+   }
+
+   if ((info->bucket > NUM_FREELIST_BUCKETS) ||
+       ((info->bucket < NUM_FREELIST_BUCKETS) && (info->slab_offset >= SLAB_SIZE))) {
+      static unsigned c1_reported;
+
+      if (c1_reported < 8u) {
+         c1_reported++;
+         printf("ralloc: C1-hunt: implausible gc header: ptr=%p hdr=%p off=%u bucket=%u flags=0x%02x ret=%p\n",
+                ptr, (void *)info, (unsigned)info->slab_offset, (unsigned)info->bucket,
+                (unsigned)info->flags, __builtin_return_address(0));
+      }
+   }
+#endif
+
    return info;
 }
 
diff --git a/src/vulkan/runtime/vk_cmd_copy.c b/src/vulkan/runtime/vk_cmd_copy.c
index 5b4ef8a28d2..398fb72be29 100644
--- a/src/vulkan/runtime/vk_cmd_copy.c
+++ b/src/vulkan/runtime/vk_cmd_copy.c
@@ -24,6 +24,10 @@
 #include "vk_common_entrypoints.h"
 #include "vk_device.h"
 #include "vk_util.h"
+#if defined(__phoenix__)
+#include <stdio.h>    /* #29 triage trampoline trace */
+#include <stdlib.h>   /* getenv (V3DV_DBGCOPY gate) */
+#endif
 
 VKAPI_ATTR void VKAPI_CALL
 vk_common_CmdCopyBuffer(VkCommandBuffer commandBuffer,
@@ -128,6 +132,22 @@ vk_common_CmdCopyBufferToImage(VkCommandBuffer commandBuffer,
          .imageOffset         = pRegions[r].imageOffset,
          .imageExtent         = pRegions[r].imageExtent,
       };
+#if defined(__phoenix__)
+      /* #29 black-texture triage: this legacy->Copy2 trampoline is the boundary between
+       * vkQuake's region array (proven >=1x1 by q_max) and what V3DV's CmdCopyBufferToImage2
+       * receives (observed 0x1). Print the extent BEFORE (incoming pRegions) and AFTER (the
+       * converted region2s) the field copy: equal+nonzero here but 0 at v3dv = corruption
+       * downstream of this trampoline; 0 already on `in` = upstream (vkQuake/proc-addr); 0
+       * only on `out` = a struct/field mismatch in this very copy. */
+      if (getenv("V3DV_DBGCOPY"))
+      fprintf(stderr, "vk-trampoline: CmdCopyBufferToImage region %u in=%ux%ux%u out=%ux%ux%u "
+              "(sizeof in=%u out=%u)\n", r,
+              pRegions[r].imageExtent.width, pRegions[r].imageExtent.height,
+              pRegions[r].imageExtent.depth,
+              region2s[r].imageExtent.width, region2s[r].imageExtent.height,
+              region2s[r].imageExtent.depth,
+              (unsigned)sizeof(VkBufferImageCopy), (unsigned)sizeof(VkBufferImageCopy2));
+#endif
    }
 
    VkCopyBufferToImageInfo2 info = {
diff --git a/src/vulkan/runtime/vk_image.c b/src/vulkan/runtime/vk_image.c
index bda7fc77d1b..15db3eec0e0 100644
--- a/src/vulkan/runtime/vk_image.c
+++ b/src/vulkan/runtime/vk_image.c
@@ -23,7 +23,7 @@
 
 #include "vk_image.h"
 
-#if DETECT_OS_LINUX || DETECT_OS_BSD
+#if DETECT_OS_LINUX || DETECT_OS_BSD || defined(__phoenix__) /* Phoenix V3DV: emulates the Linux DRM ABI; keep drm_format_mod */
 #include <drm-uapi/drm_fourcc.h>
 #endif
 
@@ -105,7 +105,7 @@ vk_image_init(struct vk_device *device,
       vk_find_struct_const(pCreateInfo->pNext, WSI_IMAGE_CREATE_INFO_MESA);
    image->wsi_legacy_scanout = wsi_info && wsi_info->scanout;
 
-#if DETECT_OS_LINUX || DETECT_OS_BSD
+#if DETECT_OS_LINUX || DETECT_OS_BSD || defined(__phoenix__) /* Phoenix V3DV: emulates the Linux DRM ABI; keep drm_format_mod */
    image->drm_format_mod = ((1ULL << 56) - 1) /* DRM_FORMAT_MOD_INVALID */;
 #endif
 
@@ -185,7 +185,7 @@ vk_image_destroy(struct vk_device *device,
    vk_object_free(device, alloc, image);
 }
 
-#if DETECT_OS_LINUX || DETECT_OS_BSD
+#if DETECT_OS_LINUX || DETECT_OS_BSD || defined(__phoenix__) /* Phoenix V3DV: emulates the Linux DRM ABI; keep drm_format_mod */
 VKAPI_ATTR VkResult VKAPI_CALL
 vk_common_GetImageDrmFormatModifierPropertiesEXT(UNUSED VkDevice device,
                                                  VkImage _image,
diff --git a/src/vulkan/runtime/vk_image.h b/src/vulkan/runtime/vk_image.h
index f560519c16f..5f03ccd49ef 100644
--- a/src/vulkan/runtime/vk_image.h
+++ b/src/vulkan/runtime/vk_image.h
@@ -77,7 +77,7 @@ struct vk_image {
    /* wsi_image_create_info::scanout */
    bool wsi_legacy_scanout;
 
-#if DETECT_OS_LINUX || DETECT_OS_BSD
+#if DETECT_OS_LINUX || DETECT_OS_BSD || defined(__phoenix__) /* Phoenix V3DV: emulates the Linux DRM ABI; keep drm_format_mod */
    /* VK_EXT_drm_format_modifier
     *
     * Initialized by vk_image_create/init() to DRM_FORMAT_MOD_INVALID.  It's
