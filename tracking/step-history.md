# Step History

Use this file to record completed, blocked, or abandoned implementation steps.

| Step ID | Title | Result | Manifest / SHAs | Validation Summary | Next Recommended Step |
| --- | --- | --- | --- | --- | --- |
| `STEP-0001` | Bootstrap upstream worktrees and baseline integration state | completed | `manifests/2026-03-19-baseline-clones.md` | the initial coordination-selected Phoenix repos were cloned into `sources/`; baseline SHAs recorded | install host prerequisites, create the `phoenix-dev` Linux VM, and verify the first clean Linux build |
| `STEP-0002` | Install missing host prerequisites for the Linux VM workflow | completed | not applicable | `limactl`, `yq`, `socat`, `picocom`, `mformat`, and `socket_vmnet` are now available on the host | create and verify the `phoenix-dev` Linux VM |
| `STEP-0003` | Create and verify the `phoenix-dev` Linux VM | completed | not applicable | `phoenix-dev` created on `ubuntu-24.04`, Lima reports `Running`, and `limactl shell -y phoenix-dev sh -lc 'echo ready && uname -a'` succeeded | install the documented Linux packages inside the VM |
| `STEP-0004` | Install the documented Linux package baseline inside `phoenix-dev` | completed | not applicable | the Ubuntu 24.04 guest package baseline installed successfully and the required commands resolve inside `phoenix-dev` | complete the sibling Phoenix repo set needed for local builds |
| `STEP-0005` | Complete the sibling Phoenix repo set required for local builds | completed | `manifests/2026-03-19-full-project-repo-set.md` | the local `sources/` inventory now matches the current `phoenix-rtos-project/.gitmodules` repo set needed for clean local builds | define the local buildroot wiring between `phoenix-rtos-project` and the sibling repos |
| `STEP-0006` | Define the local buildroot wiring for the sibling-clone workflow | completed | not applicable | `scripts/prepare-buildroot.sh` now prepares a disposable buildroot and was verified inside `phoenix-dev`, including the fallback to VM-local storage when the shared workspace is read-only | verify one clean Phoenix Linux build in the VM using the documented buildroot workflow |
| `STEP-0007` | Verify a clean baseline Phoenix Linux build in `phoenix-dev` | completed | `manifests/2026-03-19-host-generic-pc-baseline-build.md` | a clean `host-generic-pc` build completed successfully inside `phoenix-dev` using the disposable buildroot workflow, and `_build`, `_fs`, and `_boot` output paths were verified | provision the Phoenix AArch64 cross toolchain required for validating the first real AArch64 refactors |
| `STEP-0008` | Provision the Phoenix AArch64 toolchain in `phoenix-dev` | completed | `manifests/2026-03-20-aarch64-toolchain-bootstrap.md` | the `aarch64-phoenix` toolchain now resolves in `phoenix-dev`, and the copied-buildroot workflow is documented as the safe current AArch64 validation lane in the read-only Lima mount | generalize the top-level AArch64 platform Makefile selection while keeping `aarch64a53-zynqmp` building cleanly |
| `STEP-0009` | Generalize top-level AArch64 platform Makefile selection | completed | `manifests/2026-03-20-aarch64-platform-makefile-selection.md` | the top-level AArch64 Makefiles now select platform Makefiles by subfamily rather than a literal `zynqmp` substring match, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | define the first narrow step toward a generic non-Xilinx AArch64 QEMU lane |
| `STEP-0010` | Define the first narrow generic AArch64 QEMU lane step | completed | `manifests/2026-03-20-aarch64-qemu-lane-scope.md` | local `virt` inspection showed that the first real blocker is kernel DTB recognition of root-level GIC and PL011 nodes, so the next code step should be parser work rather than target-metadata-only work | extend the kernel AArch64 DTB parser for the first `virt`-style nodes while preserving the existing ZynqMP path |
| `STEP-0011` | Extend kernel AArch64 DTB parsing for first `virt`-style nodes | completed | `manifests/2026-03-20-aarch64-dtb-node-recognition.md` | the kernel DTB parser now recognizes shallow `pl011@...` and `intc@...` nodes, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | generalize GIC `reg` decoding in the kernel DTB parser so it handles the 16-byte tuple layout used by QEMU `virt` while preserving the existing ZynqMP layout |
| `STEP-0012` | Generalize kernel AArch64 GIC `reg` decoding for `virt`-style tuples | completed | `manifests/2026-03-20-aarch64-gic-reg-decoding.md` | the kernel DTB parser now decodes both the existing 12-byte GIC `reg` tuples and the 16-byte tuples used by local QEMU `virt`, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | expose architectural timer interrupt metadata from the AArch64 DTB parser without introducing the runtime timer implementation yet |
| `STEP-0013` | Expose architectural timer interrupts from the AArch64 DTB parser | completed | `manifests/2026-03-20-aarch64-dtb-timer-metadata.md` | the AArch64 DTB API now exposes the root-level timer interrupt metadata needed for later generic timer work, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | define the first runtime-oriented kernel follow-up after the DTB preparation series so the next change does not widen uncontrollably |
| `STEP-0014` | Define the first runtime-oriented kernel step after DTB preparation | completed | `manifests/2026-03-20-aarch64-runtime-step-scope.md` | the first selected runtime-oriented follow-up is to remove the hard `TIMER_IRQ_ID` dependency from the common AArch64 GICv2 code by moving timer IRQ knowledge behind the timer HAL API | add a timer IRQ query to the timer HAL and switch common AArch64 GICv2 code to use it |
| `STEP-0015` | Move AArch64 timer IRQ knowledge behind the timer HAL | completed | `manifests/2026-03-20-aarch64-timer-irq-api.md` | the common AArch64 GICv2 code now queries the timer IRQ through the timer HAL API instead of using the `TIMER_IRQ_ID` macro directly, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | add reusable AArch64 architectural timer sysreg helpers needed for the future generic ARM timer backend |
| `STEP-0016` | Add AArch64 architectural timer sysreg helpers | completed | `manifests/2026-03-20-aarch64-timer-sysreg-helpers.md` | reusable AArch64 architectural timer register helpers now exist in `hal/aarch64/aarch64.h`, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | define the first common AArch64 generic timer backend step before introducing the first substantial new timer runtime code |
| `STEP-0017` | Define the first common AArch64 generic timer backend step | blocked | not applicable | the step was intentionally paused before implementation when the user requested an unattended long-running workflow update; it should be resumed after the workflow change is committed | add the unattended-session workflow, then return to the generic timer backend planning step |
| `STEP-0018` | Add unattended long-running session workflow | completed | `manifests/2026-03-20-unattended-agent-mode.md` | unattended mode is now explicitly documented as an opt-in workflow with auto-continue rules and hard stop conditions, and the main reading/control docs now reference it | return to the generic timer backend planning step under the new unattended workflow |
| `STEP-0019` | Define the first common AArch64 generic timer backend step | completed | `manifests/2026-03-20-aarch64-generic-timer-step-scope.md` | the selected backend shape is a directly selectable common AArch64 timer implementation, and the next smallest implementation step is to codify timer-source selection in the DTB API before introducing the backend itself | add AArch64 generic timer source selection helpers to the DTB API |
| `STEP-0020` | Add AArch64 generic timer source selection helpers | completed | `manifests/2026-03-20-aarch64-dtb-timer-source-selection.md` | the AArch64 DTB API now exposes an explicit selected generic timer source and IRQ, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | define the first directly selectable common AArch64 generic timer backend step from the new DTB-selection baseline |
| `STEP-0021` | Define the first directly selectable common AArch64 timer backend step | completed | `manifests/2026-03-20-aarch64-direct-timer-backend-step-scope.md` | a direct generic EL1 timer backend was found to be premature because the current wakeup path can program the timer from non-CPU0 contexts; the next safe code step is timer-backend selection scaffolding only | add explicit AArch64 timer-backend selection scaffolding while preserving the current ZynqMP runtime path |
| `STEP-0022` | Add AArch64 timer-backend selection scaffolding | completed | `manifests/2026-03-20-aarch64-timer-backend-selection-hook.md` | the common AArch64 kernel Makefile now exposes an explicit timer-backend object hook, the current ZynqMP backend still builds through it, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | make the common AArch64 GICv2 handler-registration path PPI-safe before introducing a timer IRQ backed by a private peripheral interrupt |
| `STEP-0023` | Make AArch64 GICv2 handler registration PPI-safe | completed | `manifests/2026-03-20-aarch64-gicv2-ppi-safe-handler-registration.md` | common AArch64 GICv2 handler registration now avoids SPI-style CPU retargeting for SGI/PPI interrupts, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | expose a targeted AArch64 SGI helper so later timer-path work can notify CPU 0 directly without relying only on broadcast IPIs |
| `STEP-0024` | Add targeted AArch64 SGI helper for future timer updates | completed | `manifests/2026-03-20-aarch64-targeted-sgi-helper.md` | common AArch64 code now exposes a targeted SGI helper, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev`; the next blocker is explicit SGI reservation and notification-contract design | define the first CPU0-directed timer wakeup-notification step before changing timer or scheduler runtime logic |
| `STEP-0025` | Define the first CPU0-directed timer wakeup-notification step | completed | `manifests/2026-03-20-aarch64-timer-wakeup-notification-step-scope.md` | the first notification path is now scoped explicitly: reserve SGI `1` as `TIMER_WAKEUP_IRQ` and handle it in `proc/threads.c` under the scheduler spinlock to recompute CPU0’s wakeup deadline | implement the guarded CPU0-directed timer wakeup-notification path in the scheduler |
| `STEP-0026` | Implement CPU0-directed timer wakeup notification | completed | `manifests/2026-03-20-aarch64-cpu0-wakeup-timer-notification.md` | AArch64 now reserves a scheduler wakeup SGI and the scheduler can coalesce remote wakeup requests and route wakeup-deadline recomputation back to CPU 0; the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | define the first source-agnostic common AArch64 architectural timer helper step |
| `STEP-0027` | Define the first source-agnostic AArch64 timer helper step | completed | `manifests/2026-03-20-aarch64-gtimer-helper-step-scope.md` | the first common helper slice is now scoped explicitly: add a compiled `hal/aarch64/gtimer.[ch]` layer that hides physical-versus-virtual sysreg differences while leaving current runtime behavior unchanged | implement the compiled common AArch64 `gtimer` helper layer |
| `STEP-0028` | Implement common AArch64 `gtimer` helper layer | completed | `manifests/2026-03-20-aarch64-common-gtimer-helpers.md` | the common AArch64 build now compiles a source-keyed `gtimer` helper layer, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | define the first generic AArch64 timer backend skeleton step on top of the helper layer |
| `STEP-0029` | Define the first generic AArch64 timer backend skeleton step | completed | `manifests/2026-03-20-aarch64-gtimer-backend-skeleton-scope.md` | the first backend-skeleton slice is now scoped explicitly: add a compiled backend-state layer that owns the selected source, IRQ, and frequency while keeping public `hal_timer*` entry points untouched | implement the compiled generic AArch64 timer backend state skeleton |
| `STEP-0030` | Implement generic AArch64 timer backend state skeleton | completed | `manifests/2026-03-20-aarch64-gtimer-backend-state-skeleton.md` | the common AArch64 build now compiles a backend-state layer that owns selected source, IRQ, and frequency, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | define the first backend behavior-helper step on top of that state layer |
| `STEP-0031` | Define the first backend behavior-helper step | completed | `manifests/2026-03-20-aarch64-gtimer-backend-behavior-scope.md` | the first backend behavior slice is now scoped explicitly: add state-based raw-count and microsecond conversion helpers on top of the new backend state layer | implement the state-based raw-count and microsecond conversion helpers |
| `STEP-0032` | Implement backend-state time conversion helpers | completed | `manifests/2026-03-20-aarch64-gtimer-backend-time-helpers.md` | the backend-state layer now exposes raw-count, count-to-microseconds, and current-time helpers, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | define the first backend wait-to-ticks helper step needed before relative wakeup programming |
| `STEP-0033` | Define the first backend wait-to-ticks helper step | completed | `manifests/2026-03-20-aarch64-gtimer-wait-to-ticks-scope.md` | the next helper slice is now scoped explicitly: convert relative microseconds to architectural timer ticks from backend state before any timer-register programming is introduced | implement the backend-state microseconds-to-relative-ticks helper |
| `STEP-0034` | Implement backend-state wait-to-ticks helper | completed | `manifests/2026-03-20-aarch64-gtimer-backend-wait-to-ticks-helper.md` | the backend-state layer now exposes a reusable microseconds-to-ticks helper, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | define the first backend timer-register wrapper step on top of the current helper layer |
| `STEP-0035` | Define the first backend timer-register wrapper step | completed | `manifests/2026-03-20-aarch64-gtimer-register-wrapper-scope.md` | the next helper slice is now scoped explicitly: add state-keyed control and timer-register wrappers before introducing timer-arming policy | implement the backend-state timer-register wrappers |
| `STEP-0036` | Implement backend-state timer-register wrappers | completed | `manifests/2026-03-20-aarch64-gtimer-register-wrappers.md` | the backend-state layer now exposes state-keyed control and timer-register wrappers, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | define the first backend timer-arming policy step |
| `STEP-0037` | Define the first backend timer-arming policy step | completed | `manifests/2026-03-20-aarch64-gtimer-arming-scope.md` | the next helper slice is now scoped explicitly: add a backend-state relative timer-arming helper before public HAL takeover or IRQ registration | implement the backend-state timer-arming helper |
| `STEP-0038` | Implement backend-state timer-arming helper | completed | `manifests/2026-03-20-aarch64-gtimer-wakeup-helper.md` | the backend-state layer now exposes a backend-local wakeup helper that arms the selected architectural timer for bounded positive waits, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | define the first backend IRQ-ownership helper step |
| `STEP-0039` | Define the first backend IRQ-ownership helper step | completed | `manifests/2026-03-20-aarch64-gtimer-irq-ownership-scope.md` | the next helper slice is now scoped explicitly: add backend-state IRQ query and handler-registration helpers before public timer-HAL takeover | implement the backend-state IRQ query and handler-registration helpers |
| `STEP-0040` | Implement backend-state IRQ-ownership helpers | completed | `manifests/2026-03-20-aarch64-gtimer-irq-helpers.md` | the backend-state layer now exposes IRQ query and handler-registration helpers, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | define the first public common AArch64 timer-HAL wrapper step |
| `STEP-0041` | Define the first public AArch64 timer-hook step | completed | `manifests/2026-03-20-aarch64-public-timer-hook-scope.md` | the next safe slice is now scoped explicitly: split the AArch64 timer build hook so a future common public timer implementation can be selected without changing current runtime behavior | implement the explicit public timer-implementation object hook while keeping ZynqMP selected |
| `STEP-0042` | Implement explicit AArch64 public timer hook | completed | `manifests/2026-03-20-aarch64-public-timer-hook.md` | the AArch64 build now exposes an explicit public timer-implementation hook, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | define the first public common AArch64 timer-HAL wrapper step |
| `STEP-0043` | Define first validated public common AArch64 timer wrapper step | completed | `manifests/2026-03-20-aarch64-public-timer-wrapper-scope.md` | the next safe slice is now scoped explicitly: add an AArch64 timer-implementation override hook so a future common public timer file can be validated without switching the default platform timer | implement the explicit AArch64 timer-implementation override hook |
| `STEP-0044` | Implement AArch64 timer-implementation override hook | completed | `manifests/2026-03-20-aarch64-timer-implementation-override-hook.md` | the AArch64 build now exposes an explicit timer-implementation override hook, and the existing `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` | define the first common public AArch64 timer wrapper-file step |
| `STEP-0045` | Define first common public AArch64 timer wrapper-file step | completed | `manifests/2026-03-20-aarch64-public-timer-file-scope.md` | the next safe slice is now scoped explicitly: add a new common public timer file and validate it through the explicit override hook instead of changing the default target selection | implement the first common public AArch64 timer file |
| `STEP-0046` | Implement first common public AArch64 timer file | completed | `manifests/2026-03-20-aarch64-common-gtimer-timer-file.md` | the kernel now provides a common public AArch64 timer implementation file that can be selected through `AARCH64_TIMER_IMPL_OVERRIDE`, and the copied-buildroot `aarch64a53-zynqmp-qemu` build still succeeds in `phoenix-dev` with the override enabled | define a boot-first fast-lane policy so the next implementation steps bias toward the earliest realistic Pi 4 boot path |
| `STEP-0047` | Define boot-first fast-lane execution policy | completed | `manifests/2026-03-20-boot-first-fast-lane-policy.md` | the coordination docs now explicitly prioritize the earliest realistic Pi 4 boot path while preserving the one-step, validation, manifest, and per-repo commit rules | define the first non-Xilinx generic AArch64 QEMU `virt` milestone under the new boot-first policy |
| `STEP-0048` | Define first non-Xilinx generic AArch64 QEMU `virt` milestone | completed | `manifests/2026-03-20-aarch64-generic-virt-milestone-scope.md` | the first generic AArch64 QEMU milestone is now explicitly defined as `aarch64a53-generic-qemu`, and the first selected code step is to make `phoenix-rtos-build` recognize `aarch64a53-generic` before touching kernel or project targets | add build-system recognition for `aarch64a53-generic` and a matching core-build entry point |
| `STEP-0049` | Add build-system recognition for `aarch64a53-generic` | completed | `manifests/2026-03-20-aarch64-generic-build-recognition.md` | `phoenix-rtos-build` now recognizes `aarch64a53-generic`, a matching core-build entry point exists, and the validation lane succeeds in `phoenix-dev` with the Phoenix AArch64 toolchain on `PATH` | define the first kernel-side generic platform scaffolding step |
| `STEP-0050` | Define first kernel-side generic platform scaffolding step | completed | `manifests/2026-03-20-aarch64-generic-kernel-scaffold-scope.md` | the first generic kernel scaffold is now explicitly bounded to one new platform directory, one matching include directory, a temporary stub console, and default selection of the common timer file, validated later by a direct `phoenix-rtos-kernel` build in `phoenix-dev` | implement the first kernel-side generic platform scaffold |
| `STEP-0051` | Implement first kernel-side generic platform scaffold | completed | `manifests/2026-03-20-aarch64-generic-kernel-scaffold.md` | `aarch64a53-generic` now builds as a kernel target in the VM-local copied buildroot with a temporary empty `board_config.h` shim, and the generic platform selects the common AArch64 timer file by default | define the first reusable PL011 kernel-console step |
| `STEP-0052` | Define first reusable PL011 kernel-console step | completed | `manifests/2026-03-20-aarch64-pl011-kernel-console-scope.md` | the first reusable PL011 kernel-console slice is now bounded to one common helper and one generic-console integration step, explicitly assuming preconfigured UART state for now | implement the first reusable PL011 kernel-console path |
| `STEP-0053` | Implement first reusable PL011 kernel-console path | completed | `manifests/2026-03-20-aarch64-pl011-kernel-console.md` | the generic kernel console now uses a reusable DTB-backed PL011 transmit path, and the generic kernel target still links successfully in the VM-local copied buildroot with the temporary `board_config.h` shim | define the first `plo`-side generic AArch64 scaffolding step |
| `STEP-0054` | Define first `plo`-side generic AArch64 scaffolding step | completed | `manifests/2026-03-20-aarch64-plo-generic-scaffold-scope.md` | the first generic `plo` scaffold is now bounded to one target-local linker template plus minimal `_init`, HAL, console, timer, and interrupt files, validated later by a direct `make -C plo base_noimg` lane in `phoenix-dev` | implement the first generic AArch64 `plo` scaffold |
| `STEP-0055` | Implement first generic AArch64 `plo` scaffold | completed | `manifests/2026-03-20-aarch64-plo-generic-scaffold.md` | `aarch64a53-generic` now builds `plo` directly as `plo-aarch64a53-generic.elf` in the VM-local copied buildroot, with an EL3-centric QEMU-`virt` scaffold, preconfigured PL011 console path, polling timer, and generic GIC setup | define the first `aarch64a53-generic-qemu` project entry-point step |
| `STEP-0056` | Define first `aarch64a53-generic-qemu` project entry-point step | completed | `manifests/2026-03-20-aarch64-generic-qemu-project-step-scope.md` | the first generic QEMU project entry is now bounded to one target directory, one project directory, and one runtime script, using a RAM-backed `loader.disk` loaded into `ram0` on QEMU `virt` because the new generic `plo` path has `ram-storage` but not a flash or virtio boot device yet | implement the first `aarch64a53-generic-qemu` project entry point |
| `STEP-0057` | Implement first `aarch64a53-generic-qemu` project entry point | completed | `manifests/2026-03-20-aarch64-generic-qemu-project-entry.md` | `phoenix-rtos-project` now provides the first generic AArch64 QEMU target, project, and launcher script, and the build produces `_boot/aarch64a53-generic-qemu/plo.elf` plus `loader.disk` through a kernel-prebuilt `host project image` lane in `phoenix-dev` | define the first emulated generic AArch64 smoke-lane step |
| `STEP-0058` | Define first emulated generic AArch64 smoke-lane step | completed | `manifests/2026-03-20-aarch64-generic-qemu-smoke-step-scope.md` | the first smoke lane is now fixed as `timeout 10s ./scripts/aarch64a53-generic-qemu.sh` inside the copied buildroot in `phoenix-dev`, with the first accepted success signal set to the `Phoenix-RTOS loader v.` serial banner | run the first end-to-end `aarch64a53-generic-qemu` smoke command on QEMU `virt` |
| `STEP-0059` | Run first end-to-end `aarch64a53-generic-qemu` smoke command | completed | `manifests/2026-03-20-aarch64-generic-qemu-first-smoke.md` | the first smoke run failed before QEMU launch because `scripts/aarch64a53-generic-qemu.sh` is not executable in the copied buildroot, so no serial boot evidence was reached yet | define the smallest launcher-fix step so the generic QEMU smoke lane can actually start QEMU |
| `STEP-0060` | Define smallest generic QEMU launcher-fix step | completed | `manifests/2026-03-20-aarch64-generic-qemu-launcher-fix-scope.md` | the smallest fix is now explicitly narrowed to making `scripts/aarch64a53-generic-qemu.sh` executable like the other Phoenix QEMU launchers, with no content changes | set the generic QEMU launcher executable and rerun the unchanged smoke command |
| `STEP-0061` | Make generic QEMU launcher executable and rerun smoke command | completed | `manifests/2026-03-20-aarch64-generic-qemu-launcher-fix.md` | the launcher fix now lets the smoke lane start QEMU, but the generic QEMU run still reaches the timeout with no serial output, so the next blocker is true early-runtime silence rather than script execution | define the smallest first-boot-output debugging step for the silent generic QEMU lane |
| `STEP-0062` | Define first boot-output debugging step for silent generic QEMU lane | completed | `manifests/2026-03-20-aarch64-generic-qemu-serial-fix-scope.md` | local `virt,secure=on` inspection shows that the generic `plo` console address matches the normal `chosen` PL011 at `0x09000000`, so the smallest first output-recovery change is launcher-side serial routing rather than loader code | route the non-secure generic QEMU PL011 to stdio and rerun the unchanged smoke command |
| `STEP-0063` | Route generic QEMU non-secure PL011 to stdio and rerun smoke command | completed | `manifests/2026-03-20-aarch64-generic-qemu-serial-fix.md` | the generic QEMU lane now reaches visible `plo` output on serial, and the next blocker is that `plo` cannot open `user.plo` from `ram0` during the pre-init script | define the smallest generic pre-init fix needed to let `plo` open `user.plo` from `ram0` |
| `STEP-0064` | Define smallest generic pre-init fix for `user.plo` loading | completed | `manifests/2026-03-20-aarch64-generic-qemu-phfs-fix-scope.md` | the generic AArch64 pre-init is missing the `phfs ram0 4.0 raw` setup that comparable RAM-backed Phoenix targets use before calling `user.plo`, so that one-line PHFS setup is now the selected next fix | mount the RAM-backed loader image as raw PHFS in generic pre-init and rerun the unchanged smoke command |
| `STEP-0065` | Mount `ram0` PHFS in generic pre-init and rerun smoke command | completed | `manifests/2026-03-20-aarch64-generic-qemu-phfs-fix.md` | the generic AArch64 lane now gets past `user.plo` loading in `plo`, and the next silence appears after the pre-init handoff, which points to the first kernel-stage visibility gap | define the smallest kernel-stage visibility or handoff fix implied by that new silence |
| `STEP-0066` | Define smallest generic QEMU DTB handoff fix | completed | `manifests/2026-03-20-aarch64-generic-qemu-dtb-fix-scope.md` | the AArch64 kernel requires `system.dtb` before it can initialize its early console, so the smallest fix is to generate `${PREFIX_ROOTFS}/etc/system.dtb` in the generic QEMU project build and load it from `user.plo` | generate `system.dtb` into `${PREFIX_ROOTFS}` during the generic QEMU project build, load it in `user.plo`, and rerun the smoke command |
| `STEP-0067` | Generate generic QEMU `system.dtb`, load it in `user.plo`, and rerun smoke command | completed | `manifests/2026-03-20-aarch64-generic-qemu-dtb-fix.md` | the generic QEMU lane now generates, packages, and loads `system.dtb`, but the run is still silent after the handoff, which points to the next blocker being early kernel console selection rather than missing DTB data | define the smallest stdout-path-aware DTB or console-selection fix for the generic AArch64 kernel |
| `STEP-0068` | Define stdout-path-aware serial-selection fix for generic AArch64 kernel | completed | `manifests/2026-03-20-aarch64-generic-qemu-stdout-path-scope.md` | the generic kernel console currently takes `serials[0]`, but the local `virt,secure=on` DTB lists the secure PL011 before the normal `chosen.stdout-path` UART, so the smallest clean fix is to add a stdout-path-aware DTB helper and use it in the generic console | implement `chosen.stdout-path`-aware console serial selection in the AArch64 DTB layer and generic kernel console, then rerun the generic QEMU smoke lane |
| `STEP-0069` | Implement stdout-path-aware generic AArch64 console selection | completed | `manifests/2026-03-20-aarch64-generic-qemu-stdout-path-fix.md` | the generic QEMU lane now reaches visible Phoenix kernel output after the `plo` handoff, and the shared `aarch64a53-zynqmp-qemu` build still succeeds with the new DTB console-selection helper | define the first repo-by-repo generic userspace build-unblock step so the generic QEMU lane can move beyond a kernel-only smoke target |
| `STEP-0070` | Define first generic userspace build-unblock step after kernel banner milestone | completed | `manifests/2026-03-20-aarch64-generic-userspace-unblock-scope.md` | the safest first repo-local unblock is now identified as `phoenix-rtos-filesystems`, because its current `aarch64a53-zynqmp` target file is already board-agnostic and can be mirrored to `aarch64a53-generic` without dragging in device-specific assumptions | add the generic AArch64 target makefile to `phoenix-rtos-filesystems` and validate that repo on the generic target |
| `STEP-0071` | Add generic AArch64 filesystem target makefile | completed | `manifests/2026-03-20-aarch64-generic-filesystems-target.md` | `phoenix-rtos-filesystems` now exposes a generic AArch64 target file and validates successfully for `aarch64a53-generic-qemu` when built with the generic project path supplied for `board_config.h` | define the generic AArch64 `phoenix-rtos-utils` target unblock step |
| `STEP-0072` | Define generic AArch64 `phoenix-rtos-utils` target unblock step | completed | `manifests/2026-03-20-aarch64-generic-utils-target-scope.md` | the existing `phoenix-rtos-utils` target file is board-agnostic and contains only `psh`, so the next safe repo-local unblock is to mirror it to `aarch64a53-generic` | add the generic AArch64 target makefile to `phoenix-rtos-utils` and validate that repo on the generic target |
| `STEP-0073` | Add generic AArch64 utils target makefile | completed | `manifests/2026-03-20-aarch64-generic-utils-target.md` | `phoenix-rtos-utils` now exposes a generic AArch64 target file and validates successfully for `aarch64a53-generic-qemu`, producing `psh` on the generic target | define the smallest generic AArch64 `libphoenix` reboot-support step |
| `STEP-0074` | Define smallest generic AArch64 `libphoenix` reboot-support step | completed | `manifests/2026-03-20-aarch64-generic-libphoenix-reboot-scope.md` | `libphoenix/arch/aarch64/reboot.c` only uses generic `platformctl` interfaces, so the next smallest safe change is to remove the unused ZynqMP-only include guard and hard generic-target `#error` | make `libphoenix` AArch64 reboot support generic and validate `libphoenix` directly on the generic target |
| `STEP-0075` | Make `libphoenix` AArch64 reboot support generic | completed | `manifests/2026-03-20-aarch64-generic-libphoenix-reboot.md` | `libphoenix` now builds successfully for `aarch64a53-generic-qemu`, and `arch/aarch64/reboot.c` now handles both the ZynqMP and generic `platformctl_t` layouts cleanly | define the next generic full-build unblock after `libphoenix` |
| `STEP-0076` | Define the next generic full-build unblock after `libphoenix` | completed | `manifests/2026-03-20-aarch64-generic-full-build-unblock-scope.md` | the broader generic `host project image` lane now succeeds from the current copied-buildroot baseline, so there is no new repo-local full-build blocker to remove before returning to runtime progress | define the first post-build generic QEMU runtime-unblock step |
| `STEP-0077` | Define the first post-build generic QEMU runtime-unblock step | completed | `manifests/2026-03-20-aarch64-generic-runtime-unblock-scope.md` | the refreshed generic QEMU smoke lane is still kernel-only by construction: `user.plo` loads only the kernel and DTB, and there is still no PL011 tty driver or generic devices target | define the first generic AArch64 devices-target step for the PL011 console path |
| `STEP-0078` | Define the first generic AArch64 devices-target step for the PL011 console path | completed | `manifests/2026-03-20-aarch64-generic-devices-target-scope.md` | `phoenix-rtos-devices` fails immediately on the generic target because `_targets/Makefile.aarch64a53-generic` is missing, so that file is the smallest repo-local pre-driver fix | add the generic AArch64 devices target makefile and validate the repo on the generic target |
| `STEP-0079` | Add the generic AArch64 devices target makefile | completed | `manifests/2026-03-20-aarch64-generic-devices-target.md` | `phoenix-rtos-devices` now exposes a generic AArch64 target file and validates successfully for `aarch64a53-generic-qemu`, removing the target-selection blocker ahead of PL011 driver work | define the first reusable PL011 tty driver step |
| `STEP-0080` | Define the first reusable PL011 tty driver step | completed | `manifests/2026-03-20-aarch64-pl011-tty-scope.md` | the smallest reusable driver slice is now fixed as a single-instance polling `pl011-tty` with `libtty`, `libklog`, `/dev/tty0`, and `/dev/console`, configured first through `board_config.h` | implement the first polling PL011 tty driver scaffold and validate it directly on the generic target |
| `STEP-0081` | Implement the first polling PL011 tty driver scaffold | completed | `manifests/2026-03-20-aarch64-pl011-tty.md` | the new `pl011-tty` component now builds directly for `aarch64a53-generic-qemu`, providing the first reusable userspace PL011 console scaffold for the fast generic/Pi 4 path | define the first generic PL011 target-integration step |
| `STEP-0082` | Define the first generic PL011 target-integration step | completed | `manifests/2026-03-20-aarch64-pl011-target-integration-scope.md` | the smallest useful integration is to add `pl011-tty` to the generic devices target default component set before moving on to board-config or runtime-image wiring | add `pl011-tty` to the generic AArch64 devices target default components and validate `phoenix-rtos-devices all` |
| `STEP-0083` | Add `pl011-tty` to the generic devices target defaults | completed | `manifests/2026-03-20-aarch64-pl011-target-integration.md` | `phoenix-rtos-devices all` now builds the generic target with `pl011-tty` selected by default, so the next smallest blocker is the missing board-config wiring that gives the driver real runtime parameters | define the first generic PL011 board-config wiring step |
| `STEP-0084` | Define the first generic PL011 board-config wiring step | completed | `manifests/2026-03-20-aarch64-pl011-board-config-scope.md` | local QEMU `virt,secure=on` inspection confirms that the generic-QEMU board config should point `pl011-tty` at the non-secure UART `0x09000000` with a `24 MHz` input clock | wire the generic QEMU PL011 base and clock into `board_config.h` |
| `STEP-0085` | Wire generic QEMU PL011 values into `board_config.h` | completed | `manifests/2026-03-20-aarch64-pl011-board-config.md` | the generic QEMU project now supplies the PL011 base and clock required by `pl011-tty`, and the generic devices lane remains green with that populated board config | define the first generic `user.plo` integration step for `dummyfs` and `pl011-tty` |
| `STEP-0086` | Define the first generic `user.plo` integration step for `dummyfs` and `pl011-tty` | completed | `manifests/2026-03-20-aarch64-pl011-user-plo-scope.md` | the smallest viable runtime image step is now fixed: load `dummyfs;-N;devfs;-D` first, then `pl011-tty`, and leave `psh` for a later step | add `dummyfs` and `pl011-tty` to the generic `user.plo` in that order and rerun the generic QEMU smoke lane |
| `STEP-0087` | Add `dummyfs` and `pl011-tty` to the generic `user.plo` | completed | `manifests/2026-03-20-aarch64-pl011-user-plo.md` | the generic image now packages `dummyfs` and `pl011-tty`, but the smoke output still stops at the first kernel banner line, so the next smallest fast-lane choice should come from that updated runtime state | define the first generic `psh` integration step after the console-prep smoke rerun |
| `STEP-0088` | Define the first generic `psh` integration step after the console-prep smoke rerun | completed | `manifests/2026-03-20-aarch64-generic-psh-scope.md` | the smallest useful next step is to add plain `psh` to the generic image, matching the proven minimal `dummyfs + tty + psh` pattern used by another generic target | add `psh` to the generic `user.plo`, rebuild the needed artifacts, and rerun the generic QEMU smoke lane |
| `STEP-0089` | Add `psh` to the generic `user.plo` | completed | `manifests/2026-03-20-aarch64-generic-psh.md` | the generic image now packages `dummyfs`, `pl011-tty`, and `psh`, but the visible smoke output still does not move beyond the first kernel banner line | define the first generic userspace-start diagnostic step |
| `STEP-0090` | Define the first generic userspace-start diagnostic step | completed | `manifests/2026-03-20-aarch64-generic-userspace-diagnostic-scope.md` | the smallest high-signal diagnostic is now fixed as a raw startup banner emitted directly by `pl011-tty`, which can distinguish “userspace not reached” from “userspace reached but silent” without broadening into kernel tracing | add a direct PL011 startup banner to `pl011-tty`, rebuild the needed artifacts, and rerun the generic QEMU smoke lane |
| `STEP-0091` | Add a direct PL011 startup banner to `pl011-tty` | completed | `manifests/2026-03-20-aarch64-generic-userspace-diagnostic.md` | the generic QEMU smoke output now includes `pl011-tty: started`, proving that the packaged userspace path reaches the console driver on the non-secure PL011 | define the first console-ready diagnostic step after `pl011-tty: started` |
| `STEP-0092` | Define the first console-ready diagnostic step after `pl011-tty: started` | completed | `manifests/2026-03-20-aarch64-generic-console-ready-diagnostic-scope.md` | the smallest useful follow-up is a second raw PL011 banner emitted only after successful `_PATH_CONSOLE` registration, which distinguishes driver initialization from console readiness without broadening into shell or kernel tracing | add a direct PL011 console-ready banner after `/dev/console` registration |
| `STEP-0093` | Add a direct PL011 console-ready banner after `/dev/console` registration | completed | `manifests/2026-03-20-aarch64-generic-console-ready-diagnostic.md` | the new banner builds and validates, but it never appears in 12-second or 20-second generic QEMU smoke runs, proving that the current runtime path does not reach successful `_PATH_CONSOLE` registration in the current boot window | define the first `/dev/tty0` registration diagnostic after the missing console-ready banner |
| `STEP-0094` | Define the first `/dev/tty0` registration diagnostic after the missing console-ready banner | completed | `manifests/2026-03-20-aarch64-generic-tty0-diagnostic-scope.md` | the smallest useful follow-up is a raw `/dev/tty0` registration banner, because `/dev/tty0` registration is the immediate remaining split point between `pl011_init()` completion and `_PATH_CONSOLE` registration | add a direct PL011 `/dev/tty0` registration banner |
| `STEP-0095` | Add a direct PL011 `/dev/tty0` registration banner | completed | `manifests/2026-03-20-aarch64-generic-tty0-diagnostic.md` | the new banner builds and validates, but it never appears in the generic QEMU smoke run, proving that the current runtime path does not reach successful `/dev/tty0` registration in the current boot window | define the first generic startup-timing test after the missing `/dev/tty0` banner |
| `STEP-0096` | Define the first generic startup-timing test after the missing `/dev/tty0` banner | completed | `manifests/2026-03-20-aarch64-generic-devfs-wait-scope.md` | a single `wait 500` between `dummyfs;-N;devfs;-D` and `pl011-tty` is the smallest runtime test of the observed startup-order hypothesis, and it is more likely to move the fast lane forward than another equally blind driver-only marker | add a short generic `user.plo` wait before `pl011-tty` |
| `STEP-0097` | Add a short generic `user.plo` wait before `pl011-tty` | abandoned | `manifests/2026-03-20-aarch64-generic-wait-test.md` | the QEMU run and `plo/cmds/wait.c` source inspection show that `wait` is an interactive loader console command rather than a passive delay, so it cannot be used as a non-interactive timing fix in the generic fast lane; the experiment was reverted | define the first Pi 4-specific no-hardware scaffold step after the generic QEMU runtime boundary |
| `STEP-0098` | Define the first Pi 4-specific no-hardware scaffold step after the generic QEMU runtime boundary | completed | `manifests/2026-03-20-aarch64-rpi4b-project-scope.md` | the smallest Pi 4-oriented step is a project-local scaffold `aarch64a53-generic-rpi4b` layered on top of the existing generic target, because it adds board-local files without widening the cross-repo target matrix | add the first Pi 4 project-local scaffold on top of `aarch64a53-generic` |
| `STEP-0099` | Add the first Pi 4 project-local scaffold on top of `aarch64a53-generic` | completed | `manifests/2026-03-20-aarch64-rpi4b-project-scaffold.md` | `phoenix-rtos-project` now exposes a build-validated `aarch64a53-generic-rpi4b` project with Pi 4 board config and a project-local `user.plo` override, while still reusing the existing generic target machinery | define the first Pi 4 firmware-facing boot-staging step |
| `STEP-0100` | Define the first Pi 4 firmware-facing boot-staging step | completed | `manifests/2026-03-20-aarch64-rpi4b-firmware-staging-scope.md` | the smallest next step is a project-local Pi 4 boot-tree staging change that emits a firmware-facing `config.txt` and renamed raw `plo` image, while explicitly deferring DTB staging and EL2/EL3 handoff work | stage the first Pi 4 firmware boot-tree artifacts |
| `STEP-0101` | Stage the first Pi 4 firmware boot-tree artifacts | completed | `manifests/2026-03-20-aarch64-rpi4b-firmware-staging.md` | the Pi 4 scaffold now stages a firmware-facing boot tree with `config.txt` and `kernel8.img`, but the staged tree still lacks a DTB and the generic loader still assumes EL3 entry | define the first optional Pi 4 DTB staging hook |
| `STEP-0102` | Define the first optional Pi 4 DTB staging hook | completed | `manifests/2026-03-20-aarch64-rpi4b-dtb-hook-scope.md` | the smallest next DTB step is a project-local optional hook that stages a supplied Pi 4 DTB when available, without making an external DTB blob mandatory for every build | add the first optional Pi 4 DTB staging hook |
| `STEP-0103` | Add the first optional Pi 4 DTB staging hook | completed | `manifests/2026-03-20-aarch64-rpi4b-dtb-hook.md` | the Pi 4 project now stages `bcm2711-rpi-4-b.dtb` when it is supplied through `RPI4B_DTB_PATH` or a project-local file, while the default no-DTB build remains green and stages only `config.txt` plus `kernel8.img` | define the first generic loader entry step that removes the EL3-only Pi 4 boot blocker |
| `STEP-0104` | Define the first generic loader entry step for Pi 4 firmware handoff | completed | `manifests/2026-03-20-aarch64-generic-plo-entry-scope.md` | source inspection plus QEMU reset logging now bound the next step tightly: the generic loader must accept EL1, EL2, and EL3 entry, and generic QEMU already provides distinct no-hardware EL1 and EL2 validation modes | implement generic `plo` multi-EL entry and kernel handoff support |
| `STEP-0105` | Implement generic `plo` multi-EL entry and kernel handoff support | completed | `manifests/2026-03-20-aarch64-generic-plo-entry.md` | generic `plo` now boots and hands off correctly from EL3, EL2, and EL1 in the generic QEMU lane, while the Pi 4 scaffold still builds; the next Pi 4 blocker is how firmware-booted `kernel8.img` receives the remaining Phoenix payload | define the first Pi 4 firmware payload-staging step after multi-EL loader bring-up |
| `STEP-0106` | Define the first Pi 4 firmware payload-staging step after multi-EL loader bring-up | completed | `manifests/2026-03-20-aarch64-rpi4b-payload-staging-scope.md` | the smallest next Pi 4 step is to stage `loader.disk` into the firmware-facing boot tree and have Raspberry Pi firmware preload it to generic `plo` `RAM_ADDR`, reusing the existing `ram0` payload path instead of adding storage drivers | stage `loader.disk` for Pi 4 firmware preload at the generic `ram0` address |
| `STEP-0107` | Stage `loader.disk` for Pi 4 firmware preload at the generic `ram0` address | completed | `manifests/2026-03-20-aarch64-rpi4b-payload-staging.md` | the Pi 4 boot tree now stages `loader.disk` and explicitly preloads it to `0x48000000`, which closes the raw payload-transport gap while keeping the generic `ram0` path intact; the next Pi 4 blocker is supplying `system.dtb` to the kernel payload | define the first Pi 4 DTB propagation step into the kernel-visible payload |
| `STEP-0108` | Define the first Pi 4 DTB propagation step into the kernel-visible payload | completed | `manifests/2026-03-20-aarch64-rpi4b-dtb-propagation-scope.md` | the smallest next step is to reuse the existing optional Pi 4 DTB source, copy it into `${PREFIX_ROOTFS}/etc/system.dtb`, and restore the `blob` load entry in the Pi 4 `user.plo.yaml`, keeping the generic kernel DTB contract intact | propagate the Pi 4 DTB into the kernel-visible payload |
| `STEP-0109` | Propagate the Pi 4 DTB into the kernel-visible payload | completed | `manifests/2026-03-20-aarch64-rpi4b-dtb-propagation.md` | supplied-DTB builds now copy the Pi 4 DTB into `${PREFIX_ROOTFS}/etc/system.dtb`, stage it in the firmware boot tree, and emit the matching `blob` load entry in the generated Pi 4 loader script, while default no-DTB builds stay green | define the first Pi 4 `plo` address-override step so the loader stops assuming QEMU `virt` UART and GIC base addresses on real hardware |
| `STEP-0110` | Define the first Pi 4 `plo` address-override step | completed | `manifests/2026-03-20-aarch64-rpi4b-plo-address-override-scope.md` | the smallest next step is to make the generic AArch64 `plo` UART and GIC base addresses board-overridable through `board_config.h`, preserving the current QEMU defaults while letting the Pi 4 project supply BCM2711-specific MMIO bases | make generic `plo` MMIO base addresses board-overridable for Pi 4 |
| `STEP-0111` | Make generic `plo` MMIO base addresses board-overridable for Pi 4 | completed | `manifests/2026-03-20-aarch64-rpi4b-plo-address-overrides.md` | generic `plo` now resolves BCM2711 MMIO bases on the Pi 4 project while preserving the QEMU `virt` defaults for the generic smoke lane, and the Pi 4 firmware `kernel_address` already matches the loader link address | define the first reproducible Pi 4 boot-artifact packaging step |
| `STEP-0112` | Define the first Pi 4 firmware-file staging step | completed | `manifests/2026-03-20-aarch64-rpi4b-firmware-file-staging-scope.md` | the smallest next step is to let the Pi 4 project consume an operator-supplied Raspberry Pi firmware-file directory and copy those firmware files into the staged boot tree while keeping default no-firmware builds green | stage operator-supplied Raspberry Pi firmware files into the Pi 4 boot tree |
| `STEP-0113` | Stage operator-supplied Raspberry Pi firmware files into the Pi 4 boot tree | completed | `manifests/2026-03-20-aarch64-rpi4b-firmware-file-staging.md` | the Pi 4 project now accepts operator-supplied firmware files through `RPI4B_FIRMWARE_DIR` or a project-local `firmware/` directory and stages the required files into the firmware boot tree while default no-firmware builds stay green | refresh the Linux VM QEMU lane to a `raspi4b`-capable stable release and validate a first Pi 4 emulation smoke path |
| `STEP-0114` | Refresh the Linux VM QEMU lane to a `raspi4b`-capable stable release | completed | `manifests/2026-03-20-aarch64-rpi4b-qemu-refresh.md` | `phoenix-dev` now has a VM-local official QEMU `10.2.2` lane with `raspi4b`, the packaged Ubuntu `8.2.2` QEMU remains fallback-only, and the first Pi 4 smoke now fails in boot progress rather than in environment setup | define the first bounded emulated Pi 4 boot blocker after the no-output `raspi4b` smoke |
| `STEP-0115` | Define the first bounded emulated Pi 4 boot blocker after the no-output `raspi4b` smoke | completed | `manifests/2026-03-20-aarch64-rpi4b-qemu-raw-image-load-mismatch-scope.md` | QEMU `raspi4b` direct raw-kernel boot is now the most likely blocker because raw AArch64 images are loaded at `0x00080000` while the Pi 4 Phoenix `kernel8.img` is linked for `0x40080000`; the next smallest step is to validate `plo.elf` as the QEMU `-kernel` | validate `plo.elf` as the first `raspi4b` QEMU kernel handoff |
| `STEP-0116` | Validate `plo.elf` as the first `raspi4b` QEMU kernel handoff | completed | `manifests/2026-03-20-aarch64-rpi4b-qemu-plo-elf-handoff.md` | the Pi 4 QEMU lane now reaches visible `plo` output with `plo.elf` as `-kernel`, but then falls into an early exception storm because QEMU does not apply its Linux-only secondary boot stub to ELF payloads and generic AArch64 `plo` has no secondary-core containment yet | define the first generic AArch64 `plo` secondary-core containment step for Pi 4 QEMU |
| `STEP-0117` | Define the first generic AArch64 `plo` secondary-core containment step for Pi 4 QEMU | completed | `manifests/2026-03-20-aarch64-generic-plo-secondary-containment-scope.md` | the smallest next patch is now explicit: add a generic AArch64 `plo` non-primary-core trap and release flag, mirroring the existing ZynqMP containment pattern while keeping the change localized to generic startup and jump-to-kernel code | implement generic AArch64 `plo` secondary-core containment |
| `STEP-0118` | Implement generic AArch64 `plo` secondary-core containment | completed | `manifests/2026-03-20-aarch64-generic-plo-secondary-containment.md` | generic AArch64 `plo` now traps non-primary cores before `_startc`, the generic `virt` lane still reaches kernel and userspace output, and the Pi 4 `plo.elf` lane now reaches a stable post-alias boundary instead of an early multi-core exception storm | define the first DTB-backed Pi 4 QEMU follow-up step |
| `STEP-0119` | Define the first DTB-backed Pi 4 QEMU follow-up step | completed | `manifests/2026-03-20-aarch64-rpi4b-dtb-backed-qemu-scope.md` | the next smallest Pi 4 blocker is now DTB-backed kernel handoff, because the known-good generic lane includes `system.dtb` while the current Pi 4 validation build does not | validate the Pi 4 QEMU lane with an explicit DTB passed through `RPI4B_DTB_PATH` |
| `STEP-0120` | Validate the Pi 4 QEMU lane with an explicit DTB passed through `RPI4B_DTB_PATH` | completed | `manifests/2026-03-20-aarch64-rpi4b-explicit-dtb-validation.md` | an explicit Pi 4 DTB moves the `raspi4b` QEMU lane up to `pl011-tty: started`, proving that DTB-backed handoff is required and that the next fast blocker is beyond loader transport and DTB presence | define the first shared console-readiness step after DTB-backed Pi 4 validation |
| `STEP-0121` | Define the first shared console-readiness step after DTB-backed Pi 4 validation | completed | `manifests/2026-03-20-aarch64-console-readiness-scope.md` | the next smallest fast-lane blocker is now shared between generic and Pi 4 QEMU lanes: both reach `pl011-tty: started` but not console readiness, so the next step should target `pl011-tty` device registration rather than more Pi 4 loader or DTB work | scope the smallest `pl011-tty` console-readiness implementation step |
| `STEP-0122` | Scope the smallest `pl011-tty` console-readiness implementation step | completed | `manifests/2026-03-20-aarch64-pl011-console-retry-scope.md` | the smallest next patch is now explicit: add a bounded driver-local `create_dev()` retry path in `pl011-tty` for `/dev/tty0` and `/dev/console` rather than widening into loader-script or devfs redesign | implement bounded `create_dev()` retry handling in `pl011-tty` |
| `STEP-0123` | Implement bounded `create_dev()` retry handling in `pl011-tty` | completed | `manifests/2026-03-20-aarch64-pl011-console-retry.md` | the bounded retry experiment produced no new console output on either the generic or Pi 4 DTB-backed QEMU lanes, so the patch was reverted and the next step must be more diagnostic | scope the smallest raw registration-diagnostic step in `pl011-tty` |
| `STEP-0124` | Scope the smallest raw registration-diagnostic step in `pl011-tty` | completed | `manifests/2026-03-20-aarch64-pl011-registration-diagnostic-scope.md` | the next smallest high-signal step is to emit raw UART-side progress markers around `/dev/tty0` and `/dev/console` registration, because the current boundary cannot be distinguished from stderr or blind retries alone | implement raw UART-side registration diagnostics in `pl011-tty` |
| `STEP-0125` | Implement raw UART-side registration diagnostics in `pl011-tty` | completed | `manifests/2026-03-20-aarch64-pl011-registration-diagnostics.md` | both the generic and Pi 4 DTB-backed QEMU lanes now prove that execution reaches the start of `/dev/tty0` registration and then stops inside the shared `create_dev()` path | scope the smallest shared `create_dev()` diagnostic step |
| `STEP-0126` | Scope the smallest shared `create_dev()` diagnostic step | completed | `manifests/2026-03-20-aarch64-create-dev-diagnostic-scope.md` | the next smallest useful step is now explicit: instrument the shared `create_dev()` path in `libphoenix` so the serial output can distinguish devfs lookup retry from create-message blocking | implement bounded `create_dev()` diagnostics in the shared registration path |
| `STEP-0127` | Implement bounded `create_dev()` diagnostics in the shared registration path | completed | `manifests/2026-03-20-aarch64-create-dev-debug-negative-result.md` | the temporary `debug()`-based `libphoenix` probe produced no new visible markers on either QEMU lane and was reverted, so `debug()` is not a useful signal path for this boot window | move the diagnostics to the kernel syscall side |
| `STEP-0128` | Implement bounded kernel-side `create_dev()` syscall diagnostics | completed | `manifests/2026-03-20-aarch64-create-dev-syscall-diagnostics.md` | the generic lane now proves that `lookup("devfs", ...)` returns before the hang, and the boundary is therefore between lookup return and final `msgSend()` entry inside `create_dev()` | add a bounded post-lookup user-space probe that is visible on stdout |
| `STEP-0129` | Add a bounded post-lookup user-space probe that is visible on stdout | completed | `manifests/2026-03-20-aarch64-create-dev-stdout-probe-negative-result.md` | the temporary stdout probe produced no visible new markers on either QEMU lane and was reverted, so fd-1 writes are not a useful signal path for this boot window | replace the first `/dev/tty0` `create_dev()` call with a local raw lookup diagnostic helper |
| `STEP-0130` | Replace the first `/dev/tty0` `create_dev()` call with a local raw lookup diagnostic helper | completed | `manifests/2026-03-20-aarch64-pl011-tty0-devfs-lookup.md` | the local raw helper now proves that the first `lookup("devfs", ...)` fails quickly on both the generic and Pi 4 DTB-backed QEMU lanes | test whether dummyfs non-filesystem startup gating is the reason `devfs` is absent |
| `STEP-0131` | Test whether dummyfs non-filesystem stdout gating blocks `devfs` lookup | completed | `manifests/2026-03-20-aarch64-dummyfs-nonfs-stdout-gate-negative-result.md` | removing the dummyfs non-filesystem stdout wait changed nothing and was reverted, so that gate is not the blocker | measure delayed `devfs` availability with a bounded `pl011-tty` retry window |
| `STEP-0132` | Measure delayed `devfs` availability with a bounded `pl011-tty` retry window | completed | `manifests/2026-03-20-aarch64-pl011-delayed-devfs-retry-window.md` | both QEMU lanes now reach a retry marker and then stall before lookup completion, which narrows the blocker to a later lookup path that no longer returns promptly | define the smallest `dummyfs` main-loop visibility step |
| `STEP-0133` | Define the smallest `dummyfs` main-loop visibility step | completed | `manifests/2026-03-20-aarch64-dummyfs-main-loop-visibility-scope.md` | the next bounded diagnostic is now fixed: add kernel-console `debug()` markers in `dummyfs` around non-filesystem registration, initialization, and the first `mtLookup` handling path | implement those `dummyfs` visibility markers and rerun both QEMU lanes |
| `STEP-0134` | Instrument `dummyfs` lifecycle visibility on the kernel console | completed | `manifests/2026-03-20-aarch64-dummyfs-nonfs-startup-visibility.md` | the generic lane now proves that the `devfs` dummyfs instance registers and reaches `initialized` only after the first retry marker, but the blocked later lookup still never reaches that non-filesystem instance | define the smallest root-dummyfs lookup visibility step |
| `STEP-0135` | Define the smallest root-dummyfs lookup visibility step | completed | `manifests/2026-03-20-aarch64-dummyfs-root-lookup-visibility-scope.md` | the next bounded diagnostic is now fixed: extend the existing `dummyfs` markers so the generic lane can distinguish root-instance initialization and first `mtLookup` handling from the later non-filesystem `devfs` startup | implement the root-dummyfs lookup visibility markers and rerun both QEMU lanes |
| `STEP-0136` | Instrument root-dummyfs lookup visibility | completed | `manifests/2026-03-20-aarch64-dummyfs-root-lookup-visibility.md` | the generic lane still shows only the later `devfs` startup markers and no root-instance markers, which invalidates the root-dummyfs hypothesis for the current image layout | define the smallest kernel name-service visibility step |
| `STEP-0137` | Define the smallest kernel name-service visibility step | completed | `manifests/2026-03-20-aarch64-proc-name-lookup-visibility-scope.md` | the next bounded diagnostic is now fixed: instrument `proc/name.c` for `/` and `devfs` state transitions so the generic lane can distinguish no-root fast failure, cached `devfs`, and root-mediated blocking | implement the bounded kernel name-service visibility markers and rerun both QEMU lanes |
| `STEP-0138` | Instrument kernel name-service visibility for `devfs` | completed | `manifests/2026-03-20-aarch64-proc-name-lookup-visibility.md` | the generic lane now proves the first `lookup("devfs")` is a no-root fast failure and that `devfs` registers later, but the retry path never re-enters the kernel lookup code at all | define the smallest post-`usleep()` wake-return visibility step |
| `STEP-0139` | Define the smallest post-`usleep()` wake-return visibility step | completed | `manifests/2026-03-20-aarch64-pl011-retry-wake-visibility-scope.md` | the next bounded diagnostic is now fixed: add a raw `pl011-tty` marker immediately after the bounded retry-loop `usleep()` returns so the fast lane can distinguish sleep-stall from second-lookup blocking | implement the post-`usleep()` wake-return marker and rerun both QEMU lanes |
| `STEP-0140` | Implement the post-`usleep()` wake-return marker and rerun both QEMU lanes | completed | `manifests/2026-03-20-aarch64-pl011-retry-wake-visibility.md` | both the generic and Pi 4 DTB-backed lanes now prove that the first bounded retry path sleeps and never wakes, so the next bounded blocker is inside the common sleep / timer wakeup path rather than in a second `devfs` lookup | scope the smallest kernel-side sleep / wakeup programming visibility step |
| `STEP-0141` | Scope kernel sleep / wakeup programming visibility | completed | `manifests/2026-03-20-aarch64-kernel-sleep-visibility-scope.md` | the next bounded diagnostic is now fixed: add tightly filtered kernel markers in `proc/threads.c` for the first `nsleep()` enqueue, wakeup programming, and timer-interrupt delivery point | implement the filtered sleep / wakeup visibility markers and rerun both QEMU lanes |
| `STEP-0142` | Instrument kernel sleep / wakeup visibility | completed | `manifests/2026-03-20-aarch64-kernel-sleep-visibility.md` | the generic lane now proves that the blocked retry path reaches `proc_threadNanoSleep()` and wakeup programming but never reaches `threads_timeintr()`, so the next bounded blocker is the common AArch64 timer source / IRQ-delivery path | scope the smallest common AArch64 timer source / IRQ visibility step |
| `STEP-0143` | Scope common AArch64 timer source / IRQ visibility | completed | `manifests/2026-03-20-aarch64-gtimer-visibility-scope.md` | the next bounded diagnostic is now fixed: add one-time source / IRQ / wakeup-arm markers in `hal/aarch64/gtimer_timer.c`, because that file already owns the selected timer state without needing broader DTB or GIC changes | implement the filtered common AArch64 timer visibility markers and rerun both QEMU lanes |
| `STEP-0144` | Instrument common AArch64 timer source / IRQ visibility | completed | `manifests/2026-03-20-aarch64-gtimer-visibility.md` | the generic lane now proves that the common timer frontend selects `physical-nonsecure irq 30` and arms `1000 us`, but no timer interrupt reaches `threads_timeintr()`, so the next bounded blocker is the GIC-side handler-registration / dispatch path | scope the smallest GIC timer registration / dispatch visibility step |
| `STEP-0145` | Scope GIC timer registration / dispatch visibility | completed | `manifests/2026-03-20-aarch64-gic-timer-visibility-scope.md` | the next bounded diagnostic is now fixed: add tightly filtered markers in `hal/aarch64/interrupts_gicv2.c` for timer-handler registration and first timer-IRQ dispatch | implement the filtered GIC timer registration / dispatch markers and rerun both QEMU lanes |
| `STEP-0146` | Instrument GIC timer registration / dispatch visibility | completed | `manifests/2026-03-20-aarch64-gic-timer-visibility.md` | the generic lane now proves that the selected timer IRQ is registered in GICv2 but never dispatched, so the next bounded blocker is the timer-source / interrupt-generation side before GIC delivery | scope the first timer-source selection experiment |
| `STEP-0147` | Scope first timer-source selection experiment | completed | `manifests/2026-03-20-aarch64-timer-source-experiment-scope.md` | the next bounded experiment is now fixed: change only the common DTB timer-source preference order so the virtual timer is selected before the physical non-secure timer | implement the virtual-first timer-source experiment and rerun both QEMU lanes |
| `STEP-0148` | Implement virtual-first timer-source experiment | completed | `manifests/2026-03-20-aarch64-virtual-timer-experiment.md` | the generic lane switches from `physical-nonsecure irq 30` to `virtual irq 27`, but dispatch is still absent, so the next bounded code clue is explicit GIC configuration for timer PPIs | scope the first GIC PPI-configuration experiment |
| `STEP-0149` | Scope first GIC PPI-configuration experiment | completed | `manifests/2026-03-20-aarch64-gic-ppi-config-scope.md` | the next bounded experiment is now fixed: apply explicit interrupt configuration to non-SGI timer PPIs during handler registration while preserving the current no-retargeting rule | implement the PPI-configuration experiment and rerun both QEMU lanes |
| `STEP-0150` | Implement GIC PPI-configuration experiment | completed | `manifests/2026-03-20-aarch64-gic-ppi-config-experiment.md` | explicit PPI configuration still does not restore timer dispatch, so the next bounded clue is the architectural timer sysreg write path itself | scope the first architectural-timer write-barrier experiment |
| `STEP-0151` | Scope first architectural-timer write-barrier experiment | completed | `manifests/2026-03-20-aarch64-gtimer-write-barrier-scope.md` | the next bounded experiment is now fixed: add explicit barriers after architectural timer sysreg writes while leaving timer-source and GIC policy unchanged | implement the architectural-timer write-barrier experiment and rerun both QEMU lanes |
| `STEP-0152` | Implement architectural-timer write-barrier experiment | completed | `manifests/2026-03-20-aarch64-timer-write-barrier-experiment.md` | explicit instruction barriers after timer sysreg writes still do not restore timer dispatch on the generic fast lane, and the Pi 4 DTB-backed lane remains unchanged | scope the first architectural-timer register-readback experiment |
| `STEP-0153` | Scope first architectural-timer register-readback experiment | completed | `manifests/2026-03-20-aarch64-timer-readback-scope.md` | the next bounded experiment is now fixed: read back the selected architectural timer control state and timer value immediately after wakeup programming | implement the architectural-timer register-readback experiment and rerun both QEMU lanes |
| `STEP-0154` | Implement architectural-timer register-readback experiment | completed | `manifests/2026-03-20-aarch64-timer-readback-experiment.md` | the generic lane now proves the selected timer is genuinely armed with `ctl 0x1` and a live non-zero `tval`, so the next bounded clue is GIC-side IRQ state rather than timer programming | scope the first GIC timer-state visibility step |
| `STEP-0155` | Scope first GIC timer-state visibility step | completed | `manifests/2026-03-20-aarch64-gic-timer-state-scope.md` | the next bounded experiment is now fixed: read back the selected timer IRQ's interrupt-group and enable state immediately after handler registration | implement the GIC timer-state visibility step and rerun both QEMU lanes |
| `STEP-0156` | Implement the GIC timer-state visibility step | completed | `manifests/2026-03-20-aarch64-gic-timer-state-experiment.md` | the generic lane now proves the selected timer IRQ still reads back as `grp 0 en 0`, so the next bounded fix is a timer-only Group 1 experiment | scope the first timer-IRQ Group 1 experiment |
| `STEP-0157` | Scope first timer-IRQ Group 1 experiment | completed | `manifests/2026-03-20-aarch64-timer-group1-scope.md` | the next bounded experiment is now fixed: move only the selected timer IRQ to Group 1 before enabling it and keep the timer-state trace intact | implement the timer-IRQ Group 1 experiment and rerun both QEMU lanes |
| `STEP-0158` | Implement the timer-IRQ Group 1 experiment | completed | `manifests/2026-03-20-aarch64-timer-group1-experiment.md` | the generic lane still reads back `grp 0 en 0`, so the next bounded fix must move above the kernel into generic `plo` EL3 GIC initialization | scope the first generic `plo` EL3 GIC group-initialization experiment |
| `STEP-0159` | Scope first generic `plo` EL3 GIC group-initialization experiment | completed | `manifests/2026-03-20-aarch64-plo-gic-group-init-scope.md` | the next bounded experiment is now fixed: add EL3-aware Group 1 GIC initialization in generic `plo` and validate it through the existing kernel-side timer IRQ traces | implement the generic `plo` EL3 GIC group-initialization experiment and rerun both QEMU lanes |
| `STEP-0160` | Implement generic `plo` EL3 GIC group-initialization experiment | completed | `manifests/2026-03-20-aarch64-plo-gic-group-init-experiment.md` | the generic lane now dispatches the timer IRQ, wakes `pl011-tty`, and reaches successful tty and console registration, while the Pi 4 lane remains unchanged | scope generic `plo` entry-EL visibility on the Pi 4 lane |
| `STEP-0161` | Scope generic `plo` entry-EL visibility on the Pi 4 lane | completed | `manifests/2026-03-20-aarch64-plo-entry-el-scope.md` | the next bounded experiment is now fixed: print the generic loader entry EL on both fast lanes so the Pi 4 path can be compared directly with the working generic EL3 path | implement generic `plo` entry-EL visibility and rerun both QEMU lanes |
| `STEP-0162` | Implement generic `plo` entry-EL visibility | completed | `manifests/2026-03-20-aarch64-plo-entry-el-experiment.md` | both lanes reach `plo` in EL3, so the next bounded Pi 4 clue is the current stub DTB input rather than an exception-level mismatch | scope Pi 4 validation with an official firmware DTB |
| `STEP-0163` | Scope Pi 4 validation with an official firmware DTB | completed | `manifests/2026-03-20-aarch64-rpi4b-official-dtb-scope.md` | the next bounded experiment is now fixed: rerun the Pi 4 lane with a real Raspberry Pi firmware DTB and record the exact source revision used | validate the Pi 4 QEMU lane with an official firmware DTB |
| `STEP-0164` | Validate the Pi 4 QEMU lane with an official firmware DTB | completed | `manifests/2026-03-20-aarch64-rpi4b-official-dtb-validation.md` | the official Raspberry Pi firmware DTB moves the Pi 4 boundary back into loader pre-init, proving the old 274-byte stub was masking an earlier `call ram0 user.plo` blocker | scope Pi 4 loader user-script execution visibility |
| `STEP-0165` | Scope Pi 4 loader user-script execution visibility | completed | `manifests/2026-03-20-aarch64-rpi4b-call-visibility-scope.md` | the smallest next split is a `plo/cmds/call.c`-only visibility patch for open success, magic success, and each parsed line before `cmd_parse()` | implement filtered Pi 4 loader call visibility |
| `STEP-0166` | Implement filtered Pi 4 loader call visibility | completed | `manifests/2026-03-20-aarch64-rpi4b-call-visibility.md`, `plo 5e4dbcf` | Pi 4 now proves it executes the entire `user.plo` script through `go!`, so the current board-specific boundary is no longer script loading but the post-`go!` handoff path | scope the first post-`go!` Pi 4 handoff visibility step |
| `STEP-0167` | Scope first post-`go!` Pi 4 handoff visibility step | completed | `manifests/2026-03-20-aarch64-rpi4b-go-visibility-scope.md` | the smallest next split is a `plo/cmds/go.c`-only patch with raw markers before and after loader cleanup and immediately before the jump | implement filtered post-`go!` Pi 4 visibility |
| `STEP-0168` | Implement filtered post-`go!` Pi 4 visibility | completed | `manifests/2026-03-20-aarch64-rpi4b-go-visibility.md`, `plo 6f2cbf1` | Pi 4 now proves it completes `cmd_go()` cleanup and reaches `go: jump`, so the current board-specific boundary is inside `hal_cpuJump()` or the immediate EL-exit handoff | scope the first Pi 4 `hal_cpuJump()` / EL-exit visibility step |
| `STEP-0169` | Scope first Pi 4 `hal_cpuJump()` / EL-exit visibility step | completed | `manifests/2026-03-20-aarch64-rpi4b-jump-visibility-scope.md` | the smallest next split is a `plo/hal/aarch64/generic/hal.c`-only patch with raw markers around interrupt disable and the call into `hal_exitToEL1()` | implement filtered Pi 4 `hal_cpuJump()` visibility |
| `STEP-0170` | Implement filtered Pi 4 `hal_cpuJump()` visibility | completed | `manifests/2026-03-20-aarch64-rpi4b-jump-visibility.md`, `plo 568eabe` | Pi 4 now proves it reaches the exact call into `hal_exitToEL1()`, so the current board-specific boundary is inside the assembly EL handoff or immediately after it | scope the first assembly-side Pi 4 EL-exit visibility step |
| `STEP-0171` | Scope first assembly-side Pi 4 EL-exit visibility step | completed | `manifests/2026-03-20-aarch64-rpi4b-el-exit-visibility-scope.md` | the smallest next split is a `plo/hal/aarch64/generic/_init.S`-only patch with raw UART markers at `hal_exitToEL1()` entry and just before the final transfer instruction | implement assembly-side Pi 4 EL-exit visibility |
| `STEP-0172` | Implement assembly-side Pi 4 EL-exit visibility | completed | `manifests/2026-03-20-aarch64-rpi4b-el-exit-visibility.md`, `plo a2e266c` | Pi 4 now proves it reaches the EL3 `eret` transfer point itself and shows repeated markers from multiple cores, so the next bounded move is to validate whether the same multi-core handoff symptom appears on generic `virt -smp 4` | validate generic multi-core loader handoff behavior |
| `STEP-0173` | Validate generic multi-core loader handoff behavior | completed | `manifests/2026-03-20-aarch64-generic-qemu-smp4-handoff-validation.md` | generic `virt -smp 4` also shows repeated EL3 handoff markers but still boots, so the repeated marker pattern is not the Pi 4 failure by itself | scope earliest kernel-entry visibility on Pi 4 |
| `STEP-0174` | Scope single-core handoff experiment for the generic target | completed | `manifests/2026-03-20-aarch64-generic-single-core-handoff-scope.md` | the current generic kernel target is explicitly single-core while the generic loader releases trapped secondaries into the handoff path, so the next highest-signal bounded experiment is to keep non-boot CPUs parked across the current generic handoff | implement generic loader secondary-core containment across the current single-core handoff |
| `STEP-0175` | Keep generic secondary cores parked across kernel handoff | completed | `manifests/2026-03-20-aarch64-generic-secondary-core-containment.md`, `plo 7aa5938` | the containment experiment removes the repeated Pi 4 handoff markers but the Pi 4 lane still stops after a single `A3` and before the kernel banner, so the next bounded step is now earliest kernel-entry visibility | scope the smallest earliest-kernel-entry visibility step |
| `STEP-0176` | Scope earliest generic AArch64 kernel-entry visibility | completed | `manifests/2026-03-20-aarch64-kernel-entry-visibility-scope.md` | the smallest visibility mechanism is a raw PL011 marker at generic kernel `_start`, using the project `board_config.h` path for QEMU and Pi 4 UART base values | implement the earliest generic AArch64 kernel-entry visibility marker |
| `STEP-0177` | Add earliest generic AArch64 kernel-entry visibility | completed | `manifests/2026-03-20-aarch64-kernel-entry-visibility.md`, `phoenix-rtos-kernel 624a08e8` | Pi 4 now proves it reaches generic kernel `_start` by printing `K` after `A3`, so the remaining failure is inside the first kernel instructions after entry | scope the first post-entry early-init split around the A53-specific setup block |
| `STEP-0178` | Scope first post-entry early-init split around the A53-specific setup block | completed | `manifests/2026-03-20-aarch64-kernel-a53-block-scope.md` | the next bounded visibility target is the `__TARGET_AARCH64A53` system-register block in `hal/aarch64/_init.S`, because the current Pi 4 lane is built as `aarch64a53` but runs on `cortex-a72` in QEMU | implement raw post-entry markers around the A53-specific kernel setup block |
| `STEP-0179` | Split generic kernel early init around the A53-specific setup block | completed | `manifests/2026-03-20-aarch64-kernel-a53-block-visibility.md`, `phoenix-rtos-kernel ab205d09` | Pi 4 prints `KLM`, so it gets past the A53-specific block too; the current failure is later in early init, but the project should now pivot toward a real Cortex-A72-capable target path instead of continuing under `aarch64a53` | scope the first minimal Cortex-A72-capable generic target path for Pi 4 |
| `STEP-0180` | Scope the first minimal Cortex-A72-capable generic target path for Pi 4 | completed | `manifests/2026-03-20-aarch64-rpi4b-a72-pivot-scope.md` | Pi 4 is BCM2711/Cortex-A72, there is no `aarch64a72` target family yet, and the smallest enabling step is to generalize generic loader naming away from hard `aarch64a53` assumptions so a second generic CPU family can be added cleanly | generalize generic loader artifact naming for the first A72-capable target path |
| `STEP-0181` | Generalize generic artifact naming for the first A72-capable target path | completed | `manifests/2026-03-20-aarch64-generic-a72-loader-aliases.md`, `plo c233c13` | generic `plo` can now select an A72 generic kernel image and linker script without breaking the existing A53 lanes, so the next bounded step is the first actual A72 target scaffold | scope the first buildable `aarch64a72-generic-rpi4b` scaffold |
| `STEP-0182` | Scope the first buildable `aarch64a72-generic-rpi4b` scaffold | completed | `manifests/2026-03-20-aarch64-rpi4b-a72-scaffold-scope.md` | the smallest scaffold step is now fixed: admit `aarch64a72-generic`, add the generic target directory, add the Pi 4 A72 project directory, and validate the build | implement the first buildable `aarch64a72-generic-rpi4b` scaffold |
| `STEP-0183` | Implement the first buildable `aarch64a72-generic-rpi4b` scaffold | completed | `manifests/2026-03-20-aarch64-rpi4b-a72-scaffold.md`, `phoenix-rtos-build f05f148`, `phoenix-rtos-project 002d561`, `phoenix-rtos-filesystems 468bea5`, `phoenix-rtos-devices fcb651c`, `phoenix-rtos-utils f76e2c7` | the first buildable A72 Pi 4 scaffold now exists locally, and the A72 build plus both preserved A53 regression builds complete successfully in `phoenix-dev` | scope the first runtime validation of the A72 Pi 4 lane under QEMU |
| `STEP-0184` | Scope the first runtime validation of the A72 Pi 4 lane | completed | `manifests/2026-03-20-aarch64-rpi4b-a72-runtime-validation-scope.md` | the smallest next move is validation-only: run the new `aarch64a72-generic-rpi4b` image under `qemu-system-aarch64 -M raspi4b` with the official firmware DTB and compare the earliest visible boundary against the A53 diagnostic lane | validate the `aarch64a72-generic-rpi4b` Pi 4 QEMU lane |
| `STEP-0185` | Validate the `aarch64a72-generic-rpi4b` Pi 4 QEMU lane | completed | `manifests/2026-03-20-aarch64-rpi4b-a72-runtime-validation.md` | the first A72 Pi 4 runtime lane reaches the same `A3KLM` boundary as the current A53 diagnostic lane, so target identity alone does not move the failure | scope a narrow post-`KLM` early-kernel visibility split |
| `STEP-0186` | Scope the first post-`KLM` early-kernel visibility split | completed | `manifests/2026-03-20-aarch64-post-klm-visibility-scope.md` | the smallest next patch is two raw UART markers in common `_init.S`: one at `_core_0_virtual` and one immediately before branching to `main()` | implement the first post-`KLM` early-kernel visibility split |
| `STEP-0187` | Fix misleading generic AArch64 identification strings | completed | `manifests/2026-03-20-aarch64-generic-id-string-fix.md`, `plo a1a4745`, `phoenix-rtos-kernel c4264f07` | the A72 Pi 4 lane now reports `Cortex-A72 Generic` while the A53 lane still reports `Cortex-A53 Generic`, removing the misleading loader banner without changing the current `A3KLM` boot boundary | implement the first post-`KLM` early-kernel visibility split |
| `STEP-0188` | Try raw UART visibility after the `ttbr1` switch | completed | `manifests/2026-03-20-aarch64-post-ttbr1-uart-visibility-negative-result.md` | raw UART markers placed after the `ttbr1_el1` switch never printed on either lane and temporarily regressed the generic fast lane, so that probe style is invalid there | move the split to the first C-managed console points |
| `STEP-0189` | Split the first C-managed console points after `KLM` | completed | `manifests/2026-03-20-aarch64-first-c-entry-visibility.md`, `phoenix-rtos-kernel 960e607f` | the generic lane reaches both `_hal_consoleInit()` and the return to `main()`, while Pi 4 reaches neither, so the live blocker is now before successful console initialization | split the generic AArch64 console-init path |
| `STEP-0190` | Split the generic AArch64 console-init path | completed | `manifests/2026-03-20-aarch64-generic-console-init-visibility.md`, `phoenix-rtos-kernel 370c771f` | the generic lane reaches successful PL011 mapping while Pi 4 reaches none of the console-init markers, and the Pi 4 DTB clearly exposes bus addresses plus `/soc/ranges`, making missing address translation the strongest next blocker | implement a minimal DTB address-translation step for Pi 4 serial MMIO |
| `STEP-0191` | Translate Pi 4 serial MMIO through DTB `/soc/ranges` | completed | `manifests/2026-03-20-aarch64-rpi4b-dtb-serial-ranges-translation.md`, `phoenix-rtos-kernel 05c5ed9a` | the DTB parser now decodes `/soc` serial `reg` cells correctly and applies `/soc/ranges` translation, the generic lane still boots cleanly, and the Pi 4 A72 lane now reaches console init and the kernel banner before aborting later in `_map_init` | validate whether direct `raspi4b` QEMU now needs a QEMU-side DTB memory fix because firmware DTB customization is missing |
| `STEP-0192` | Validate the Pi 4 QEMU DTB memory-fix hypothesis | completed | `manifests/2026-03-20-aarch64-rpi4b-qemu-dtb-memory-hypothesis.md` | a one-off 2 GiB `memory@0/reg` patch changed nothing, while official Raspberry Pi kernel DTS files confirmed both that the memory node is bootloader-filled and that `stdout-path` targets the auxiliary UART | instrument the Pi 4 `_vm_init` / `_map_init` boundary directly |
| `STEP-0193` | Instrument the Pi 4 `_vm_init` / `_map_init` boundary | completed | `manifests/2026-03-20-aarch64-rpi4b-map-zero-free-visibility.md`, `phoenix-rtos-kernel 71fbea74` | the Pi 4 lane now proves `_map_init` reaches `map: zero free` and then aborts in the link loop, which points back to earlier DTB-backed memory-bank parsing instead of later VM logic | generalize root memory `reg` parsing to honor root DTB cell widths |
| `STEP-0194` | Parse root memory `reg` using DTB cell widths | completed | `manifests/2026-03-20-aarch64-rpi4b-root-memory-cell-parsing.md`, `phoenix-rtos-kernel 8a05f2ad` | the root memory parser fix keeps the generic lane healthy, official Pi 4 QEMU still aborts with firmware-unfilled `memory@0`, and a one-off patched DTB now moves Pi 4 past `_map_init` into a later timer or wakeup stall | automate the QEMU-only Pi 4 DTB memory fix so the emulated A72 lane no longer needs manual DTB surgery |
| `STEP-0195` | Automate the Pi 4 QEMU-only DTB memory fix | completed | `manifests/2026-03-20-aarch64-rpi4b-qemu-dtb-memory-hook.md`, `phoenix-rtos-project fd762d8` | the Pi 4 A72 build now accepts `RPI4B_QEMU_MEMORY_SIZE`, stages a patched DTB automatically, and the `raspi4b` lane reaches the same later post-`dummyfs` stall without manual `fdtput` work | scope the first Pi 4 patched-lane timer-source or interrupt-delivery follow-up |
| `STEP-0196` | Scope the first Pi 4 patched-lane timer follow-up | completed | `manifests/2026-03-20-aarch64-rpi4b-post-dummyfs-timer-scope.md` | the selected next experiment is to force the Pi 4 patched lane from the current virtual timer choice to the already-supported non-secure physical timer, because that changes only one runtime variable and directly tests the most suspicious difference behind the current post-`dummyfs` stall | implement the bounded Pi 4 physical-timer-source experiment |
| `STEP-0197` | Force the Pi 4 patched lane to the physical timer | completed | `manifests/2026-03-20-aarch64-rpi4b-physical-timer-experiment.md`, `phoenix-rtos-kernel c971d9b6`, `phoenix-rtos-project 9c9f109` | the Pi 4 patched lane now reports `physical-nonsecure irq 30` but still never dispatches or wakes the tty retry path, so timer-source choice is ruled out and the next step shifts to GIC pending-state visibility | scope the first Pi 4 timer-IRQ pending-state experiment |
| `STEP-0198` | Scope the Pi 4 timer-IRQ pending-state experiment | completed | `manifests/2026-03-20-aarch64-rpi4b-timer-pending-scope.md` | the selected next experiment is a first-arm pending-state probe for the selected timer IRQ, because that cleanly distinguishes “timer never asserted” from “asserted but never dispatched” without widening into scheduler or VM changes | implement the bounded Pi 4 timer pending-state probe |
| `STEP-0199` | Probe the Pi 4 timer IRQ pending state | completed | `manifests/2026-03-20-aarch64-rpi4b-timer-pending-probe.md`, `phoenix-rtos-kernel cace74c5` | the generic lane reports `gtimer: pending 1`, while the Pi 4 patched lane reports `gtimer: pending 0`; the timer IRQ never becomes pending on Pi 4 in the bounded probe window, so the next step moves earlier to timer countdown behavior itself | scope a bounded timer-countdown readback experiment |
| `STEP-0200` | Scope the Pi 4 timer-countdown readback experiment | completed | `manifests/2026-03-20-aarch64-rpi4b-timer-countdown-scope.md` | the next bounded move is a first-arm post-wait countdown readback, because it stays before the pending boundary and cleanly distinguishes a dead timer from a counting timer that still never asserts | implement the bounded first-arm timer-countdown readback experiment |
| `STEP-0201` | Read back the Pi 4 timer countdown after the first arm | completed | `manifests/2026-03-20-aarch64-rpi4b-timer-countdown-readback.md`, `phoenix-rtos-kernel 117e2076` | the Pi 4 timer does count down and reaches local interrupt status (`ctl 0x5`), but the earlier GIC pending probe still reports `0`, so the next step should stay on the timer-to-GIC seam and test whether the current PPI pending view is incomplete or wrong | analyze an external Pi 4 bring-up reference and then scope one bounded GIC PPI-state follow-up |
| `STEP-0202` | Analyze the external `rpi4-osdev` Pi 4 bring-up reference | completed | `manifests/2026-03-20-aarch64-rpi4b-external-reference-review.md` | the external review grew to include `rpi4-osdev`, Circle, `rpi-os`, and the OSDev Pi bare-bones article; together they confirm low peripheral mode, the normal `0x80000` AArch64 boot convention, and that Circle is the strongest external reference for the current Pi 4 physical-timer plus GIC seam | scope one bounded Pi 4 GIC PPI-state experiment |
| `STEP-0203` | Scope the Pi 4 GIC PPI-state follow-up | completed | `manifests/2026-03-20-aarch64-rpi4b-gic-ppi-state-scope.md` | the next bounded move is to compare the current `ISPENDR`-based timer pending readback with a private pending-state view for the same PPI, because Circle confirms the timer and IRQ identity are already correct and the remaining uncertainty is now the GIC view itself | implement the bounded private-pending-state readback experiment |
| `STEP-0204` | Read back Pi 4 private timer pending state | completed | `manifests/2026-03-20-aarch64-rpi4b-private-pending-readback.md`, `phoenix-rtos-kernel f7fb72da` | the private pending-state readback stays `0` on both lanes, including the working generic lane where `ISPENDR` becomes `1`, so `PPISR` is not the missing Pi 4 signal source; the next bounded move should test the visible Group 0 vs Group 1 timer difference | scope one bounded timer-group experiment |
| `STEP-0205` | Scope the Pi 4 timer-group experiment | completed | `manifests/2026-03-20-aarch64-rpi4b-timer-group-scope.md` | the selected next move is to make the timer IRQ group board-overridable and force the Pi 4 A72 lane to Group 0 for one bounded experiment, because that is the strongest remaining runtime difference between the working generic lane and the stuck Pi 4 lane | implement the bounded Pi 4 timer-group override experiment |
| `STEP-0206` | Force the Pi 4 timer IRQ into Group 0 | completed | `manifests/2026-03-20-aarch64-rpi4b-timer-group-override.md`, `phoenix-rtos-kernel 1ecadcc5`, `phoenix-rtos-project 8f82463` | forcing Group 0 changes the Pi 4 timer registration readback from `grp 1` to `grp 0`, but dispatch is still absent, so the timer-group difference is ruled out and the next move should restore the baseline before changing another runtime variable | restore the Pi 4 timer-group baseline |
| `STEP-0207` | Restore the Pi 4 timer-group baseline | completed | `manifests/2026-03-20-aarch64-rpi4b-timer-group-baseline-restore.md`, `phoenix-rtos-project 44e4c5e` | the Pi 4 A72 lane returns to `gic: timer handler set grp 1 en 1`, so the failed Group 0 board override is no longer active and the next experiment can again change exactly one variable | scope the smallest Pi 4 local-interrupt-routing experiment |
| `STEP-0208` | Scope the Pi 4 local-interrupt-routing experiment | completed | `manifests/2026-03-20-aarch64-rpi4b-local-interrupt-routing-scope.md` | Circle's `bcm2836.h` and `interrupt.cpp` make the BCM2711 local interrupt controller the strongest next hypothesis, so the next bounded move should enable `ARM_LOCAL_TIMER_INT_CONTROL0` bit `1` for the Pi 4 timer path and read back `ARM_LOCAL_IRQ_PENDING0` in the existing timer probe | implement the bounded Pi 4 local interrupt routing experiment |
| `STEP-0209` | Implement the Pi 4 local interrupt routing experiment | completed | `manifests/2026-03-20-aarch64-rpi4b-local-interrupt-routing.md`, `phoenix-rtos-kernel 2203295e`, `phoenix-rtos-project da3174a` | the Pi 4 lane now writes and reports `gic: local timer route 0x2`, but `ARM_LOCAL_IRQ_PENDING0` still stays `0x0` and timer dispatch is still absent, so the route-enable write alone is not the missing piece | scope a one-variable Pi 4 local prescaler experiment |
| `STEP-0210` | Scope the Pi 4 local prescaler experiment | completed | `manifests/2026-03-20-aarch64-rpi4b-local-prescaler-scope.md` | Circle's `sysinit.cpp` makes `ARM_LOCAL_PRESCALER = 39768216U` the strongest next one-variable follow-up inside the same local interrupt controller block, so the next bounded move should add exactly that write and compare the existing local pending evidence | implement the bounded Pi 4 local prescaler experiment |
| `STEP-0211` | Implement the Pi 4 local prescaler experiment | completed | `manifests/2026-03-20-aarch64-rpi4b-local-prescaler.md`, `phoenix-rtos-kernel 0c788fe0`, `phoenix-rtos-project 69cccfa` | the Pi 4 lane now reports both `gic: local prescaler 39768216` and `gic: local timer route 0x2`, but `gtimer: local pending 0x0` and GIC dispatch are still absent, so the prescaler write also fails to move the QEMU lane | analyze the Pi 4 QEMU timer wiring directly |
| `STEP-0212` | Analyze the Pi 4 QEMU timer wiring | completed | `manifests/2026-03-20-aarch64-rpi4b-qemu-timer-wiring-analysis.md` | local QEMU 10.2.2 source proves that `raspi4b` wires `GTIMER_PHYS` directly to GIC PPI 14 in `bcm2838.c`, so the local controller experiments are not on the active QEMU timer path | restore the QEMU fast lane by removing the local-controller detour and scope the next direct GTIMER-to-GIC follow-up |
| `STEP-0213` | Restore the Pi 4 QEMU fast lane after the local-controller detour | completed | `manifests/2026-03-20-aarch64-rpi4b-qemu-fast-lane-restore.md`, `phoenix-rtos-kernel 7e9bd2ff`, `phoenix-rtos-project f2932d6` | the Pi 4 QEMU lane is back to the pre-detour evidence set with `grp 1`, `pending 0`, `ppi pending 0`, and no dispatch, so the next bounded move can operate only on the direct GTIMER-to-GIC path | scope a one-variable GIC CPU-interface pending probe |
| `STEP-0214` | Scope the Pi 4 GIC CPU-interface pending probe | completed | `manifests/2026-03-20-aarch64-rpi4b-gic-cpu-interface-scope.md` | the next smallest direct-GIC split is to read `GICC_HPPIR`, because that distinguishes “CPU interface sees a pending interrupt” from “nothing is visible even there” without changing timer programming | implement the bounded Pi 4 GIC CPU-interface pending probe |
| `STEP-0215` | Implement the Pi 4 GIC CPU-interface pending probe | completed | `manifests/2026-03-20-aarch64-rpi4b-gic-cpu-interface.md`, `phoenix-rtos-kernel 8a5babd6` | the generic lane reports `gtimer: hppir 1023` after a successful timer dispatch, while the Pi 4 lane reports `gtimer: hppir 0` with no dispatch, so the next bounded move should explain the Pi 4 CPU-interface view rather than changing timer programming again | scope a follow-up on the CPU-interface alias or security view |
| `STEP-0216` | Scope the Pi 4 GIC CPU-interface alias probe | completed | `manifests/2026-03-20-aarch64-rpi4b-gic-cpu-alias-scope.md` | `GICC_AHPPIR` is the smallest alternate CPU-interface view already available in the current kernel register map, so the next bounded move should add exactly that one readback and compare it with the existing `hppir` result | implement the bounded Pi 4 GIC CPU-interface alias probe |
| `STEP-0217` | Implement the Pi 4 GIC CPU-interface alias probe | completed | `manifests/2026-03-20-aarch64-rpi4b-gic-cpu-alias.md`, `phoenix-rtos-kernel 1535a69f` | the generic lane reports `ahppir 0` while the Pi 4 lane reports `ahppir 524`; that result alone is not self-explanatory, and the stronger next clue is now the DTB interrupt-controller matcher and GIC base provenance | scope a bounded GIC base provenance probe |
| `STEP-0218` | Scope the Pi 4 GIC base provenance probe | completed | `manifests/2026-03-20-aarch64-rpi4b-gic-base-scope.md` | the strongest new hypothesis is that the Pi 4 lane may be using the wrong GIC CPU-interface base, so the next bounded move should print the actual `gicd` and `gicc` addresses selected at runtime | implement the bounded GIC base provenance trace |
| `STEP-0219` | Scope a debugger-assisted Pi 4 QEMU inspection lane | completed | `manifests/2026-03-20-aarch64-rpi4b-gdb-lane-scope.md` | the current Pi 4 build already has symbolized ELFs and `gdb-multiarch` is now available in `phoenix-dev`, so the next bounded move should use the QEMU gdbstub to inspect the live GIC base and CPU-interface state instead of adding another print-only patch first | establish the bounded Pi 4 `raspi4b` QEMU gdbstub inspection lane |
| `STEP-0220` | Establish the Pi 4 `raspi4b` QEMU gdbstub inspection lane | completed | `manifests/2026-03-20-aarch64-rpi4b-gic-gdb-base-provenance.md` | the first debugger-assisted Pi 4 attach proves that the live interrupt state can be inspected through the gdbstub without another kernel patch, and it captures the current mapped distributor and CPU-interface pointers for the stuck lane | scope a bounded generic-versus-Pi4 CPU-interface register comparison |
| `STEP-0221` | Scope a comparative GIC CPU-interface register snapshot via GDB | completed | `manifests/2026-03-20-aarch64-gic-gdb-comparison-scope.md` | the first Pi 4 gdbstub attach narrows the next uncertainty to the meaning of the live CPU-interface register state, so the next bounded move should read the same small register set on both the generic and Pi 4 lanes | implement the bounded generic-versus-Pi4 CPU-interface register snapshot |
| `STEP-0222` | Implement the bounded generic-versus-Pi4 GIC CPU-interface register snapshot | completed | `manifests/2026-03-20-aarch64-gic-gdb-register-comparison.md` | the live cross-lane snapshot confirms that the Pi 4 CPU-interface register view is materially different from the working generic lane, and local QEMU source also shows that the existing `AHPPIR` offset is not explicitly implemented, so the next bounded move should inspect the exact pre-map `dtb_getGIC()` return values | scope a direct pre-map GIC-base inspection at `_hal_interruptsInit()` |
| `STEP-0223` | Scope a direct pre-map GIC-base inspection at `_hal_interruptsInit()` | completed | `manifests/2026-03-20-aarch64-gic-pre-map-scope.md` | `_hal_interruptsInit+64` is the precise post-`dtb_getGIC()` / pre-map stop point, and the returned `gicd` / `gicc` locals are recoverable from `sp + 0x48` and `sp + 0x40` | implement the cross-lane pre-map `dtb_getGIC()` base inspection |
| `STEP-0224` | Implement the cross-lane pre-map `dtb_getGIC()` base inspection | completed | `manifests/2026-03-20-aarch64-gic-pre-map-values.md` | generic returns the expected non-zero GIC base pair, while Pi 4 returns `gicd = 0` and `gicc = 0`, so the active blocker is now clearly inside DTB interrupt-controller discovery or `reg` decoding | scope the smallest Pi 4 DTB interrupt-controller fix |
| `STEP-0225` | Scope the smallest Pi 4 DTB interrupt-controller fix | completed | `manifests/2026-03-20-aarch64-pi4-gic-dtb-fix-scope.md` | the smallest correct fix is to recognize the Pi 4 `/soc/interrupt-controller@...` node and decode its `reg` tuples through `/soc` cell widths plus `ranges` translation | implement the bounded Pi 4 GIC DTB discovery fix |
| `STEP-0226` | Implement the bounded Pi 4 GIC DTB discovery fix | completed | `manifests/2026-03-20-aarch64-pi4-gic-dtb-fix.md`, `phoenix-rtos-kernel 32c807d6` | the bounded `dtb.c` fix restores non-zero Pi 4 GIC discovery and brings the `raspi4b` QEMU lane through timer dispatch, tty registration, console readiness, and early syspage-program startup | scope one bounded later-boot parity check |
| `STEP-0227` | Scope one bounded later-boot Pi 4 parity check | completed | `manifests/2026-03-20-aarch64-pi4-later-boot-parity-scope.md` | within the current 15-second QEMU window, Pi 4 and generic now reach the same visible later-boot band through `dummyfs: initialized`, so the next blocker is no longer uniquely Pi 4-specific in the observed window | scope a shared later-boot visibility step |
| `STEP-0228` | Scope the smallest shared later-boot visibility step | completed | `manifests/2026-03-21-aarch64-later-boot-spawn-visibility-scope.md` | the smallest shared later-boot question is whether the kernel actually completes the syspage app spawn loop, so the next step should add bounded before/after spawn markers in `main_initthr()` | implement bounded syspage spawn visibility |
| `STEP-0229` | Implement bounded syspage spawn visibility | completed | `phoenix-rtos-kernel 5421a83f` | both generic and Pi 4 lanes prove that `dummyfs`, `pl011-tty`, and `psh` all spawn successfully, so the next blocker is no longer in the kernel syspage spawn loop itself | scope a read-only interactive shell probe |
| `STEP-0230` | Scope the later-boot interactive shell probe | completed | `manifests/2026-03-21-aarch64-later-boot-interactive-probe-scope.md` | since both lanes already spawn `psh`, the smallest next question is whether `psh` is live and responsive on the console without any further code changes | implement the bounded interactive console probe |
| `STEP-0231` | Implement the bounded interactive console probe | completed | `manifests/2026-03-21-aarch64-later-boot-interactive-probe.md` | the generic interactive PTY probe shows UART RX echo but no prompt or command response, so the next blocker is now inside the shared `psh` startup path rather than kernel-side spawning | scope the smallest `psh`-local startup visibility step |
| `STEP-0232` | Scope the smallest `psh`-local startup visibility step | completed | `manifests/2026-03-21-aarch64-psh-startup-visibility-scope.md` | the next useful split is now explicit: add a few raw markers in `psh.c` and `pshapp.c` so the silence can be divided into pre-interactive, tty-open, terminal-control, or first-read boundaries | implement bounded `psh` startup visibility |
| `STEP-0233` | Implement bounded `psh` startup visibility | completed | `manifests/2026-03-21-aarch64-psh-startup-visibility.md`, `phoenix-rtos-utils c190ceb` | neither generic nor Pi 4 shows any `psh:` marker within 30 seconds, so the next useful split now has to sit below shell-visible stdio | scope the smallest below-stdio `psh` process-entry visibility step |
| `STEP-0234` | Scope the smallest below-stdio `psh` process-entry visibility step | completed | `manifests/2026-03-21-aarch64-psh-first-user-schedule-scope.md` | the narrowest reliable hook is now `_threads_schedule()` just before `hal_cpuRestore()`, filtered to the `psh` process and user-mode return only | implement bounded `psh` first-user-schedule visibility |
| `STEP-0235` | Implement bounded `psh` first-user-schedule visibility | completed | `manifests/2026-03-21-aarch64-psh-first-user-schedule.md`, `phoenix-rtos-kernel 95715a1b` | both generic and Pi 4 prove that `psh` reaches first user-mode execution, so the next blocker is later inside the shell startup logic | scope the smallest kernel-side `psh` root-lookup success trace |
| `STEP-0236` | Scope the smallest kernel-side `psh` root-lookup success trace | completed | `manifests/2026-03-21-aarch64-psh-root-lookup-scope.md` | the next earliest reliable hook is now `syscalls_lookup()` filtered to process `psh`, path `/`, and success only | implement bounded `psh` root-lookup success visibility |
| `STEP-0237` | Implement bounded `psh` root-lookup success visibility | completed | `manifests/2026-03-21-aarch64-psh-root-lookup.md`, `phoenix-rtos-kernel a15db9d7` | neither lane prints a successful `psh` root lookup within 30 seconds, so the next split must distinguish repeated failure from no lookup reached | scope the smallest first-attempt `psh` root-lookup trace |
| `STEP-0238` | Scope the smallest first-attempt `psh` root-lookup trace | completed | `manifests/2026-03-21-aarch64-psh-root-lookup-result-scope.md` | the smallest remaining split is to keep the hook in `syscalls_lookup()` but print the first `psh` lookup of `/` regardless of result, with the result code attached | implement bounded first-result visibility for `psh` `lookup("/")` |
| `STEP-0239` | Implement bounded first-result visibility for `psh` `lookup("/")` | completed | `manifests/2026-03-21-aarch64-psh-root-lookup-result.md`, `phoenix-rtos-kernel 60909ccd` | both generic and Pi 4 now prove that `psh` calls `lookup("/")` and gets `-22`, so the next step should inspect the shared `lookup()` contract instead of adding more blind runtime probes | scope the smallest shared `lookup()`-contract review |
| `STEP-0240` | Scope the smallest shared `lookup()`-contract review | completed | `manifests/2026-03-21-aarch64-rootfs-gap-scope.md` | source review proves the shared `-22` is simply “root filesystem not registered yet”, and the fast-lane images currently start `devfs` but no root dummyfs instance | implement the smallest root-dummyfs fast-lane image change |
| `STEP-0241` | Implement the smallest root-dummyfs fast-lane image change | completed | `manifests/2026-03-21-aarch64-root-dummyfs-fastlane.md`, `phoenix-rtos-project 1be31fd` | both generic and Pi 4 now register `/`, recover from the old `lookup("/") -> -22` stall, and advance to `psh: tty open` / `psh: app done` | scope the smallest `psh_ttyopen()` failure split |
| `STEP-0242` | Scope the smallest `psh_ttyopen()` failure split | completed | `manifests/2026-03-21-aarch64-psh-ttyopen-scope.md` | `psh` now reaches `psh_ttyopen("/dev/console")` on both lanes and exits before `psh: tty ready`, so the next useful move is to expose the first shared failure reason from `psh_ttyopen()` itself | implement bounded `psh_ttyopen()` failure visibility |
| `STEP-0243` | Implement bounded `psh_ttyopen()` failure visibility | completed | `manifests/2026-03-21-aarch64-psh-ttyopen-result.md`, `phoenix-rtos-utils 26eb2e2` | both fast lanes now prove the same next blocker, `open("/dev/console") -> -ENOENT`, so the next step should move from shell-local tracing to the shared console path and registration contract | scope the smallest `/dev/console` lookup split |
| `STEP-0244` | Scope the smallest `/dev/console` lookup split | completed | `manifests/2026-03-21-aarch64-console-path-scope.md` | source review proves this is not a trivial wrong-string bug; `pl011-tty` already registers `_PATH_CONSOLE`, so the next smallest move is a kernel-side first-result trace for `lookup(\"/dev/console\")` | implement bounded `/dev/console` lookup visibility |
| `STEP-0245` | Implement bounded `/dev/console` lookup visibility | completed | `manifests/2026-03-21-aarch64-console-lookup-via-syscalls.md`, `phoenix-rtos-kernel ab1e2f89` | the trace produces a useful negative result: the failing `/dev/console` open path never reaches `syscalls_lookup()`, and source review shows `open()` goes through `posix_open()` and `proc_lookup()` instead | scope the smallest `posix_open()` or `proc_lookup()` console split |
| `STEP-0246` | Scope the smallest `posix_open()` or `proc_lookup()` console split | completed | `manifests/2026-03-21-aarch64-posix-open-console-scope.md` | source review narrows the next seam to `posix_open()` itself, because that is where the failing `proc_lookup(filename, ...)` call is made for `open("/dev/console")` | implement bounded `posix_open()` console visibility |
| `STEP-0247` | Implement bounded `posix_open()` console visibility | completed | `manifests/2026-03-21-aarch64-posix-open-console-result.md`, `phoenix-rtos-kernel 28c2908b` | both generic and Pi 4 produce the same negative result: the failing console open still does not reach `posix_open()`, so the next shared seam is in libphoenix before the kernel open syscall | scope the smallest libphoenix-side open split |
| `STEP-0248` | Scope the smallest libphoenix-side open split | completed | `manifests/2026-03-21-aarch64-libphoenix-open-console-scope.md` | source review narrows the next seam to libphoenix `open()` itself, because it performs `stat()` and `resolve_path()` before `sys_open()` | implement bounded libphoenix `open("/dev/console")` visibility |
| `STEP-0249` | Implement bounded libphoenix `open("/dev/console")` visibility | completed | `manifests/2026-03-21-aarch64-libphoenix-open-console-result.md`, `libphoenix 81ffc84` | both fast lanes still show `open -2`, but neither the libphoenix nor kernel open traces fire, so the next strongest hypothesis is now call-path or symbol mismatch | scope the smallest binary or symbol inspection step |
| `STEP-0250` | Scope the smallest binary or symbol inspection step | completed | `manifests/2026-03-21-aarch64-console-callpath-scope.md` | the next tight seam is no longer another path-resolution trace; it is inspection of the built `psh` image and its `open` references | inspect the built `psh` image and `open` references |
| `STEP-0251` | Inspect the built `psh` image and `open` references | completed | `manifests/2026-03-21-aarch64-gdb-console-open-path.md` | built-image inspection plus a bounded QEMU gdbstub session prove that `psh_ttyopen("/dev/console")` really reaches libphoenix `open()`, `stat()` returns `-1`, `resolve_path()` returns `NULL`, and `sys_open()` is never reached | scope the smallest `resolve_path("/dev/console")` failure split |
| `STEP-0252` | Scope the smallest `resolve_path("/dev/console")` failure split | completed | `manifests/2026-03-21-aarch64-resolve-path-dev-seam.md` | source review plus a second bounded gdbstub pass prove that both the `stat()` and direct `open()` path-resolution calls fail earlier than the `console` leaf, at intermediate `/dev` lookup with `ENOENT`; the next smallest move is therefore a project-local `/dev` namespace fix before shell startup | scope the smallest pre-`psh` `/dev` namespace fix |
| `STEP-0253` | Scope the smallest pre-`psh` `/dev` namespace fix | completed | `manifests/2026-03-21-aarch64-pre-psh-dev-bind-scope.md` | source review proves the smallest fast-lane fix is project-local startup wiring: stage `psh` aliases for `mkdir` and `bind`, then run them before the final shell so `devfs` becomes visible at `/dev` | implement the pre-`psh` `/dev` bind fast-lane fix |
| `STEP-0254` | Implement the pre-`psh` `/dev` bind fast-lane fix | completed | `manifests/2026-03-21-aarch64-fastlane-dev-bind.md`, `phoenix-rtos-project 1187e5a` | both generic and Pi 4 QEMU now move past the old `/dev/console` failure and reach `psh: tty ready`, `psh: readcmd`, and `(psh)%`; the next small step should clean up obsolete console-path probes | scope cleanup of obsolete console-path probes |
| `STEP-0255` | Scope cleanup of obsolete console-path probes | completed | `manifests/2026-03-21-aarch64-console-probe-cleanup-scope.md` | source review narrows the cleanup to the exact `/dev/console` investigation traces in `psh`, `libphoenix`, and the kernel open/lookup paths, while leaving broader boot-band markers in place for now | remove obsolete console-path probes after the fast-lane fix |
| `STEP-0256` | Remove obsolete console-path probes after the fast-lane fix | completed | `manifests/2026-03-21-aarch64-console-probe-cleanup.md`, `phoenix-rtos-utils c9d1504`, `libphoenix 118dea3`, `phoenix-rtos-kernel 08261996` | the prompt-reaching fast lane now survives on both generic and Pi 4 QEMU without the old `/dev/console` probes, so the next smallest useful move is to validate one real shell command through the new prompt | scope the smallest interactive shell-smoke validation step |
| `STEP-0257` | Scope the smallest interactive shell-smoke validation step | completed | `manifests/2026-03-21-aarch64-shell-smoke-scope.md` | source review selects the built-in `help` command as the smallest deterministic interactive smoke, with success defined as `Available commands:` followed by a returned `(psh)%` prompt on both QEMU lanes | implement the first interactive shell-smoke validation |
| `STEP-0258` | Implement the first interactive shell-smoke validation | completed | `manifests/2026-03-21-aarch64-shell-smoke-validation.md` | the first command-level smoke is now proven on Pi 4 `raspi4b`, but the generic `virt` lane still stalls after echoed `help`; the next bounded move is to adjust only the generic QEMU stdin path, not Phoenix source | scope the smallest generic-`virt` stdin-path adjustment |
| `STEP-0259` | Scope the smallest generic-`virt` stdin-path adjustment | completed | `manifests/2026-03-21-aarch64-generic-shell-stdin-scope.md` | the smallest next move is now fixed: replace the generic interactive smoke launch's `-serial mon:stdio -serial null -display none` path with `-nographic -monitor none`, matching the working Pi 4 stdio model as closely as possible | implement the generic-`virt` stdin-path adjustment |
| `STEP-0260` | Implement the generic-`virt` stdin-path adjustment | completed | `manifests/2026-03-21-aarch64-generic-shell-stdin-validation.md` | generic `virt` now matches the Pi 4 `help` smoke with `-nographic -monitor none`, proving the earlier generic-only failure was a QEMU stdio-launch issue rather than a Phoenix shell bug | scope the smallest reusable QEMU shell-smoke helper |
| `STEP-0261` | Scope the smallest reusable QEMU shell-smoke helper | completed | `manifests/2026-03-21-aarch64-qemu-shell-smoke-helper-scope.md` | the smallest useful helper is one host-side shell script in `scripts/` that wraps the validated VM-side `expect` smoke for `generic` and `rpi4b` only | implement the reusable QEMU shell-smoke helper |
| `STEP-0262` | Implement the reusable QEMU shell-smoke helper | completed | `manifests/2026-03-21-aarch64-qemu-shell-smoke-helper.md` | `scripts/qemu-shell-smoke.sh` now reproduces the validated `help` smoke on both generic and Pi 4 fast QEMU lanes, so the next small shell-level move is to validate one external applet path | scope the smallest external-applet shell smoke |
| `STEP-0263` | Scope the smallest external-applet shell smoke | completed | `manifests/2026-03-21-aarch64-external-applet-smoke-scope.md` | the smallest next command is `ls /`, because it exercises a real applet path and should deterministically show `dev` plus `syspage` on the current fast-lane root namespace | implement the first external-applet shell smoke |
| `STEP-0264` | Implement the first external-applet shell smoke | completed | `manifests/2026-03-21-aarch64-external-applet-smoke-validation.md` | `ls /` turns out to be a poor smoke shape because its natural markers collide with earlier boot text like `create_dev`, so the next bounded move is to switch to a unique-token external applet command | scope the smallest unique-token external-applet smoke |
| `STEP-0265` | Scope the smallest unique-token external-applet smoke | completed | `manifests/2026-03-21-aarch64-unique-token-applet-scope.md` | the next smallest external-applet smoke is `echo codex-smoke-echo`, because its output token is unique and does not collide with current boot-band logs | implement the unique-token external-applet smoke |
| `STEP-0266` | Implement the unique-token external-applet smoke | completed | `manifests/2026-03-21-aarch64-unique-token-applet-validation.md` | `echo codex-smoke-echo` still does not prove applet stdout because the token appears in the echoed command line itself, so the next bounded move is to use an applet command whose output differs from its invocation text | scope the smallest distinct-output external-applet smoke |
| `STEP-0267` | Scope the smallest distinct-output external-applet smoke | completed | `manifests/2026-03-21-aarch64-distinct-output-applet-scope.md` | the smallest next applet smoke is `echo -h`, because its usage text is distinct from the echoed command line and should provide a clean external-applet proof point | implement the distinct-output external-applet smoke |
| `STEP-0268` | Implement the distinct-output external-applet smoke | completed | `manifests/2026-03-21-aarch64-distinct-output-applet-validation.md` | both generic and Pi 4 now pass the clean external-applet smoke `echo -h`, so the next bounded move is to pivot back toward firmware-style Pi 4 boot artifact preparation | scope the smallest firmware-style Pi 4 boot artifact step |
| `STEP-0269` | Scope the smallest firmware-style Pi 4 boot artifact step | completed | `manifests/2026-03-21-aarch64-rpi4b-firmware-artifact-scope.md` | the next smallest no-hardware Pi 4 boot-path step is a helper that assembles a firmware-visible boot tree from the staged `rpi4b/` outputs plus a provided Raspberry Pi firmware directory | implement the Pi 4 firmware-boot-tree assembly helper |
| `STEP-0270` | Implement the Pi 4 firmware-boot-tree assembly helper | completed | `manifests/2026-03-21-aarch64-rpi4b-firmware-artifact-helper.md` | `scripts/assemble-rpi4b-bootfs.sh` now produces a firmware-visible Pi 4 boot tree from the staged outputs plus firmware files, so the next bounded artifact step is to turn that tree into a portable firmware-partition image | scope the smallest Pi 4 firmware-partition image step |
| `STEP-0271` | Scope the smallest Pi 4 firmware-partition image step | completed | `manifests/2026-03-21-aarch64-rpi4b-fat-image-scope.md` | the smallest next artifact is now fixed: build one FAT firmware image from the assembled `rpi4b-bootfs` directory using the already available `mformat` and `mcopy` tools in `phoenix-dev` | implement the Pi 4 FAT firmware-image helper |
| `STEP-0272` | Implement the Pi 4 FAT firmware-image helper | completed | `manifests/2026-03-21-aarch64-rpi4b-fat-image-helper.md` | `scripts/assemble-rpi4b-bootfs-img.sh` now builds a portable FAT Pi 4 boot image from the assembled boot tree, so the next bounded move is to decide whether this is already the first real-device artifact or whether a larger media image is still needed | scope the smallest post-FAT-image real-device artifact step |
| `STEP-0273` | Scope the smallest post-FAT-image real-device artifact step | completed | `manifests/2026-03-21-aarch64-post-fat-image-artifact-scope.md` | the current `rpi4b-bootfs.img` is now selected as the first device-facing Pi 4 artifact, because the early boot path still fits entirely in the firmware-visible FAT partition | scope the smallest operator-facing Pi 4 FAT-image handoff step |
| `STEP-0274` | Scope the smallest operator-facing Pi 4 FAT-image handoff step | completed | `manifests/2026-03-21-aarch64-fat-image-handoff-scope.md` | the smallest next improvement is a host-side export helper for the current VM-local Pi 4 FAT image, so the operator can actually use it on macOS without manual VM path spelunking | implement the Pi 4 FAT-image export helper |
| `STEP-0275` | Implement the Pi 4 FAT-image export helper | completed | `manifests/2026-03-21-aarch64-rpi4b-fat-image-export-helper.md` | `scripts/export-rpi4b-fat-image.sh` now exports the VM-local Pi 4 FAT image into `artifacts/rpi4b/rpi4b-bootfs.img`, and the host-visible artifact matches the VM-local source by SHA-256 | scope the smallest full Pi 4 SD-card image step |
| `STEP-0276` | Scope the smallest full Pi 4 SD-card image step | completed | `manifests/2026-03-21-aarch64-rpi4b-sdimg-scope.md` | the next bounded artifact is now fixed: build a one-partition MBR Pi 4 disk image around the existing `rpi4b-bootfs.img`, because that matches normal SD-card flashing workflows without widening into persistent-rootfs work | implement the smallest full Pi 4 SD-card image helper |
| `STEP-0277` | Implement the smallest full Pi 4 SD-card image helper | completed | `manifests/2026-03-21-aarch64-rpi4b-sdimg-helper.md` | `scripts/assemble-rpi4b-sdimg.sh` now produces a VM-local `rpi4b-sd.img` with a valid DOS partition table, one bootable FAT32 partition, and the expected embedded firmware-visible boot payload | scope the smallest host-visible Pi 4 SD-card image handoff step |
| `STEP-0278` | Scope the smallest host-visible Pi 4 SD-card image handoff step | completed | `manifests/2026-03-21-aarch64-rpi4b-sdimg-export-scope.md` | the next bounded move is now fixed: export the VM-local `rpi4b-sd.img` into the host workspace instead of folding export into the builder or widening into flashing automation | implement the smallest host-visible Pi 4 SD-card image export helper |
| `STEP-0279` | Implement the smallest host-visible Pi 4 SD-card image export helper | completed | `manifests/2026-03-21-aarch64-rpi4b-sdimg-export-helper.md` | `scripts/export-rpi4b-sdimg.sh` now exports the VM-local Pi 4 disk image into `artifacts/rpi4b/rpi4b-sd.img`, and the host-visible artifact matches the VM-local source by SHA-256 | scope the first macOS flashing-workflow documentation step |
| `STEP-0280` | Scope the first macOS flashing-workflow documentation step | completed | `manifests/2026-03-21-aarch64-rpi4b-macos-flash-doc-scope.md` | the next bounded move is now fixed: add one explicit macOS flashing workflow plus current no-UART first-boot expectations to the operator runbook | implement the first macOS flashing-workflow runbook step |
| `STEP-0281` | Implement the first macOS flashing-workflow runbook step | completed | `manifests/2026-03-21-aarch64-rpi4b-macos-flash-doc.md` | the runbook now includes an explicit macOS flashing procedure for `rpi4b-sd.img`, plus current no-UART first-boot limits, so the next bounded technical move should target alternate observability rather than more artifact work | scope the smallest alternate-observability step for a no-UART Pi 4 lab |
| `STEP-0282` | Scope the smallest alternate-observability step for a no-UART Pi 4 lab | completed | `manifests/2026-03-21-aarch64-rpi4b-no-uart-observability-scope.md` | the next bounded technical move is now fixed: start with a `plo`-side mailbox framebuffer visibility step, because it is visible on HDMI, testable under `raspi4b`, and narrower than early networking or full runtime display support | implement the first Pi 4 `plo` mailbox-framebuffer visibility step |
| `STEP-0283` | Implement the first Pi 4 `plo` mailbox-framebuffer visibility step | completed | `manifests/2026-03-21-aarch64-rpi4b-plo-hdmi-visibility.md` | the Pi 4 `plo` path now allocates a framebuffer and paints a visible top-left marker under `raspi4b` QEMU; the decisive fix was moving the property-mailbox request buffer from the high generic `plo` image range into low physical memory | scope the smallest automated regression check for the new Pi 4 HDMI visibility path |
| `STEP-0284` | Scope the smallest automated regression check for the Pi 4 HDMI visibility path | completed | `manifests/2026-03-21-aarch64-rpi4b-hdmi-smoke-scope.md` | the smallest next move is now fixed: add one coordination-repo helper that runs the existing Pi 4 QEMU lane, captures one framebuffer dump, and validates the current marker pixels | implement the Pi 4 HDMI visibility smoke helper |
| `STEP-0285` | Implement the Pi 4 HDMI visibility smoke helper | completed | `manifests/2026-03-21-aarch64-rpi4b-hdmi-smoke-helper.md` | the current Pi 4 early HDMI marker is now guarded by one command, so the next bounded move should target the smallest real-hardware HDMI readiness refinement rather than more ad hoc QEMU framebuffer work | scope the smallest real-hardware-oriented HDMI refinement for the first Pi 4 trial |
| `STEP-0286` | Scope the smallest real-hardware-oriented HDMI refinement for the first Pi 4 trial | completed | `manifests/2026-03-21-aarch64-rpi4b-hdmi-firmware-refinement-scope.md` | the smallest next refinement is now fixed: harden the Pi 4 firmware staging config with forced HDMI output and disabled overscan before widening into broader display policy | implement the Pi 4 firmware-stage HDMI refinement |
| `STEP-0287` | Implement the Pi 4 firmware-stage HDMI refinement | completed | `manifests/2026-03-21-aarch64-rpi4b-hdmi-firmware-refinement.md` | the Pi 4 project `config.txt` now forces HDMI output and disables firmware overscan, and the rebuilt staged artifact contains those settings | scope the smallest refreshed host-artifact handoff step after the HDMI firmware refinement |
| `STEP-0288` | Scope the smallest refreshed host-artifact handoff step after the HDMI firmware refinement | completed | `manifests/2026-03-21-aarch64-rpi4b-refreshed-image-handoff-scope.md` | the smallest next move is now fixed: rerun the existing Pi 4 SD-image export helper and record the updated host-visible image checksum | refresh the host-visible Pi 4 SD image after the HDMI firmware refinement |
| `STEP-0289` | Refresh the host-visible Pi 4 SD image after the HDMI firmware refinement | completed | `manifests/2026-03-21-aarch64-rpi4b-sdimg-refresh-gap.md` | the direct export assumption was false: after the latest Pi 4 project/image rebuild, the VM-local `rpi4b-sd.img` no longer exists until the dedicated SD-image assembly helper is rerun | scope the smallest corrected refreshed-image sequence |
| `STEP-0290` | Scope the smallest corrected refreshed-image sequence | completed | `manifests/2026-03-21-aarch64-rpi4b-corrected-image-refresh-scope.md` | the corrected minimal sequence is now fixed: rerun the SD-image assembly helper, then rerun the export helper, then record the refreshed checksum | assemble and export the refreshed Pi 4 SD image |
| `STEP-0291` | Assemble and export the refreshed Pi 4 SD image | completed | `manifests/2026-03-21-aarch64-rpi4b-artifact-chain-gap.md` | the SD-image assembly assumption was still incomplete: it also needs a rebuilt `rpi4b-bootfs.img`, so the actual refreshed-image path must rerun the full helper chain from bootfs assembly onward | scope the smallest corrected full artifact-refresh chain |
| `STEP-0292` | Scope the smallest corrected full artifact-refresh chain | completed | `manifests/2026-03-21-aarch64-rpi4b-full-artifact-refresh-scope.md` | the corrected full helper chain is now fixed: bootfs tree, bootfs image, SD image, then export | rerun the full Pi 4 artifact-refresh chain |
| `STEP-0293` | Rerun the full Pi 4 artifact-refresh chain | completed | `manifests/2026-03-21-aarch64-rpi4b-full-artifact-refresh.md` | the full helper chain succeeded, and the host-visible `artifacts/rpi4b/rpi4b-sd.img` is now refreshed with SHA-256 `acfdb8c251be03a716cdd9811b151c412de1e3a11c24db76ed5a476d8fc8f107` | scope the smallest final pre-hardware Pi 4 trial refinement |
| `STEP-0294` | Scope the smallest final pre-hardware Pi 4 trial refinement | completed | `manifests/2026-03-21-aarch64-final-prehardware-refinement-scope.md` | the highest-value remaining refinement is now explicitly selected as a detailed Circle review rather than another small image or checklist tweak | review Circle for Pi 4 video and USB keyboard bring-up guidance |
| `STEP-0295` | Review Circle for Pi 4 video and USB keyboard bring-up guidance | completed | `manifests/2026-03-21-circle-pi4-video-usb-review.md` | Circle strongly validates the current low-memory mailbox framebuffer path, confirms that Pi 4 USB keyboard depends on PCIe plus xHCI, and therefore pushes the next Phoenix step back toward HDMI-visible progress rather than early USB work | scope the smallest post-Circle Pi 4 HDMI refinement |
| `STEP-0296` | Scope the smallest post-Circle Pi 4 HDMI refinement | completed | `manifests/2026-03-21-aarch64-rpi4b-post-circle-hdmi-scope.md` | the selected next step is a tiny staged `plo` HDMI progress indicator: improve what the no-UART Pi 4 screen communicates without widening into a text console or USB work | implement the staged Pi 4 `plo` HDMI progress indicator |
| `STEP-0297` | Implement the staged Pi 4 `plo` HDMI progress indicator | completed | `manifests/2026-03-21-aarch64-rpi4b-staged-hdmi-progress.md`, `plo f10aefd` | the Pi 4 `plo` HDMI path now shows a three-stage top-left progress panel under `raspi4b` QEMU, which gives the no-UART board trial more useful location information than a single rectangle | scope the refreshed Pi 4 SD-image handoff after staged HDMI progress |
| `STEP-0298` | Scope the refreshed Pi 4 SD-image handoff after staged HDMI progress | completed | `manifests/2026-03-21-aarch64-rpi4b-staged-hdmi-refresh-scope.md` | no new sequencing is needed: the existing full helper chain from `STEP-0293` remains the correct way to propagate the staged HDMI indicator into the host-visible Pi 4 SD image | refresh the host-visible Pi 4 SD image after staged HDMI progress |
| `STEP-0299` | Refresh the host-visible Pi 4 SD image after staged HDMI progress | completed | `manifests/2026-03-21-aarch64-rpi4b-staged-hdmi-image-refresh.md` | the exported host-visible Pi 4 SD image has been refreshed and now carries the staged HDMI progress panel, with validated SHA-256 `b2f3a33fe7b4e96d364b6e7579350d7c548359701cbaf0e9ac6b86fbf18860b0` | scope the first manual Pi 4 board trial with staged HDMI progress |
| `STEP-0300` | Scope the first manual Pi 4 board trial with staged HDMI progress | abandoned | not applicable | the user explicitly redirected the project away from SD-image and manual-trial work until HDMI text output exists, so this step was intentionally retired before implementation | scope the first AArch64 framebuffer-graphmode exposure step for Pi 4 HDMI text |
| `STEP-0303` | Implement the first Pi 4 framebuffer text output in `pl011-tty` | completed | `manifests/2026-03-21-aarch64-rpi4b-hdmi-text-console.md`, `phoenix-rtos-kernel 276c0c4d`, `plo a21a680`, `phoenix-rtos-devices 83c9167` | the Pi 4 A72 QEMU lane now renders a black HDMI text console with white glyphs, and the refreshed `scripts/qemu-rpi4b-hdmi-smoke.sh` now validates text output rather than the old stage panel | refresh and export the Pi 4 SD-card image from this validated HDMI-text baseline |
| `STEP-0304` | Refresh and export the Pi 4 SD image from the validated HDMI text baseline | abandoned | not applicable | the user explicitly deferred SD-image refresh after the HDMI text-console milestone, so this step was intentionally retired before running the export helpers | scope the first post-HDMI-text real-hardware readiness step without refreshing artifacts |
| `STEP-0305` | Scope the first post-HDMI-text real-hardware readiness step without refreshing artifacts | completed | not applicable | the repo now treats the HDMI text-console milestone as the current real-device baseline, the runbook reflects the newer black-background white-text signature, and the deferred SD-export step is no longer the active goal | scope the smallest post-HDMI-text Pi 4 shell-smoke regression step |
| `STEP-0306` | Scope the smallest post-HDMI-text Pi 4 shell-smoke regression step | abandoned | not applicable | the user explicitly redirected the next goal from shell-smoke cleanup to USB keyboard work for real-device interactivity, so the shell-smoke follow-up was retired before implementation | scope the first reusable USB keyboard implementation step after the HDMI text-console milestone |
| `STEP-0307` | Scope the first reusable USB keyboard implementation step after the HDMI text-console milestone | completed | not applicable | source review fixed the next keyboard seam as a generic HID boot-keyboard class driver rather than an oversized Pi 4 PCIe plus xHCI jump, and the repo now records that Pi 4 keyboard transport remains a later dependency | implement the first reusable USB HID boot-keyboard driver foundation |
| `STEP-0308` | Implement the first reusable USB HID boot-keyboard driver foundation | completed | `manifests/2026-03-21-usbkbd-foundation.md`, `phoenix-rtos-devices 684468f`, `phoenix-rtos-build 9da1732` | Phoenix now has a reusable `usbkbd` class-driver foundation that builds cleanly with the available AArch64 toolchain, while Pi 4 keyboard transport remains explicitly deferred to later PCIe plus xHCI work | scope the smallest tty-bridge step from `/dev/kbd0` into `pl011-tty` |
| `STEP-0309` | Scope the smallest tty-bridge step from `/dev/kbd0` into `pl011-tty` | completed | not applicable | source review fixed the smallest bridge as a retrying `/dev/kbd0` reader thread inside `pl011-tty`, keeping `libtty` as the single console discipline instead of widening into a separate keyboard console stack | implement the smallest `/dev/kbd0` to `pl011-tty` bridge |
| `STEP-0310` | Implement the smallest `/dev/kbd0` to `pl011-tty` bridge | completed | `manifests/2026-03-21-pl011-usbkbd-bridge.md`, `phoenix-rtos-devices f70b1db`, `phoenix-rtos-project e61f067` | the Pi 4 A72 project now enables a small `pl011-tty` keyboard bridge, so once USB transport exists the current HDMI text console can become interactive through the existing `libtty` path | scope the first Pi 4 USB transport milestone after the keyboard bridge |
| `STEP-0311` | Scope the first Pi 4 USB transport milestone after the keyboard bridge | completed | `manifests/2026-03-21-rpi4b-usb-transport-scope.md` | the next real Pi 4 USB milestone is now explicitly BCM2711 PCIe root-complex bring-up plus config-space enumeration; xHCI is intentionally deferred until after that transport base exists | implement the smallest PCIe server config-space abstraction needed for a later BCM2711 backend |
| `STEP-0312` | Implement PCIe server config-space access abstraction for BCM2711 follow-up | completed | `manifests/2026-03-21-bcm2711-pcie-cfgio-abstraction.md`, `phoenix-rtos-devices f8540f0` | the platform-agnostic PCIe server scan path now uses a small server-local config-space backend interface instead of hardcoded ECAM access, and the current Xilinx and Pi 4 compile validation baselines still hold | scope the first BCM2711-specific indexed config-space backend step |
| `STEP-0313` | Scope the first BCM2711 PCIe config-space backend step | completed | `manifests/2026-03-21-bcm2711-pcie-backend-scope.md` | the next bounded transport slice is now fixed as a compile-only BCM2711 indexed config-space backend behind `pcie_cfgio_t`, with link training, DMA windows, MSI, and xHCI still explicitly out of scope | implement the compile-only BCM2711 indexed config-space backend |
| `STEP-0314` | Implement the compile-only BCM2711 indexed config-space backend | completed | `manifests/2026-03-21-bcm2711-indexed-cfg-backend.md`, `phoenix-rtos-devices 63556bc`, `phoenix-rtos-project c34f012` | the Pi 4 path now has a BCM2711-specific indexed config-space backend behind `pcie_cfgio_t`, and both the preserved Xilinx compile lane and the Pi 4 A72 full-build regression still pass | scope the smallest BCM2711 host-bridge initialization step |
| `STEP-0315` | Scope the smallest BCM2711 host-bridge initialization step | completed | `manifests/2026-03-21-bcm2711-host-init-scope.md` | the next bounded host-bridge slice is now fixed as reset sequencing, revision read, and early `MISC_CTRL` preparation before outbound windows or link-up handling | implement the first BCM2711 host-bridge preparation hook |
| `STEP-0316` | Implement the first BCM2711 host-bridge preparation hook | completed | `manifests/2026-03-21-bcm2711-host-init-prepare.md`, `phoenix-rtos-devices cfa3e06` | the BCM2711 backend now performs early reset/revision/basic-`MISC_CTRL` preparation while preserving the current Pi 4 and Xilinx compile baselines | scope the smallest BCM2711 link-bring-up step |
| `STEP-0317` | Scope the smallest BCM2711 link-bring-up step | completed | `manifests/2026-03-21-bcm2711-link-scope.md` | the next bounded link slice is now fixed as `PERST` release, 100 ms wait, link-state sampling, and RC-mode sampling before outbound-window setup or downstream enumeration | implement the smallest BCM2711 link-bring-up slice |
| `STEP-0318` | Implement the smallest BCM2711 link-bring-up slice | completed | `manifests/2026-03-21-bcm2711-link-state-gating.md`, `phoenix-rtos-devices e7ece12` | the BCM2711 backend now releases `PERST`, samples link and RC-mode state, and uses that state to gate downstream config-space access while preserving the current compile baselines | scope the smallest outbound-window and root-bridge shaping step |
| `STEP-0319` | Scope the smallest outbound-window and root-bridge shaping step | completed | `manifests/2026-03-21-bcm2711-outbound-scope.md` | the next bounded slice is now fixed as one outbound window plus root-bridge class-code shaping after the sampled link gate, while still keeping real downstream enumeration out of scope | implement the smallest outbound-window and root-bridge shaping step |
| `STEP-0320` | Implement the smallest outbound-window and root-bridge shaping step | completed | `manifests/2026-03-21-bcm2711-outbound-root-shape.md`, `phoenix-rtos-devices cab0ca1`, `phoenix-rtos-project ba1a0c3` | the BCM2711 backend now programs one outbound window, RC BAR2, and root-bridge class code behind the sampled link gate while preserving the current compile baselines | scope the smallest root-bridge memory-window and downstream-bus exposure step |
| `STEP-0321` | Scope the smallest root-bridge memory-window and downstream-bus exposure step | completed | `manifests/2026-03-21-bcm2711-bridge-exposure-scope.md` | the next bounded slice is now fixed as bridge-side cache-line, bus-number, memory-window, and command exposure on bus `0` before any endpoint-specific claim | implement the smallest root-bridge memory-window and downstream-bus exposure step |
| `STEP-0322` | Implement the smallest root-bridge memory-window and downstream-bus exposure step | completed | `manifests/2026-03-21-bcm2711-bridge-exposure.md`, `phoenix-rtos-devices d7e5ec7` | the BCM2711 backend now exposes bridge-side cache-line, bus-number, memory-window, and command state on the root bridge while preserving the current compile baselines | scope the smallest first endpoint-visibility and enumeration step |
| `STEP-0323` | Scope the smallest first endpoint-visibility and enumeration step | completed | `manifests/2026-03-21-bcm2711-endpoint-visibility-scope.md` | the next bounded slice is now fixed as the remaining Circle-style bridge-capability enablement: root-bridge parity plus PCIe root-control CRS software visibility before any direct downstream endpoint claim | implement the smallest bridge-capability enablement step for first downstream endpoint visibility |
| `STEP-0324` | Implement the smallest bridge-capability enablement step for first downstream endpoint visibility | completed | `manifests/2026-03-21-bcm2711-endpoint-visibility.md`, `phoenix-rtos-devices d6fe26d` | the BCM2711 backend now also enables root-bridge parity and PCIe root-control CRS software visibility behind the sampled link gate while preserving the current compile baselines | scope the smallest first downstream endpoint readback step |
| `STEP-0325` | Scope the smallest Pi 4 project-integration step for the current PCIe server | completed | `manifests/2026-03-21-rpi4b-pcie-project-integration-scope.md` | the more practical immediate gap is now explicit: the Pi 4 image did not stage the `pcie` server at all, so the next bounded move is to include it in the Pi 4 target and `user.plo` script | implement the smallest Pi 4 project-integration step for the current PCIe server |
| `STEP-0326` | Implement the smallest Pi 4 project-integration step for the current PCIe server | completed | `manifests/2026-03-21-rpi4b-pcie-project-integration.md`, `phoenix-rtos-devices 7af4e98`, `phoenix-rtos-project ce18718` | the Pi 4 A72 image path now includes and stages `pcie`, so the current BCM2711 transport work is finally reachable on real hardware without changing the HDMI-text baseline | scope the smallest first direct downstream endpoint-readback step |
| `STEP-0327` | Scope the smallest first direct downstream endpoint-readback step | completed | `manifests/2026-03-21-rpi4b-vl805-fastpath-scope.md` | the fastest practical next USB move is now explicit: use a Pi 4 VL805 fast path with fixed downstream BDF/class assumptions instead of inventing a generic AArch64 PCI service before the first keyboard goal | implement the board-level Pi 4 VL805 fast-path constants |
| `STEP-0328` | Implement the board-level Pi 4 VL805 fast-path constants | completed | `manifests/2026-03-21-rpi4b-vl805-fastpath-constants.md`, `phoenix-rtos-project 0db048e` | the Pi 4 board config now carries the first VL805 fast-path constants, which gives later xHCI code a bounded discovery contract without changing the current staged runtime image | scope the first compile-only Pi 4 xHCI HCD skeleton and discovery stub |
| `STEP-0329` | Scope the first compile-only Pi 4 xHCI HCD skeleton and discovery stub | completed | `manifests/2026-03-21-rpi4b-xhci-skeleton-scope.md` | the smallest safe first xHCI move is now fixed: one compile-only `libusbxhci` skeleton plus one Pi 4 discovery stub, kept completely out of the live image path until runtime initialization is real | implement the first compile-only Pi 4 xHCI HCD skeleton and discovery stub |
| `STEP-0330` | Implement the first compile-only Pi 4 xHCI HCD skeleton and discovery stub | completed | `manifests/2026-03-21-rpi4b-xhci-skeleton.md`, `phoenix-rtos-devices 64c95b1`, `phoenix-rtos-usb 1210902` | the first `libusbxhci` skeleton now compiles on the Pi 4 A72 lane, and the generic USB host binary now also compiles on A72 after a small `uintptr_t` portability fix in `phoenix-rtos-usb` | scope the smallest A72 USB build-glue step for the new xHCI pieces |
| `STEP-0331` | Scope the smallest A72 USB build-glue step for the new xHCI pieces | completed | `manifests/2026-03-21-aarch64a72-usb-build-glue-scope.md` | the next bounded move is now fixed: bring the xHCI skeleton, USB host binary, and keyboard pieces into the normal A72 build, but keep them out of the live Pi 4 image script | implement the smallest A72 USB build-glue step for the new xHCI pieces |
| `STEP-0332` | Implement the smallest A72 USB build-glue step for the new xHCI pieces | completed | `manifests/2026-03-21-aarch64a72-usb-build-glue.md`, `phoenix-rtos-build 962da50`, `phoenix-rtos-devices 65e4334` | the normal A72 build now produces `/sbin/usb` and `/sbin/usbkbd` together with the xHCI skeleton pieces, while the live Pi 4 staged program list remains unchanged | scope the smallest first runtime-safe xHCI initialization slice |
| `STEP-0333` | Scope the smallest first runtime-safe xHCI initialization slice | completed | `manifests/2026-03-21-xhci-runtime-safe-init-scope.md` | the next bounded move is now fixed: map the Pi 4 xHCI MMIO window, read the capability header, and still fail cleanly before enumeration or staged runtime USB support | implement the first runtime-safe xHCI initialization slice |
| `STEP-0334` | Implement the first runtime-safe xHCI initialization slice | completed | `manifests/2026-03-21-xhci-runtime-safe-init.md`, `phoenix-rtos-devices dd6a9fa` | the Pi 4 xHCI path now maps MMIO and validates the capability header before failing cleanly, and the full `aarch64a72-generic-rpi4b` build still passes with the live staged image unchanged | scope the smallest first xHCI controller-reset slice |
| `STEP-0335` | Scope the smallest first xHCI controller-reset slice | completed | `manifests/2026-03-21-xhci-reset-scope.md` | the next bounded move is now fixed: derive the operational-register base from `CAPLENGTH`, perform the first controller reset, and still stop before page-size, rings, interrupts, or enumeration | implement the first xHCI controller-reset slice |
| `STEP-0336` | Implement the first xHCI controller-reset slice | completed | `manifests/2026-03-21-xhci-reset.md`, `phoenix-rtos-devices e90f12d` | the Pi 4 xHCI path now performs the first bounded controller reset after the capability probe, and the full `aarch64a72-generic-rpi4b` build still passes with the live staged image unchanged | scope the smallest Pi 4 firmware `notify xHCI reset` step |
| `STEP-0337` | Scope the smallest Pi 4 firmware `notify xHCI reset` step | completed | `manifests/2026-03-21-rpi4b-xhci-firmware-notify-scope.md` | the next Pi 4-specific move is now fixed: issue the firmware reset-notify property tag from the `pcie` server, where the BCM2711 reset and VL805 enable path already lives and where user space can supply a firmware-visible buffer cleanly | implement the Pi 4 firmware `notify xHCI reset` step |
| `STEP-0338` | Implement the Pi 4 firmware `notify xHCI reset` step | completed | `manifests/2026-03-21-rpi4b-xhci-firmware-notify.md`, `phoenix-rtos-devices 88cf140`, `phoenix-rtos-project 0ff6c3e` | the Pi 4 `pcie` server now performs the first VL805 firmware reset-notify handshake before enabling the fixed xHCI endpoint, and the full `aarch64a72-generic-rpi4b` build still passes with the live staged image unchanged | scope the smallest post-notify xHCI readiness refinement |
| `STEP-0339` | Scope the smallest post-notify xHCI readiness refinement | completed | `manifests/2026-03-21-xhci-post-notify-readiness-scope.md` | the next bounded xHCI move is now fixed: validate 4K page support and extract the basic port-count seam after reset and firmware notify, while still staying far away from root-hub and ring work | implement the smallest post-notify xHCI readiness refinement |
| `STEP-0340` | Implement the smallest post-notify xHCI readiness refinement | completed | `manifests/2026-03-21-xhci-post-notify-readiness.md`, `phoenix-rtos-devices 4e46b4b` | the Pi 4 xHCI path now validates 4K page support and a non-zero port count after reset, and the full `aarch64a72-generic-rpi4b` build still passes with the live staged image unchanged | scope the smallest next xHCI structural-capability refinement |
| `STEP-0341` | Scope the smallest next xHCI structural-capability refinement | completed | `manifests/2026-03-22-xhci-capability-shape-scope.md` | the next bounded xHCI move is now fixed: extract max slots, max scratchpad buffers, and context-size expectations while still staying pre-root-hub, pre-ring, and pre-enumeration | implement the next xHCI structural-capability refinement |
| `STEP-0342` | Implement the next xHCI structural-capability refinement | completed | `manifests/2026-03-22-xhci-capability-shape.md`, `phoenix-rtos-devices 1c69f0a` | the Pi 4 xHCI path now extracts max slots, scratchpad-buffer count, and context size, and rejects zero-slot or non-32-byte-context controllers while preserving the clean Pi 4 A72 build baseline | scope the smallest controller-register layout refinement after the new capability-shape data |
| `STEP-0344` | Implement the smallest xHCI controller-register layout refinement | completed | `manifests/2026-03-22-xhci-register-layout.md`, `phoenix-rtos-devices f77c997` | the Pi 4 xHCI path now extracts `DBOFF` and `RTSOFF` and rejects implausible register layouts while preserving the clean Pi 4 A72 build baseline | scope the smallest pre-interrupt xHCI capability refinement |
| `STEP-0345` | Scope the smallest pre-interrupt xHCI capability refinement | completed | `manifests/2026-03-22-xhci-interrupter-shape-scope.md` | the next bounded xHCI move is now fixed: extract max interrupters, interrupt moderation scale, and maximum ERST size while still staying pre-interrupt-enable, pre-ring-allocation, and pre-enumeration | implement the smallest pre-interrupt xHCI capability refinement |
| `STEP-0346` | Implement the smallest pre-interrupt xHCI capability refinement | completed | `manifests/2026-03-22-xhci-interrupter-shape.md`, `phoenix-rtos-devices 22ff627` | the Pi 4 xHCI path now extracts max interrupters, interrupt moderation scale, and maximum ERST size, and rejects zero-interrupter controllers while preserving the clean Pi 4 A72 build baseline | scope the smallest xHCI addressability and scratchpad-capability refinement |
| `STEP-0347` | Scope the smallest xHCI addressability and scratchpad-capability refinement | completed | `manifests/2026-03-22-xhci-scratchpad-capability-scope.md` | the next bounded xHCI move is now fixed: extract 64-bit-addressing, scratchpad-restore, and primary-stream-array-size capability bits while still staying pre-DCBAA, pre-scratchpad-allocation, pre-ring, and pre-enumeration | implement the smallest xHCI addressability and scratchpad-capability refinement |
| `STEP-0348` | Implement the smallest xHCI addressability and scratchpad-capability refinement | completed | `manifests/2026-03-22-xhci-scratchpad-capability.md`, `phoenix-rtos-devices b54cdcd` | the Pi 4 xHCI path now extracts 64-bit-addressing, scratchpad-restore, and primary-stream-array-size capability bits while preserving the clean Pi 4 A72 build baseline | scope the smallest xHCI operational memory-layout register step |
| `STEP-0349` | Scope the smallest xHCI operational memory-layout register step | completed | `manifests/2026-03-22-xhci-memory-layout-scope.md` | the next bounded xHCI move is now fixed: capture `DCBAAP` and `CRCR` locations as the first operational memory-layout seam while still staying pre-programming and pre-enumeration | implement the smallest xHCI operational memory-layout register step |
| `STEP-0350` | Implement the smallest xHCI operational memory-layout register step | completed | `manifests/2026-03-22-xhci-memory-layout.md`, `phoenix-rtos-devices b234294` | the Pi 4 xHCI path now extracts `CRCR` and `DCBAAP` state and applies minimal reserved-bit and 64-bit-addressing sanity checks while preserving the clean Pi 4 A72 build baseline | scope the smallest xHCI memory-allocation step for `DCBAA` and the command ring |
| `STEP-0351` | Scope the smallest xHCI memory-allocation step for `DCBAA` and the command ring | completed | `manifests/2026-03-22-xhci-command-space-scope.md` | the next bounded xHCI move is now fixed: allocate `DCBAA` and the first command-ring segment while still staying pre-register-programming, pre-doorbell, pre-interrupt-enable, and pre-enumeration | implement the smallest xHCI memory-allocation step for `DCBAA` and the command ring |
| `STEP-0352` | Implement the smallest xHCI memory-allocation step for `DCBAA` and the command ring | completed | `manifests/2026-03-22-xhci-command-space.md`, `phoenix-rtos-devices 6704d82` | the Pi 4 xHCI path now allocates aligned `DCBAA` and command-ring memory objects and records their physical addresses while preserving the clean Pi 4 A72 build baseline | scope the smallest xHCI register-programming step for `DCBAA`, `CRCR`, and `CONFIG` |
| `STEP-0353` | Scope the smallest xHCI register-programming step for `DCBAA`, `CRCR`, and `CONFIG` | completed | `manifests/2026-03-22-xhci-register-programming-scope.md` | the next bounded xHCI move is now fixed: program `DCBAAP`, `CRCR`, and `CONFIG` while still staying pre-run, pre-doorbell, pre-interrupt-enable, and pre-enumeration | implement the smallest xHCI register-programming step for `DCBAA`, `CRCR`, and `CONFIG` |
| `STEP-0354` | Implement the smallest xHCI register-programming step for `DCBAA`, `CRCR`, and `CONFIG` | completed | `manifests/2026-03-22-xhci-register-programming.md`, `phoenix-rtos-devices 617f0c2` | the Pi 4 xHCI path now programs `DCBAAP`, `CRCR`, and `CONFIG` and verifies the programmed state while still staying pre-run, pre-doorbell, pre-interrupt-enable, and pre-enumeration | scope the smallest Pi 4 shell-side fix for the current `/dev/console` startup race |
| `STEP-0355` | Scope the smallest Pi 4 `/dev/console` startup-race fix after the xHCI validation pass | completed | `manifests/2026-03-22-pi4-console-open-race-scope.md` | bounded Pi 4 QEMU gdbstub passes now show that `resolve_path(\"/dev/console\")` succeeds but `open()` still returns `-1` five times, so the next smallest move is a shell-side retry-policy fix rather than another path-resolution or xHCI change | implement the smallest bounded `psh_ttyopen()` retry-policy refinement |
| `STEP-0356` | Implement the smallest bounded `psh_ttyopen()` retry-policy refinement for the Pi 4 shell startup race | completed | `manifests/2026-03-22-pi4-psh-ttyopen-retry.md`, `phoenix-rtos-utils afe54cf` | the Pi 4 shell path now reaches `psh: tty ready`, prompt, and `help` again, so the remaining automated-smoke issue is stale debug-probe noise rather than the earlier console-open race | scope the smallest cleanup step for the stale Pi 4 shell-smoke probes |
| `STEP-0357` | Scope the smallest cleanup step for stale Pi 4 shell-smoke probe noise | completed | `manifests/2026-03-22-pi4-shell-smoke-cleanup-scope.md` | the next bounded move is now fixed as removal of the obsolete kernel `create_dev` probes that still interleave with the Pi 4 shell-smoke prompt after the shell itself is already functional | implement the smallest cleanup step for the stale `create_dev` probes |
| `STEP-0358` | Implement the smallest cleanup step for the stale `create_dev` probes | completed | `manifests/2026-03-22-pi4-shell-smoke-probe-cleanup.md`, `phoenix-rtos-kernel 3182c7e7` | the stale `create_dev` probes are now gone and the generic shell, Pi 4 shell, and Pi 4 HDMI QEMU gates are all clean again | scope the smallest xHCI pre-run operational step beyond command-space register programming |
| `STEP-0359` | Scope the smallest xHCI pre-run operational step beyond command-space register programming | completed | `manifests/2026-03-22-xhci-run-state-scope.md` | the next bounded xHCI move is now fixed as a `RUN/STOP` self-test with halted-state verification while still staying pre-event-ring, pre-interrupt, pre-doorbell, and pre-enumeration | implement the smallest xHCI run-state self-test beyond command-space binding |
| `STEP-0360` | Implement the smallest xHCI run-state self-test beyond command-space binding | completed | `manifests/2026-03-22-xhci-run-state.md`, `phoenix-rtos-devices 07fbf65` | the Pi 4 xHCI path now validates a bounded halted-to-run-to-halted controller transition while still staying pre-event-ring, pre-interrupt-enable, pre-doorbell, and pre-enumeration | scope the smallest xHCI event-ring allocation step |
| `STEP-0361` | Scope the smallest xHCI event-ring allocation step | completed | `manifests/2026-03-22-xhci-event-ring-allocation-scope.md` | the next bounded xHCI move is now fixed as one event-ring segment plus one ERST block for interrupter 0, kept pre-runtime-register-programming and pre-enumeration | implement the smallest xHCI event-ring allocation step |
| `STEP-0362` | Implement the smallest xHCI event-ring allocation step | completed | `manifests/2026-03-22-xhci-event-ring-allocation.md`, `phoenix-rtos-devices 6a5e8dd` | the Pi 4 xHCI path now allocates one event-ring segment and one ERST block and populates the first ERST entry for interrupter 0 while still staying pre-runtime-register-programming and pre-enumeration | scope the smallest xHCI runtime-register programming step for the new event ring |
| `STEP-0363` | Scope the smallest xHCI runtime-register programming step for the new event ring | completed | `manifests/2026-03-22-xhci-event-ring-programming-scope.md` | the next bounded xHCI move is now fixed as programming `ERSTSZ`, `ERSTBA`, and `ERDP` for interrupter 0 while still staying pre-interrupt-enable, pre-doorbell, and pre-enumeration | implement the smallest xHCI runtime-register programming step for the new event ring |
| `STEP-0364` | Implement the smallest xHCI runtime-register programming step for the new event ring | completed | `manifests/2026-03-22-xhci-event-ring-programming.md`, `phoenix-rtos-devices e550dbb` | the Pi 4 xHCI path now programs and reads back `ERSTSZ`, `ERSTBA`, and `ERDP` for interrupter 0 while still staying pre-interrupt-enable, pre-doorbell, and pre-enumeration | scope the smallest xHCI command-ring layout initialization step |
| `STEP-0365` | Scope the smallest xHCI command-ring layout initialization step | completed | `manifests/2026-03-22-xhci-command-ring-layout-scope.md` | the next bounded xHCI move is now fixed as initializing the allocated command ring as a real ring with a final link TRB and initial cycle-state contract while still staying pre-doorbell and pre-enumeration | implement the smallest xHCI command-ring layout initialization step |
| `STEP-0366` | Implement the smallest xHCI command-ring layout initialization step | completed | `manifests/2026-03-22-xhci-command-ring-layout.md`, `phoenix-rtos-devices 7fc6420` | the Pi 4 xHCI path now initializes a real command-ring layout with a final link TRB and initial cycle-state contract while still staying pre-doorbell, pre-command-submission, and pre-enumeration | scope the smallest polled xHCI command-submission step |
| `STEP-0367` | Scope the smallest polled xHCI command-submission step | completed | `manifests/2026-03-22-xhci-polled-command-scope.md` | because the current Pi 4 xHCI discovery stub still advertises `irq = 0`, the next bounded move is now fixed as a single-command polled no-op submission path rather than interrupt enablement | implement the smallest polled xHCI command-submission step |
| `STEP-0368` | Implement the smallest polled xHCI command-submission step | completed | `manifests/2026-03-22-xhci-polled-command.md`, `phoenix-rtos-devices 6eb4e26` | the Pi 4 xHCI path now contains a compile-validated polled no-op command self-test, but the project is still not ready for live USB staging because the roothub request path remains stubbed | scope the smallest xHCI roothub control/request step |
| `STEP-0369` | Scope the smallest xHCI roothub control/request step | completed | `manifests/2026-03-22-xhci-roothub-scope.md` | the next bounded USB-host move is now fixed as the first roothub request subset behind `usb_isRoothub(pipe->dev)`, because live `/sbin/usb` staging is still premature without it | implement the smallest xHCI roothub control/request step |
| `STEP-0370` | Implement the smallest xHCI roothub control/request step | completed | `manifests/2026-03-22-xhci-roothub.md`, `phoenix-rtos-devices c9ee03d` | the Pi 4 xHCI path now contains the first bounded Phoenix roothub contract, but `/sbin/usb` is still not stageable because `xhci_init()` still returns `-ENOSYS` after the internal controller tests | scope the smallest xHCI init-success step needed before live `/sbin/usb` staging |
| `STEP-0371` | Scope the smallest xHCI init-success step before live `/sbin/usb` staging | completed | `manifests/2026-03-22-xhci-init-success-scope.md` | the next bounded USB-host move is now fixed as letting `xhci_init()` survive as a roothub-only host, because roothub enumeration now uses only the already-implemented control path and the hub interrupt transfer can stay pending | implement the smallest xHCI init-success step |
| `STEP-0372` | Implement the smallest xHCI init-success step | completed | `manifests/2026-03-22-xhci-init-success.md`, `phoenix-rtos-devices 5593054` | the Pi 4 xHCI path now survives the current roothub-ready init sequence, so the next concrete blocker before live `usb` staging is no longer controller lifetime but roothub status delivery on the current no-IRQ path | scope the smallest xHCI roothub status-delivery step |
| `STEP-0373` | Scope the smallest xHCI roothub status-delivery step | completed | `manifests/2026-03-22-xhci-roothub-status-scope.md` | the next bounded xHCI move is now fixed as a temporary polling path that completes the pending root-hub interrupt transfer when `xhci_getHubStatus()` reports change bits, because the current Pi 4 path still reports `irq = 0` | implement the smallest xHCI roothub status-delivery step |
| `STEP-0374` | Implement the smallest xHCI roothub status-delivery step | completed | `manifests/2026-03-22-xhci-roothub-status.md`, `phoenix-rtos-devices 10f29da` | the Pi 4 xHCI path now has a bounded temporary roothub polling bridge on the current no-IRQ path, so the next concrete blocker for keyboard support is no longer plug-event delivery but the first non-roothub xHCI transfer path for child-device enumeration | scope the smallest xHCI non-roothub device-enumeration step |
| `STEP-0375` | Scope the smallest xHCI non-roothub device-enumeration step | completed | `manifests/2026-03-22-xhci-enable-slot-scope.md` | the next bounded child-device seam is now fixed as an internal `Enable Slot` command path, because post-roothub enumeration cannot reach `Address Device` or endpoint-0 control transfers before xHCI can allocate a slot | implement the smallest xHCI `Enable Slot` step |
| `STEP-0376` | Implement the smallest xHCI `Enable Slot` step | completed | `manifests/2026-03-22-xhci-enable-slot.md`, `phoenix-rtos-devices 32c6f09` | the Pi 4 xHCI path now has the first real child-device command beyond the roothub, so the next concrete blocker is no longer slot allocation but the first `Address Device` and endpoint-0 control path | scope the smallest xHCI `Address Device` step |
| `STEP-0377` | Scope the smallest xHCI `Address Device` step | completed | `manifests/2026-03-22-xhci-address-device-scope.md` | the next bounded xHCI move is now fixed as the first per-slot controller-owned object behind the enabled slot: device context, input context, EP0 ring backing, and DCBAA binding | implement the first xHCI slot-context allocation and DCBAA binding step |
| `STEP-0378` | Implement the first xHCI slot-context allocation and DCBAA binding step | completed | `manifests/2026-03-22-xhci-slot-space.md`, `phoenix-rtos-devices 6c9138b` | the Pi 4 xHCI path now allocates the first per-slot controller-owned memory objects and binds the enabled slot into the DCBAA, so the next blocker is minimal `Address Device` context preparation rather than slot memory ownership | scope the smallest xHCI context-population step before `Address Device` |
| `STEP-0379` | Scope the smallest xHCI context-population step before `Address Device` | completed | `manifests/2026-03-22-xhci-context-population-scope.md` | the next bounded xHCI move is now fixed as EP0 ring initialization plus the minimum input/slot/EP0 context fields derived from `usb_dev_t`, while still staying below command execution and below endpoint-0 transfers | implement the smallest xHCI context-population step before `Address Device` |
| `STEP-0380` | Implement the smallest xHCI context-population step before `Address Device` | completed | `manifests/2026-03-22-xhci-context-population.md`, `phoenix-rtos-devices 13ae2c2` | the Pi 4 xHCI path now prepares the first direct-root-port `Address Device` input-context state, so the next blocker is the address-ownership contract between Phoenix USB enumeration and xHCI slot IDs | scope the smallest xHCI address-contract step before `Address Device` |
| `STEP-0381` | Scope the smallest xHCI address-contract step before `Address Device` | completed | `manifests/2026-03-22-xhci-address-contract-scope.md` | the next bounded xHCI move is now fixed as handling only `REQ_SET_ADDRESS` under an explicit slot-ID-equals-address contract, instead of silently inventing a broad translation layer | implement the bounded xHCI `REQ_SET_ADDRESS` path under the temporary slot-ID contract |
| `STEP-0382` | Implement the bounded xHCI `REQ_SET_ADDRESS` path under the temporary slot-ID contract | completed | `manifests/2026-03-22-xhci-address-device.md`, `phoenix-rtos-devices 8c98260` | the Pi 4 xHCI path now has the first real command-backed child-device action through `Address Device`, so the next blocker is the first non-`SET_ADDRESS` endpoint-0 control path needed for descriptor reads | scope the smallest non-`SET_ADDRESS` xHCI endpoint-0 control-transfer step |
| `STEP-0383` | Scope the smallest non-`SET_ADDRESS` xHCI endpoint-0 control-transfer step | completed | `manifests/2026-03-22-xhci-get-descriptor-scope.md` | the next bounded xHCI move is now fixed as a synchronous polled EP0 `REQ_GET_DESCRIPTOR` control-read for the first direct-root-port child, instead of widening immediately into a generic endpoint-0 engine | implement the bounded xHCI EP0 `GET_DESCRIPTOR` control-read step |
| `STEP-0384` | Implement the bounded xHCI EP0 `GET_DESCRIPTOR` control-read step | completed | `manifests/2026-03-22-xhci-get-descriptor.md` | the Pi 4 xHCI path now has the first real non-`SET_ADDRESS` endpoint-0 child-device transfer, so the next blocker is the first post-enumeration control-write path used by real drivers such as `usbkbd` | scope the smallest xHCI post-enumeration control-write step |
| `STEP-0385` | Scope the smallest xHCI post-enumeration control-write step | completed | `manifests/2026-03-22-xhci-control-write-scope.md` | the next bounded xHCI move is now fixed as one EP0 control-write/no-data helper covering the current required post-enumeration writes, instead of a dead-end `SET_CONFIGURATION` special case or a generic control engine | implement the bounded xHCI EP0 control-write/no-data path |
| `STEP-0343` | Scope the smallest xHCI controller-register layout refinement | completed | `manifests/2026-03-22-xhci-register-layout-scope.md` | the next bounded xHCI move is now fixed: extract `DBOFF` and `RTSOFF` plus the smallest sane-layout checks needed before any future interrupter or ring step | implement the smallest xHCI controller-register layout refinement |
| `STEP-0297` | Implement the staged Pi 4 `plo` HDMI progress indicator | completed | `manifests/2026-03-21-aarch64-rpi4b-staged-hdmi-progress.md`, `plo TBD` | the Pi 4 `plo` HDMI path now shows a three-stage top-left progress panel under `raspi4b` QEMU, which gives the no-UART board trial more useful location information than a single rectangle | scope the refreshed Pi 4 SD-image handoff after staged HDMI progress |
